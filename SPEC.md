# SPEC

## §G GOAL

Agent-operated CRM. Gmail = comms layer. Claude Code = strategist; internal Pydantic AI agent = tactical executor (routing, auto-reply, follow-up).

## §C CONSTRAINTS

- Python 3.14. basedpyright strict. ruff. TDD per CLAUDE.md.
- PostgreSQL 18. psycopg, raw SQL via `psycopg.sql`. no ORM.
- IDs = UUIDv7 (`uuid.uuid7()` via `_new_id()`).
- Gmail scope = `gmail.modify` only. no additional Gmail scopes.
- Drive scope = `drive.readonly` only. Read-only KB grounding. no write/modify.
- Calendar scope = `calendar.events.readonly` only. read-only event ingestion (§V.126). no event create/update from the app.
- Auth = service account & domain-wide delegation per §V.37. Credential source in {file path (`google_application_credentials` setting or `GOOGLE_APPLICATION_CREDENTIALS` env var), Application Default Credentials (e.g. GCE metadata server, Workload Identity, Cloud Run identity)}. Per-account impersonation: file creds -> `credentials.with_subject(email)`; ADC creds -> `service_account.Credentials(signer=iam.Signer(...), service_account_email=<sa>, subject=email, ...)`. no OAuth user login.
- Email body = plain text only (control chars stripped). no HTML body persistence. no embeddings, no vector store, no ingestion pipeline.
- KB files = `.md` in Drive folder named in `workflow.instructions`. no in-app PDF/Docs/HTML conversion.
- ASCII-only project artifacts (code, docs, CLI output). Agent-generated email body content exempt.
- Tool pattern: typed sig, DI deps, dict return, error dicts on failure (no raise to agent).

## §I INTERFACES

- cli: `mailpilot <noun> <verb> [args]` -> JSON on stdout, exit code 0 or 1, errors to stderr.
  - nouns: `account`, `company`, `contact`, `workflow`, `enrollment`, `task`, `email`, `meeting`, `activity`, `tag`, `note`, `template`, `db`.
  - verbs: `list|search|view|create|update|disable|enable|add|remove|reply|send|start|stop|cancel|retry|run|sync|export|import|init|migrate|check`.
  - top-level: `run` (sync & task loop), `status`, `config get|set`, global `--version|--debug|--completion|--skill`.
  - envelope: `list|search|sync|export|import` -> `{"<plural>": [...], "ok": true}` (`db export|import` exception -> `{"db": {...}, "ok": true}` status object like other `db` verbs, §V.121); `view|create|update|disable|enable|add|remove|reply|send|start|stop|cancel|retry|init|migrate|check` -> `{"<singular>": {...}, "ok": true}`; err -> `{"error": CODE, "message": TEXT, "ok": false}`.
  - email projection: `email view|list` ! project `route_method` in {`classified`, `thread_match`, `rfc_message_id_match`, `skipped_outside_window`, `skipped_no_workflows`, `skipped_predates_workflows`, `skipped_no_inbound_workflows`} so operator audits routing decision from CLI w/o Logfire.
  - contact projection: `contact list|search` ! project `title` + `company_domain` (company denorm per §V.5) so operator reads role + org from CLI w/o a separate company lookup.
  - company projection: `company list` projects `contact_count` (child COUNT, LEFT JOIN contact, incl. disabled per §V.96) + filters `--max-contacts N` / `--min-contacts N` (inclusive, compose w/ `--has-profile`) so operator finds zero-contact + under-cap companies w/o the per-company N+1 probe; `company list` default-excludes disabled companies (§V.114), `--include-disabled` opts in.
  - `template` = read-only, code-defined (registry in `src/mailpilot/agent/templates.py` per §V.44). Verbs: `list [--direction inbound|outbound]`, `view NAME`. no create/update/delete — new template = code change + PR.
  - `workflow import` accepts `--file <path>`: `.toml` single-workflow catalog entry | directory (glob `*.toml`); TOML-only, no JSON, no stdin (§V.103). `workflow export --account-email <addr> --out-dir <dir>` writes one `*.toml`/workflow (def fields, name-sorted) + JSON status envelope on stdout (§V.103, §V.3).
  - account-requiring cmds (`email send|reply`, `workflow create|export|import`) take a single `--account-email` (polymorphic — email | UUID, §V.107); `account sync` makes it optional (all accounts when omitted) + accepts `--since <iso>` to bound the initial full-INBOX backfill. keyed-entity verb targets + Scope/owner options use the natural key (account email, company domain, contact email, tag name), UUID still accepted; single-entity verb target = positional `<key>` arg, never `--<entity>-id` (§V.107).
  - CLI API standard (§V.107, §V.115, §V.116) — verb roles: Read {list, search, view}; Mutate {create, add, remove, update, disable, enable} (`create` = new entity from own fields; `add`/`remove` = link/unlink to an owner; `disable` = soft-retire, `enable` = clear the soft-disable; no `delete`, no hard-delete §V.10/§V.114); Lifecycle {start, stop, cancel, retry, enrollment `run`} carry per-entity state guards (`invalid_state`, §V.109); Action {send, reply, sync} = external Gmail effect; Schema/catalog {init, migrate, check, export, import}. `activity create` -> `activity add` (attaches event to a required owner). `list` filter flags = six-family taxonomy (§V.115); error envelope `error` code = closed vocabulary — domain codes {`not_found`, `invalid_state`, `missing_filter`, `already_exists`, `schema_migration_pending`, `schema_drift`, ...} around the §V.54 constraint/validation half, new code = spec change.
  - `db` = schema provisioning/migration noun, off the connection hot path (§V.110). Verbs: `init` (provision empty DB — apply `schema.sql` + stamp `schema_metadata`; refuses if `account` exists, no `--force`; idempotent no-op-with-message if current), `migrate` (apply pending `migrations/NNN_*.sql` in order, each own txn, record in `schema_migrations`; no-op if none), `check` (emit report `{recorded_hash, current_hash, applied, pending, verdict}`; verdict=current -> `ok:true` exit 0; pending|drift -> `schema_migration_pending`|`schema_drift` error envelope w/ report inlined + exit 1 per §V.4/§V.109 — scriptable deploy gate), `export` (`--file <path>` writes one JSON snapshot bundle = tag vocabulary + company + contact to disk + `{"db":{...}}` status on stdout; read-only + drift-tolerant like `check`), `import` (`--file <path>` reads the bundle, restores tags -> companies -> contacts by natural key, per-row errors continue, dead-stops on drift|pending §V.109; same `{"db":{...}}` envelope). per §V.108-110, §V.121.
  - `meeting` noun (§V.125-126): rows ingested from Google Calendar by CalendarClient, NOT operator-created. verbs `list`, `view`, `add` (link an attendee contact per §V.115 link semantics), `update` (edit summary/status), `cancel` (status -> cancelled); no `create` (ingestion owns row creation), JSON envelope per §V.4.
- agent tools (`src/mailpilot/agent/tools.py`): `send_email`, `reply_email`, `search_emails`, `read_email`, `read_contact`, `read_company`, `list_enrollments`, `create_task`, `cancel_task`, `conclude_enrollment`, `disable_contact`, `list_drive_markdown`, `read_drive_markdown`, `search_drive_markdown`, `noop`. all tool -> typed sig, dict return, err dict on failure.
- pubsub (`src/mailpilot/pubsub.py`): topic `mailpilot-topic-dev`, sub `mailpilot-sub-dev` (defaults; per-env override via `MAILPILOT_GOOGLE_PUBSUB_TOPIC` / `..._SUBSCRIPTION`). `setup_pubsub()` idempotent. `start_subscriber(settings, callback)` streaming pull. `make_notification_callback(queue, wakeup_event)` decode -> enqueue -> `wakeup_event.set()`. `renew_watches()` refresh @ T-24h.
- config (`src/mailpilot/settings.py`): `database_url`, `anthropic_api_key`, `anthropic_model` (default `claude-sonnet-4-6`), `anthropic_base_url` (default `https://api.anthropic.com`; override -> Anthropic-compatible endpoint e.g. `https://api.novita.ai/anthropic`; not secret per §V.86), `google_application_credentials`, `google_pubsub_topic`, `google_pubsub_subscription`, `logfire_token`, `logfire_environment` in {`development`, `production`}, `run_interval` (default 60s), `max_concurrent_tasks` (default 10).
- module: `src/mailpilot/gmail.py` -> `GmailClient`; `src/mailpilot/drive.py` -> `DriveClient`; `src/mailpilot/calendar.py` -> `CalendarClient` (§V.126). Mirror shape.
- entrypoint: `mailpilot = "mailpilot.cli:main"`.

## §V INVARIANTS

V1: every CLI cmd loads settings first; DB + network init after (settings-first)
V2: cli.py module-level imports = click only; heavy deps lazy-import inside cmd fns
V3: stdout = strict JSON only (all flags, incl --debug); operator lifecycle + errors -> stderr; Logfire console exporter ! target stderr (ConsoleOptions output=sys.stderr), never stdout — output unset defaults stdout so console lines corrupt JSON envelope
V4: every cmd output ! match §I.cli envelope; error path -> {"error","message","ok":false} + exit 1
V5: list/view rows carry parent denorm joined @ fetch — workflow rows + account_email; enrollment rows + workflow_name + contact_email + contact_name; contact list/search rows + company_domain (LEFT JOIN company ON company_id, NULL when company_id NULL); the company_domain join now backs the `--company-domain` Scope reference (resolve-then-scope §V.107, §V.115 family 1), no longer an exact Text-match filter — unknown domain -> `not_found` not silent `[]`; every FK projection stays feed-able by natural key
V7: EmailSummary projection ! include gmail_thread_id, is_routed, route_method + recipients (To/Cc/Bcc address map mirroring the Email base field) — operator audits routing from CLI w/o Logfire, a single bulk `email list` exposes each message's recipients w/o a per-row `email view`; list_emails SELECT projects recipients so the map populates (! default-empty), §V.122 keys campaign-test delivery on it
V8: contact/company views inline <= 10 latest notes (_INLINE_NOTES_CAP, 2 queries, no JOIN) + total count; ContactView/CompanyView projection = base-entity superset — ContactView carries every Contact column (incl. title + email_confidence §V.95) + company_domain (LEFT JOIN company §V.5), so `contact view` carries every `contact list` field + the singular `{"contact":{...}}` field set is verb-invariant across view/create/update/disable; view model omitting a base column -> Pydantic default extra=ignore silently strips it from `**model_dump()` construction (invisible projection drift), so view-model field set test-tracked vs base + summary; agent read_contact/read_company route through load_contact_view/load_company_view -> agent + CLI context byte-identical; _BASE protocol carries personalize-via-notes directive
V10: tag disable = soft — disabled_reason set marks disabled row, reversible via `tag enable <name>` (clears disabled_reason, disabled_reason IS NOT NULL gate blocks enabling an active tag); disabled_reason IS NULL gate blocks double-disable; vocabulary-tag disable/enable write no activity — an activity needs a contact or company owner (§V.17), a `tag` row has neither; list hides disabled unless --include-disabled; `tag disable <name>` retires a vocabulary-table (`tag`) entry (§V.116), NOT a per-owner link — unlinking an owner is the `tag remove` verb, a distinct lifecycle
V11: status payload = fixed envelope {version, schema, sync_loop, accounts, tasks, config, counts}; schema block carries three-state `verdict` in {current, pending, drift} + recorded_hash/current_hash + applied/pending migration counts (not a bare drift bool, §V.109); tasks block carries pending, failed_24h, scheduled_future, oldest_pending_age_seconds, max_attempt_count_pending
V12: IDs minted client-side via _new_id() -> UUIDv7; enrollment addressed by scalar id, composite (workflow_id, contact_id) retired from signatures
V13: tag + note target = XOR — exactly one of {contact_id, company_id} set (schema CHECK)
V14: activity + note append-only — INSERT only, no update/delete fns
V15: enrollment.status in {active, disabled} (no `paused` — only active runs §V.83, so any operator halt = disabled); disabled = operator halt, requires non-empty disabled_reason (CHECK) + enrollment_disabled activity, reversible via `enrollment enable <id>` (status disabled->active, clears disabled_reason, status<>'disabled' gate blocks enabling a live enrollment, emits enrollment_enabled activity); `enrollment disable`/`enable` are the sole halt/resume surface (NO `enrollment update` status verb); outcomes live on activity timeline via record_enrollment_outcome (accepts only completed|failed), enrollment row untouched
V16: race-safe create — UNIQUE-bearing create_X uses ON CONFLICT DO NOTHING -> None to race loser, exactly 1 row persists; bulk variants converge to shared ids; CLI surfaces duplicate_key envelope
V17: activity targets >= 1 of {contact_id, company_id}, both allowed (multi-target); list_activities enforces same
V18: schema drift = live structure diverged from `schema.sql` w/ no migration path (manual edit | DB ahead of code), hash-mismatch primitive §V.19 — distinct from `pending` (unapplied `migrations/`, §V.108); drift response tiered per §V.109 (`status`/`db check` tolerate+report, `run`+mutations dead-stop), no longer warn-only
V19: schema hash = sha256 over normalized schema.sql (strip -- comments, collapse whitespace)
V20: email.route_method NULL or in 7-value enum (schema CHECK, set per §I.cli); non-NULL -> is_routed=TRUE; NULL + is_routed=TRUE = pipeline ran, no match ("unrouted" = span-only label)
V21: background loops wake on events not timers — wakeup_event set by Pub/Sub notify + pg NOTIFY task_pending (INSERT + retry-UPDATE triggers); run_interval tick = fallback only
V22: <= 1 routing.route_email span lifecycle per email_id — History-API re-delivery + repeat sync sweep never trigger second route pass (is_routed gate); duplicate route spans inflate metrics + mask classifier regressions
V23: task drain = bounded pool <= max_concurrent_tasks; each worker owns its psycopg.Connection; atomic claim blocks re-dispatch of in-flight tasks; each worker roots its own trace — drain worker detaches the dispatching tick's sync.loop.iteration OTel context before run.execute_task (py3.14 ThreadPoolExecutor.submit propagates the active span); trace_id 1:1 w/ agent.invoke
V24: main loop never blocks on task futures — reaper collects on later ticks + emits task.drain
V25: advisory locks 2-tier — coarse (workflow_id, contact_id) + task-scoped (task_id split-half CRC32 pair); lock acquired before agent.invoke span opens (loser -> None, no span); contention -> reschedule w/o attempt_count bump, scheduled_at push fires task_pending_trigger
V26: agent.invoke span trigger attr in {enrollment_run, enrollment_schedule, task, email, manual} = caller path
V27: routing pipeline order: thread match -> RFC message-id match -> LLM classify, all account-scoped; classifier = single-turn, no tools, body truncated @ 16384 chars, hallucinated workflow_id coerced to None, zero active inbound workflows -> no LLM call; every outcome marks is_routed=TRUE w/ distinct route_method
V28: task.enrollment_id NOT NULL; workflow_id + contact_id denorm retained for filters; enrollment guaranteed @ route time via _ensure_enrollment — ON CONFLICT once, enrollment_added activity on first insert only
V29: trigger email body inlined once under "New inbound email:"; excluded from email_history — no prompt duplicate
V30: prompt framing follows trigger — first-reach-out (enrollment_run + enrollment_schedule byte-identical) vs deferred-task vs inbound; inbound email present -> email framing wins; no synthesized task_description
V31: protocol branches on trigger — trigger='task' -> terminal-outcome instruction (record_enrollment_outcome); other triggers -> initial-send-only
V32: enrollment_schedule = distinct trigger label (observability split from enrollment_run); --scheduled-at -> pending first-touch task (email_id NULL), idempotent, rejected for inbound workflows
V33: enrollment self-loop rejected — contact.email == workflow account email, case-insensitive
V34: every Drive call carries Shared-Drive flags — corpora="allDrives", supportsAllDrives=True, includeItemsFromAllDrives=True
V35: Drive KB isolation = per-account impersonation (DWD with_subject); account reads only files its identity can read; list/search filter mimeType text/markdown + trashed=false; content decoded UTF-8 errors=replace
V37: auth = service account + domain-wide delegation; file creds -> with_subject(email); ADC -> iam.Signer credentials w/ subject=email; no OAuth user login
V38: Drive tools registered sequential=True — serializes parallel dispatch (shared httplib2.Http thread-unsafe, max concurrent transport = 1); transport faults (HttpError, TimeoutError, OSError) -> structured drive_unavailable dict, never bare raise
V39: agent tool failure -> error dict {error, message}, never exception to agent; agent re-drafts via tool-error path
V40: protocol fragment naming tools ! name >= 2 distinct tools, never exactly 1
V41: KB grounding rules (search-first, 2-search budget then single list, read top >= 3 hits, per-target search budget on compare) live in the workflow definition's `instructions` field (§V.103), not a code-defined template protocol fragment; inbound-google-drive template binds the Drive tool set but carries no grounding fragment
V42: outbound body format lint — rejection algorithm + _BASE pipe-table mandate — recipe → .claude/check-extras.md §V.42
V44: agent shape owned by code-defined template registry — TEMPLATES keys == WorkflowTemplateName members; WorkflowTemplate frozen; every template carries non-empty protocol + tools + description; workflow.template + type immutable post-create (update raises ValueError), type derived from template
V45: protocol composed in canonical fragment order _BASE -> trigger branch -> _MUST_SEND -> _DECLINE -> _NO_FABRICATION; _MUST_SEND = email-universal prompt-side mirror of the §V.120 runtime guard — instructs the model to end every trigger turn in a `reply_email`/`send_email` ToolReturn or an explicit `noop`, drafting a reply in reasoning is not sending; composed into protocol_post of all three templates, lives as a template fragment not workflow.instructions — must-send = direction-universal mechanics, ! workflow-specific policy (contrast §V.41 KB grounding); every fragment workflow-neutral or email-universal — workflow-specific behavioral instructions (KB-grounding discipline, folder-specific protocol) live in workflow.instructions (§V.103), never a template fragment; template owns tool set + fragment protocol, no per-workflow fragment override; core software carries only email-universal mechanics + guardrails (composition, threading, send-guards, format glue) — workflow-specific business requirements (content correctness, grounding accuracy, domain policy) live in workflow.instructions + verified at test-time, never a bespoke core runtime guard (see §T.145); agent-facing prompt text carries zero SPEC citation — no fragment string nor model-visible tool description embeds `§V/§T/§B.<n>`; recipe → .claude/check-extras.md §V.45 (closes §B.79)
V46: template name = <direction>-<data-system>; prefix == direction field
V47: Anthropic calls set anthropic_cache_instructions=True + anthropic_cache_tool_definitions=True (classifier + workflow agent); agent.invoke span carries cache_read_input_tokens + cache_creation_input_tokens
V48: Anthropic HTTP timeout = 240s (4x httpx default); APITimeoutError + httpx.ReadTimeout classified terminal not transient — mid-turn tool side-effects make retry unsafe
V49: bounded auto-retry — 4 attempts, execute_task per-task classification, manual retry = failed/cancelled only — retry matrix → .claude/check-extras.md §V.49
V51: every logfire.exception site reachable from `mailpilot run` ! paired operator_event("error", source=..., message=...); contract test sweeps run-reachable modules for logfire.exception sites — each ! operator_event("error") in same except block
V52: logfire.configure(environment=settings.logfire_environment) -> spans carry deployment_environment; cloud queries filter by env
V53: agent tool spans come from logfire.instrument_pydantic_ai() (gen_ai.tool.name attr); no logfire.span inside agent tools; agents carry explicit names mailpilot.classifier + mailpilot.workflow
V54: every CLI mutation = logfire.span("<noun>.<verb>") + paired operator_event w/ changed-field list; psycopg constraint -> envelope code {UniqueViolation: duplicate_key, ForeignKeyViolation: foreign_key_violation, NotNullViolation: not_null_violation, CheckViolation: check_violation}; other psycopg.Error -> database_error; pydantic ValidationError -> validation_error; logfire.exception + operator_event("error") fire before envelope; non-psycopg exceptions re-raise w/o envelope; controlled output_error exit (SystemExit, the envelope path) absorbed at the cli_mutation boundary — caught inside the `with logfire.span` block (both command-body envelope + the psycopg/ValidationError translation branch), span closes clean, SystemExit re-raised after so exit 1 + stderr envelope + empty stdout (§V.3) hold; a business-outcome envelope (duplicate_key | validation_error | not_found | any output_error code) never surfaces as a Logfire error/exception (no exception.escaped on the <noun>.<verb> span); only a genuine non-SystemExit Exception marks the span (closes §B.107)
V55: tool_response span attr exempt from Logfire scrubbing; all other attrs scrubbed
V62: /release extends /gh:release — post-tag: push main + v<x.y.z>, uv build asserts dist/mailpilot-<x.y.z>-py3-none-any.whl exists, gh release create v<x.y.z> --verify-tag --notes-from-tag attaches wheel; confirm-before-mutate gate covers push + upload; version source = pyproject.toml [project].version; deploy = published wheel asset — tag-only release not deployable
V67: persisted outbound in_reply_to + references_header mirror wire MIME headers exactlyV69: tick classifying >= 1 inbound -> next tick forces full sweep + wakeup_event set
V71: per-task reply-rejection counter (reply_rejection_scope) — format_check rejections cap 3; past cap bypasses; outside scope always enforces
V72: company.profile JSONB validated vs CompanyProfile — required {summary, products, target_customers, sources} non-empty; timezone optional, null on multi-zone; malformed -> validation_error
V73: skill-body-embedded Workflow snippet runnable as authored — (a) zero free vars; (b) args-as-collection guard; (c) prose-matches-dispatch; (d) saved-workflow byte-identical — recipe → .claude/check-extras.md §V.73
V74: CSV ingestion uses RFC-4180 parser (csv module / csv.DictReader); redirect resolution = hop-agnostic curl -sL; scope .claude/skills/**; .claude/workflows/*.js excluded — recipe → .claude/check-extras.md §V.74
V75: sync incremental via History API from gmail_history_id checkpoint; history 404 -> full INBOX re-sync; first sync (last_synced_at NULL) forces full-INBOX listing regardless of watch-anchored gmail_history_id -> hydrates pre-existing INBOX state, reply-safe via §V76; checkpoint snapshot pre-fetch, last_synced_at post-store; message 404 mid-batch -> skip (deleted); transient mid-batch fetch failure (429 'Too many concurrent requests' | 5xx) -> bounded backoff retry, NEVER dropped (sibling of the 404-skip branch, not the same skip); gmail_history_id checkpoint advances only past persisted messages — a message that failed to fetch+store keeps the checkpoint behind so the next incremental sweep re-collects it (exhausted-retry batch raises, sync_account never commits a checkpoint past unstored mail); get_messages_batch fan-out bounded under Gmail's per-user concurrent-request cap (_BATCH_SIZE keeps concurrent messages.get sub-requests below the limit) (closes §B.105)
V76: routing eligibility window — received_at older than 7 days, zero active workflows, or predates earliest active workflow -> is_routed=TRUE w/ matching skipped_* route_method, no LLM call
V77: outbound email row persists only after Gmail accepts send — Gmail failure -> no orphan row; inverse send-success-then-insert-conflict (post-send create_email ON CONFLICT (gmail_message_id) DO NOTHING -> None, row already carries the id) -> recover existing row via get_email_by_gmail_message_id + return (idempotent send), never raise/orphan a delivered message; genuinely unrecoverable (no gmail id, or conflicting row vanished) -> log orphan_gmail_send + raise
V78: outbound MIME stamped X-MailPilot-Version always + X-MailPilot-Account-Id when account-bound; replies set In-Reply-To + References, References defaults to in_reply_to; send path threads via optional thread_id — `send_email` agent tool + `_wrap_send_email` + `email_ops.send_email` forward thread_id to `sync.send_email` (the CLI `email send --thread-id` path), supplied w/o explicit in_reply_to -> `_resolve_threading_headers` derives In-Reply-To/References from prior local thread rows; multi-touch outbound sequence threads natively — capture the returned gmail_thread_id on touch 1, pass it as thread_id on later touches, no contact_id requirement (unlike reply §V.79), retiring the reply_email-on-own-outbound workaround
V79: send/reply guards — disabled contact OR disabled account blocks send + reply; cold-send cooldown 30 days per (account, contact, workflow); reply requires original gmail_thread_id + contact_id (typed errors); reply subject gets "Re: " prefix unless already prefixed, case-insensitive
V80: bounce handling — sender local-part in {mailer-daemon, postmaster} (case-insensitive) or label contains "BOUNCE" -> most recent outbound in same thread + account marked bounced + contact disabled w/ "bounced:" reason prefix; unsubscribe path uses "unsubscribed:" prefix; `contact enable <ref>` clears disabled_reason regardless of prefix (operator owns consent, no unsubscribe carve-out), disabled_reason IS NOT NULL gate blocks enabling an active contact, operator-only (NOT an agent tool — the agent disables on bounce/unsubscribe, never re-enables)
V81: agent run ! call >= 1 tool; noop(reason) = explicit no-op escape; zero tool calls -> AgentDidNotUseToolsError
V82: agent email history scoped to (account_id, contact_id, workflow_id) — other workflows' mail excluded
V83: execute_task pre-flight cancels task when workflow inactive/missing, contact disabled/missing, enrollment missing or status != active
V84: pubsub notification callback acks unconditionally (decode error + missing emailAddress included); sets wakeup_event when supplied
V85: settings precedence kwargs > MAILPILOT_* env > ~/.mailpilot/config.json > defaults; config file auto-created on first load
V86: secret settings (anthropic_api_key, logfire_token, database_url) redacted as '***' in telemetry; config.set event logs key + changed flag
V87: cross-account isolation — thread + RFC message-id lookups scoped to account_id; agent read_email cross-account -> None (prompt-injection guard)
V88: entity enums enforced by schema CHECK — workflow.template/type/status, enrollment.status, email.direction/status/route_method, task.status, activity.type; value sets authoritative in schema.sql
V89: singleton rows — schema_metadata id=1, sync_status id='singleton'
V90: natural-key UNIQUE — account.email, company.domain, contact.email, workflow(account_id, name), enrollment(workflow_id, contact_id), email.gmail_message_id nullable-unique; tag.name globally unique (vocabulary row §V.116, replaces prior per-owner-active partial index), tag_assignment UNIQUE per (tag_id, owner); these natural keys = canonical CLI identifiers (account email, company domain, contact email, tag name) — case-insensitive command-line handles resolved polymorphic (§V.107), unknown key -> `not_found` (§V.94)
V91: tag/note mutation + its activity row commit in one transaction — both or neither
V92: email render = Markdown -> HTML inline styles only, no stylesheet; THEMES = {blue, green, orange, purple, red, slate}; None/unknown theme -> blue fallback
V93: operator_event -> stderr single line "HH:MM:SS event=NAME k=v ..."; newlines collapsed to space; whitespace values double-quoted, inner quotes escaped
V94: CLI FK validation precedes mutation — referenced entity missing -> error envelope, no partial write
V95: contact lead-metadata = flat columns not JSONB — contact.title TEXT NULL (role label); contact.email_confidence INT NULL, schema CHECK email_confidence BETWEEN 0 AND 100; NULL = Bouncer unknown (unbilled, unverified) = high risk; email_confidence = sole email-risk score (low = high risk); operator review filter contact list --max-email-confidence N surfaces email_confidence <= N OR IS NULL so admit-all (§V.96) never drops unknowns — SQL inequality alone (NULL excluded) is the trap; no ContactProfile model
V96: lead-contacts discovery — discover set = company.profile IS NOT NULL and contact-count < 5; <= 5 contacts/company/run; admit-all — every discovered+verified email -> contact row, low/NULL email_confidence flags risk in run summary never gates admission; persistence memoizes via contact.email UNIQUE §V.90 -> idempotent re-run skips existing, no re-discovery of known-bad addresses; discover set 1-query-expressible — CompanySummary projects contact_count (LEFT JOIN contact COUNT, incl. disabled so it tracks the memoization rule, ! active-only); company list --max-contacts N (count <= N) + --min-contacts N (count >= N) inclusive thresholds mirror §V.95 --max/--min-email-confidence; company list --has-profile --max-contacts 4 --no-tag no-contacts-found --no-tag contacts-exhausted == discover set in one call (! the N+1 has-profile + per-company contact-list probe); negative-verdict memoization — a finder cycle adding ZERO new contacts for a below-cap profiled company memoizes out, branching on the typed `reason_code` enum (CONTACT_RESULT_SCHEMA, NOT the free-text reason prefix): status=skipped + reason_code `no_decision_makers` marks `no-contacts-found`, status=skipped + reason_code `all_already_seeded` (finder re-found people but every discovered email is already a contact row, contacts_created==0) marks `contacts-exhausted`; both §V.116 tags applied at run end -> `--no-tag no-contacts-found --no-tag contacts-exhausted` drops both classes from the discover set; status=failed (transient vendor/transport err) NEVER marks (retryable), blanket contact_count==0 sweep NOT the trigger — finder verdict is; negative mirror of positive memoization (persisted contact rows §V.90) — both converge re-runs + stop re-burning Hunter/TheOrg credits on un-discoverable AND exhausted companies (closes §B.95, §B.101)
V97: lead-companies + lead-contacts run-summary completeness — batch gate capping below stale-count -> run summary carries deferred = stale-count - processed (rows left profile/contact-NULL for a follow-up run); all stale processed -> deferred 0 or omitted; bare created|enriched|seeded counts never the sole remainder signal
V98: lead-companies seed collision visibility — resolved-apex duplicate_key recorded in run-summary collapsed when incoming CSV display name diverges from the owning company's name; fires intra-batch AND onto a previously-seeded row; same-name re-seed stays silent existing; bare existing:N never the sole entity-merge signal
V99: every path `.claude/skills/**` cites resolves on disk; cited-but-absent = erroring recovery instruction — recipe → .claude/check-extras.md §V.99
V100: skill-body progressive-disclosure — live procedure inline; rarely-loaded material + sibling shared prose in `references/*.md`; body >~500 lines -> extract — recipe → .claude/check-extras.md §V.100
V101: skill-body obligation vocabulary — `.claude/skills/**` prose marks a hard requirement w/ an explicit word (`MUST` / `required`), never bare ` ! ` as must; backticked shell (`[ ! -f ]`, `!=`) + SPEC.md/telegraph register exempt. Scope = `.claude/skills/**/*.md` prose, grep ` ! ` -> zero must-sense hits — recipe → .claude/check-extras.md §V.101
V102: project-skill frontmatter hygiene — every `.claude/skills/**/SKILL.md` sets `allowed-tools` + `argument-hint`; `description` = triggering intent only; `allowed-tools` enumerates only tools the skill orchestrator body itself invokes — a tool only a spawned sub-agent calls (own tool palette, not gated by the orchestrator grant) is a zero-body-use grant, banned — recipe → .claude/check-extras.md §V.102
V103: file-based workflow defs = `workflows/*.toml`, 1 workflow/file, pure TOML (stdlib tomllib, no new dep); fields = Workflow row 1:1 {name, template, theme, goal, instructions}, instructions = TOML multi-line literal string. workflow import/export = TOML-only (! JSON, ! stdin): `workflow import --file X.toml` parses -> one row + shared validation (malformed/missing-required -> validation_error per §V.54, no partial write §V.94); `--file <dir>` globs `*.toml` (batch catalog apply, per-row errors continue); `workflow export --account-id A --out-dir D` writes 1 `*.toml`/workflow (def fields, name-sorted) + JSON status envelope on stdout (paths written) per §V.3 (! TOML on stdout); export->dir->import round-trip idempotent; `workflows/` = gitignored symlink -> independent repo kborovik/workflows @ /Users/kb/github/workflows (! submodule, no pointer); symlink resolves skill-cited `workflows/` paths on disk (satisfies §V.99); root `workflows/*.toml` (CRM defs) distinct from `.claude/workflows/*.js` (Claude Code scripts §V.73-74)
V104: mailpilot-reply-test reply-loop guard — live reply-test (`.claude/skills/mailpilot-reply-test`) requires `outbound@lab5.ca` have no active workflow; `inbound-google-drive` agent reply lands in outbound mailbox -> `skipped_no_workflows` (§V.76), no second reply; any active outbound workflow re-enters routing -> inbound<->outbound auto-reply loop; no-outbound-workflow = load-bearing test precondition not incidental
V105: mailpilot-reply-test grading model — in-scope cases graded deterministically (expected-token substring presence, `score_replies.py`, false-PASS-at-worst never false-FAIL); in-scope `expected_tokens` MUST be atomic — each token a single contiguous value (model id | bare number | number+short-unit) the reply cannot restructure away, NEVER a multi-word column header nor a value depending on verbatim-header reproduction or a particular table layout (a brittle multi-word token false-FAILs a layout-restructured grounded reply, breaking the false-PASS-at-worst contract; `_loose` salvages only number+short-unit drift); `select_cases.py` selection guard (>= 2 tokens len >= 5) keeps real signal after a brittle token splits to atomic facts; atomicity guard = allowlist (token holds iff an atomic value shape — model id | bare number | number+short-unit, optionally trailing a short qualifier like `15 ppm RO Feed`), NOT a single brittle-shape denylist — a verb-bearing sentence fragment or multi-word product description is brittle too, not only `Label (Qualifier)` table-headers; out-scope + compare cases graded by a Sonnet judge sub-agent whose verdict is of record — `score_replies.py` emits advisory signals (token_hits, fabrication_candidates, has_table) for those types but no longer decides them; judge reads {reply body, case rubric, deterministic signals, source datasheet} -> {verdict PASS|FAIL, rationale}; no deterministic-only mandate for NL-shaped grading (polite-decline, fabrication, cross-datasheet compare) — perfect det. script unachievable (closes §B.88, §B.102, §B.109)
V106: Drive search_markdown query = whitespace-tokenized, per-token `fullText contains '<token>'` OR-joined (raw token retained -> hyphenated model tried whole + split), results union+dedupe by file_id, ~8-token cap; single salient term surfaces the file; never a single whole-phrase `fullText contains '{query}'` predicate (Drive punctuation-tokenizes + AND-joins -> false-negative on hyphenated/multi-word)
V107: CLI entity reference = natural key where §V.90 defines one, UUID otherwise; resolution polymorphic — value matching UUIDv7 shape (`8-4-4-4-12` hex) resolves by id, any other value by natural key (domain carries dots, email carries at-sign — never collide), case-insensitive via existing `get_<entity>_by_<key>` helpers (`get_account_by_email`, `get_company_by_domain`, `get_contact_by_email`), unknown key -> `not_found` (§V.94, no write). Keyed entities (account=email, company=domain, contact=email, tag=name) addressed by natural key; keyless (email, note, task, workflow, enrollment — nullable/composite keys) by UUID. Every single-entity verb target = positional `<key>` arg, NEVER `--<entity>-id` option (fixes `task retry --task-id`). Scope/owner option named for owner natural key (`--company-domain`, `--contact-email`). Account-requiring cmds (`email send|reply`, `workflow create|export|import`) take a single `--account-email` (the prior `(--account-id|--account-email)` exactly-one pair collapses — one polymorphic flag resolves email | UUID, shared resolver helper); `--company-id`/`--contact-id` -> `--company-domain`/`--contact-email` everywhere they name a target | owner. `account sync` makes `--account-email` optional (all accounts omitted, the one resolved account given) + `account sync --since <iso>` bounds the initial full-INBOX backfill (Gmail `after:` on the full-listing path §V.75; incremental history syncs ignore it) — an Action-verb param, not the Lifecycle list-filter `--since` (§V.115)
V108: forward-only migration registry — `migrations/NNN_*.sql` @ package root (monotonic int prefix, no down-migrations, shipped in wheel); `db migrate` applies pending in order, each in own transaction, records `schema_migrations(version PK, name, applied_at, mailpilot_version)` (non-singleton, cf §V.89), then re-stamps `schema_metadata.schema_hash` + `mailpilot_version` to the canonical `schema.sql` hash (§V.19) on success — re-baselining even at 0-pending when every shipped migration is applied but the recorded hash is stale, so a migrated-forward populated DB resolves verdict `current` not phantom `drift` (§V.109); `schema.sql` stays canonical declarative full-schema (fresh-DB build + hash identity §V.19); identity invariant — fresh `db init` from `schema.sql` == apply-all-migrations-from-zero, byte-identical structure (test-enforced, blocks silent divergence) (closes §B.97)
V109: three-state schema verdict + tiered gate — verdict in {current, pending, drift} breaks the metadata-row-missing vs table-missing -> None collapse (`_read_schema_metadata`): ledger-behind = `pending`, hash-mismatch | manual-edit = `drift`; read-only diagnosis (`status`, `db check`) tolerates + reports; `run` + every CLI mutation dead-stops -> drift = `schema_drift` envelope, pending = `schema_migration_pending` envelope, both + exit 1 (envelope-code family §V.54); two codes since remedy differs (drift = investigate divergence, pending = run `db migrate`); fail at startup not mid-batch vs mismatched schema
V110: provisioning off the hot path — `initialize_database()` = connect + verify, not connect + provision; empty-DB auto-provision survives only when `account` table absent (data-loss-free, keeps `make clean` + test fixtures ergonomic); populated DB never mutates structure as a connection side-effect; `db init` (provision empty, refuses if `account` exists, no `--force` footgun) + `db migrate` (advance populated) are the explicit forward paths, displacing `make clean` (drop + re-apply, total wipe) as the sole populated-schema advance
V111: operator-facing CLI help carries zero SPEC citation — every Click command/group `--help` (docstring-derived long+short help + option `help=` text) renders free of `§V/§T/§B.<n>`; guard walks the command tree, scans each rendered help -> zero `§[VTB]\.[0-9]+` hits; operator-facing twin of §V.45 (agent-prompt text), CLI `--help` surface not template registry (closes §B.91)
V112: lead-companies scoped-invocation enrich-scope — domain/URL/UUID args enrich ONLY rows resolved or seeded this run, never the global profile-NULL backlog; seed_companies.py emits seeded_stale (rows created/matched this run via touched_apexes accumulator) distinct from global stale; fast path feeds seeded_stale for domain/URL-token runs, stale for file/bare runs; global stale set never the dispatch fan-out for a scoped arg (closes §B.92)
V113: contact-finder email verify — Bouncer real-time single GET `/v1.1/email/verify?email=` ONE call/email (<=5/company; per-email billing so == batch credit cost), NEVER POST `/email/verify/batch/sync`; empty body | 4xx/5xx | missing status = verify FAILURE (retry once, then NULL w/ noted reason), never a clean Bouncer status="unknown"; guard greps contact-finder.md = batch/sync absent + single-verify present
V114: company soft-disable — company.disabled_reason TEXT NULL (non-NULL = disabled, carries human reason); `company disable <id> --reason <text>` sets it, disabled_reason IS NULL gate blocks double-disable (mirrors §V.10); `company list` hides disabled unless `--include-disabled`; `company enable <ref>` clears disabled_reason (mirror of disable, disabled_reason IS NOT NULL gate blocks re-enabling an active company), part of the uniform disable/enable verb pairing across company|contact|tag|enrollment (§V.10/§V.15/§V.80); operator-only surface (out-of-business/not-a-fit) — the lead-contacts negative-verdict memoization moved to the `no-contacts-found` tag (§V.96, §V.116)
V115: CLI `list` filter flag = closed six-family taxonomy, each family fixed naming + semantics — (1) Scope: named for parent natural key (`--company-domain`, `--contact-email`) | `--<noun>-id <UUID>` for keyless parent (`--workflow-id`), resolves polymorphic (§V.107), validates via `get_<entity>` -> `not_found` on absent parent (§V.94); (2) Enum: `--<axis>` type=click.Choice mirroring schema CHECK set (§V.88), never free string (fixes `--route-method`); (3) Range: `--min-<field>`/`--max-<field>`, both inclusive + composable, NULL-incl where nullable + meaningful (§V.95); (4) Presence: `--has-<field>/--no-<field>` single tri-state default=None (Click derives `has_<field>` param from positive side -> no `--no-has-<field>` artifact), replaces two-flag XOR; (5) Text-match: field-named (`--title`, `--from`, `--to`), exact only on `list`, case-fold per natural-key semantics (§V.90), substring/fuzzy -> `search` verb only (`--company-domain` no longer Text-match — it = Scope ref §V.107, unknown domain -> `not_found` not `[]`); (6) Lifecycle: `--include-disabled` (is_flag default False, §V.10/§V.114) + `--since`/`--until <ISO>` closed inclusive interval `[since,until]` over one declared column (`created_at` default; `updated_at` enrollment; `scheduled_at` task), help= names the column, every list offers both. `--limit <int>` (default 100) = sole result control (no `--offset`/`--order-by`/`--desc`), alongside filters but not a filter. `--direction` = single canonical inbound/outbound axis name across email + workflow + template (workflow `--type` renamed, no back-compat alias; internal `workflow_type` may stay). Families realized as shared Click decorators (`limit_option`, `time_window_options(col)`, `include_disabled_option`, `scope_option`, `enum_option`, `range_options`, `presence_option`) composed fixed-order in `cli.py`/`_filters.py`; new list flag = vocabulary decorator | spec change; help= free of §-cites (§V.111)
V116: tags = operator-maintained controlled vocabulary, two tables — `tag` (vocabulary, one row/defined tag, `name` globally unique §V.90, soft-delete via disabled_reason §V.10) + `tag_assignment` (link, one row/(tag, owner), owner XOR company|contact mirroring §V.13); `tag add --tag <name> (--company-domain <d> | --contact-email <e>)` errors `not_found` on undefined tag, NEVER auto-creates (Enum-family rule §V.115 applied to data); `tag create <name>` defines an enum value, `tag disable <name>` retires it (§V.10), `tag remove` unlinks (inverse of add, retires neither side); `tag list` = vocabulary + projected `usage_count`, owner-free, `tag list --company-domain <d>` lists one owner's tags; `--tag <name>` resolves through unique name (§V.107, operators never paste tag ids). `company list --tag <name>` + `contact list --tag <name>` = Enum-family membership filter over assignment join; `company list --no-tag <name>` = negated membership filter, repeatable (each `--no-tag` one negated-membership predicate, all intersected) — the lone exception to intersection-only composition (no `--or`/`--not`/`--exclude`), exists for memoization: lead-contacts marks a below-cap profiled company `no-contacts-found` after a zero-decision-maker finder cycle and `contacts-exhausted` after an all-already-seeded one, drops both from the next discover set via `--no-tag no-contacts-found --no-tag contacts-exhausted` (§V.96); `--no-tag` resolves through vocabulary (undefined -> `not_found`), composes w/ `--tag` as intersection (carries A not B); tag carries `name` only (category/description/hierarchy deferred)
V117: batch-gate option distinctness — every gate option maps to a distinct batch at the current stale-count; fixed-cap option suppressed when stale-count <= cap (`First 24` dropped at stale-count <= 24, == `All <N>` there); `First 9` always distinct (gate fires only at stale-count > 9); rule stated once in `references/lead-pipeline-conventions.md` Batch-gate § per §V.100, both sibling skills /lead-companies + /lead-contacts apply it; pre-cap option construction, sibling to post-cap run-summary §V.97 (closes §B.98)
V118: account soft-disable — account.disabled_reason TEXT NULL (non-NULL = disabled, carries human reason); `account disable <ref> --reason <text>` sets it (disabled_reason IS NULL gate blocks double-disable), `account enable <ref>` clears it (disabled_reason IS NOT NULL gate blocks enabling an active account), part of the uniform disable/enable verb pairing (§V.10/§V.15/§V.80/§V.114); a disabled account is gated everywhere it would touch Gmail — sync loop skips it, `account sync` all-accounts mode skips it, `renew_watches()` skips it, send + reply refuse it (§V.79); `account list` default-hides disabled, `--include-disabled` opts in (mirror §V.114); operator-only (the agent never disables or enables an account)
V119: destructive DB op (make clean, any dropdb) backs up first — make db-backup runs one `mailpilot db export --file ~/Documents/MailPilot/snap-<ts>.json` (single JSON snapshot bundle = tag vocabulary + company + contact, §V.121), CLI writes the file + exit 0 before any dropdb, failed export aborts the drop (fail-closed, no `|| true` swallow); restore = `mailpilot db import --file <snap>`, agent-operable by /mailpilot-campaign-test; company + contact = paid live data (discovery credits §V.96, §V.113), never wiped without a durable backup; backup dir ~/Documents/MailPilot/ iCloud-synced (portable across macOS instances), outside the dropped DB
V120: trigger run sends OR takes an explicit noop — every send-obligated trigger turn MUST leave a `reply_email`|`send_email` ToolReturnPart w/o `error` key in the ledger, else a successful `noop` ({acknowledged: true}) OR a `conclude_enrollment` terminal (§V.127); send-obligated = inbound (`email is not None` + trigger in {email, task}, §V.26) OR outbound first reach-out (trigger in {enrollment_run, enrollment_schedule}, `email is None`); inbound answers via `reply_email`, outbound opens via `send_email`, either may instead `noop` to decline or `conclude_enrollment` to end the sequence (§V.127); `manual` exempt (direct-programmatic/test path); strengthens §V.81 (>=1 tool) to >=1 tool that is the send (model calling read/search tools then narrating the message body in `<thinking>` w/o a `send_email`|`reply_email` call satisfies §V.81 yet sends nothing); run-loop guard `_sent_reply(result)` walks `result.all_messages()` (mirrors `_extract_tool_errors`) after the §V.81 tool-count check, neither send nor noop nor conclude_enrollment -> `AgentCompletedWithoutReplyError`; non-transient (§V.49 is_transient False on the unrecognised class) -> task terminal `failed` + `operator_event("error")` via `_handle_agent_failure`, NEVER silent `completed`; decline = explicit noop only, a dropped send is a failure not a success; prompt-side preventive = `_MUST_SEND` template fragment (§V.45) (closes §B.103, §B.106)
V121: db-level snapshot bundle (`db export`/`db import`, §I.cli db noun) — `db export --file <path>` writes one JSON bundle to disk + `{"db":{path, companies:N, contacts:M, tags:K}, "ok":true}` status to stdout (singular envelope like `db check` §V.3, ! the plural export/import array); `db import --file <path>` reads the bundle, restores fixed code order tags-vocabulary -> companies -> contacts, same status envelope. Bundle = `{schema_version:int, exported_at:ts, tags:[{name, disabled_reason}], companies:[{...profile, disabled_reason..., tags:[name,...]}], contacts:[{...title, email_confidence, disabled_reason..., company_domain, tags:[name,...]}]}`. Scope = tag vocabulary + company + contact tables ONLY — emails, activities, notes, workflows, enrollments, tasks, accounts excluded. Tags embed under owner row; vocabulary rides own `tags` section so a disabled|unassigned tag survives the round-trip; vocabulary-first restore lets each assignment resolve by name w/o auto-create (§V.116 Enum-family forbids tag auto-create). Every link resolves by natural key — company domain, contact email, tag name; source-DB UUID NEVER forwarded, contact re-links to company by domain (§V.90) NEVER the exported company_id (carries the §B.104 lesson into the bundle). Per-row errors continue the batch — FK-unresolvable row records a per-row error entry, NEVER a batch-aborting raise. `db export` read-only + drift-tolerant (like `db check`); `db import` = mutation, dead-stops on drift|pending -> `schema_drift`|`schema_migration_pending` envelope, no partial write (§V.109). export->fresh-import round-trip field-identical on a fully-populated company+contact+tags (test-enforced) — restored company carries profile, restored contact carries title + email_confidence, both carry tags; replaces per-entity company/contact export/import (removed) — `company view`/`contact view` cover ad-hoc single-entity reads
V122: mailpilot-campaign-test delivery verification keys each arrival on its unique recipient alias, never the agent-written subject — sequence N owns alias inbound{N}@lab5.ca (one send each, §I aliases inbound1..9@lab5.ca), the alias rides the received To header (preserved through catch-all routing into inbound@lab5.ca), so verify_delivery.py builds an alias->sequence map from the single bulk `email list` recipients projection (§V.7) + counts a sequence delivered when its alias appears; subject = agent-generated + collision-prone, NEVER an identity key — two sends sharing one subject both count delivered; verdict PASS when every sent sequence's alias arrives; guard = verify_delivery.py matches on alias (! the retired {subject: sequence} dict) + a regression test (two sends one subject -> both delivered) (closes §B.108)
V123: reply-cancels-followups — inbound reply routing to an enrollment cancels that enrollment's pending future follow-up tasks; routing.route_email on a successful inbound match (workflow_id resolved + contact_id present) bulk-cancels task rows WHERE enrollment_id matches AND status='pending' AND scheduled_at > now(), EXCLUDING the operator first-touch (context->>'trigger' = 'enrollment_schedule' §V.32 preserved — agent create_task follow-ups may also carry email_id NULL so first-touch keys on the trigger label NOT email_id); status pending->cancelled mirrors cancel_task; platform safety net (firing guards §V.83 carry no terminal-outcome check, record_enrollment_outcome activity-only §V.15) so a replied prospect never receives a later scheduled cold touch; per-touch self-guard (pre-send history scan) stays valid; NO new agent tool, NO new CLI verb (closes §B.110)
V124: workflow.goal (renamed from objective via migration 006 §V.108) = workflow success condition — observable outcome that concludes the enrollment once reached (e.g. prospect books a Google Meet); free-text prose, agent-interpreted, ONE field two readers: (1) conclusion — agent calls conclude_enrollment (designs/calendar-meetings.md) when it judges goal met -> disposition meeting_booked or unreachable -> do_not_contact|contact_later; system concludes deterministically when a hard signal proves goal met (the calendar booking concludes every active outbound enrollment regardless of stated goal); (2) classification — goal stays classify.py semantic-match key for inbound workflow routing (§V.76); record_enrollment_outcome stays the internal recorder (§V.15), conclude_enrollment the agent terminal — the two compose; `_DEFERRED_TASK_TASK` fragment (§V.45) names the goal not an objective; structured/typed goal deferred (disposition enum carries the structured terminal)
V125: meeting = first-class entity peer to email — `meeting` table cols {id, google_event_id, meet_url, summary, scheduled_at, ends_at, status, created_at, updated_at}, google_event_id nullable-unique (idempotent ingest, mirrors email.gmail_message_id §V.90), status CHECK in {scheduled, completed, cancelled, no_show}; `meeting_attendee(meeting_id, contact_id)` link table UNIQUE per pair (mirrors tag_assignment §V.116), one meeting links >=1 attendee, attendees matched to contacts by email (unmatched email = no link); status col = operator record-keeping only, gates NOTHING — booking conclusion (§V.128) fires at booking regardless of later completed|no_show
V126: CalendarClient (`calendar.py`, mirror shape GmailClient/DriveClient §I) reads Google Calendar — service account + DWD, per-account with_subject(email), scope `calendar.events.readonly` (§C, first scope beyond gmail.modify + drive.readonly); sync loop polls upcoming events on run_interval tick (§V.21 fallback only — event-wake push channels deferred #154), upserts one `meeting` row/event idempotently on google_event_id (re-poll = no dup row §V.125) + links each email-matched attendee to a contact via meeting_attendee; read-only — NO event create|update from the app
V127: conclude_enrollment(disposition, note, reschedule_at) = single agent-facing terminal tool, disposition in {meeting_booked, do_not_contact, contact_later} — minimal-decision-surface: model picks ONE disposition + writes a note, system runs the side effects deterministically; meeting_booked -> record_enrollment_outcome (§V.15) + cancel_enrollment_followup_tasks (§T.182) + booking note (the "I booked" reply path, distinct from calendar detection §V.128); do_not_contact -> conclude + cancel + disable_contact (§V.79, §V.80) + note; contact_later -> conclude + cancel + scheduled re-enrollment task at reschedule_at (agent-supplied, default >=3 months out when omitted, self-fires §V.32 over an inert timestamp column) + note; counts as a valid send-obligation terminal like noop (§V.120); record_enrollment_outcome leaves the agent tool set -> system-internal recorder (§V.15, §V.124), conclude_enrollment the sole agent terminal
V128: calendar booking concludes enrollments deterministically, NO agent turn — for each attendee contact (§V.125) holding an active outbound enrollment the system concludes via record_enrollment_outcome (§V.15) + cancels pending future follow-ups via cancel_enrollment_followup_tasks (§T.182) + writes a system booking note; fan-out fires for EVERY active outbound enrollment the attendee holds — a booked meeting outranks any cold sequence regardless of stated goal (§V.124); cancel_enrollment_followup_tasks now fires from three sites — inbound reply routing (§V.123), calendar booking ingestion (§V.126), conclude_enrollment (§V.127) — first-touch exclusion (§V.32) holds at every site

## §T TASKS

## archived: §T.109..§T.132 → SPEC.archive.md (23 rows)
id|status|task|cites
T137|x|impl §V.23 per §B.82 — detach OTel ctx in `_execute_task_in_worker`; fresh trace root per agent.invoke|V23,V52,B82
T138|x|add mailpilot-reply-test skill — live e2e reply-test (outbound@lab5.ca->inbound@lab5.ca, `inbound-google-drive` auto-reply, grade vs QA-Pairs.json, Logfire tokens+latency, Opus failure escalation); replaces removed test-google-drive; reply-loop guard|V104,V99,V100
T139|x|impl §V.42(+) per §B.83 — strip permissive "may use Markdown" line from `_BASE` in templates.py; extend guard test to assert `rg 'may use Markdown'` zero hits|V42,B83
T140|x|impl §V.45(+) per §B.84 — strip §-cites from the six registered tool docstrings; broaden guard test + check-extras §V.45 recipe to grep `§[VTB]\.[0-9]+` over per-tool docstrings (model-visible descriptions), not just composed protocol|V45,B84
T141|x|impl §V.102(+) per §B.85 — add `allowed-tools` + `argument-hint` to mailpilot-reply-test SKILL.md frontmatter|V102,B85
T142|x|impl §V.102(+) per §B.86 — drop `mcp__claude_ai_logfire__query_run` from mailpilot-reply-test SKILL.md `allowed-tools` (orchestrator never invokes it; Phase-4 sub-agent does, own palette)|V102,B86
T143|x|impl §V.77(+) per §B.87 — send_email recovers the existing row via get_email_by_gmail_message_id on post-send create_email ON-CONFLICT None (idempotent send), raises only when genuinely unrecoverable; add test_send_email_recovers_existing_row_on_duplicate_gmail_message_id|V77,B87
T144|x|impl §V.105(+) per §B.88 — refactor score_replies.py out-scope+compare to advisory signals; Sonnet judge as verdict-of-record for NL-shaped cases; update references/grading.md + SKILL.md scoring phase|V105,B88
T145|x|abolish runtime fact-check per §V.71 amend — remove _fact_check_body + send_email/reply_email call sites + read_ledger plumbing + reply_rejection fact_check half + check-extras fact-check recipe; numeric-spec grounding caught at test-time via reply-test grading (§V.105) not runtime|V71,V42,V105
T146|x|impl §V.106(+) per §B.89 — search_markdown tokenizes query on whitespace, per-token `fullText contains` OR-joined (raw token retained), union+dedupe by file_id, ~8-token cap; add pytest-httpx test asserting hyphenated/multi-word query emits OR predicates + unions results|V106,B89
T147|x|impl §V.103(∆) — workflow import/export TOML-only; drop JSON+stdin paths; add workflow export --account-id --out-dir|V103
T148|x|impl §V.107(+) — shared _resolve_account_id; --account-email on email send|reply + workflow create|export|import|V107
T149|x|impl §V.75(∆) per §B.90 — first sync forces full path when last_synced_at NULL|V75,B90
T150|x|impl §V.108(+) — migrations/ registry + schema_migrations ledger; db migrate applies pending; identity test|V108
T151|x|impl §V.109(+) + §V.11(∆) + §V.18(∆) — three-state schema verdict; run+mutation dead-stop on drift/pending|V109,V11,V18
T152|x|impl §V.110(+) + §I db noun — initialize_database connect+verify; db init|migrate|check CLI|V110
T153|x|impl §V.111(+) per §B.91 — strip §-cites from Click command/group docstrings + option `help=` strings in cli.py (db group + init/migrate/check, company profile help=, etc.); add guard test walking the Click tree asserting each rendered `--help` carries zero `§[VTB]\.[0-9]+`|V111,B91
T154|x|impl §V.112(+) per §B.92 — seeded_stale distinct from global stale in seed_companies.py; SKILL.md fast-path selects by arg type|V112,B92
T155|x|impl §V.113(+) per §B.93 — contact-finder.md verify step -> Bouncer real-time single GET /email/verify (per-email, <=5), drop batch/sync; empty|4xx/5xx|missing-status = verify-failure not clean unknown; add guard test grepping contact-finder.md (batch/sync absent, single-verify present)|V113,B93
T156|x|impl §V.8(∆) per §B.94 — extend ContactView w/ title + email_confidence + company_domain; verb-invariant test|V8,B94
T157|x|impl §V.96(∆) — CompanySummary + list_companies contact_count; --max-contacts/--min-contacts|V96
T158|x|impl §V.114(+) + §V.96(∆) per §B.95 — company.disabled_reason col + migration; `company disable` verb + double-disable gate; `company list` default-excludes disabled + --include-disabled; Company/CompanySummary models; lead-contacts disables company on contact-finder zero-genuine verdict (status=skipped no-DM reason, NEVER status=failed) w/ no_contacts_found:<date> at run end + SKILL.md prose|V114,V96,B95
T159|x|impl §V.108(∆) per §B.97 — `migrate_database` re-stamps `schema_metadata.schema_hash` + `mailpilot_version` to canonical hash as final step of successful migrate, re-baselines @ 0-pending when all migrations applied but recorded hash stale -> verdict `current`; regression test (init at stale hash -> migrate -> assert verdict current, not drift)|V108,B97
T160|x|impl §V.115(+) — six-family filter taxonomy + shared Click decorators (`limit_option`, `time_window_options`, `include_disabled_option`, `scope_option`, `enum_option`, `range_options`, `presence_option`); retrofit all 11 list cmds one pass — rename `--type`->`--direction`, `--route-method`->click.Choice, `--title` exact (substring -> search), add `--until` everywhere, company presence -> `--has-profile/--no-profile` tri-state|V115,V88,V20,V95,V96,V114,V111
T161|x|impl §I.cli verb+target fixes — `task retry` target positional `<task_id>`, `activity create` -> `activity add`, verb set gains `remove`|V107
T162|x|impl §V.107(∆) natural-key sweep — keyed-entity verb targets + Scope/owner options accept natural key (UUID still resolved), `(--account-id|--account-email)` pair collapses to single `--account-email`, `--company-id`/`--contact-id` -> `--company-domain`/`--contact-email`, `--company-domain` moves Text-match -> Scope, `account sync` optional `--account-email` + `--since` backfill bound (§V.75)|V107,V90,V5,V75,V94
T163|x|impl §V.116(+) tag controlled vocabulary — `tag` + `tag_assignment` two-table schema + forward migration (each distinct name -> one vocabulary row §V.90, each existing tag row -> one assignment, §V.108); redesigned `tag create|view|disable|add|remove|search|list`; `usage_count` projection; `company|contact list --tag`/`--no-tag` filters|V116,V90,V10,V108
T164|x|sweep lead-pipeline skills + `scripts/seed_companies.py` onto the standard — domain resolution -> exact `company view <domain>` (drop fuzzy `company search "<arg>" --limit 1` + `fetch_owner_name` client filter), propagate renamed flags into skill prose|V112,V96,V99
T165|x|impl §V.117(+) per §B.98 — `references/lead-pipeline-conventions.md` Batch-gate §: state distinct-batch-option rule + drop `First 25` when stale-count <= 25; sibling SKILL.md cap-example prose stays aligned per §V.100|V117,V100
T166|x|align lead-pipeline create-duplicate detection to §V.3 per §B.99 — error envelope (duplicate_key/already_exists) rides stderr not stdout: seed_companies.py classify the duplicate by exit 1 + stderr envelope not proc.stdout, correct lead-pipeline-conventions.md + contact-finder.md + sibling SKILL.md prose (drop "envelope on stdout" + "capture stdout only" for the error case)|V3,B99
T167|x|impl §V.10(∆) + §V.15(∆) + §V.80(∆) + §V.114(∆) per §B.100 — enable mirror for every disable verb: `enable_company`/`enable_contact`/`enable_tag` clear disabled_reason + `enable_enrollment` flips status disabled->active + clears disabled_reason in database.py (each gated on currently-disabled, mirrors double-disable gate); cli.py `company|contact|tag|enrollment enable` verbs emit `<entity>.enable` event + enable activity where disable writes one (tag_enabled/enrollment_enabled); contact enable clears any reason (no unsubscribe carve-out), operator-only not an agent tool; correct `company disable`+`update_company` docstrings naming `company update` as re-enable path|V10,V15,V80,V114,B100
T168|x|collapse enrollment to {active, disabled} per §V.15(∆) — drop `paused`: schema.sql enrollment.status CHECK IN ('active','disabled') (was active/paused/disabled), migration `004_*.sql` UPDATE status='paused'->'disabled' w/ literal disabled_reason then ALTER the status CHECK (§V.108 byte-identity: fresh init == migrate-from-zero); remove `enrollment update` command + active/paused toggle path in cli.py + `update_enrollment`; stop emitting `enrollment_paused`/`enrollment_resumed` (disable/enable emit enrollment_disabled/enabled; retain both activity.type enum values for historical rows)|V15,V83,V108,V88
T169|x|impl §V.118(+) + §V.79(∆) — account soft-disable: `disable_account`/`enable_account` in database.py (set/clear disabled_reason, disabled_reason IS NULL / IS NOT NULL gates), list_accounts default-excludes disabled + `include_disabled` param, cli.py `account disable <ref> --reason`/`account enable <ref>` verbs (mirror company) + `account list --include-disabled`; gate disabled accounts out of the sync loop + `account sync` all-accounts mode + `renew_watches()` (sync.py) + send/reply guard (§V.79); register `enable` in §I verb set; no migration (column exists)|V118,V79
T170|x|drop dead `tag_disabled` activity.type value — never emitted (vocabulary-tag disable/enable write no activity per §V.10/§V.17), no historical row carries it (unlike §T.168 enrollment retain); remove from schema.sql activity CHECK + models.py ActivityType + cli.py _ACTIVITY_TYPES; migration 005 ALTERs the CHECK; §V.108 byte-identity (fresh init == migrate-from-zero)|V10,V17,V108
T171|x|impl §V.96(∆) + §V.116(∆) per §B.101 — generalize negative-verdict memoization to the below-cap all-already-seeded case: add typed `reason_code` enum (`no_decision_makers` | `all_already_seeded` | transient) to CONTACT_RESULT_SCHEMA in `lead-contacts-find.js` + the §V.73 byte-identical skill-mirror snippet + `contact-finder` agent verdict; memoization stage branches on reason_code not the free-text reason prefix — `no_decision_makers` -> tag `no-contacts-found`, `all_already_seeded` (contacts_created==0) -> tag `contacts-exhausted`, status=failed NEVER tags; skill defines BOTH vocabulary tags up front via idempotent `tag create`; stale query gains the second exclusion `--no-tag no-contacts-found --no-tag contacts-exhausted`; `company list`/`contact list --no-tag` repeatable (Click multiple=True -> `exclude_tag` becomes `exclude_tags` list, one NOT EXISTS per tag) in cli.py + list_companies/list_contacts; run-summary `memoized` counts both tag classes|V96,V116,B101
T172|x|impl §V.119 — make db-backup target (mailpilot company export + contact export to ~/Documents/MailPilot/, timestamped JSON, fail-closed no `|| true`); make clean depends on db-backup, runs before dropdb|V119
T173|x|impl §V.105(∆) per §B.102 — split brittle multi-word in-scope token in QA-Pairs.json qa-in-015 (`Maximum (15 ppm RO Feed)` -> `15 ppm RO Feed`); add guard asserting every in-scope `expected_tokens` entry atomic (reject header-shaped `Label (Qualifier)` / multi-word-layout-dependent token)|V105,B102
T174|x|impl §V.120(+) per §B.103 — run-loop reply guard: add `_sent_reply(result)` next to `_extract_tool_errors` in `src/mailpilot/agent/invoke.py` (walks `result.all_messages()` for a `reply_email`|`send_email` ToolReturnPart w/o `error` key, a successful `noop` ledger entry counts as explicit decline); after the §V.81 tool-count check raise new `AgentCompletedWithoutReplyError` (add to `src/mailpilot/exceptions.py`) when an inbound trigger (`email is not None`, trigger in {email, task}) left neither; class unrecognised by `is_transient` (`src/mailpilot/agent/retry.py`) -> `_handle_agent_failure` (`src/mailpilot/run.py`) takes it terminal `failed` + `operator_event("error")`; reinforce the `reply_email` instruction in `workflows/mailpilot-demo.toml` (drafting reply text in reasoning is not sending; instruction later lifted to the `_MUST_SEND` template fragment per §V.45, §T.175)|V120,B103
T175|x|impl §V.45(∆) — lift terminal-reply instruction from workflow.instructions to a code-defined `_MUST_SEND` template fragment: add `_MUST_SEND` constant in `src/mailpilot/agent/templates.py` (end every trigger turn in a `reply_email`/`send_email` tool call or explicit `noop`; drafting in reasoning is not sending; no §-cite in the model-visible string per §V.45), compose into protocol_post for all three templates in order `_BASE -> trigger branch -> _MUST_SEND -> _DECLINE -> _NO_FABRICATION`; delete the `# Sending the reply` block from `workflows/mailpilot-demo.toml` (submodule) so the instruction has one home; extend the protocol-composition contract test to assert `_MUST_SEND` present + ordered|V45,V120
T176|x|impl §V.121(+) + §V.119(∆) per §B.104 — `db export`/`db import` snapshot bundle (tag vocabulary + company + contact): export writes one JSON bundle `{schema_version, exported_at, tags, companies w/ embedded tags, contacts w/ company_domain + embedded tags}` + `{"db":{...}}` status; import restores tags -> companies -> contacts by natural key (domain/email/tag name, never the source company_id), per-row error continues the batch, dead-stops on drift|pending (§V.109); drop per-entity `company`/`contact` export+import commands; make db-backup -> one `db export --file`; add export->fresh-import round-trip field-identical test (fully-populated company+contact+tags)|V121,V119,B104
T177|x|impl §V.75(∆) per §B.105 — get_messages_batch classifies the batch callback exception (404 skip, transient 429|5xx retain + bounded-backoff retry, exhausted -> raise) so per-sub-request rate-limits are never dropped; lower _BATCH_SIZE under Gmail's per-user concurrent-request cap; sync_account never advances gmail_history_id past unstored messages (propagate the exhausted-retry raise so the checkpoint stays behind + the next sweep re-collects); add tests — pytest-httpx 429-then-200 mid-batch re-fetches + stores the message, and checkpoint-unchanged-on-fetch-failure regression|V75,B105
T178|x|impl §V.120(∆) per §B.106 — widen the run-loop send-guard from inbound-only to email-universal: the `AgentCompletedWithoutReplyError` gate in `src/mailpilot/agent/invoke.py` (now `email is not None and trigger in {email, task} and not _sent_reply(result)`) also fires on an outbound first reach-out (`email is None` + trigger in {enrollment_run, enrollment_schedule}) that left no `send_email` + no `noop`; `_sent_reply` already accepts `send_email`|`reply_email`|`noop` so the walker is unchanged, only the trigger gate widens; `manual` stays exempt; add failing tests — outbound enrollment_run run calling only read tools (no send_email) raises `AgentCompletedWithoutReplyError` + `_handle_agent_failure` takes it terminal `failed` (non-transient via is_transient), plus a control where the outbound run calls send_email + completes|V120,B106
T179|x|impl §V.54(∆) per §B.107 — absorb the controlled output_error SystemExit at the cli_mutation boundary (`src/mailpilot/operator_log.py`): catch SystemExit inside the `with logfire.span` block for both the command-body envelope path and the psycopg/ValidationError translation branch, let the span close clean, re-raise after so exit 1 + stderr envelope + empty stdout hold; only a genuine non-SystemExit Exception still marks the span; extend the account.create duplicate-key telemetry test to assert the parent `account.create` span carries no exception event (is_exception false), not just the absence of a `.failed` child|V54,B107
T180|x|impl §V.7(∆) + §V.122(+) per §B.108 — add `recipients` to EmailSummary (models.py) + list_emails SELECT/projection so `email list` rows carry the To/Cc/Bcc map; rewrite `.claude/skills/mailpilot-campaign-test/scripts/verify_delivery.py` to build an alias->sequence map (sends[].alias, §I aliases) from the bulk listing recipients + match each arrival's alias, retiring the `{subject: sequence}` dict; EmailSummary-recipients projection test + two-sends-one-subject both-delivered regression test|V7,V122,B108
T181|x|impl §V.105(∆) per §B.109 — flip `_is_brittle_inscope_token` to allowlist (atomic = model id | bare number | number+short-unit + optional short trailing qualifier; reject verb-bearing sentence fragment + multi-word product description, not only `Label (Qualifier)`); atomize QA-Pairs.json qa-in-006 (-> `over 99%`, `98%`) + qa-in-009 (-> `40`) in-scope tokens, keep qa-in-015 `15 ppm RO Feed`; extend `test_inscope_expected_tokens_are_atomic` w/ sentence-fragment + product-description coverage; a case whose atomic split drops below the `select_cases.py` >=2-tokens-len>=5 guard falls out of in-scope grading (acceptable per false-PASS-at-worst)|V105,B109
T182|x|impl §V.123(+) per §B.110 — add cancel_enrollment_followup_tasks(connection, enrollment_id) -> list[Task] in database.py (UPDATE task SET status='cancelled', completed_at=CURRENT_TIMESTAMP WHERE enrollment_id=%(id)s AND status='pending' AND scheduled_at > CURRENT_TIMESTAMP AND COALESCE(context->>'trigger','') <> 'enrollment_schedule' RETURNING *); call from routing.route_email on every successful inbound match after the enrollment resolves — NOT only the first-insert branch of _ensure_enrollment (reply case = enrollment pre-exists, create_enrollment returns None §V.28) so resolve via get_enrollment(workflow_id, contact_id) then cancel; failing tests — reply cancels a future agent follow-up (email_id NULL + set), preserves the enrollment_schedule first-touch, skips already-due + non-pending tasks, no-op when contact_id None|V123,B110,V28,V32
T183|x|impl §V.78(∆) per GH #152 — expose outbound thread-continuation on send_email: add optional thread_id to `_wrap_send_email` (agent/invoke.py) + `send_email` (agent/tools.py) + `email_ops.send_email`, forwarded to existing `sync.send_email(thread_id=...)`; no contact_id requirement; agent captures returned gmail_thread_id on touch 1, passes as thread_id on later touches; add test — send_email w/ thread_id derives In-Reply-To/References from prior row + returns the same gmail_thread_id|V78
T184|x|fold-in §V.124(+) + §V.103(∆) per designs/workflow-goal.md — rename workflow.objective -> goal everywhere + define the goal §V: migration `006_rename_workflow_objective_to_goal.sql` (`ALTER TABLE workflow RENAME COLUMN objective TO goal`, byte-identity §V.108), schema.sql col `goal TEXT NOT NULL DEFAULT ''`, models.py `Workflow.goal`, database.py (search_workflows SQL + update_workflow allowed set + `_WORKFLOW_IMPORT_UPDATABLE`), cli.py (`--objective`->`--goal` on workflow create|update, help + validation + TOML export/import), classify.py (column read + prompt text + dict key), invoke.py (`Goal:` prompt line), templates.py `_DEFERRED_TASK_TASK` -> "After achieving the workflow goal", tools.py + routing.py comment/prompt text; workflows/*.toml `objective`->`goal` (mailpilot-demo + ai-engineering, commit+push detached repo); tests over CLI flag + model field + TOML round-trip + classify + invoke prompt|V124,V103,V108
T185|x|fold-in §V.125(+) per designs/calendar-meetings.md — schema: `meeting` table (cols id, google_event_id nullable-unique, meet_url, summary, scheduled_at, ends_at, status CHECK in {scheduled, completed, cancelled, no_show}, created_at, updated_at) + `meeting_attendee(meeting_id, contact_id)` link table UNIQUE per pair; migration `007_add_meeting_tables.sql` (byte-identity vs schema.sql §V.108) + schema.sql + models.py (Meeting + MeetingAttendee) + database.py (create_meeting/get_meeting/list_meetings/upsert_meeting keyed on google_event_id + link_meeting_attendee per create_X/get_X/list_X conventions); tests over idempotent upsert (re-insert same google_event_id = no dup row) + attendee-pair UNIQUE|V125,V108
T186|x|fold-in §V.126(+) + §V.128(+) per designs/calendar-meetings.md — CalendarClient in `calendar.py` (mirror GmailClient/DriveClient — service account + DWD, with_subject, scope calendar.events.readonly §C) lists upcoming events; sync loop polls on run_interval tick (§V.21 fallback) + upserts one meeting row/event idempotently (§V.125) + links email-matched attendees; booking-conclusion fan-out — each attendee contact w/ an active outbound enrollment -> record_enrollment_outcome (§V.15) + cancel_enrollment_followup_tasks (§T.182) + system booking note, fires for EVERY active outbound enrollment, first-touch exclusion (§V.32) held; tests over idempotent re-poll (no dup), attendee linkage, multi-attendee conclusion, first-touch preserved|V126,V128,V21
T187|.|fold-in §V.127(+) + §V.120(∆) per designs/calendar-meetings.md — conclude_enrollment(disposition, note, reschedule_at) agent tool (tools.py + invoke.py wrap), disposition in {meeting_booked, do_not_contact, contact_later}; system side effects per disposition (meeting_booked -> conclude + cancel; do_not_contact -> conclude + cancel + disable_contact §V.79/§V.80; contact_later -> conclude + cancel + scheduled re-enrollment task at reschedule_at default >=3mo §V.32); add conclude_enrollment to the §V.120 send-obligation terminal set (_sent_reply walker accepts it like noop); drop record_enrollment_outcome from the agent tool set (§I) -> system-internal (§V.15, §V.124); tests over each disposition's side effects + conclude_enrollment satisfies the send guard + record_enrollment_outcome absent from the agent tool set|V127,V120,V15
T188|.|fold-in §V.125(+) + §I per designs/calendar-meetings.md — `meeting` CLI noun: `list`, `view`, `add` (link an attendee contact per §V.115 link semantics), `update` (edit summary/status), `cancel` (status -> cancelled); meeting rows ingested by CalendarClient (§V.126) so NO CLI `create`; JSON envelope per §V.4; tests over list/view projection + add attendee + cancel|V125,V126

## §B BUGS

## archived: §B.71..§B.90 → SPEC.archive.md (18 rows)
id|date|cause|fix
B91|2026-06-16|`mailpilot db --help` renders SPEC cites via Click fn docstring + option help= strings|V111
B92|2026-06-17|seed_companies.py query_stale (`company list --no-profile`) emits global profile-NULL set; domain arg enriches backlog, not scoped rows; trips >10 batch gate|V112
B93|2026-06-17|contact-finder used Bouncer POST /email/verify/batch/sync (async, returns [] cold emails); agent read [] as status=unknown -> NULL; §B.76 patched symptom not upstream cause|V113
B94|2026-06-17|ContactView omits title + email_confidence; `**contact.model_dump()` via Pydantic extra=ignore silently strips both -> `contact view` + read_contact return fewer fields than `contact list`|V8
B95|2026-06-18|lead-contacts re-dispatches a contact-finder every run for profiled companies that yield zero contacts (no row persists to memoize the empty verdict); `company list --has-profile --max-contacts 0` accumulates, re-burning Hunter/TheOrg credits|V96
B96|2026-06-18|schema.sql moved `company.disabled_reason` mid-table (after `profile`, mirroring `account` col order) while migration 002 `ADD COLUMN` appends it last -> fresh `db init` diverges from migrate-from-zero, breaking §V.108 byte-identity (identity test caught); un-migrated populated DB surfaces it as `company list --has-profile --max-contacts 0` -> `UndefinedColumn c.disabled_reason`|V108
B97|2026-06-18|`db migrate` never re-stamped `schema_metadata.schema_hash` (sole writer = `_provision_schema` empty-DB path) -> a DB init'd before a schema change then migrated forward keeps the provision-time hash; `determine_schema_verdict` sees recorded≠current + 0-pending -> phantom `drift`, dead-stopping `run` + every mutation (§V.109) w/ no forward path (migrate no-op @ 0-pending, init refuses populated)|V108
B98|2026-06-20|batch gate (`references/lead-pipeline-conventions.md`) lists fixed `First 25` cap unconditionally at stale-count > 9; at stale-count 10-25 `First 25` caps to all rows == `All <N>`, two options dispatch one identical batch (bare /lead-contacts run, stale=10); option set never gated on distinct-batch mapping|V117
B99|2026-06-20|lead-pipeline create-duplicate detection (seed_companies.py + lead-pipeline-conventions.md + contact-finder.md) reads the error envelope (duplicate_key/already_exists) from stdout; output_error writes it to stderr per §V.3 — stdout empty on a duplicate, seed_companies.py parses empty -> JSONDecodeError -> misclassifies the duplicate as error, the "existing" branch never fires|V3
B100|2026-06-21|§V.114 promised re-enable by clearing disabled_reason but no CLI cleared it — `company update` whitelists only name+profile; `company disable`+`update_company` docstrings named `company update` as re-enable path, never wired; carquest.ca (no_contacts_found:2026-06-20) stuck disabled CLI-only; widened review found contact/tag/enrollment disable also lacked an enable mirror|V114
B101|2026-06-22|lead-contacts negative-verdict memoization (§V.96) tagged only the zero-decision-maker verdict; a below-cap profiled company whose finder re-found people but whose every discovered email was already seeded (status=skipped, all-already-seeded reason) was explicitly NOT tagged, re-entered the discover set every run + re-burned Hunter/TheOrg/Bouncer credits for zero new contacts (§B.95 recurrence, partial-seed case); prose-prefix reason match (not a typed reason_code) made the branch brittle|V96,V116
B102|2026-06-22|in-scope grader false-FAILed a correct grounded cited reply (qa-in-015): rubric expected_token `Maximum (15 ppm RO Feed)` = verbatim multi-word column header, reply rendered the fact across two table cells so the contiguous-substring match in `_grade_inscope` never hit (`_loose` fires only for number+unit) — violates §V.105 false-PASS-at-worst-never-false-FAIL contract; test-harness defect, fact present + cited|V105
B103|2026-06-22|inbound reply-test compare case qa-cmp-006 got no auto-reply yet run + task both recorded `completed` — model called search_drive_markdown + read_drive_markdown 4x (tool_call_count=4, tool_error_count=0) then drafted the full C403-vs-WA012 compare in `<thinking>` ("I have the data I need ... Let me reply now") but emitted no `reply_email` tool call; §V.81 enforces >=1 tool call, nothing enforced that the inbound trigger produced a reply, so the run-loop passed a reply-less completion as success w/ no error + no retry (model minimax/minimax-m3, run d2303565)|V120
B104|2026-06-22|company/contact declarative import drop exported fields + break linkage (export->import not faithful, violates §V.121): company import calls create_company(name,domain) only — profile + disabled_reason + id dropped; contact import calls create_contact(email,first/last,company_id) only — title + email_confidence + disabled_reason + id dropped, and forwards the exported company_id (source-DB UUID) verbatim while company import mints fresh ids, so the FK resolves to no row in a fresh DB -> psycopg ForeignKeyViolation; import loop catches only validation_error + duplicate per row, so the uncaught FK aborts the whole batch; surfaced restoring ~/Documents/MailPilot/ canonical backups for /mailpilot-campaign-test — companies returned profile-less (empty read_company grounding), contacts title-less, contact batch FK-aborted|V121
B105|2026-06-23|inbound sync silently + permanently loses Gmail messages on transient batch-fetch 429s: get_messages_batch @_retry_on_transient catches only a top-level batch.execute() raise, never a per-sub-request 429 ('Too many concurrent requests') delivered to the callback — callback drops every non-404 error to an unused failed_ids list, returns only the successes; sync_account then advances gmail_history_id to the pre-fetch checkpoint regardless, so dropped messages fall below the checkpoint + the next incremental _collect_new_message_ids never re-collects them — permanent loss; surfaced by /mailpilot-campaign-test run 5373f10e — verify sync of inbound@lab5.ca fired ~100 concurrent messages.get in one batch (_BATCH_SIZE=100), hit 62 429s one burst, lost 3/9 delivered emails across 4 sync attempts|V75
B106|2026-06-24|outbound campaign first reach-out drafted the cold email as final assistant text + never sent, yet run recorded `completed` — §V.120 send-guard `_sent_reply` gated inbound-only (`email is not None` + trigger in {email, task}), so an outbound trigger (`email is None`, trigger in {enrollment_run, enrollment_schedule}) bypassed it: agent called read_contact + read_company (tool_call_count=2, tool_error_count=0) then emitted the email body as final text w/o a `send_email` call, §V.81 passed (>=1 tool) + inbound guard skipped -> reply-less completion passed as success, no error no retry, email never reached recipient (model zai-org/glm-5.2, /mailpilot-campaign-test run 6542df4e run #4, 8 of 9 sent)|V120
B107|2026-06-24|controlled output_error exit (SystemExit(1)) escaped through the logfire.span in cli_mutation (operator_log.py:79) -> business-outcome envelopes (duplicate_key etc.) recorded as error-level exception spans (exception.escaped=True); SystemExit is BaseException so except-Exception skipped it (clean stderr/stdout/exit 1, no .failed child span) yet logfire.span.__exit__ still recorded the escaping exit; duplicate-key telemetry test only asserts no .failed child, never the parent span clean; two account.create SystemExit:1 spans surfaced as Logfire errors (inbound@lab5.ca + outbound@lab5.ca duplicate-key re-create)|V54
B108|2026-06-24|mailpilot-campaign-test verify_delivery.py keyed delivery on the exact agent-written subject (`{subject: sequence}` dict) — two sends sharing one subject collapse to a single key, the later sequence overwrites the earlier, both arrivals map to the survivor + the overwritten sequence counts missing; run 3d479ca1 sent 9, all 9 arrived, GHY seq 2 (inbound2) + seq 3 (inbound3) shared subject "AI for customs inquiry triage and tariff lookups at GHY International" -> delivery.json delivered [1,3,4,5,6,7,8,9] missing [2] FAIL though both delivered; EmailSummary lacked recipients so the single bulk `email list` could not expose the per-sequence alias|V7,V122
B109|2026-06-24|mailpilot-reply-test in-scope atomicity guard `_is_brittle_inscope_token` = denylist matching only the `Label (Qualifier)` regex, so a non-header non-atomic token slips through: qa-in-006 carried sentence fragments `KDF 55 medium can remove over 99%` + `remove up to 98% of water-soluble cations`, qa-in-009 carried `(40) 8" TFC spiral wound membranes`; a grounded reply restructuring the phrase false-FAILs the contiguous-substring match (§B.102 recurrence, non-`Label (Qualifier)` shape) — guard must allowlist atomic shapes, not denylist one brittle shape|V105
B110|2026-06-25|inbound reply routing to an enrollment leaves its future pending follow-up tasks scheduled — firing guards (§V.83) gate on workflow/contact/enrollment-active only (no terminal-outcome check) + record_enrollment_outcome='completed' is activity-only (§V.15, enrollment row untouched), so a replied prospect can still receive a later cold breakup touch unless the workflow self-guards (GH #151)|V123
