# SPEC

## §G GOAL

Agent-operated CRM. Gmail = comms layer. Claude Code = strategist; internal Pydantic AI agent = tactical executor (routing, auto-reply, follow-up).

## §C CONSTRAINTS

- Python 3.14. basedpyright strict. ruff. TDD per CLAUDE.md.
- PostgreSQL 18. psycopg, raw SQL via `psycopg.sql`. ⊥ ORM.
- IDs = UUIDv7 (`uuid.uuid7()` via `_new_id()`).
- Gmail scope = `gmail.modify` only. ⊥ additional Gmail scopes.
- Drive scope = `drive.readonly` only. Read-only KB grounding. ⊥ write/modify.
- Auth = service account & domain-wide delegation per §V.37. Credential source ∈ {file path (`google_application_credentials` setting ∨ `GOOGLE_APPLICATION_CREDENTIALS` env var), Application Default Credentials (e.g. GCE metadata server, Workload Identity, Cloud Run identity)}. Per-account impersonation: file creds → `credentials.with_subject(email)`; ADC creds → `service_account.Credentials(signer=iam.Signer(...), service_account_email=<sa>, subject=email, ...)`. ⊥ OAuth user login.
- Email body = plain text only (control chars stripped). ⊥ HTML body persistence. ⊥ embeddings, ⊥ vector store, ⊥ ingestion pipeline.
- KB files = `.md` in Drive folder named in `workflow.instructions`. ⊥ in-app PDF/Docs/HTML conversion.
- ASCII-only project artifacts (code, docs, CLI output). Agent-generated email body content exempt.
- Tool pattern: typed sig, DI deps, dict return, error dicts on failure (⊥ raise to agent).

## §I INTERFACES

- cli: `mailpilot <noun> <verb> [args]` → JSON on stdout, exit code 0 ∨ 1, errors to stderr.
  - nouns: `account`, `company`, `contact`, `workflow`, `enrollment`, `task`, `email`, `activity`, `tag`, `note`, `template`.
  - verbs: `list|search|view|create|update|disable|add|reply|send|start|stop|cancel|retry|run|sync|export|import`.
  - top-level: `run` (sync & task loop), `status`, `config get|set`, global `--version|--debug|--completion|--skill`.
  - envelope: `list|search|sync|export|import` → `{"<plural>": [...], "ok": true}`; `view|create|update|disable|add|reply|send|start|stop|cancel|retry` → `{"<singular>": {...}, "ok": true}`; err → `{"error": CODE, "message": TEXT, "ok": false}`.
  - email projection: `email view|list` ! project `route_method` ∈ {`classified`, `thread_match`, `rfc_message_id_match`, `skipped_outside_window`, `skipped_no_workflows`, `skipped_predates_workflows`, `skipped_no_inbound_workflows`} ∴ operator audits routing decision from CLI w/o Logfire.
  - `template` = read-only, code-defined (registry in `src/mailpilot/agent/templates.py` per §V.44). Verbs: `list [--direction inbound|outbound]`, `view NAME`. ⊥ create/update/delete — new template = code change + PR.
- agent tools (`src/mailpilot/agent/tools.py`): `send_email`, `reply_email`, `search_emails`, `read_email`, `read_contact`, `read_company`, `list_enrollments`, `create_task`, `cancel_task`, `record_enrollment_outcome`, `disable_contact`, `list_drive_markdown`, `read_drive_markdown`, `search_drive_markdown`, `noop`. ∀ tool → typed sig, dict return, err dict on failure.
- pubsub (`src/mailpilot/pubsub.py`): topic `mailpilot-topic-dev`, sub `mailpilot-sub-dev` (defaults; per-env override via `MAILPILOT_GOOGLE_PUBSUB_TOPIC` / `..._SUBSCRIPTION`). `setup_pubsub()` idempotent. `start_subscriber(settings, callback)` streaming pull. `make_notification_callback(queue, wakeup_event)` decode → enqueue → `wakeup_event.set()`. `renew_watches()` refresh @ T-24h.
- config (`src/mailpilot/settings.py`): `database_url`, `anthropic_api_key`, `anthropic_model` (default `claude-sonnet-4-6`), `google_application_credentials`, `google_pubsub_topic`, `google_pubsub_subscription`, `logfire_token`, `logfire_environment` ∈ {`development`, `production`}, `run_interval` (default 60s), `max_concurrent_tasks` (default 4).
- module: `src/mailpilot/gmail.py` → `GmailClient`; `src/mailpilot/drive.py` → `DriveClient`. Mirror shape.
- entrypoint: `mailpilot = "mailpilot.cli:main"`.

## §V INVARIANTS

V1: ∀ Settings consumer → receive `Settings` instance. ⊥ pass API keys / config values as separate fn args. CLI loads via `get_settings()` & forwards.
V2: `cli.py` module top-level imports → only `click`. Heavy deps (`logfire`, `psycopg`, `pydantic`, `mailpilot.*`) ! lazy inside fn bodies. Why: `--help` ~50ms.
V3: ∀ JSON-yielding single-shot CLI command → stdout strict-JSON. ⊥ preceded ∨ interleaved by operator-log lines. `operator_event(...)` → stderr always. Long-running `mailpilot run` exempt (operator console).
V4: CLI envelope shape: `list|search|sync|export|import` → `{"<plural>": [...], "ok": true}`; `view|create|update|disable|add|reply|send|start|stop|cancel|retry` → `{"<singular>": {...}, "ok": true}`; ⊥ inline entity fields at top level. Use `output_entity("<singular>", model)` & `output({"<plural>": [...]})`.
V5: ∀ list|search row projects via `<Entity>Summary` — fields ⊆ {id, natural identifier, filter fields exposed as `--flag`, timestamp ordered by, parent-entity natural identifier paired w/ already-included parent FK (e.g., `account_email` alongside `account_id`)}. ⊥ long-text (`body_text`, `instructions`, `objective`), ⊥ JSON blobs, ⊥ variable-cardinality lists, ⊥ Gmail bookkeeping. Why parent-NI clause: operator-facing list output often pivots on parent NI (account email, company name); raw FK alone forces round-trip via `<parent> view <fk>` → noisy ∧ slow. Denormalized parent-NI ≡ display-only ergonomic join-saver; paired-w/-FK constraint preserves FK ≡ canonical key per §V.7 (NI ⊥ writable, ⊥ filter target — ⊥ `--account-email` flag). View/list parity: `<Entity>Summary` ships denormalized parent field (parent-NI ∨ short display label joined from parent table) ⇒ same field ! ∈ same entity's view envelope inner-dict. View ⊇ Summary on parent-denorm set. Inversion (view strips fields operator just saw ∈ list) ≡ regression class: operator scans list, sees parent context, runs `view <id>` to drill, loses context (§B.49). Impl-choice per entity: (a) promote denorm onto row model loaded via JOIN at fetch (Workflow.account_email precedent), ∨ (b) separate `<Entity>View` projection (ContactView/CompanyView per §V.8). @ amend time: EnrollmentSummary carries `contact_email` ∧ `contact_name` joined from contact ∴ enrollment view ! carry both; EnrollmentSummary further ! gain `workflow_name` paired w/ `workflow_id` (workflow NI = workflow.name) ∧ same field lands on view side per parity. Peer Summary classes ⊥ denorm available parent-NI (e.g. ContactSummary missing `company_name`) ≡ separate `/sdd:check` audit class, ⊥ in-scope this row.
V6: `mailpilot --skill` → stdout = packaged `src/mailpilot/SKILL.md` body verbatim, exit 0, ⊥ JSON envelope (matches `--version` / `--help` shape; §V.3 binds noun-verb cmds only). Source resolved via `importlib.resources.files("mailpilot").joinpath("SKILL.md")` ∴ survives wheel install. `pyproject.toml` ! include `SKILL.md` as package data (uv-build equivalent of `force-include`). Skill content audience = external LLM agent w/ `mailpilot` installed as CLI dep; scope = (a) noun-verb grammar + envelope shape per §V.4 + exit codes, (b) common task recipes (enrollment, send, route query, export/import), (c) settings keys per §I.config + `mailpilot config set` usage. ⊥ contain DB schema, ⊥ Pydantic AI / template internals (§V.44-§V.46 = package-internal). Test asserts `mailpilot --skill` stdout byte-equal to packaged file ∧ `importlib.resources` path resolves post-install. Content correctness — SKILL.md body ⊥ drift from live `src/mailpilot/cli.py` @click decorators ∧ `src/mailpilot/settings.py` `Settings.model_fields`; audit recipe → `.claude/check-extras.md §V.6`. (+) Audit-recipe scope extends to `.claude/skills/**/*.md` operator-facing skill bodies for the same retired-vocab drift class — §B.65 instance: `smoke-test/SKILL.md:40` carried `tag remove`/`enrollment remove` post-T91/T92 verb retirement (4 skills under `.claude/skills/`, 65 CLI-vocab refs @ amend time = meaningful drift surface). Same per-noun verb roster (i) ∧ per-verb `--flag` (ii) checks from `.claude/check-extras.md §V.6` apply; settings-key (iii) ∧ env-prefix (iv) checks ⊥ apply (skill bodies ⊥ enumerate settings). Shipping-mechanics test (byte-equal, `importlib.resources`) remains scoped to `src/mailpilot/SKILL.md` only — operator-internal skills ⊥ ship as package data. Doc ≡ hand-authored ⊥ generated ∴ flag rename ∨ verb add ∨ Settings field add silently invalidates content; §B.47 instances @ amend time: `--body-text` ∈ recipes (live = `--body`); `email reply <EMAIL_ID>` ∧ `account sync <ACCOUNT_ID>` ∧ `enrollment {run,view,update} <ENROLLMENT_ID>` positional forms (live = flagged); verb roster ⊥ ∋ `{sync, disable, retry, get, set}`; settings ⊥ ∋ `max_concurrent_tasks`; env-prefix described as "uppercase of same name" (actual `MAILPILOT_*`). Why: external agents discover capability via `--help` (single vector); FS-path discovery brittle across install methods (pip vs pipx vs uv tool vs vendored); content drift propagates to downstream agent runtimes that build commands from the doc ∴ shipping-mechanics test (byte-equal) ⊥ sufficient — need live-surface audit binding the doc body to the canonical sources.
V7: ∀ `<Entity>Summary` (list-view projection) ! ∋ (a) FK columns the row's `<Entity>` schema declares as FK to other entity tables (e.g., `activity.workflow_id`, `activity.email_id`, `activity.task_id`), ∧ (b) external-system join keys the entity carries to participate in pipelines outside the DB (e.g., `email.gmail_thread_id` used by routing pipeline thread_match per §V.27). Why: FK columns ∧ external-system join keys ≡ canonical join surface for downstream reporting; omitting them from list responses forces consumers to parse `detail` JSONB display labels (`detail.workflow_name` ≡ display label, ⊥ FK), fall back to raw SQL, ∨ per-row round-trip via `view`. External-system keys ≡ same recurrence class as DB FKs: §B.39 — smoke-test 2026-05-28 A4 needed `gmail_thread_id` to confirm threading, found absent from `EmailSummary`, forced round-trip via `email view` per row. §V.5 enumerates allowed Summary fields w/o explicitly binding FK-inclusion — V51 binds it. `TaskSummary` (`src/mailpilot/models.py:273-283`) already complies; `ActivitySummary` precedent (§B.27, fixed §T.46); `EmailSummary` (`src/mailpilot/models.py:225-238`) ≡ sole external-key violator @ amend time per §B.39.
V8: CLI `contact view` ∧ `company view` envelope inner-dict ≡ agent tool `read_contact` ∧ `read_company` return (byte-identical modulo §V.4 wrap). Single helper `load_contact_view(conn, id) → ContactView` ∧ `load_company_view(conn, id) → CompanyView` renders both surfaces. `ContactView` ≡ `Contact` + `notes: list[Note]` (cap N=10, ORDER BY `created_at` DESC) + `notes_total: int` + `company_notes: list[Note]` (parent company's notes when `contact.company_id IS NOT NULL`, else `[]`) + `company_notes_total: int`. `CompanyView` ≡ `Company` + `notes` + `notes_total` (⊥ `company_notes` — root entity). Full `Note.body` verbatim, ⊥ preview. `ContactView` ∧ `CompanyView` ≡ view-only projections (separate from DB-row models, mirroring `<Entity>Summary` precedent). Cap = hardcoded `_INLINE_NOTES_CAP = 10` in `database.py`. ⊥ `Settings` field — YAGNI. `_BASE` protocol fragment (`src/mailpilot/agent/templates.py`, §V.45) gains one-liner: `"If read_contact or read_company returns notes or company_notes, treat them as context for personalizing your response. Never invent facts about a contact or company that aren't supported by their notes."` Why: single rendering path = single test contract; operator ∧ agent see identical context ∴ personalization observable from CLI without invoking agent; §V.5 Summary rule unaffected (binds list/search only, view exempt); same fabrication-class guard §V.40 / §V.57 / §V.55 already enforce for KB, extended to CRM facts.
V9: ∀ CLI cmd reading from stdin (fallback path when `--file` omitted) → ! `sys.stdin.isatty()` guard BEFORE `sys.stdin.read()`; `isatty() == True` → `output_error("no input: provide --file PATH or pipe JSON via stdin", "validation_error")` (SystemExit per §V.3 error envelope) ⊥ proceed to `read()`. Sites @ amend time: `src/mailpilot/cli.py:632` (`company import`), `:920` (`contact import`), `:2249` (`workflow import`). Why: `sys.stdin.read()` on a TTY blocks indefinitely waiting for EOF → indistinguishable from a frozen process (⊥ prompt, ⊥ error, ⊥ recovery signal); operator runs `mailpilot <noun> import` interactively expecting a `--file`-or-error response, gets a silent hang instead (§B.29). Future stdin-fallback cmds inherit obligation by convention — invariant binds the surface uniformly, ⊥ per-cmd guard duplication.
V10: `mailpilot <noun> disable <id-form> --reason <text>` soft-disables an entity via terminal lifecycle exit. Coverage: noun ∈ {`contact`, `enrollment`, `tag`}; id-form per-noun — `contact` = `--contact-id <id>`; `enrollment` = positional `<enrollment_id>` (scalar id per §T.77); `tag` = exactly one of `--contact-id <id>` ∨ `--company-id <id>` (XOR per §V.13) plus positional `<name>` (mirrors `tag add`). ∀ handler ! validate `reason.strip() ≠ ""` (else `output_error("reason cannot be empty", "validation_error")` per §V.4); ! read pre-row via `before = get_<noun>(connection, id)` (`before is None` → `output_error(f"<noun> not found: {id}", "not_found")`); under `cli_mutation("<noun>", "disable", entity_id=id)` span → `updated = database.disable_<noun>(connection, id, reason)` → compute `changed` per per-noun field-diff (below) → `operator_event("<noun>.disable", entity_id=id, changed=changed)` per §V.54 `update`/`disable` semantics. Envelope = singular per §V.4: `{"<noun>": {<full row w/ disabled fields set>}, "ok": true}` — row retained ∴ ⊥ composite-key projection (distinct from hard-delete `remove` per amended §V.4). Per-noun specifics: (a) **contact** — storage = standalone `contact.disabled_reason TEXT` column (⊥ status enum); `changed = ["disabled_reason"] if before.disabled_reason != updated.disabled_reason else []` ∴ first disable ∨ reason-change → `["disabled_reason"]`; idempotent re-disable w/ same `reason` → `[]`. ⊥ `contact_disabled` activity row emission (consistency w/ existing `contact create`/`update` handlers ∧ agent-tool `disable_contact` (`src/mailpilot/agent/tools.py:274`) — operator-event ∈ §V.54 IS the audit trail). Index = partial `idx_contact_active` per §T.47. Re-enable surface deferred (separable). Hard-delete `contact remove` deferred per issue #128 author "(1) could be added later" — separable surface. (b) **enrollment** — storage = extends `enrollment.status` enum per §V.15(+); `database.disable_enrollment` sets `status='disabled'` ∧ `disabled_reason=<reason>` in one UPDATE. `changed = ["status", "disabled_reason"] if (before.status != updated.status or before.disabled_reason != updated.disabled_reason) else []` ∴ first disable → `["status", "disabled_reason"]`; reason-only change → `["disabled_reason"]`; idempotent → `[]`. ! emit one `activity` row `type='enrollment_disabled'` (new value joining `enrollment_*` family ∈ `activity.type` CHECK in `schema.sql:161-169`), `enrollment_id=<id>`, `contact_id=<enrollment.contact_id>`, `workflow_id=<enrollment.workflow_id>`, `summary=<reason>`, `detail={"reason": <reason>}` — joins single-stream enrollment timeline alongside peer `enrollment_completed`/`enrollment_failed`/`enrollment_paused`/`enrollment_resumed`. §V.14 honored (append, ⊥ update); §V.17 honored (contact_id present). Terminal-only — `disabled` ≡ terminal per §V.15(+) ∴ ⊥ `enrollment resume`/`enrollment enable` cmd surface; re-enroll = fresh `enrollment add` w/ new id. Agent runtime + tools filtering `enrollment.status` ! treat `disabled` as out-of-scope (skip same as `paused`). §V.31 (`record_enrollment_outcome` reserved for `trigger=task` outbound-template path) untouched — operator-disable emits `enrollment_disabled` activity; agent-side terminal outcomes `enrollment_completed`/`enrollment_failed` distinct surface. `mailpilot enrollment remove` hard-delete CLI cmd DROPPED per §B.63 — disable replaces, no surface coexistence. Surfaces existing `database.disable_contact` ∧ agent-tool `disable_contact` to operator-CLI ∴ symmetric w/ agent-runtime (contact only; ⊥ agent-tool `disable_enrollment` @ amend time). (c) **tag** — id-form per-noun XOR per §V.13: exactly one of `--contact-id <id>` ∨ `--company-id <id>` plus positional `<name>`; `--reason <text>` per §V.10 envelope shape. Storage = standalone `tag.disabled_reason TEXT` column (mirrors contact pattern; tag is leaf entity w/ no existing lifecycle states ∴ ⊥ introduce status enum). Coupling CHECK on `tag`: `(disabled_reason IS NULL OR TRIM(disabled_reason) <> '')`. Partial unique indexes `idx_tag_contact_unique` ∧ `idx_tag_company_unique` (`schema.sql:195-198`) extended to also scope `AND disabled_reason IS NULL` ∴ re-add of same tag name after disable admitted (active row distinct from disabled rows by name). `database.disable_contact_tag(connection, contact_id, name, reason) -> Tag | None` ∧ `database.disable_company_tag(connection, company_id, name, reason) -> Tag | None` — single txn: UPDATE tag SET disabled_reason=%s WHERE (contact_id|company_id)=%s AND name=%s AND disabled_reason IS NULL RETURNING * + INSERT activity row `type='tag_disabled'`, `summary=f"Disabled tag {normalized}"`, `detail={"tag": normalized, "reason": reason}`, owning contact_id (∨ NULL for company-target), company_id (set on company-target ∨ JOIN-lookup from contact). `changed = ["disabled_reason"] if before.disabled_reason != updated.disabled_reason else []` ∴ first disable → `["disabled_reason"]`; not-found ∨ already-disabled → returns `None` → `output_error(f"tag '<name>' not found on <owner>", "not_found")`. ! emit `activity` row `type='tag_disabled'` (new value joining `tag_added`/legacy `tag_removed` family ∈ `activity.type` CHECK `schema.sql:164` — extend CHECK w/ `'tag_disabled'`, retain `'tag_removed'` for legacy data; ⊥ new rows of that type emitted post-amend). §V.14 honored (append, ⊥ update); §V.17 honored (contact_id ∨ company_id present per XOR). Terminal-only — re-enable surface deferred (separable); re-add same name ≡ new `tag` row via `tag add` (admitted by partial-unique scoping above). Tag-bearing query sites (`list_tags`, `list_contacts_by_tag`, `list_companies_by_tag`, `search_tags` ∈ `src/mailpilot/database.py:2868-2969`) ! gain `include_disabled: bool = False` kwarg defaulting to `WHERE disabled_reason IS NULL` filter — mirrors contact `list_contacts` precedent (`database.py:929-943`); CLI `tag list` / `tag search` ! expose `--include-disabled` flag forwarding into kwarg. `mailpilot tag remove` hard-delete CLI cmd DROPPED — disable replaces, no surface coexistence. Drop `database.delete_tag`, `database.remove_contact_tag`, `database.remove_company_tag` (renamed → `disable_*` siblings). Orthogonal to §V.32 / §V.33 (`enrollment add` per-cmd rules, different verb) ∧ §V.54 (meta-instrumentation contract — §V.10 binds per-cmd CLI surface, §V.54 binds telemetry shape). Why (original contact-coverage): §B.32 surfaced contact-lifecycle gap during issue #127 rollback (operator stuck w/ orphan contact row; only cleanup path = raw `DELETE FROM contact WHERE id = ...` bypassing future referential-integrity guards); `disable` ≡ cleaner default per issue #128 author reasoning (preserves audit history, mirrors existing agent tool, uses existing `disabled_reason` column + `idx_contact_active` partial index from §T.47). Why-amend (enrollment coverage): §B.63 — `mailpilot enrollment remove` hard-DELETE FK-violates on `activity.enrollment_id` for every enrollment that ever produced lifecycle activity (always true: `enrollment_added` activity emitted @ creation per §V.17 multi-target); shape ⊥ compatible w/ append-only audit per §V.14 (cascade DELETE on activity destroys audit; SET NULL via UPDATE-on-activity violates §V.14 fn-rule). Replace `remove` w/ terminal soft-disable extending §V.10 surface — preserves single-stream audit, unified CLI verb shape across nouns. Adaptations from contact-disable: (i) reuse status enum (single live-state signal per §V.15(+)) vs. parallel `disabled_at` column; (ii) emit activity row (joins existing `enrollment_*` family); (iii) terminal-only (`paused` already covers reversible halt). Why-amend (tag coverage): tags promoted from passive labels to first-class filter dimension (operator queries contacts/companies by tag). Hard-delete loses filterable history (operator cannot answer "contacts ever tagged 'vip'") ∧ forces clean-slate re-add @ data-loss cost; soft-disable preserves row + audit + re-enable optionality (deferred separable). Distinct from §B.63 enrollment driver (FK-violation forced): `tag.id` ⊥ FK-referenced (leaf entity per `schema.sql:182-199` — `activity` references owning contact_id/company_id, ⊥ tag_id) ∴ hard-delete structurally admitted but semantically retired. Storage choice = standalone `disabled_reason` (vs. status enum) preserves tag's stateless-label shape (⊥ operational states beyond presence/absence) while admitting single terminal-disabled signal. Partial unique-index scoping admits re-add per `idx_contact_active` precedent (§T.47).
V11: `mailpilot status` envelope inner payload ≡ block layout for LLM-agent troubleshooting. Top-level blocks under `{"status": {...}, "ok": true}` envelope (per §V.4): `version` (mailpilot package version str — top-level "what binary am I running?"), `schema` per §V.18 (`{hash, applied_at, drift}`), `sync_loop` (`{running, pid, started_at, heartbeat_at, heartbeat_age_seconds}`; null when ⊥ running), `accounts[]` (per-row `{id, email, disabled, last_synced_at, last_synced_age_seconds, gmail_history_id, watch_expiration, watch_expires_in_hours}`; `disabled: bool` ≡ `disabled_reason IS NOT NULL`; `last_synced_age_seconds` ∧ `watch_expires_in_hours` server-computed via SQL `EXTRACT(EPOCH FROM (now() - col))::int` ∴ ⊥ Python clock skew; null timestamp → null age), `tasks` (`{pending, failed_24h, scheduled_future, oldest_pending_age_seconds, max_attempt_count_pending}`; `failed_24h` ≡ `WHERE status='failed' ∧ completed_at >= now() - interval '24 hours'`; `scheduled_future` ≡ pending w/ `scheduled_at > now()`; `oldest_pending_age_seconds` ≡ `now() - min(scheduled_at) WHERE status='pending' ∧ scheduled_at <= now()`), `config` (`{anthropic_api_key_set, anthropic_model, logfire_environment, logfire_token_set, google_pubsub_topic, google_pubsub_subscription, database_url}`; secrets surfaced as `*_set: bool` only — ⊥ echo values; `database_url` scrubbed to `<scheme>://<host>[:<port>]/<db>` via `urllib.parse.urlsplit` reconstruction ∴ ⊥ leak creds into agent transcripts / Logfire spans), `counts` (`{companies, contacts, workflows, emails, activities, tags, notes}` — demoted sanity tail; `accounts` dropped, derivable as `len(status.accounts)`). Backwards compat ⊥ retained — operator confirmed no scripts rely on prior shape; release-note bump in same commit per §T.29 / §T.54 precedent. Why: prior payload ≡ entity-counts dump + sync-running bit ∴ ⊥ discriminating for "what's wrong?"; reshape surfaces staleness (sync_loop ∧ account ages), queue health (tasks), config sanity (secrets-as-bool, scrubbed `database_url`), schema state per §V.18 — counts demoted to sanity tail; secrets-as-bool + db-url scrub prevent credential leak into agent transcripts ∧ Logfire spans.
V12: ∀ ID = UUIDv7 via `_new_id()` (`uuid.uuid7()`). ⊥ uuid4, ⊥ serial.
V13: `tag`, `note` rows → exactly one of {`contact_id`, `company_id`} set (XOR). Enforced via schema CHECK on `tag` & `note` in `schema.sql`.
V14: `activity` & `note` append-only. ⊥ update fn.
V15: `enrollment.status` ∈ {`active`, `paused`, `disabled`} (schema CHECK on `enrollment.status` in `schema.sql`). `active`/`paused` ≡ operational (paused = temporarily halted, resumable); `disabled` ≡ terminal operator-killed lifecycle exit (⊥ resume — re-enroll = new row via `enrollment add` per §V.10 enrollment-coverage clause). Agent records terminal outcomes via `record_enrollment_outcome` → activity log; ⊥ mutate `enrollment.status` as terminal signal. Operator transitions enrollment → `disabled` via `mailpilot enrollment disable` per §V.10. New column `enrollment.disabled_reason TEXT` nullable; coupling CHECK on `enrollment` enforces `(status = 'disabled') = (disabled_reason IS NOT NULL AND TRIM(disabled_reason) <> '')` — disabled ⇒ reason present; ⊥ disabled ⇒ `disabled_reason` MUST be NULL (prevents stale reason lingering on un-disabled rows). Agent runtime + tools filtering `enrollment.status` ! treat `disabled` as out-of-scope (same skip handling as `paused`). Why-amend: §B.63 — `mailpilot enrollment remove` shape ⊥ compatible w/ append-only audit per §V.14; replace w/ terminal soft-disable via status enum extension. Single status surface keeps "agent allowed to process this enrollment?" answerable w/o joining a parallel `disabled_at` column. Reason column (vs. activity-only) keeps "why is this disabled?" queryable w/o joining activity (operator audit ergonomic).
V16: Race-safe natural-unique-key INSERT: `INSERT ... ON CONFLICT (<unique_cols>) DO NOTHING RETURNING *` → `Model | None`. `None` ≡ row already exists (concurrent worker won ∨ operator re-create). Sites: `create_email` (gmail_message_id — History re-delivery idempotent, original scope), `create_enrollment` ((workflow_id, contact_id)), `create_contacts_bulk` (email), `create_tag` / `add_contact_tag` / `add_company_tag` (partial unique idx). (+) Extended to `create_account` (email), `create_company` (domain), `create_contact` (email), `create_workflow` ((account_id, name)) — pre-amend bare INSERT raised `psycopg.errors.UniqueViolation` on duplicate. CLI handlers `account create` / `company create` / `contact create` / `workflow create` branch on `None` return → `output_error(f"<noun> with <natural_key>={value!r} already exists", "duplicate_key")` per §V.4 envelope (singular emit, exit 1); `operator_event("<noun>.create", ..., changed=[])` per §V.54 idempotent re-invoke semantics (∅ field set); logfire span outcome ≡ ok (⊥ `<noun>.create.failed` ∴ ⊥ pollute failed-span dashboard w/ expected operator-retry outcome). §V.54 envelope-translation still binds remaining constraint classes (FK / NOT NULL / CHECK); natural-key collision now handled @ source ∴ ⊥ raises → ⊥ §V.54 catch-path for this class. Sync-path callers (e.g. `create_or_get_contact_by_email` reads via `get_contact_by_email` then falls through to `create_contact`) treat `None` as "lost the race" → re-fetch existing row, return; concurrent §V.23 worker TOCTOU safe by construction. Why-amend: §B.64 — `mailpilot company create --domain lab5.ca` re-run emitted `company.create.failed` logfire span + `duplicate_key` envelope; envelope per §V.54 correct (no traceback per §B.52 fix held) but `.failed` span outcome ≡ wrong signal (expected operator re-run, ⊥ system error). Class-asymmetry visible @ amend time: `create_contacts_bulk` race-safe peer to non-race-safe `create_contact`; generalization closes asymmetry.
V17: `activity` row → ≥1 of {`contact_id`, `company_id`} set; both allowed. Enforced via schema CHECK on `activity` in `schema.sql`. Why: activity may attach to contact, company, or both (e.g., email sent to contact at a known company).
V18: `schema_metadata` row ! recorded w/ current `mailpilot_version` + normalized `schema_hash` @ schema apply time (probe-empty branch of `initialize_database` ∈ `src/mailpilot/database.py`). Subsequent connections (probe-non-empty branch) ! compare current normalized hash against recorded; mismatch ∨ row-missing → `logfire.warn("schema drift detected", recorded_version, current_version, recorded_hash, current_hash)` + paired `operator_event("schema.drift", recorded_version, current_version, recorded_hash, current_hash)` (extends §V.51 pattern to warn-class event since drift ⊥ exception). ⊥ SystemExit — operator owns remediation via `make clean` (data-destructive, prod = scheduled window). Reset op ! cover ∀ DBs schema.sql owns ({`mailpilot`, `mailpilot_test`} @ amend time) — partial coverage → test DB retains stale NOT-NULL cols post-shrink → pytest fails opaque `psycopg.errors.NotNullViolation` ⊥ surfaced by startup probe (test fixture truncates rather than re-applies schema ∴ probe ⊥ runs). Version-only diff w/ matching hash ≡ silent (patch release w/o schema touch). `mailpilot status` envelope ! surface `schema: {hash, applied_at, drift}` block (rename of prior `schema_metadata`; `mailpilot_version` promoted to top-level `status.version` per §V.11 ∴ ⊥ duplicated inside `schema`) ∴ drift state pull-able alongside push warning. Table shape: `id INT PRIMARY KEY DEFAULT 1 CHECK (id=1)` + `mailpilot_version TEXT NOT NULL` + `schema_hash TEXT NOT NULL` + `applied_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` ∴ row count ∈ {0, 1}. No backfill (`make clean` recreates per §T.13 / §T.34 / §T.47 precedent). Why: prod wheel-install runs against persisted DB; `initialize_database` skips schema apply when probe finds `account` table (deliberate per `task_pending_trigger` AccessExclusiveLock vs sync-loop RowExclusiveLock deadlock) ∴ schema additions silently absent on prod until operator runs `make clean`; first write hitting missing col raises opaque `psycopg.errors.UndefinedColumn` ∴ drift class invisible; warn @ startup binds drift class explicitly. No fail-fast — `mailpilot run` ≡ HA-critical, warn-loud > hard-outage.
V19: `schema_hash` ≡ sha256 of normalized `schema.sql`: (1) strip `--`-line comments via `re.sub(r"--[^\n]*", "", sql)`, (2) collapse whitespace runs via `re.sub(r"\s+", " ", normalized).strip()`, (3) `hashlib.sha256(normalized.encode("utf-8")).hexdigest()`. `_compute_schema_hash(sql: str) -> str` helper ∈ `src/mailpilot/database.py`. Why: trivial reformatting (blank line shuffle, comment add) else flips raw-bytes hash → false-positive drift warning floods logs; normalization scopes hash sensitivity to SQL-meaningful changes.
V20: `email.route_method` ∈ §I enum {`thread_match`, `rfc_message_id_match`, `classified`, `skipped_outside_window`, `skipped_no_workflows`, `skipped_predates_workflows`, `skipped_no_inbound_workflows`} — schema CHECK on `email` ∈ `schema.sql`. NULL admitted ↔ row ∈ {outbound, bounce, pre-route placeholder} (routing pipeline ⊥ recorded a decision). Coupling CHECK: `route_method IS NULL OR is_routed = TRUE` — recorded decision ⇒ pipeline ran; outbound/bounce-as-origin rows ⊥ violate (NULL satisfies). Inverse (`is_routed=TRUE ⇒ route_method NOT NULL`) ⊥ hold — outbound/bounce-as-origin routed-by-virtue-of-origin w/o enum value ∴ coupling one-directional. New `route_method` values land via SPEC §I amend → schema migration → code; ⊥ added in code alone. Why: §T.70 persisted column to give operators CLI audit surface w/o Logfire; §I enumerates legal values; ⊥ SQL gate ∴ typos & stale strings land silently. CHECK closes §I-vs-DB drift class @ schema layer, parallel to §V.13 / §V.15 / §V.17 / §V.44 (per-entity-column CHECK invariants).
V21: `mailpilot run` main loop ! `wait()` on shared `wakeup_event` set by Pub/Sub callback / `LISTEN/NOTIFY` / signal handlers. Clear `wakeup_event` BEFORE processing → mid-iter events re-trigger next wait. Periodic timer = upper-bound fallback only. Canonical: `start_sync_loop` in `src/mailpilot/sync.py`.
V22: ∀ email → ≤1 `routing.route_email` span lifecycle per `email_id`. History-API re-delivery & repeat sync sweep ⊥ trigger second route pass. Why: duplicate route spans inflate metrics & mask classifier regressions (e.g., A4 single delivery → 2 `skipped_no_workflows` spans observed).
V23: Task drain MUST dispatch ready tasks via bounded concurrent worker pool. `_drain_pending_tasks` (`src/mailpilot/sync.py:418-431`) ! replace sequential `for task in pending: execute_task(...)` w/ `concurrent.futures.ThreadPoolExecutor(max_workers=settings.max_concurrent_tasks)` ∧ `submit` per pending task; futures tracked via `as_completed` for `task.drain` operator_event timing aggregation. New `Settings.max_concurrent_tasks: int = 4` (`src/mailpilot/settings.py`) bounds pool size ∴ production deploy can tune per-instance. Per-task exception handling preserves §V.49 retry classification (transient → reschedule, terminal → fail) — uncaught exceptions ∈ one worker ! ⊥ poison sibling futures. Worker isolation: each future opens its own `psycopg.Connection` via `settings.connection()` ∴ ⊥ shared cursor / transaction races across concurrent agent runs. Pool size = 1 (regression-mode) preserves strict sequential behavior. ∧ dispatcher `_drain_pending_tasks` (`src/mailpilot/sync.py:447`) ! atomically claim each pending row before pool submission so re-tick during in-flight worker execution ⊥ re-select the same row. Concrete impl options: (a) `list_pending_tasks` UPDATE-RETURNING flip `status='pending'→'running'` (∨ equiv `claimed_at IS NOT NULL` column) ∈ single SQL statement; (b) ∨ `SELECT ... FOR UPDATE SKIP LOCKED` + immediate status flip ∈ same txn; (c) ∨ in-process set tracking in-flight task ids excluding them from re-dispatch. ⊥ plain `SELECT * FROM task WHERE status='pending'` per current `database.py:2281`. Advisory lock per §V.25 ≡ necessary ⊥ sufficient — catches concurrent attempts, ⊥ sequential re-pick post worker completion. New state ('running' ∨ `claimed_at`) ! reset on worker exception (paired w/ `_handle_agent_failure`). Why: smoke-test 2026-05-28 B7.5 — 2 concurrently-arrived inbound emails on demo workflow ran `agent.invoke` spans strictly sequential (span#1 22s 15:14:36→15:14:58, span#2 30s 15:14:58→15:15:29, 3ms gap, zero overlap) → 2nd msg stalled full duration of 1st (~30s here, up to 90s near SLA) → lab5.ca/mailpilot/ 90s promise eaten under traffic burst. Drive `Tool(... sequential=True)` (§V.38) ⊥ sufficient — intra-invocation only (one agent.invoke, multiple tool calls); inter-invocation serialization @ drain layer ≡ orthogonal recurrence class. Bounded pool also prevents traffic-burst from self-DoSing Anthropic rate limit ∧ Gmail send quota. Why-amend (atomic claim): §B.50 — 1 task emitted 12 sequential `run.execute_task` spans (25 triggers → 121 emails, ~4× amplification); §V.25 lock covered concurrent path ⊥ sequential re-pick post-completion. ∧ `Settings.max_concurrent_tasks` default ! satisfy `≥ ceil(25×35/90)=10`; default `4` predates burst-load gate. Why-amend: §B.51 — 24-email run default-4 → 6 pool waves × ~35s avg → max_latency=235s (>110s burst ceiling), p95=217s (>90s SLA).
V24: Pub/Sub-notify-driven sync ! ⊥ serialize w/ in-flight `agent.invoke`. When `pubsub.notify` for account X arrives while ≥1 `agent.invoke` span(s) mid-execution (typically dispatched via §V.23 worker pool), main run loop ! reach `sync.account.run(X)` ≤ 5s post-notify regardless of in-flight `agent.invoke(s)` duration. ∴ Pub/Sub → sync → routing → task-create pipeline (upstream of drain) ⊥ blocked on drain completion. Impl-side: `_drain_pending_tasks` (`src/mailpilot/sync.py`) ! ⊥ block main loop on `as_completed` over worker-pool futures past one main-loop tick budget; main loop returns from `_drain_pending_tasks` ∧ continues `wakeup_event.wait()` per §V.21 while futures complete ∈ background. Why: smoke-test 2026-05-28 B7 (62s end-to-end vs then-60s SLA; 31s `agent.invoke` + 32s pre-agent overhead) ∧ B7.5 (LATENCY_B75b=72s; both emails delivered to Gmail @ 15:01:25, first sync picked up only B75a, agent.invoke for B75a held loop 15:01:34→15:02:05 (31s), during entire 31s window ⊥ `sync.account.run` fired on inbound account, B75b sat in Gmail 44s before routing → second sync fired 0.1s after agent.invoke ended @ 15:02:05) — both root-caused to sync-blocked-by-drain serialization. §V.23 (drain pool) ≡ necessary ⊥ sufficient: pool dispatches concurrent agent.invoke for tasks that ALREADY exist; new-email task creation lives upstream ∴ blocked separately. lab5.ca/mailpilot/ 90s SLA per §V.59/§V.61 budgets agent reasoning time ∴ any sync-blocking overhead between Pub/Sub notify ∧ agent.invoke start consumes that budget directly. Orthogonal to §V.23 (intra-drain concurrency) ∧ §V.21 (event-driven wake) — §V.24 binds the sync-vs-drain inter-layer contract.
V25: agent advisory lock (`_try_acquire_advisory_lock` ∈ `src/mailpilot/agent/invoke.py`) ! scope to unit of work, ⊥ to `(workflow_id, contact_id)`. Drain path (`trigger='task'`, dispatch via §V.23 pool) → lock key derived from `task.id` (unique per drained unit; concurrent distinct tasks for same contact ⊥ contend). Synchronous CLI path (`trigger ∈ {enrollment_run, enrollment_schedule, manual}`, ⊥ `task` row) → keep `(workflow_id, contact_id)` key (preserves original purpose: prevent operator-initiated double-run on same enrollment). Lock-held branch in drain path (`src/mailpilot/run.py:132-137`) ! reschedule task (∨ `pg_notify` on release) so subsequent ticks make forward progress w/o relying on ambient Pub/Sub traffic; current `return` after `run.task.lock_held` leaves task `pending` w/ ⊥ wakeup signal — direct cause of noop-span flood (3 noop `agent.invoke` spans / real invocation observed in §B.42 smoke run). Why: §V.23 binds concurrent dispatch ∧ §V.24 binds sync-vs-drain decouple — both correct — coarse agent-side mutex undoes concurrency for common B7.5 case (two inbound emails, same contact). Coarse lock was correct when only operator paths existed (one enrollment at a time); drain path's multi-task-per-enrollment surface invalidates assumption.
V26: `agent.invoke` span ! `trigger` attr ∈ {`enrollment_run`, `enrollment_schedule`, `task`, `email`, `manual`}, explicit caller-passed. ⊥ heuristic inference from arg presence. Span ! also carry `email_id` attr when invocation triggered by an inbound email (`trigger='email'`, ∨ `trigger='task'` w/ `task.email_id IS NOT NULL`). Why: conflated trigger labels mask operator-initiated retries as task drains, breaking Logfire regression detection; `enrollment_schedule` distinguishes CLI-scheduled first-touch (drained from task queue per §V.32) from operator-immediate `enrollment_run` (synchronous CLI) — agent behaviour identical, observability separates origin. `email_id` ≡ load-bearing dimension for per-message cost/latency analysis ∧ smoke-test concurrent-send regression gates (§B.37 — 2026-05-28 B7.5 gate queried `email_id` to disambiguate B75a vs B75b, found absent ∴ gate emitted false-zero rows).
V27: Routing pipeline: `thread_match` (Gmail thread_id) → RFC `In-Reply-To` / `References` match → LLM `_try_classify(candidates=active inbound workflows for account)` → unrouted.
V28: Inbound task creation filter (`create_tasks_for_routed_emails` in `database.py`): `direction='inbound' AND workflow_id IS NOT NULL AND contact_id IS NOT NULL AND e.created_at >= w.created_at AND NOT EXISTS (task WHERE email_id=e.id)`. ∀ task row → `task.enrollment_id TEXT NOT NULL REFERENCES enrollment(id)` (schema FK ∴ ⊥ task ∃ ⊥ enrollment row). Inbound path: created task row populates `enrollment_id` from existing enrollment matched by `(workflow_id, contact_id)` — row guaranteed present (`routing.py:_ensure_enrollment` runs upstream in inbound pipeline ∧ emits `enrollment_added` activity there ∴ ⊥ second activity emit @ task-creation site). Outbound first-touch per §V.32 — enrollment row created before scheduling first task; first-touch task carries `enrollment_id` of freshly-created enrollment. Why: outbound mailbox seeing own send ⊥ create task; History re-delivery idempotent; ⊥ contact → ⊥ enrollment ∴ ⊥ task; `e.created_at` = DB insert time (⊥ Gmail `received_at`) excludes pre-workflow inbox traffic — emails Gmail-received before workflow exists but synced after ! ⊥ trigger retroactive auto-tasks. Why-amend (task→enrollment FK): pre-amend denorm on `task` ≡ unenforced link ∴ task ∃ ⊥ enrollment row; §T.77 closes @ schema layer.
V29: ∀ inbound `agent.invoke` prompt → trigger email body inlined at most once. `_build_user_prompt` (`src/mailpilot/agent/invoke.py`) ! exclude `email.id` from `email_history` before passing to `_format_email_history`. Fresh-thread invocation w/ trigger as only contact email → render `No prior email history with this contact.`; ⊥ `Email history (1 messages):` w/ trigger as sole entry. Why: `email_history` query (`database.list_emails(contact_id, account_id, workflow_id)`) returns trigger row, `_format_trigger` re-inlines same body under `New inbound email:` → body printed twice. `_BASE` system-prompt fragment ("When a trigger email is included in your prompt, its full body is already provided -- do not call read_email to fetch it again") already assumes single inclusion.
V30: ∀ `agent.invoke` → user-prompt trigger block ! match span's `trigger` attribute (§V.26). `_format_trigger` (`src/mailpilot/agent/invoke.py`) ! receive explicit `trigger` arg & branch: `email` (∃ email) → `New inbound email:` block; `task` (∃ persisted task row) → `Deferred task:` block w/ description & context; `enrollment_run` ∨ `enrollment_schedule` → first-reach-out framing (e.g. `First reach-out for this enrollment. Compose the initial outbound message per workflow objective & instructions.`) — identical text for both since both signify "first outbound message; ⊥ prior context"; `manual` ∨ outbound fallback → existing `This is an outbound invocation. ...` text. ⊥ `Deferred task:` block when `trigger ≠ task`. Why: §V.26 enforces span-attribute correctness, but prompt framing carries the actual signal the model reads — `enrollment_run` initial reach-out framed as resumed deferred work risks resumption-shaped reasoning (look for prior context that ⊥ exists, call `list_enrollments` to "see where it left off"). Synthesising `task_description="manual <type> run"` at the caller is a code smell hiding the real `trigger` from the prompt builder.
V31: outbound-template agent (`outbound-general`) ⊥ call `record_enrollment_outcome` inside `agent.invoke` w/ `trigger ∈ {enrollment_run, enrollment_schedule, manual}` — tool reserved for `trigger=task` invocations on an enrollment w/ prior outbound email already in thread. `enrollment_schedule` joins the no-record-outcome set because it shares first-touch semantics w/ `enrollment_run` per §V.30 (drained from task queue but agent context ≡ "first outbound message"). Structural gate options (impl-side): (a) omit `record_enrollment_outcome` from outbound agent's bound tool list when `trigger ≠ task`, ∨ (b) `_DEFERRED_TASK` protocol fragment branches on `trigger` per §V.30 — `trigger=enrollment_run` → fragment says "Send the initial email & stop; do not call record_enrollment_outcome; outcome will be assessed when a reply arrives". Why: §B.25 — `_DEFERRED_TASK` text "After completing the workflow objective for a contact, call record_enrollment_outcome" (`src/mailpilot/agent/templates.py:69-74`) treats first-reach-out send as objective-completion → premature `enrollment_completed` activity row; arrival of decline-reply later → second terminal `enrollment_failed` row for same enrollment (two terminal activities per single enrollment). Per-fixture `workflow.instructions` appended after `_DEFERRED_TASK` per §V.45 ordering ⊥ reliably override template-fragment instruction in model reasoning ∴ gate ! be structural ∨ trigger-aware at protocol layer, ⊥ at fixture layer.
V32: `mailpilot enrollment add` accepts optional `--scheduled-at <ISO>` flag. Present → CLI inserts a `task` row w/ that `scheduled_at`, `description="scheduled first reach-out"`, `context={"trigger": "enrollment_schedule"}`, `enrollment_id=<created enrollment.id>`, `email_id` NULL after `create_enrollment`; existing drain (`execute_task` ∈ `src/mailpilot/run.py`) picks up when `scheduled_at <= now()` per §V.21 ∧ invokes the workflow agent w/ `trigger="enrollment_schedule"` per §V.26 (read from `task.context.trigger`, default `"task"`). Absent → behavior unchanged (enrollment row only, no task; manual `mailpilot enrollment run` required). Restricted to outbound workflows: inbound workflow + `--scheduled-at` → `output_error("--scheduled-at only valid for outbound workflows", "invalid_state")` per §V.4 (inbound is reactive ∴ scheduled first-touch nonsensical). Idempotency: re-running w/ `--scheduled-at` on enrollment that already has a pending first-touch task (filter: `task.enrollment_id = E ∧ task.email_id IS NULL ∧ task.status = 'pending'`) returns existing enrollment, ⊥ duplicate task insert. CLI envelope unchanged (`{"enrollment": {...}, "ok": true}` per §V.4); side-effect task surfaces in `mailpilot task list`. Operator-event extended per §V.54: `enrollment.add` event carries `scheduled_at` field when flag set, `changed` list adds `"scheduled_first_send"`. Why: outbound first-send today = manual-only (`enrollment run`, `src/mailpilot/cli.py:2437`); operators want fire-and-forget scheduling (campaign sends, time-zone-aware delivery, batched enrollment w/ staggered first-touch). Lightweight surface — task queue + drain loop already handle deferred execution per §V.21 ∴ this row binds (a) operator-facing CLI entry, (b) trigger semantics so agent ⊥ read drained task as resumed deferred work (§B.25 recurrence class). Orthogonal to §V.28 (inbound auto-task creation, different surface: routing pipeline ⊥ CLI handler, different lifecycle: reactive ⊥ proactive).
V33: `mailpilot enrollment add` ! reject when `contact.email` (case-insensitive ASCII compare) ≡ workflow's owning account email. Handler resolves account via `get_account(connection, workflow.account_id)` after existing FK validation of workflow ∧ contact; match → `output_error(f"cannot enroll contact {contact.email} in workflow {workflow.name}: contact email matches workflow's account email", "self_loop")` per §V.4; ⊥ call `create_enrollment`, ⊥ enter `cli_mutation` span ∴ ⊥ `enrollment.add` operator event ∧ ⊥ `enrollment_added` activity row. Why: contact-email ≡ account-email creates semantic self-loop — inbound workflow notionally processes mail it sent to itself; outbound notionally addresses own mailbox; either direction degenerate. Orthogonal to §V.32 (`--scheduled-at` flag mechanics, outbound-only, scheduling-class) — §V.33 unconditional, direction-agnostic, validation-class. Reject ≡ minimal point of intervention per issue #127 author reasoning (contacts global, constraint per-workflow); broader `contact create` rejection deferred (separable surface).
V34: Drive list ∧ search queries share Shared-Drive flags: `corpora="allDrives"`, `supportsAllDrives=True`, `includeItemsFromAllDrives=True`. List query: `mimeType='text/markdown' AND parents in '<folder_id>' AND trashed = false`; `read_drive_markdown` ! `supportsAllDrives=True` on `files.get` & `files.get_media`. (+) Search query (`DriveClient.search_markdown`): `mimeType='text/markdown' AND '<folder_id>' in parents AND fullText contains '<query>' AND trashed = false`; returns `[{"file_id", "name"}]` ranked by Drive native relevance; ⊥ snippet, ⊥ embeddings, ⊥ semantic match, ⊥ cross-folder. Why: KB folder ? lives in Shared Drive; default corpora excludes SD children → silent empty for impersonated SD member. (+) Search: KB grounding scales past full-folder enumeration; scope locked per §V.35.
V35: Folder access = Drive permission of impersonated user. Folder ID ∉ secrets, ∉ access grants. Multi-tenant isolation delegated to Drive permission model.
V36: FOLDED → §V.34 (search query variant).
V37: ∀ Google API client construction (Gmail, Drive, Pub/Sub) → credential source resolved via fallback chain `google_application_credentials` setting → `GOOGLE_APPLICATION_CREDENTIALS` env var → Application Default Credentials (ADC). DWD impersonation: file creds → `creds.with_subject(email)`; ADC creds → `service_account.Credentials(signer=iam.Signer(Request(), source_creds, sa_email), service_account_email=sa_email, subject=email, scopes=..., token_uri="https://oauth2.googleapis.com/token")` via IAM Credentials API (signs JWTs remotely; no local private key). New Google client modules ! route through `gmail.build_delegated_credentials(scopes, subject)` (impersonated) ∨ `gmail.build_default_credentials(scopes)` (non-impersonated). `mailpilot.sync` Pub/Sub & watch-renewal gates ! use `gmail.has_google_credentials()` (file ∨ ADC reachable), ⊥ raw `settings.google_application_credentials` truthiness. Project-ID resolution → `gmail.resolve_project_id()` (JSON `project_id` field ∨ ADC default project). Required IAM in ADC mode: instance SA holds `roles/iam.serviceAccountTokenCreator` on itself; IAM Credentials API enabled. Why: pre-fix code raised SystemExit when both setting & env empty → ADC blocked entirely → GCE deploy of `mailpilot-1.lab5-mailpilot-prd1` crash-looped 46+ iters until §B.15 fix; centralised helpers ≡ single audit point so a new client (Calendar, Sheets, …) cannot re-introduce the gap.
V38: ∀ agent tool whose underlying client ⊥ thread-safe (e.g. `httplib2.Http`-backed `googleapiclient.discovery.Resource`, ∨ any shared mutable transport across calls) → `Tool(...)` ! `sequential=True` on registration ∈ `src/mailpilot/agent/templates.py`. Pydantic AI dispatcher serializes that tool's invocations even when model emits in parallel; thread-safe peer tools retain parallel dispatch. @ amend time binds `_DRIVE` tuple ∈ `inbound-google-drive` template (`list_drive_markdown`, `read_drive_markdown`, `search_drive_markdown`). Contract test enumerates `Tool` entries per template ∴ regression caught when (a) a Drive `Tool` drops the kwarg, (b) Pydantic AI removes the flag, (c) new non-thread-safe binding lands w/o it (future Gmail-tool expansion, Calendar ∨ Sheets via `google-api-python-client`). Why: `googleapiclient.discovery.build(...)` hard-wired to `httplib2` — modern Cloud Client Libraries ⊥ cover Drive ∨ Gmail; `httplib2.Http` carries one connection-pool dict w/o locks ∴ 2 threads sharing one instance race connection state; Pydantic AI dispatches sync tools via `asyncio.to_thread` ∴ Anthropic-emitted parallel calls land on separate threads against same transport. §B.34 — `read_drive_markdown` emitted ×2 parallel, one returned 1.12s ∧ sibling hung 60.83s @ socket timeout, killed agent run. §V.49 transient classifier correctly returned `is_transient=True` but race ≡ deterministic ∴ all 4 retries hung. Structural gate @ registration ≡ single point of intervention: per-call `Http` rebuild eats DNS+TLS handshake ∧ ⊥ speak Pydantic AI's contract; async/httpx Drive transport ≡ reimpl Drive REST against httpx (large YAGNI hit).
V39: ∀ agent tool failure → return `{"error": CODE, "message": TEXT}` dict. ⊥ raise to agent.
V40: ∀ agent run → ≥1 tool call. ∀ system-prompt fragment naming example tool sequence → ≥2 tool names (e.g. `reply_email` & `noop`). Why: single-tool example collapses to literal — agent emits exactly one tool named ∴ ⊥ generalises. Decline prose lives in shared `_DECLINE` fragment composed into every template's protocol per §V.45 — one edit point; same rule binds future fragments.
V41: ∀ KB-grounded reply ∧ `search_drive_markdown` ≥2 hits → agent ! `read_drive_markdown` on top ≥3 (∨ all hits if <3) before composing reply. ∀ KB-grounded reply ∧ 2 consecutive `search_drive_markdown` calls yield insufficient hits to ground reply → agent ! fall back to `list_drive_markdown` (⊥ further search retries). Compare-and-contrast invocations (reply targets ≥2 distinct product families per classifier ∨ agent reasoning) — search-budget rule applies per-target: agent ! issue ≥1 `search_drive_markdown` w/ target-specific query per distinct product target before `list_drive_markdown` becomes acceptable for that target, even when other targets already grounded via earlier searches. Why: model-number queries (e.g., `WS36-600-2`) match both general & model-specific docs; first-hit grounding picks wrong doc → wrong specs. Search-budget cap bounds search-thrash latency — smoke-test 2026-05-28 B7 observed 5 successive search retries + 1 list fallback before reading correct docs (~3s latency burn) when agent broadened queries hoping for better relevance. Rule lives in `_DRIVE_GROUNDING` fragment bound only to `inbound-google-drive` template per §V.45; `outbound-general` & `inbound-general` templates physically cannot trigger this path. Out-of-scope decline path (§V.40) unaffected. Why-amend (per-target rule): smoke-run 2026-06-03 B7 compare (qa-cmp-011, Pure Aqua PAPV vs ROPV R8040B) — agent issued 4 searches + 1 `read_drive_markdown` (Pure Aqua) + 1 `list_drive_markdown` + 1 `read_drive_markdown` (ROPV); list fallback resolved second-target discovery but skipped per-target search budget for ROPV, masking a search-quality regression class (model-number collision would land wrong doc). Reply was groundedness-PASS that run; tool-sequencing gap ≡ latent class ∴ amend pins discovery topology before list fallback applies per-target.
V42: ∀ `reply_email` ∨ `send_email` invocation w/ body containing key-value cluster (≥3 consecutive lines each matching `^[^|]{1,80}\s+\S` — short line ≤80 chars, label + whitespace + value, ⊥ pipe on line; whitespace count ≥1 regardless of size) ∧ ⊥ `|---|` (∨ `| --- |`) pipe-table separator anywhere in body → tool ! return `{"error": "format", "message": "spec body MUST use Markdown pipe-table w/ |---| header separator"}` → agent retries per §V.39. Heuristic ! match regardless of value shape (numeric, text, units, ranges); ASCII rule-line runs (`-{6,}`, `={6,}`, `_{6,}` on standalone line) ⊥ count as pipe-table separator. Why: §B.5, §B.9, §B.14, §B.36 = same prompt-fidelity drift recurring after prompt-only fix (§T.7), numeric-only lint (§T.9), ∧ 2-space lint (§T.31); §B.14 KDF rows non-numeric (`Mesh Size  20×50`) ∴ pre-T.31 regex missed them; §B.36 single-space `**label** value` (smoke-test 2026-05-28 B4 ∧ B7) slipped pre-T.62 `\s{2,}` regex ∴ widened to `\s+`. Line-length bound `{1,80}` preserves prose immunity (multi-clause prose lines typically >80 chars). ASCII rule-lines = workaround surface. Decline path (§V.40) unaffected — decline bodies ⊥ contain spec rows.
V43: SUPERSEDED by §V.44 / §V.45.
V44: `workflow.template` ∈ {`outbound-general`, `inbound-general`, `inbound-google-drive`}. NOT NULL & schema CHECK matching `WorkflowTemplateName` Literal (`src/mailpilot/agent/templates.py`). `workflow.type` populated server-side from `TEMPLATES[template].direction` at `create_workflow`; ⊥ caller-passed `type` mismatch. `update_workflow` ! reject `template` change. Why: template change = tool-list change = destabilizing; force delete & recreate. Existing reads of `wf.type` in `routing.py`, `sync.py`, `cli.py`, `schema.sql` unchanged.
V45: Agent shape owned by template, ⊥ workflow. ∀ Agent instantiation → `Agent(name=..., deps_type=..., tools=list(TEMPLATES[wf.template].tools), instructions=TEMPLATES[wf.template].protocol + wf.instructions)`. ⊥ per-workflow tool override, ⊥ per-workflow protocol override. New tool surface (SQL, Confluence, …) → new template constant; ⊥ bolt onto workflow. Protocol = ordered concat of named fragments: `_BASE → _DEFERRED_TASK → [overlay]? → _DECLINE → _NO_FABRICATION`; `wf.instructions` appended last by the instantiation rule above. Template-specific overlays (e.g. `_DRIVE_GROUNDING` ∈ `inbound-google-drive`) slot between `_DEFERRED_TASK` & `_DECLINE` — guardrails (`_DECLINE` + `_NO_FABRICATION`) ! remain final two framing fragments so they bind regardless of overlay. Each rule lives in exactly one fragment (§V.40 → `_DECLINE`; §V.41 → `_DRIVE_GROUNDING`). Why: single edit point for invariants; structural tool gate; auditable via `WHERE template = ...`; canonical order pinned ⊥ drift between templates ∧ ⊥ accidental overlay-shadows-guardrail reorder.
V46: ∀ template name = `<direction>-<data-system>` (direction ∈ {`outbound`, `inbound`}; data-system = concrete binding: `general`, `google-drive`, future `postgres`, `confluence`). Naming by binding ⊥ abstract concept. Why: tools (`_DRIVE` tuple) & protocol fragment (`_DRIVE_GROUNDING`, mentions `search_drive_markdown` explicitly) = Google-Drive-specific; abstract names (`inbound-knowledge-base`) hide binding & create ambiguity when parallel data-source templates land.
V47: ∀ Anthropic LLM call (workflow agent in `src/mailpilot/agent/invoke.py` ∧ classifier in `src/mailpilot/agent/classify.py`) → request ! set Anthropic `cache_control` breakpoint on stable prefix (system prompt + tool definitions) so multi-turn invocations re-bill prefix as `cache_read_input_tokens` ⊥ as fresh input. `agent.invoke` rollup span attrs ! include `cache_read_input_tokens` ∧ `cache_creation_input_tokens` summed across child `chat` spans. Why: stable ~1500-token prefix repeats verbatim every turn; uncached re-billing inflates inbound multi-turn input tokens ~2x (smoke-run 2026-05-05 B4: 6 chat calls × ~1500-token prefix = ~9000 redundant tokens); rollup-level attrs avoid per-turn-span join for cost reporting & enable single-row cache-hit-rate alerting.
V48: `AnthropicModel` constructor in `src/mailpilot/agent/invoke.py` (`_build_agent`) ∧ `src/mailpilot/agent/classify.py` (`_get_model`) ! pass an `httpx.Client(timeout=httpx.Timeout(240.0))` (∨ Pydantic-AI-equiv `http_client` / `model_settings`) — explicit 240s read-timeout, 4× default 60s. ⊥ retry on `httpx.ReadTimeout` ∨ `anthropic.APITimeoutError`: a tool call mid-turn (e.g. `send_email`, `reply_email`, Drive writes) cannot be safely re-driven — idempotency ⊥ guaranteed across SMTP / Drive side-effects. ∴ timeout → bubble to `run.task.agent_failed` per §V.51, operator replay. Why: 2 prod TimeoutError events (§B.16) hit at exactly the default 60s read window during long-context multi-turn invocations (post-`read_drive_markdown` of 13KB doc → next chat completion stalled); 60s ⊥ sufficient under model load, 180s borderline (events near deploy window) ∴ 240s = headroom. Test: httpx-mock asserts client constructed w/ 240s read-timeout on `AnthropicModel(...)` in both modules; assert ⊥ retry wrapper around chat call (bare except @ `run.py:127` is the intended terminal handler).
V49: Task retry classification ≡ exception-class + status-code allow-list ∈ `src/mailpilot/agent/retry.py`. Allow-list: Drive/Gmail `googleapiclient.errors.HttpError` w/ status ∈ {500, 502, 503, 504, 429}; Anthropic `APIStatusError` w/ status ∈ {502, 503, 529}; `socket.timeout`/`TimeoutError` raised inside Drive tool spans. ∉ allow-list: `anthropic.APITimeoutError`, `httpx.ReadTimeout` — §V.48 exclusion preserved (mid-turn timeout ⊥ safely re-driven across SMTP/Drive side-effects). Max attempts = 4 (1 initial + 3 retries). Backoff schedule = [30s, 120s, 300s] + jitter, hardcoded (⊥ `Settings` field — YAGNI). Retry path: `attempt_count += 1`, `scheduled_at = now() + backoff`, `status` stays `pending`, `result` updated w/ last-failure summary; budget exhausted → `complete_task(status="failed", result={"reason": ..., "attempt_count": N, "terminal": "max_attempts"})`. `task.attempt_count INTEGER NOT NULL DEFAULT 0` added to schema; `task_pending_trigger` extended `AFTER INSERT OR UPDATE OF status, scheduled_at` (drain loop wakes on retry reschedule; terminal-status wakes accepted as benign noise). `Task` ∧ `TaskSummary` models gain `attempt_count: int`. `mailpilot task retry --task-id <ID>` manual cmd allowed on `failed`/`cancelled` rows only — refuses `completed` (tools already fired → retry = duplicate side-effects) ∧ `pending` (already queued, no-op); resets `status='pending'`, `attempt_count=0`, `scheduled_at=now()`, fires `pg_notify`. Drive socket-timeout cap via `httplib2.Http(timeout=...)` on `build_drive_service` bounds 60s Drive-side stall window so classifier sees `socket.timeout` quickly. ∀ retry path → paired `operator_event("task.retry", task_id, attempt, exc=type(exc).__name__)` per §V.51 pattern. Why: §B.16 — failed task row blocks any re-processing of same email forever (`create_tasks_for_routed_emails` ∧ `get_unprocessed_inbound_email` both filter `NOT EXISTS (task WHERE email_id=e.id)`); transient API blip ≠ terminal failure; only Anthropic *server-side status codes* (502/503/529) retry since those are returned before any tool call fires ∴ §V.48 idempotency rule honored, ⊥ amendment needed. `socket.timeout`/`TimeoutError` allow-list entry assumes real Drive-side stall (transient, independent across retry attempts); local concurrency race (§B.34) violates assumption — all 4 retries hung deterministically; §V.38 prevents recurrence @ registration ∴ allow-list assumption holds post-§V.38.
V50: ∀ `agent.invoke` w/ `input_tokens ≥ 1024` (Anthropic prompt-cache eligibility floor) → rollup span ! carry `cache_creation_input_tokens` ∧ `cache_read_input_tokens` attrs (value ≥ 0 admitted; presence asserts §V.47 plumbing intact). Strict `> 0` first-invoke outcome ⊥ unit-testable w/o live Anthropic endpoint ∴ behaviour-binding deferred to smoke-test operator inspection per §V.58 Phase 5 §1 Logfire summary. Why: prior wording bound `> 0` on first invoke per `(workflow_id, trigger)`; 2026-05-12 smoke A3 (`trigger=enrollment_run`, outbound, 4483 input_tokens) → `cache_creation_input_tokens=0`; A7 next outbound → `cache_creation_input_tokens=2282`; inbound B4 ∧ B6 wrote caches normally ∴ Anthropic-side ⊥ at fault, breakpoint silently absent on outbound first-invoke path (§B.28). /sdd:check 2026-05-27 flagged strict form UNVERIFIABLE — unit tests ⊥ assert `> 0` w/o live LLM call; attr-presence form aligns audit infra (`tests/test_agent_invoke.py` capfire spans) w/ invariant. §B.28 first-invoke skip remains observable via smoke-test Logfire summary; automated recurrence guard moves from unit-test layer to operator inspection at smoke time.
V51: ∀ new logfire.exception site reachable from `mailpilot run` → paired `operator_event("error", source=<event_name>, message=str(exc))`. Why: keeps operator stderr stream complete under journald.
V52: Logfire spans split by `deployment_environment` ∈ {`development`, `production`} from `logfire_environment` setting. Project = `mailpilot` (token-scoped). MCP queries ! `WHERE deployment_environment = '<env>'`.
V53: ∀ `running tool` span → structured `gen_ai.tool.name` attribute set (OTel GenAI semantic convention, populated by `logfire.instrument_pydantic_ai()` in `src/mailpilot/cli.py`). Logfire dashboards & alerts ! query `attributes->>'gen_ai.tool.name'`; ⊥ parse free-text `message` ("running tool: <name>"). Why: convention-aligned attribute already exists upstream; inventing sibling `tool_name` duplicates data & drifts from OTel.
V54: ∀ operator-initiated CRM config mutation via CLI = noun ∈ {account, company, contact, workflow, enrollment, tag, note} ∧ verb ∈ {create, update, disable, add, remove, start, stop, cancel, import} → CLI handler ! wrap mutation in `logfire.span("<noun>.<verb>", entity_id?, account_id?, ...attrs)`; on success → `operator_event("<noun>.<verb>", entity_id=..., account_id=..., changed=[<field>...])`; on raise → `logfire.exception(...)` + paired `operator_event("error", source="<noun>.<verb>", message=str(exc))` (same pairing shape as §V.51, extended to CLI surface — ⊥ only `mailpilot run`) + structured-envelope translation: `psycopg.Error` subclasses caught at the `cli_mutation` wrapper boundary (`src/mailpilot/operator_log.py`) after the observability pair → `output_error(str(exc), code)` w/ code derived from constraint class (`UniqueViolation`→`duplicate_key`, `ForeignKeyViolation`→`foreign_key_violation`, `NotNullViolation`→`not_null_violation`, `CheckViolation`→`check_violation`, else `database_error`); `output_error` raises `SystemExit(1)` ∴ control flow terminates cleanly — Click default handler ∧ Python default printer never see a `psycopg` exception bubble; raw `Traceback (most recent call last):` output ⊥ admitted on CLI mutation error paths, stdout stays strict-JSON-empty per §V.3, stderr carries {`event=error` operator line ∪ JSON envelope `{"error": <code>, "message": ..., "ok": false}`}. Span placement = CLI handler layer (`src/mailpilot/cli.py`) ∴ self-discriminates operator from (a) sync-driven auto-creation (e.g. `create_or_get_contact_by_email` during inbound sync — DB fn, ⊥ CLI handler), (b) agent-tool mutations already covered by §V.53 `gen_ai.tool.name`. `changed` semantics: `create` → field set populated at insert; `update`/`disable` → field names whose values actually mutated (pre-vs-post row diff via paired `get_<noun>` reads inside the span); `add`/`remove` → relational key set; `start`/`stop`/`cancel` → terminal status name; `import` emits per-row events ∴ idempotent re-run ≡ empty `changed` lists per row (verifiable audit per §V.63). Test contract: per §V.64 `capfire: CaptureLogfire` — (a) span name + attrs on success, (b) `operator_event` emission w/ correct `changed`, (c) `logfire.exception` + paired error event on raise, (d) `psycopg.Error` raised inside `cli_mutation` → exit 1, stdout empty, stderr JSON envelope w/ mapped code, ⊥ `Traceback` substring. Why: CRM config mutations = operator-curated identity state w/ zero observability today; CLI-layer placement = natural seam separating operator from sync ∧ agent paths; one uniform invariant binds the surface ∴ future nouns / verbs inherit obligation by convention ⊥ per-entity invariants. Why-amend (envelope-translation): §B.52 — `cli_mutation` re-raised after observability pair → Click default handler dumped 30-line psycopg traceback; DB constraint rejection ≡ control-path signal ⊥ crash; per-handler try/except ∝ ~25 mutation cmds ∴ single seam inherits obligation.
V55: Logfire content scrubbing ⊥ redact `tool_response` field on Pydantic-AI `running tool` spans (covers ∀ agent tool outputs — currently `read_drive_markdown` content the only sizeable body, rule binds future tools w/ structured `tool_response` payload uniformly). Impl: `logfire.configure(..., scrubbing=ScrubbingOptions(callback=<f>))` ∈ `cli.configure_logging`; `<f>(match: ScrubMatch)` returns `match.value` when `match.path[:2] == ("attributes", "tool_response")`; else returns `None` (delegate to default scrubber). Why: 2026-05-12 smoke-run B4/B6 — default substring matcher matched `"auth"` inside KB markdown (likely from `"authorized"` in `watts-smartstream-uv-disinfection.md`) → `tool_response.content` replaced w/ `"[Scrubbed due to 'auth']"` → trace structurally ⊥ show what KB content the agent grounded against ∴ §V.57 grounding regressions ⊥ verifiable from traces alone. KB markdown ≡ non-sensitive product documentation; agent tool outputs ⊥ carry secrets by design (no credentials, tokens, user PII flow through tool returns). Test: `capfire: CaptureLogfire` per §V.64 asserts `running tool` span whose `attributes.tool_response.content` contains substring `"authorized"` retains literal content (⊥ placeholder) post-scrub.
V56: smoke-test out-scope QA verifier (`.claude/skills/smoke-test/scripts/qa.py:check`) ! exclude tokens present in original question before fabrication regex match. Why: agent decline that echoes "your question about X 50,000 ppm" trips verifier despite ⊥ fabrication. `details.declined: true` authoritative when ⊥ fabrication block detected outside echoed question.
V57: smoke-test in-scope KB grounding gate (Scenario B, gate B4) ! operator-judged. Load source doc via `qa.py source --id ID` (DriveClient impersonating `inbound@lab5.ca` against `MailPilot Demo` folder) → operator emits structured JSON verdict {`qa_id`, `question`, `source_file`, `source_file_alts: list[str]` (default `[]`), `answers_question`, `every_factual_claim_supported_by_source`, `cites_source_file`, `unsupported_claims: [...]`, `verdict`}. `verdict='pass'` ↔ all 3 booleans true & `unsupported_claims=[]`. `cites_source_file=true` ↔ agent's citation ∈ {`source_file`} ∪ `source_file_alts` (set union — admits cross-source identifier collisions e.g. model `WS36-600-2` ∈ both `pure-aqua-industrial-water-softener.md` ∧ `pure-aqua-sf-100s-industrial-water-softener.md` w/ divergent specs per §B.40). ⊥ token-substring match against curated `expected_tokens`. ⊥ free-form rating — `unsupported_claims` ! name each unsupported claim verbatim (structural defence against LLM-judge sycophancy). Why: substring match false-negatives on phrasing variation (`0.48 mm` vs `0.48mm`); §V.56 digit-echo exclusion = 2nd regex band-aid for same brittleness class ∴ regex approach at limit; per-Q token curation = unbounded maintenance debt as KB grows. Out-scope decline (B6) keeps script verifier — regex appropriate for shape detection (vendor-near-digits fabrication, decline-phrase presence). `source_file_alts` admits inherent KB shape (cross-source identifier collisions ⊥ author-error) ∴ ⊥ regrade by hand each run.
V58: smoke-test final report (`.claude/skills/smoke-test/SKILL.md` Phase 5) ≡ 3 sections, actionable-only: §1 **Execution** — phase-results matrix (current Part A) + ≤3-bullet Logfire signal summary (top span by volume, error/warn count, cache-hit ratio on multi-turn invokes); one screen max. §2 **Bugs** — observable failures, each = single paragraph + `Spec action:` line per existing severity matrix (`bug` ∨ `bug+invariant` ∨ `code-only`); Critical/High w/ `bug`∨`bug+invariant` auto-invoke `/sdd:spec`; Medium/Low print-only. §3 **Invariants** — under-specified contracts surfaced by inspection (⊥ concrete failure this run), each = single paragraph + `Spec action: propose-invariant` invocation; print-only, operator pastes. Spec hand-off block unchanged. Auto-write `./smoke-test-<YYYY-MM-DD>-<HHMMSS>.md` unchanged. ⊥ Notes section, ⊥ `none`-routed Suggestions, ⊥ `propose-task` Suggestions, ⊥ Part B SQL transcript ∈ output, ⊥ Part C 13-cat narrative ∈ output. Logfire SQL queries ∧ 13-cat scan ≡ derivation aids ∈ procedure body, ⊥ rendered as report sections. Why: prior 5-part report inflated output ~3× w/ FYI prose ⊥ acted on; operator's actual need = queue of `/sdd:spec` inputs ∴ report ≡ queue, ⊥ narrative.
V59: `.claude/skills/demo-test/SKILL.md` ≡ liveness probe of public demo address `hello@lab5.ca` against deployed production instance. Source acct = `outbound@lab5.ca` (local CLI sender); target = `hello@lab5.ca` (processed by prod deploy, ⊥ local run loop). Setup: ⊥ `make clean`, ⊥ account create, ⊥ workflow import — assume warm state; pre-flight verifies `mailpilot account list` ∋ `outbound@lab5.ca`; pre-flight fails → exit `FAIL` w/ missing-prereq line ⊥ send. Scope: single in-scope question via `qa.py pick --type inscope` (reuse `.claude/skills/smoke-test/scripts/qa.py` verbatim, ⊥ new scripts); ⊥ out-of-scope arm; ⊥ Scenario A outbound flow. Subject = `[DEMO-<HHMMSS>] <topic>` (distinct prefix from `[ST-...]` per smoke-test) — fresh-randomised per run. Send via `mailpilot email send --account-id <OUTBOUND_ACCOUNT_ID> --to hello@lab5.ca --subject ... --body <question>`; capture `TEST_START` ISO before send. Gates (ALL ! pass for `PASS`): G1 reply round-trip — query Gmail directly (impersonate `outbound@lab5.ca` per §V.37; `GmailClient.list_messages(query="from:hello@lab5.ca after:<TEST_START_EPOCH>", label_ids=["INBOX"])` invoked via inline `uv run python -c '...'` ∨ thin CLI verb wrapping same — ⊥ `mailpilot email list` per §V.60) ≤18 attempts × 5s; match unique `<topic>` tail (Gmail prepends `Re: ` ∴ substring); reply present within 90s. G2 groundedness — operator-judged per §V.57, `qa.py source --id ID` loads source doc; structured JSON verdict ({`answers_question`, `every_factual_claim_supported_by_source`, `cites_source_file`, `unsupported_claims`, `verdict`}); `verdict='pass'` required. G3 Logfire production env — window `[TEST_START, now]`, filter `WHERE deployment_environment = 'production'` per §V.52; required spans present {≥1 `agent.invoke` `trigger='task'` per §V.26, ≥1 `running tool` `attributes->>'gen_ai.tool.name' = 'search_drive_markdown'` per §V.53, ≥1 `gmail.send_message`}; zero spans w/ `is_exception = true` ∨ `level ∈ {'error','warn'}` in window. Output: stdout = single line `PASS` ∨ `FAIL: <one-line reason>` + 3-bullet Logfire summary (top span by volume, error/warn count, `cache_read_input_tokens` ratio per §V.47). ⊥ phase matrix, ⊥ §1/§2/§3 sections per §V.58, ⊥ auto-write `.md` to disk, ⊥ auto-invoke `/sdd:spec`. Why: `/smoke-test` ≡ destructive 2-scenario regression suite (`make clean`, ~10min) ∴ unfit for routine "is public demo alive?" probe; `/demo-test` ≡ binary liveness check against published `lab5.ca/mailpilot/` promise running on prod deploy, exits <2min, fit for post-deploy spot-check. Why-amend (60s→90s): §B.45 — B7 64s, B75b 63s (both groundedness-PASS); multi-doc synthesis + concurrent-load contention exceeds 60s ∴ promise relaxed to 90s. Why-amend (two-budget): §B.53/§B.54 — T_delivery (Gmail Pub/Sub waves, 0-122s p95 N=25) + T_agent (25-50s) ≡ jointly-uncontrolled; 90s end-to-end promise unchanged ∴ G1 gates `agent.invoke.end - T_SEND ≤ 90s`; G1 FAIL → decomposition summary (`sla_agent_seconds` vs `sla_delivery_seconds`). §V.69/§V.70/§V.71 bind `sla_agent` ∴ smoke gate ⊥ false-FAIL on Gmail-batched wave.
V60: liveness probes (skills under `.claude/skills/<name>/SKILL.md` whose explicit purpose ≡ "is deployed system alive?" — e.g. `/demo-test` per §V.59) ! query authoritative production-facing surface directly (`GmailClient`, `DriveClient`, Logfire MCP) — ⊥ via local-mailpilot-DB reads (`mailpilot email list`, `mailpilot contact list`, etc.) that depend on a separately-started `mailpilot run` ∨ explicit sync step to be fresh. Implementation: probe SKILL.md procedure ! invoke source-of-truth API via inline `uv run python -c '...'` against `mailpilot.gmail` / `mailpilot.drive` ∨ thin `mailpilot` CLI verb wrapping same. Out-scope: full regression suites (`/smoke-test`) that own their own sync lifecycle ∧ ⊥ probes. Why: probe nominally tests deployed system; local DB only mirrors Gmail/Drive after sync; probe procedure forbids local loop (§V.59 setup) ∧ ⊥ trigger sync → local-DB poll = checking the wrong surface → false-FAIL on a healthy production (§B.23, 2026-05-11 `/demo-test` run: G1 polled 60s ∧ found nothing while production had replied <5s after send, surfaced post-hoc by manual `mailpilot account sync`).
V61: smoke-test ∧ demo-test gates w/ span-backed observables (`agent.invoke`, `running tool`, `gmail.send_message`, etc.) ! derive primary verdict from Logfire spans, ⊥ wall-clock + CLI poll. Latency SLA = `agent.invoke.end_timestamp - T_SEND` (server-computed via Logfire query), ⊥ poll-attempt count × cadence. CLI polls (e.g., `mailpilot email list`) reserved for side-effect verification only — "did the reply round-trip back to the inbox at all?" — w/ generous cap (≥120s) since reply-arrived signal ⊥ latency-budget gate. Why: §B.38 — smoke-run 2026-05-28 B7 measured agent span 51s but 5s poll first observed reply @ attempt 11 (~64s polled) → borderline gate verdict needed Logfire cross-check; B7.5 graded latency from `T_SEND` wall-clock w/o subtracting unavoidable sync-tick lag (~12s observed: T_SEND_B75=15:14:24Z, first `agent.invoke` start=15:14:36Z) → inflated measured latency above pure agent budget. Logfire ≡ authoritative source for span data (already binds §V.52 telemetry contract, §V.53 tool-name attr, §V.59 G3 demo-test gates). Orthogonal to §V.60: §V.60 binds liveness probes' data source (regression suites explicit out-scope); §V.61 binds gate-verdict source within regression suites — different scope, complementary rule. Why-amend (two-budget split): §B.53/§B.54 — pre-amend smoke gate conflated `agent.invoke` w/ Gmail-side T_delivery (waves +52/+73/+86/+100-108s N=25) → flapped on Gmail variance. `sla_agent_seconds` ≡ `agent.invoke.end − task.created_at` (drain) ∨ `invoke duration` (operator); `sla_delivery_seconds` ≡ `task.created_at − email.received_at`. Burst gate `p95(sla_agent) ≤ 75s`, steady `≤ 50s`; `sla_delivery` advisory only. G1 stays end-to-end (per §V.59); G1 FAIL → decomposition summary. Why-amend (compare-type ceiling): §B.45 (64s), §B.60 (63s), §B.61 (84s) — compare-type invocations (≥2 `read_drive_markdown` calls per `agent.invoke` trace) breach the 50s steady ceiling on every clean run; 50s structurally under-estimates 2-datasheet context + synthesis cost (in_tok ~60k vs ~22k single-source). Compare-type ceiling = 90s steady (smoke gate B7 treats `sla_agent_seconds > 90s` as Critical; 50s < sla_agent ≤ 90s advisory); steady 50s ceiling now applies to single-source ∧ decline invocations only. Burst C4.a `p95(sla_agent) ≤ 75s` gate computed over non-compare invocations; compare spans reported separately w/ advisory burst ceiling 120s.
V62: `/release` project-local slash cmd extends `/gh:release` semantics. After local SemVer tag (`/gh:release` Procedure §1–§12), `/release` ! (a) `git push origin main && git push origin v<x.y.z>`, (b) `uv build` → assert `dist/mailpilot-<x.y.z>-py3-none-any.whl` ∃, (c) `gh release create v<x.y.z> --verify-tag --notes-from-tag dist/mailpilot-<x.y.z>-py3-none-any.whl`. Confirm-before-mutate gate (per `/gh:release` §9) covers push ∧ asset upload. Manifest = `pyproject.toml [project].version`; build backend = `uv_build` ∴ wheel name pattern fixed. Why: deploy = install of published `.whl` asset reachable via `gh release download`; tag-only release ⊥ deployable. `/gh:release` plugin cmd unchanged — covers tag-only repos.
V63: `workflow export` / `workflow import` = declarative round-trip on (`account_id`, `name`). Export → `{"workflows": [{name, template, objective, instructions, theme}, ...], "ok": true}` per `--account-id`. Import → JSON from `--file` ∨ stdin; ∀ row: absent → `create_workflow` (activation = side-effect of objective ∧ instructions both non-empty per existing create-path rule); present → `update_workflow` for changed fields ⊥ `template`; `template` differs → per-row `{"error": "template_immutable"}` per §V.44, batch continues. Import ! ⊥ write `workflow.status`. Round-trip on unchanged DB ≡ ⊥ row mutations 2nd run (idempotent upsert). Why: `make clean` wipes workflow rows; declarative replay = source-of-truth ⊥ operational state ∴ status ∉ payload; template change = §V.44 destabilising → row-error, ⊥ batch abort.
V64: ∀ new agent tool → unit tests cover {hit, no-hit, error}. ∀ new CLI cmd → tests patch `get_*` FK validators.
V65: `make check` ! green (ruff & basedpyright strict & pytest).
V66: ∀ §-cite in repo prose (code comments, docstrings, SPEC.md body, CLAUDE.md, `.claude/skills/**/*.md`, `tests/`) ! dotted form `§[VTB]\.[0-9]+`. Dotless `§V<n>` / `§T<n>` / `§B<n>` ⊥ allowed. Bare-form `V<n>` / `T<n>` / `B<n>` (no `§` prefix) as spec citation in prose ⊥ allowed even though bare-form ∉ auditor grep scope (collision risk per `V32` as CSS class ∨ version label) — prose-intent = §-cite ∴ ! carry `§` prefix. Why: `/sdd:check` cite-resolution regex grep ≡ `§[VTB]\.[0-9]+` (explicit dot ∧ § prefix) ∴ dotless ∨ bare evades auditor; §B.22 (skills) + §B.24 (code-dotless) + §B.30 (code-bare) ≡ same sub-class (cite-shape malformation) recurring 3×. Bare-form ∧ wrong-section prose drift (§B.22 sub-class a) ⊥ regex-catchable — operator review only.
V67: ∀ outbound email persisted via `sync.py` send path → DB row's `in_reply_to` ∧ `references_header` ! match values passed to `gmail_client.send_message(in_reply_to=..., references=...)` for same message. `_resolve_threading_headers(...)` return locals (`resolved_in_reply_to`, `resolved_references`) ! flow into `create_email(...)` kwargs (`in_reply_to=resolved_in_reply_to`, `references_header=resolved_references`). Why: both fns accept threading-header kwargs by name ∴ Python ⊥ enforce that what you pass to one you pass to the other → outbound write path can drop the locals silently after `send_message` → DB-row threading metadata NULL while wire-state correct (§B.44). Inbound path (`sync.py:889-890`) already passes received headers ∴ rule restores wire/DB parity symmetry across directions. Recurrence class binds outbound-write column completeness for RFC 2822 wire-mirroring columns (`in_reply_to`, `references_header`); future RFC-header persistence additions inherit obligation. Test: outbound classifier-driven reply DB row's `in_reply_to`/`references_header` ≡ resolved values; `mailpilot email view <id>` returns non-NULL on mid-thread outbound.
V68: ∀ `reply_email` tool call ∈ agent.invoke → ∀ numeric-spec value-token cited ∈ proposed reply body for a model/product identifier ! appear verbatim ∈ pipe-table-row line of tool-return `content` of at-least-one same-invocation `read_drive_markdown` call. Enforcement: `reply_email` tool wrapper (`src/mailpilot/agent/tools.py`) maintains per-invocation read-ledger keyed by `file_id`; pre-send hook tokenizes proposed body, asserts ∀ candidate spec-value-token ∈ ledger corpus (per-doc scoping → `.claude/check-extras.md §V.68`), returns structured `{"error": "fact_check_mismatch", "unsupported": [...]}` on miss (agent re-drafts, ⊥ raise). ⊥ ledger (zero `read_drive_markdown` calls — e.g. out-of-scope decline) → pre-send hook no-op. Why: §B.46 — fabricated WS36-600-2 flow rates absent from both same-invocation `read_drive_markdown` returns; agent acknowledged correct values in a hedging note → ⊥ probabilistic fix; ledger-based check ≡ deterministic refusal path. Orthogonal to §V.57 (grader) ∧ §V.41 (search path). Why-amend (pipe-table-row scoping): §B.56 — plain `token in union` silent on fabricated values via prose-line collisions (`"110"` ∈ `"35-110 degrees F"`, `"125"` ∈ `"25 - 125 psi"`); column-context-blind ≡ impl gap. Why-amend (per-document scoping): §B.58 — §T.85 pipe-row-only filter + prose-only doc → empty union → 209% retry rate, 1.85M tok, 413s. Per-doc scoping: table-bearing docs → pipe-row + list-item lines; prose docs → full content (corpus algo → `.claude/check-extras.md §V.68`). Preserves §B.56 anti-collision; single-doc collision class still rejected. Why-amend (list-item inclusion): §B.60 — table-bearing docs' pipe-row-only branch excluded `"- Key: Value"` bullet lines → 10 tokens spuriously rejected; §V.71 cap fired at attempt=3 on all 5 compare invocations; B7 sla_agent=63s, C p95=93.75s, §V.70 retry rate 21.9%. Bullet-list lines carry explicit `Key:` labels ∴ context-anchored, ⊥ §B.56 prose-coincidence class; safe to admit alongside pipe-row lines.
V69: burst T_delivery contract. ∀ inbound email ∈ N-burst, `task.created_at − email.received_at` (routing + task insert lag) MUST be ≤ `burst_sla_ceiling − avg_invoke_s` ≈ 75s. Impl: after any `_run_periodic_iteration` pass that classifies N_new > 0 new inbound emails (`routing.route_email` emits a task insert), sync loop ! immediately set `wakeup_event` + `do_full_sweep=True` before returning to `wakeup_event.wait()` — eliminates inter-wave gaps driven by Gmail Pub/Sub batching delivery windows. Monitoring (smoke §C4.a): `max(task.created_at − email.received_at)` per burst window ≤ 75s asserted alongside existing latency gates. Why: §B.53 — 25-email burst Gmail delivered Pub/Sub notifications in 5 waves (+15/+25/+52/+105/+122s from T_SEND_C); wave-4 (11 invocations) started +105s, leaving ≤5s effective budget before 110s burst ceiling — infeasible regardless of pool size; §V.23(+) formula `ceil(N × avg_s / sla_s)` ≡ throughput bound only, ⊥ T_delivery bound. Orthogonal to §V.23 (pool size) ∧ §V.24 (drain ⊥ sync serialization) — §V.69 binds classification-pipeline latency, not concurrency depth. Why-amend (scope narrowing): §B.54 — §T.83 wakeup-on-new (post-§V.69 impl) closes intra-batch gap (loop wakes ≤1 tick after notify arrives) but inter-batch Pub/Sub dwell persists — post-§T.83 N=25 burst still observed waves @ +17/+52/+73/+86/+100-108s. Gmail withholds the next batch; we cannot accelerate Gmail-side notify emission. Original contract `task.created_at − email.received_at ≤ 75s` conflated our-side classification-pipeline lag w/ Gmail-side notify dwell ∴ violated on every N=25 burst regardless of code health. Narrowed scope: §V.69 binds classification-pipeline lag from notify arrival to task insert only — operationally `task.created_at − pubsub.notify.received_at ≤ 5s` (post-wakeup loop SHOULD reach `routing.route_email` ∧ task insert within one tick). Gmail-side `pubsub.notify.received_at − email.received_at` reclassifies as `sla.delivery` advisory per §V.61(+), ⊥ gated. Smoke §C4.a monitoring binds narrowed `task.created_at − pubsub.notify.received_at` metric ≤ 5s; raw `task.created_at − email.received_at` reports as advisory `sla.delivery`.
V70: burst retry-rate monitoring contract. ∀ N-burst window (P≤8, N≤25): `COUNT(agent.tool_errors spans) / COUNT(agent.invoke spans)` MUST be ≤ 5% (≤1-of-25 for N=25). Measurement: smoke §C4.a Logfire window `[T_SEND_C, T_SEND_C+300s]`. Each retry chain (fact_check_mismatch ∨ format ∨ not_found) adds avg_invoke_s (+20-70s) to the affected invocation — at 12% retry rate (3-of-25, §B.54) added latency exhausts burst-ceiling headroom independently of T_delivery. Enforcement: ≥5% retry rate ⊢ prompt-fidelity regression under load → investigate §V.41 (search-first ordering), §V.57 (KB coverage), §V.42 (format-lint sensitivity). Orthogonal to §V.69 (T_delivery from Pub/Sub wave gaps) — §V.70 binds agent-execution quality, §V.69 binds classification-pipeline timing. Why: §B.54. Why-amend (budget scoping): retry chains add latency to our-side `agent.invoke` execution, ⊥ Gmail-side T_delivery ∴ 5% cap measured against `sla.agent` per §V.61(+). Measurement window unchanged.
V71: per-invocation reply-rejection ceiling. `reply_email` ∧ `send_email` tool wrappers (`src/mailpilot/agent/tools.py`) ! maintain per-agent-invocation rejection counter keyed by `contextvars.ContextVar` set @ `run.execute_task` entry ∧ reset on task exit. Counter increments on ANY rejection returned to agent — `_check_spec_table` (§V.42 format-lint) ∧ `_fact_check_body` (§V.68 fact-check); both rejection sites call `_consume_reply_rejection` (rename `_consume_format_lint_rejection`) before returning the error dict. @ counter ≥ 3 → wrapper bypasses BOTH checks, emits `logfire.warn("reply_email.reply_rejection.cap_reached", attempt=N, rejection_type=..., workflow_id=..., email_id=...)`, proceeds to normal `email_ops.{reply,send}_email` send path. Cap = 3 (two genuine retries before override; unchanged). Why: §B.57 — smoke-run 2026-06-06 B7 compare invocation issued 17 `reply_email` calls (195s latency, >90s SLA); Scenario C burst issued up to 35 `reply_email` calls per invocation (max=495s, p95=362s, both > 110s burst ceiling ∧ 90s SLA); §V.42 has no enforcement ceiling ∴ agent loops until it accidentally finds a format that doesn't trigger the lint (≥3 consecutive key-value lines), burning unbounded LLM rounds. Per-invocation ceiling bounds worst-case latency while preserving two rounds of correctness pressure (most lint-triggers fix on first retry). Why-amend (rejection-type): §B.59 — `_fact_check_body` returned error directly ⊥ touching counter → 9 consecutive fact-check rejections bypassed cap (sla_agent=265s/p95=225s/max=531s). Extending counter to both types; bypass @ 3 preserves 2 correctness-pressure rounds.
V72: `company.profile JSONB NULL` ≡ cold-email-grade lead profile validated via Pydantic `CompanyProfile` model (`src/mailpilot/models.py`). Fields: `summary: str` (1-5 sentences — what they do, who they serve, hook), `products: list[str]`, `target_customers: str`, `timezone: str | None` (IANA, e.g. `"America/Toronto"`; null on multi-zone ∨ unclear), `sources: list[str]` (≥1 URL fetched/cited). Required ≡ {summary, products, target_customers, sources}; optional ≡ {timezone}. Full ≡ ∀ required fields non-empty; partial ≡ profile NULL ∨ required field missing/empty. `database.update_company(..., profile=<dict>)` ! validate via `CompanyProfile.model_validate(profile)` before persist; invalid → `pydantic.ValidationError` → §V.54 envelope translation to `{"error": "validation_error", ...}`. In-place updates admitted — orthogonal to §V.14 (append-only binds activity / note); profile ≠ note/activity ∴ idempotently-updatable; `company.updated_at` carries row-touch signal. Operator filter surface: `mailpilot company list --no-profile | --has-profile` → `WHERE profile IS [NOT] NULL`; flags mutually exclusive ∴ both set → `output_error("--has-profile and --no-profile are mutually exclusive", "validation_error")` per §V.4. `CompanySummary` ! project `has_profile: bool` ≡ `profile IS NOT NULL` (filter-field-as-Summary-field per §V.5). `CompanyView` auto-surfaces full `profile` blob ∵ `Company` row carries column (per §V.8 view ⊇ Company shape). CLI `mailpilot company update <ID> --profile-json '<json>'` accepts JSON text — handler `json.loads(...)` (catch `JSONDecodeError` → `output_error("invalid JSON: <e>", "validation_error")`), forwards dict to `update_company`. `--name` flag (existing) covers canonical-name updates separately ∴ profile.name ⊥ field (single canonical source per row). Schema addition: `company.profile JSONB NULL` ∈ `schema.sql`; `schema_hash` per §V.18 normalization; ⊥ index on JSONB (operator-query selectivity = NULL / NOT NULL only at v1 cardinality); ⊥ backfill — `make clean` recreates per §V.18 precedent. Why: external lead dumps (TheirStack CSV ∧ similar) seed company rows via `mailpilot company create --domain` then need cold-email-grade distillation per row; notes (§V.13 / §V.14) append-only ∴ wrong shape for idempotently-updatable lead profile; JSONB + Pydantic = in-place updates + mechanical schema check + single-column stale-query (`WHERE profile IS NULL`). Field surface trimmed to cold-email essentials: `industry` → tags (§V.10 / §V.13 segmentation surface, ⊥ profile body); `enriched_at` / `enriched_by` YAGNI (`updated_at` covers row-touch; refresh-stale flow out of scope v1); `name` lives canonical on `company.name` ∴ profile omits. Profile content agent-discovered — external sources contribute apex domain only; profile body sourced via web fetch (curl + lynx → Firecrawl fallback). Orchestration lives in `lead-encreach` skill (`.claude/skills/lead-encreach/SKILL.md`) — seed → list-stale → enrich pipeline; per-row enrichment dispatched to `company-profiler` sub-agent (`.claude/agents/company-profiler.md`, `model: sonnet`, tools scoped to `Bash` + `mcp__claude_ai_FireCrawl__firecrawl_scrape`) via Workflow `parallel()` w/ concurrency 3; AskUserQuestion batch gate @ >10 stale rows. Skill body inherits §V.6(+) vocab-drift audit per `.claude/skills/**/*.md` glob (§B.65 closure scope).

## §T TASKS

## archived: §T.1..§T.93 → SPEC.archive.md (93 rows)

id|status|task|cites
T94|x|impl §V.68(+) list-item corpus-build per §B.60 — `_fact_check_body` (`src/mailpilot/agent/tools.py:149`) table-bearing-doc branch (`:177` `pipe_lines = [line for line in lines if "|" in line]`) extends admit set: union pipe-row lines w/ list-item lines matching `re.match(r"^\s*[-*]\s", line)`. Scope = grep `rg -n '"\|" in line' src/mailpilot/agent/tools.py` → 1 hit @ `:177` (fact-check branch); `:222` ∈ `_check_spec_table` excluded (different concern per §V.42). Prose-only-doc branch unchanged (full content already admitted per §T.88). §B.56 anti-collision preserved — bullet `Key:` labels context-anchor, ⊥ cross-product prose-line leakage class. Test sweep: scope = grep `rg -n 'test_fact_check\|_fact_check_body' tests/` → extend existing fact-check tests w/ table-bearing-doc fixture carrying `"- Specific Gravity: 2.0"` bullet line — assert numeric token `2.0` ∈ corpus union, body containing `2.0` ⊥ rejected by `_fact_check_body`. Detected by /sdd:check 2026-06-09 (V68 VIOLATE).|V68,V64
T95|x|impl §V.6(+) `.claude/skills/**/*.md` vocab-drift audit per §B.65 — extend `.claude/check-extras.md §V.6` recipe steps (i) ∧ (ii) to additionally scan file set `.claude/skills/**/*.md`. Scope = grep `rg -nE '\bmailpilot (account\|company\|contact\|workflow\|enrollment\|tag\|note\|task\|email\|activity\|template) ' .claude/skills/` → 65 hits across 4 skill dirs (demo-test, lead-encreach, release, smoke-test) @ amend time. Fix L40 site: `.claude/skills/smoke-test/SKILL.md:40` → drop `tag remove` ∧ `enrollment remove` from operational-commands enumeration (verify: `mailpilot {tag,enrollment} disable` returns singular envelope per §V.10 / §V.4 → bespoke-shape claim ⊥ holds, parenthetical removal preferred over verb substitution). Test sweep: extras-recipe ⊥ test-covered ∴ manual /sdd:check post-impl validates scan emits 0 hits.|V6,V64
T96|x|impl §V.70 burst retry-rate gate @ smoke-test C4.a per §B.66 — extend existing C4.a CTE (`.claude/skills/smoke-test/SKILL.md:~920-960`) to sum rollup-span `tool_error_count` attr already set @ `src/mailpilot/agent/invoke.py:708`: add `SUM(COALESCE((attributes->>'tool_error_count')::int, 0)) AS n_tool_errors` ∈ outer select over the `burst` CTE; compute `n_tool_errors::float / NULLIF(n_invokes::float, 0) AS retry_rate`. Add assertion to C4.a "Assertions" block: `retry_rate <= 0.05` (≤5% per §V.70 ceiling) — failure ⊢ "prompt-fidelity regression under load → investigate §V.41 (search-first ordering), §V.57 (KB coverage), §V.42 (format-lint sensitivity)" per §V.70 body. Scope = grep `rg -n 'C4\.a\|n_warns\|n_exceptions' .claude/skills/smoke-test/SKILL.md` → 1 C4.a CTE block ∧ 1 Assertions bullet list. Source-of-truth rationale: rollup-attr `tool_error_count` (single CTE, ⊥ extra LEFT JOIN over `agent.tool_errors` warn spans) preferred. Test sweep: smoke-test gate is operator-judged (manual run) ∴ ⊥ automated test; manual /sdd:check post-impl re-classifies V70 HOLD.|V70,V61,V64
T97|x|impl §V.72 schema migration — add `company.profile JSONB NULL` column to `src/mailpilot/schema.sql` `company` table; ⊥ index (operator-query selectivity = NULL/NOT NULL only @ v1 cardinality, partial-index unwarranted); ⊥ backfill — `make clean` recreates per §V.18 precedent. Scope = grep `rg -n 'CREATE TABLE company' src/mailpilot/schema.sql` → 1 hit. Test: schema-apply pytest fixture detects new column via `information_schema.columns` query; `mailpilot status` envelope renders fresh `schema.hash` post-`make clean` per §V.18.|V72,V18
T98|.|impl §V.72 `CompanyProfile` Pydantic model + `update_company` validation — add `class CompanyProfile(BaseModel)` to `src/mailpilot/models.py` w/ field signatures per §V.72 row body; `database.update_company` ∈ `src/mailpilot/database.py` accepts `profile: dict | None = None` kwarg → non-None branch validates via `CompanyProfile.model_validate(profile)` (raise `pydantic.ValidationError` on mismatch), persists `json.dumps(model.model_dump(exclude_unset=True))` via `UPDATE company SET profile = %s::jsonb ...`; `Company` ∈ `models.py` gains `profile: dict | None = None` reading JSONB column. CLI `company update` ∈ `src/mailpilot/cli.py` gains `--profile-json TEXT` flag → handler `json.loads(profile_json)` (catch `JSONDecodeError` → `output_error("invalid JSON: <e>", "validation_error")` per §V.4), forward dict to `update_company`; raised `ValidationError` caught by `cli_mutation` boundary per §V.54 → envelope `{"error": "validation_error", ...}`. Test sweep: extend `tests/test_database.py` ∧ `tests/test_cli_company.py` w/ fixtures {valid-full, valid-partial-no-timezone, invalid-missing-summary, invalid-non-list-products, invalid-empty-sources, invalid-json-text} — assert validation outcome ∧ envelope shape.|V72,V54,V64
T99|.|impl §V.72 `company list --no-profile | --has-profile` filter + `CompanySummary.has_profile` projection — `database.list_companies` ∈ `src/mailpilot/database.py` accepts `has_profile: bool | None = None` kwarg → `None` (default, no filter), `True` (`WHERE profile IS NOT NULL`), `False` (`WHERE profile IS NULL`); SELECT projects `(profile IS NOT NULL) AS has_profile` alongside existing columns. CLI `company list` ∈ `src/mailpilot/cli.py` gains mutually-exclusive `--has-profile` ∧ `--no-profile` flags → both set → `output_error("--has-profile and --no-profile are mutually exclusive", "validation_error")` per §V.72. `CompanySummary` ∈ `src/mailpilot/models.py` gains `has_profile: bool` field. Test sweep: extend `tests/test_database.py` w/ 3-row fixture (`profile=NULL`, `profile={...full}`, `profile={...partial}`) — assert filter behavior ∀ kwarg setting ∧ Summary projection accuracy; CLI XOR-flag test in `tests/test_cli_company.py`.|V72,V5,V64
T100|.|impl `lead-encreach` skill @ `.claude/skills/lead-encreach/SKILL.md` per `designs/lead-encreach.md` — sub-commands {`seed <domain>...`, `seed-file <path> [--column NAME]`, `list-stale [--limit N]`, `enrich <id|domain>`, `enrich-all [--limit N]`}; file ingestion source/format-agnostic — auto-detect CSV vs plain-text from first non-empty line, apex-domain-only extraction (CSV column-header sniff `[domain, website, company_url, url]` ∨ `--column NAME` override, plain-text per-line w/ `#` comments ignored), redirect resolution via `curl -sLI`, collision-on-resolved-apex skip+log; ⊥ pre-populate profile from CSV columns ∨ text-line annotations. Concurrent enrichment via Workflow `parallel()` w/ concurrency 3 dispatched as `agent(..., {agentType: "company-profiler"})`; AskUserQuestion batch gate @ >10 stale rows (options: First-10 (Recommended) / First-25 / All-N). Skill frontmatter: `model: opus`, `allowed-tools: Bash(mailpilot company *), Bash(curl *), Bash(lynx *), Read, Task, AskUserQuestion`. Skill body inherits `## OUTPUT — "Next" block` heading per §V.6(+) user-typeable-SKILL.md contract. Test sweep: skill-body audit ⊥ test-covered (operator-judged manual /sdd:check); embed mechanical smoke via `seed acme.com beta.io` no-op re-run idempotency check post-impl.|V72,V6
T101|.|impl `company-profiler` sub-agent @ `.claude/agents/company-profiler.md` per `designs/lead-encreach.md` enrichment-agent-prompt block — frontmatter: `name: company-profiler`, `description: <design line>`, `model: sonnet`, `tools: Bash, mcp__claude_ai_FireCrawl__firecrawl_scrape`. System prompt: fetch via `curl -sL --max-time 20 -A "Mozilla/5.0" "https://<DOMAIN>/" | lynx -dump -stdin -nolist -width=120`; empty / blocked / JS-shell / <500 chars → retry `www.<DOMAIN>` ∧ `<DOMAIN>/about` ∧ Firecrawl fallback; distill into `CompanyProfile` JSON; update canonical `name` via `mailpilot company update <ID> --name "..."` if differs from placeholder; persist via `mailpilot company update <ID> --profile-json '...'`; return JSON `{company_id, domain, status: "enriched|skipped|failed", reason}`. ASCII-only constraint per §C. Generic agent name (⊥ `lead-encreach-` prefix) preserves reusability across future skills (account-create, contact-enrichment).|V72

## §B BUGS

## archived: §B.1..§B.29 → SPEC.archive.md (23 rows)

id|date|cause|fix
B30|2026-05-15|12 bare-form V<n> cites (no §) in code prose — §V.66 next workaround surface|V66
B31|2026-05-15|qa.py:16 bare-form V31 missed by §T.48 sweep — operator-review misclassified as gate ID|V66
B32|2026-05-26|enrollment add accepted contact.email ≡ workflow.account.email — semantic self-loop|V33
B33|2026-05-26|enrollment remove echoed pre-delete Enrollment ∧ claimed changed=[status] post hard-DELETE|V4,V54
B34|2026-05-28|read_drive_markdown ×2 parallel — httplib2.Http ⊥ thread-safe; all 4 §V.49 retries hung|V49,V38
B35|2026-05-28|_drain_pending_tasks sequential — 2 concurrent inbound emails ran agent.invoke spans strictly serial|V23
B36|2026-05-28|B4/B7 rendered `**label** value` single-space cluster — §V.42 \s{2,} regex missed|V42
B37|2026-05-28|agent.invoke span lacked email_id attr — B7.5 gate SQL GROUP BY email_id returned 0 rows|V26
B38|2026-05-28|smoke-test gates graded latency from wall-clock + 5s poll ⊥ Logfire spans — borderline verdicts|V61
B39|2026-05-28|gate A4 needed gmail_thread_id; EmailSummary projection omitted external-system join key|V7
B40|2026-05-28|two KB files describe same model w/ divergent specs — single source_file schema ambiguous|V57
B41|2026-05-28|main loop blocked on _drain_pending_tasks ∴ pubsub.notify for 2nd msg stalled 31s|V24
B42|2026-05-28|agent advisory lock keyed on (workflow_id, contact_id) ∴ §V.23 concurrent dispatch undone|V25
B43|2026-06-02|make clean dropped only dev DB ∴ schema.sql shrink (commit 93cbf66) left mailpilot_test w/ stale contact.domain NOT-NULL col — pytest hit psycopg.errors.NotNullViolation|V18
B44|2026-06-03|outbound send (sync.py:1223-1240) ⊥ pass resolved_in_reply_to/resolved_references to create_email; DB row's in_reply_to/references_header NULL on classifier-driven mid-thread replies despite wire-correct threading|V67
B45|2026-06-03|smoke-run B7 (compare 2 datasheets, Watts UV-COM vs Trojan UVMax Pro) agent.invoke=64s, B7.5b (2nd of 2 concurrent in-scope, WS36-600-2 flow rates) agent.invoke=63s — both substantively correct (groundedness PASS) but breached then-60s §V.59 SLA; tool work <5s each, wall-clock dominated by compose; B7 input_tokens=34926; resolution = relax §V.59 SLA to 90s + sweep call-sites per §T.74 + update lab5.ca/mailpilot/ statement (out-of-repo)|V59
B46|2026-06-03|smoke-run B7.5b agent reply (qa-in-022, WS36-600-2 flow rates) cited continuous=65 GPM, peak=120 GPM, backwash=35 GPM as primary answer; both same-invocation `read_drive_markdown` returns (`industrial-water-softener.md`, `sf-100s-industrial-water-softener.md`) contain WS36-600-2 = 110/125/30; fabricated tokens appear nowhere in either return; agent acknowledged correct values in hedging "Note" within same body; trace 019e8ef503adf84513a3cbb6948f8f87; co-occurred w/ §V.59 SLA breach (63s) under N=2 concurrent load|V68
B47|2026-06-03|src/mailpilot/SKILL.md drift from live CLI surface: `--body-text` recipes (live cli.py @click = `--body`); positional id forms for `email reply` / `account sync` / `enrollment {run,view,update,remove}` (live = flagged form, enrollment ⊥ scalar id per composite PK); verb roster ⊥ ∋ `{sync, disable, retry, get, set}`; settings ⊥ ∋ `max_concurrent_tasks` (settings.py:65); env-prefix described as "uppercase of same name" (actual `MAILPILOT_*` per `SettingsConfigDict(env_prefix=...)` ∈ settings.py:54)|V6
B48|2026-06-05|smoke run 2026-06-05: §V.42 lint fired B7-first reply_email (one 4ms agent.tool_errors span, ⊥ gmail.send_message child) but ⊥ fired B4 / B7-second / B75a / B75b reply_email — _check_spec_table replay on all 4 stored body_text returns {"error":"format"}; strip_control_chars (sole transform between tool-wrapper entry ∧ body_text col on outbound write) preserves pipes ∴ DB body ≡ tool-wrapper input body ∴ lint should have caught all 4; impl skipped lint on those 4 invocations, mechanism unknown|V42
B49|2026-06-05|enrollment list returned id, workflow_id, contact_id, contact_email, contact_name, status, updated_at; enrollment view returned bare Enrollment row (workflow_id, contact_id raw FKs; status, reason, created_at, updated_at) — view ⊊ list on parent-denorm set; operator audit during CLI consistency review surfaced inversion class (peer surface account/company/contact/workflow all satisfy view ⊇ list, enrollment alone violates)|V5
B50|2026-06-05|volume run 25 trigger sends → 121 emails (96 agent replies ≈4x amplification): single task emitted 12 sequential `run.execute_task` spans w/ same task_id, each w/ successful nested `agent.invoke` + `complete_task` call; final DB row status=completed attempt_count=0; dispatcher non-atomic claim — `list_pending_tasks` SELECT ⊥ FOR UPDATE SKIP LOCKED ⊥ status flip; §V.25 lock catches concurrent ⊥ sequential re-pick post-completion|V23
B51|2026-06-06|smoke run 2026-06-06 burst 24-email: max_concurrent_tasks=4 default violates formula ceil(25×35/90)=10; ThreadPoolExecutor(max_workers=4) queues 20-of-24 tasks → 6 sequential waves × ~35s avg → max_latency=235s (>110s burst ceiling), p95=217s (>90s SLA); Drive + routing clean (drive max_dur=1.3s, 60 overlap_pairs, 0 exceptions)|V23
B52|2026-06-05|re-run `mailpilot account create --email outbound@lab5.ca --display-name "Outbound Smoke"` against existing row emitted curated `event=error source=account.create message="duplicate key ... account_email_key ..."` operator-log line per §V.54 observability pair then dumped raw 30-line `psycopg.errors.UniqueViolation` traceback to stderr because `cli_mutation` (`operator_log.py:64-67`) re-raises after pairing ∧ Click default handler has no `psycopg.Error` translation; stdout stayed JSON-empty (§V.3 single-shot contract intact on stdout) but stderr carried unstructured traceback alongside structured event line, violating envelope expectation §V.3 + §V.4 set for CLI error paths; recurrence class = ∀ §V.54 noun.verb CLI mutation whose underlying DB INSERT/UPDATE may hit unique / FK / NOT NULL / CHECK constraint (account/company/contact/workflow re-create, CSV/JSON re-import, etc.)|V54

B53|2026-06-06|smoke C burst 25-email max_concurrent_tasks=10: Gmail Pub/Sub delivered notifications in 5 waves (+15/+25/+52/+105/+122s from T_SEND_C); wave-4 (11 invocations) started +105s; compare-inclusive avg ~50s per invoke → last span ends ~160s; §V.23(+) pool formula ⊥ includes T_delivery_p95 term; max=160s>110s burst ceiling, p95=156s>90s SLA; §V.69 T_delivery contract violated|V61,V69
B54|2026-06-06|burst-25 max=175s>110s, p95=162s>90s via two compounding factors: (a) inter-batch Pub/Sub gaps persist despite §V.69 wakeup-on-new (§T.83) — 5 classification waves at +17/+52/+73/+86/+100-108s from T_SEND_C; §T.83 wakeup closes intra-batch T_delivery ⊥ inter-batch gap; (b) 3 tool-error retry chains (fact_check_mismatch×2, not_found×1) each inflate affected invocation +40-70s; 12% retry rate (3/25) exhausts burst-ceiling headroom independently of wave timing; §V.70 retry-rate contract violated|V61,V69,V70
B55|2026-06-06|`_DRIVE_GROUNDING` fragment (`src/mailpilot/agent/templates.py:122-135`) ⊥ contains §V.41 per-target compare-and-contrast clause; fragment carries global 2-miss list-fallback only; compare invocation w/ 2-miss for target A → `list_drive_markdown` surfaces target B doc w/o target B-specific search ever issued → recreates model-number-collision class §V.41 closed @ spec but open @ impl; qa-cmp-011 (Pure Aqua PAPV vs ROPV R8040B, smoke-run 2026-06-03 B7) precedent; /sdd:check 2026-06-06 surfaced via prompt-text inspection|V41

B56|2026-06-06|smoke-run B7 compare (qa-cmp-005) + B75a direct (qa-in-022) agent fabricated WS softener model flow rates across ≥6 models — WS36-600-2 → 110 cont / 125 peak (source: 65/120), WS24-240-2.5 → 115/160 (source: 80/170), WS30-300-2.5 → 140/190 (source: 80/170), WS30-300-3 → 165/230 (source: 120/270), WS42-900-2 → 112/133 (source: 65/120), claimed range 86-1,130 GPM (source max: 1,000); wrong 100-GPM recommendation followed (agent named WS24-240-2.5 @ 80 cont as clearing 100 cont; correct answer WS30-300-3 @ 120 cont); §V.68 fact-check silent — current source docs (`pure-aqua-industrial-water-softener.md`, `pure-aqua-sf-100s-industrial-water-softener.md`) post-upstream-update carry 65 cont / 120 peak for WS36-600-2 (§B.46 historical 110/125 ⊥ live values), fabricated tokens collided via prose-line matches in `_fact_check_body` plain `token in union` — `"110"` ∈ `"35-110 degrees F"` (operating temp), `"125"` ∈ `"25 - 125 psi"` (operating pressure), `"115/140/165"` ∈ CVP commercial-softener pipe-rows (same column index, wrong product family); column-context-blind substring match = implementation gap, §V.68 spec intent ≡ pipe-table-row grounding ⊥ prose-line collision admit; reproducible on single-source direct lookup ∴ ⊥ compare-context-specific; §T.85 carries pipe-table-row scoping fix|V68
B57|2026-06-06|smoke-run 2026-06-06 B7 compare (qa-cmp-001, Dow FilmTec ECO-440i vs Hydranautics CPA4) + Scenario C burst (25 emails @ P=8): §V.42 `_check_spec_table` lint fires per-tool-call w/ ⊥ retry ceiling ∴ agent loops issuing fresh `reply_email` calls until it accidentally finds a body that misses the ≥3 consecutive key-value heuristic; B7 invocation made 17 `reply_email` calls over 195s wall-clock (>90s SLA); C burst's worst invocation made 35 `reply_email` calls over 417s, max latency 495s, p95 362s, both > 110s burst ceiling ∧ 90s SLA per §V.61; 8 `agent.tool_errors` warn-level spans across full test (32 `agent.invoke`) = 25% retry rate (vs §V.70 5% cap); content groundedness intact ∀ replies (B7 cited both datasheets correctly, no fabrications) ∴ failure mode ≡ purely latency + token waste (1.95M input tokens for 25 burst replies, expected ~500k w/o loops); root cause = `_check_spec_table` ∈ `src/mailpilot/agent/tools.py:114-145` stateless per-call ∧ wrapper has no per-invocation rejection counter|V71
B58|2026-06-06|smoke-run 2026-06-06 B4 attempt 1 (qa-in-006, `kdf-process-filtration-media.md`): prose-only KB doc, 0 pipe-table-row lines → `_fact_check_body` (`src/mailpilot/agent/tools.py:142-186`) builds empty union via §T.85 pipe-row filter (`"|" in line` for `line in content.splitlines()`) → ∀ genuine numeric citations ("55", "85", "99", "98" echoing source phrases "KDF 55 medium can remove over 99% of free chlorine", "up to 98% of water-soluble cations") flagged unsupported; agent looped 6 `reply_email` fact_check_mismatch retries, eventually `record_enrollment_outcome(failed)`, 0 replies delivered to operator. C burst (25 emails @ P=8) amplified to 46 retries / 22 invokes = 209% retry rate vs §V.70 ≤5% cap; single `sync.loop.iteration` consumed 1.85M input tokens / 413s wall-clock (trace `019e9e408a37df02d77c8371722949c7`); 2 tasks pending @ C7. §V.68(+) pipe-row scoping (§T.85) intended to kill prose-line false positives in table-bearing docs (§B.56), but uniform pipe-row-only union over the WHOLE ledger inverts ground-truth on prose-only docs. Per-document scoping (pipe-row for table-bearing docs, full-content for prose-only docs) preserves §B.56 anti-collision intent ∧ restores prose-doc grounding; §B.56 single-doc collision class (token in both prose AND pipe-row of SAME doc) still rejected under per-doc scoping. §T.88 carries per-document scoping fix|V68
B59|2026-06-06|smoke-run 2026-06-06 B7 compare (qa-cmp-001, Dow FilmTec ECO-440i + Hydranautics CPA4) + C burst 5 compare questions: `_fact_check_body` (§V.68) rejected valid grounded test-condition values (150 psi, 225 psi, 22.7 m³/d, 1500 ppm, 6000 gpd) that appear in prose sections of both datasheets — Dow ECO-440i test conditions ("2,000 ppm NaCl, 150 psi (10.3 bar), 77°F (25°C), pH 8 and 15% recovery") live in `## Specification Notes` prose, Hydranautics CPA4 test conditions ("1500 PPM NaCl solution, 225 psi (1.55 MPa) Applied Pressure, 77 °F") live in `## Test Conditions` prose; §T.88 pipe-row-only scoping (§V.68(+)) excluded those prose lines from both docs' union because each doc carries OTHER pipe tables (Product Specs, Application Data) ∴ per-doc check picked pipe-row branch ∴ prose body of same doc dropped. §V.71 retry ceiling covered only `_check_spec_table` (format-lint) hits — `_fact_check_body` rejection path called `return fact_check_error` directly (`src/mailpilot/agent/tools.py:259,308`) bypassing `_consume_format_lint_rejection` → no bypass fired; B7: 9 `fact_check_mismatch` retries + 18 re-reads, sla_agent=265s (>50s steady ceiling); C burst: p95_sla_agent=225s, max=531s (5 compare questions × up to ~10 reply_email calls each). Agent exited loop only by omitting contested values (attempt 9 switched to "Per datasheet (see note below)") ∴ B7 grounding verdict degraded to fail (answers_question=false, CPA4 permeate flow 6000 gpd omitted despite being in source). Two compounding causes: (a) §V.68 pipe-row-only scoping too narrow for table-bearing docs that embed valid citable specs in prose (test conditions, SI-unit footnotes, performance notes); narrowing the scope is reasonable for §B.56 anti-collision but inverts ground-truth for prose-table mixed docs. (b) §V.71 ceiling ≡ format-lint-only ∴ `_fact_check_body` rejection loop uncapped — orthogonal-class gap closed by extending counter to all rejection paths. §T.89 carries the counter-extension fix; the §V.68 prose-vs-table-row scoping question remains open (could broaden §T.88 to admit prose sections under headers ≠ Product Specs / Application Data, ∨ accept that §V.71 cap is the right backstop ∧ §V.68 stays narrow for collision protection)|V71
B60|2026-06-07|smoke-run 2026-06-07 B7 compare (qa-cmp-003 Clack Birm® vs Pyrolox®) + Scenario C burst 5 compare Qs: `_fact_check_body` (§V.68) rejected Physical Properties bullet-list tokens verbatim present in both source docs — Birm `"- Specific Gravity: 2.0 gm/cc"`, `"- pH range: 6.8–9.0"` (under operation conditions), `"- Mesh Size: 12 x 50"`; Pyrolox `"- Specific Gravity: 3.8"`, `"- pH: 6.5-9.0"`, `"- Bed Depth: Suggested depth 18 inches"`; Birm constraints `"D.O. content must be equal to at least 15% of the iron and 29% of the manganese content"`, `"Backwash rate: 10–12 gpm/sq. ft."` Both docs carry Order Information pipe tables ∴ §T.88 per-doc check picked pipe-row branch ∴ bullet-list Physical Properties + operation-constraint lines excluded from union. 10 tokens spuriously rejected on first compare invocation (2.0, 3.8, 6.8, 9.0, 6.5, 18, 15, 29, 10) → §V.71 cap fired at attempt=3, send proceeded (correctness preserved, latency burned). All 5 compare invocations hit cap; B7 sla_agent=63s >§V.61(+) 50s steady ceiling; C p95_sla_agent=93.75s, max=122s >§V.61(+) 75s burst ceiling; §V.70 retry rate 7/32 = 21.9% >> 5% cap (5 `cap_reached` spans + 2 stranded `agent.tool_errors`). B7 grounding verdict=pass (cap ensured send w/ values intact) ∴ ⊥ §B.59 grounding-degradation class. Cognate w/ §B.59 (same pipe-row-only root, both surface after §T.88), distinct surface: §B.59 ≡ prose-paragraph spec citations under prose headers (`## Test Conditions`, `## Specification Notes`); §B.60 ≡ bullet-list spec lines (`"- Key: Value"`) under any header. Bullet-list lines carry explicit `Key:` labels ∴ context-anchored, ⊥ admit cross-product token leakage seen in §B.56 prose-line matches (`"110"` ∈ `"35-110 degrees F"`); safe to admit alongside pipe-row lines without re-opening §B.56 class. Fix: §V.68(+) Impl extended — for table-bearing docs, union admits lines matching `r"^\s*[-*]\s"` alongside `"|"` lines; full-prose paragraph lines remain excluded per §B.56|V68
B61|2026-06-07|smoke-run 2026-06-07 B7 compare (qa-cmp-010 Watts UV-COM vs Trojan UVMax Pro): agent.invoke=84s vs §V.61(+) 50s steady ceiling. §V.71 cap behaved correctly — 2 `_check_spec_table` format rejections @ ~0.002s each → 3rd `reply_email` succeeded — ⊥ pathological loop, ⊥ fact_check_mismatch retries. 84s ≡ intrinsic compare cost: 2 datasheets loaded (in_tok=61667 vs single-source 22351), tool_call_count=8 (search ×2 + read ×2 + reply ×3 + record ×1), multi-doc synthesis. 50s steady ceiling structurally under-estimates compare-type. Pattern recurs cleanly across runs: §B.45 64s (Watts UV-COM vs Trojan, post-§T.74 90s relax pre-50s reinstate), §B.60 63s (Clack Birm vs Pyrolox, post-§T.88), §B.61 84s. ⊥ surface symptom in §V.68/§V.71 layer (both behaved correctly); ceiling itself ≡ wrong for compare-type. Fix: §V.61(+) introduces differentiated ceiling — single-source ∧ decline retain 50s steady, compare-type (≥2 `read_drive_markdown` per invocation) gets 90s steady; burst non-compare p95 ≤ 75s, compare p95 separate w/ advisory 120s. §T.90 sweeps smoke-test SKILL.md B7 gate text|V61
B62|2026-06-07|smoke-run 2026-06-07 Scenario C burst (8 emails @ P=8, mix 4 in-scope / 2 out-of-scope / 2 compare): `p95(sla_agent)=118s`, `max(sla_agent)=118s` vs §V.61(+) 75s burst ceiling. `overlap_pairs=28` (max C(8,2)) confirms all 8 invocations concurrent ∴ breach ≡ intrinsic execution duration, ⊥ serialization. Two compare questions (qa-cmp-008 Clack Birm vs Pyrolox, qa-cmp-002 Pulsafeeder vs ProMinent) dominate tail; non-compare invocations (4 in-scope + 2 out-of-scope) ≤ 50s ∴ blended p95 inflated solely by compare-type. `n_exceptions=0`, `n_warns=0`, `avg_cache_hit_ratio=0.645`, `read_drive_markdown max_dur=0.74s` (§B.34 race signature absent). All correctness gates passed: C3 routing 100% classified, C5 +8/+8/+8 activities, C6 outbound-workflow quiet under load. Cognate w/ §B.61 (single-send compare 84s, same structural root) — §B.62 ≡ burst surface of same compare-type 50s-ceiling under-estimate; blended-p95 gate (pre-§V.61(+) amend) ≡ wrong gate shape since 2 compare invocations alone shift p95 above 75s regardless of pool/concurrency health. Fix: §V.61(+) compare-type clause already amended in §B.61 commit — burst non-compare `p95(sla_agent) ≤ 75s`, compare `p95(sla_agent)` reported separately w/ advisory ceiling 120s; smoke-test SKILL.md C4.a query gate rewrite tracked by §T.90|V61
B63|2026-06-08|`mailpilot enrollment remove "019ea725-ff8f-7444-afce-baa3deb37176"` raised `psycopg.errors.ForeignKeyViolation` on `activity_enrollment_id_fkey` ("update or delete on table enrollment violates foreign key constraint ..."); `delete_enrollment` (`src/mailpilot/database.py:1625-1664`) issues bare DELETE w/ ⊥ ON DELETE clause; two FKs reference `enrollment.id` w/ ⊥ cascade — `activity.enrollment_id` (nullable, `schema.sql:160`) populated by every lifecycle event (`enrollment_added` @ creation, plus `enrollment_completed`/`enrollment_failed`/`enrollment_paused`/`enrollment_resumed`), `task.enrollment_id` (NOT NULL, `schema.sql:117`, §V.28) populated for every enrollment that ever scheduled ∨ received a task. `remove` succeeds only on enrollments w/ zero activity ∧ zero tasks — effectively never (`enrollment_added` activity emitted @ creation per §V.17 multi-target). Append-only audit per §V.14 forbids both candidate fixes that preserve audit: cascade DELETE on activity destroys audit (violates §V.14 spirit), SET NULL via UPDATE-on-activity violates §V.14 fn-rule. Resolution: drop `enrollment remove` hard-delete CLI cmd, replace w/ terminal soft-disable extending §V.10 contact-disable surface to enrollment noun; storage = extends `enrollment.status` enum {`active`, `paused`} → {`active`, `paused`, `disabled`} per §V.15(+) (single live-state signal; agent-filter sites skip `disabled` same as `paused`); `enrollment.disabled_reason TEXT` column coupled to status via CHECK; `enrollment_disabled` activity row emitted by `disable_enrollment` (append per §V.14, joins `enrollment_*` family ∈ `activity.type` CHECK). Distinct from §B.33 (cosmetic envelope-projection bug on hard-delete path) — §B.63 ≡ fundamental hard-delete-vs-append-only shape incompatibility ∴ remove cmd structurally unfit, ⊥ envelope-tweak fix|V10,V14,V15
B64|2026-06-08|`mailpilot company create --domain lab5.ca` re-run against existing row emitted logfire span `company.create.failed` w/ raw `psycopg.errors.UniqueViolation` ("duplicate key value violates unique constraint company_domain_key") attached + `event=error source=company.create message="..."` operator-log line + JSON envelope `{"error":"duplicate_key", "message":"...", "ok":false}` on stdout; envelope translation per §V.54 worked correctly (⊥ traceback per §B.52 fix held) BUT `.failed` span outcome ≡ wrong signal — expected operator-retry outcome (re-create against existing natural key) recorded as system error ∴ pollutes failed-span dashboards / alert paths. Recurrence class = ∀ `create_<noun>` ∈ `src/mailpilot/database.py` for `noun ∈ {account, company, contact, workflow}` whose bare INSERT raises UniqueViolation on natural-key collision; pre-amend §V.16 covered only `create_email` ∴ class-asymmetry visible (`create_contacts_bulk` race-safe peer to `create_contact` non-race-safe). Sync-path TOCTOU also extant: `create_or_get_contact_by_email` (`database.py:797-837`) reads via `get_contact_by_email` then falls through to `create_contact` w/o ON CONFLICT — concurrent §V.23 workers can both pass the get-check ∧ race the INSERT; sync caller has ⊥ `cli_mutation` envelope ∴ raw UniqueViolation would propagate to `_drain_pending_tasks` worker error path. Cognate w/ §B.52 (CLI traceback-leak class) — distinct surface: §B.52 fixed envelope translation @ catch-time, §B.64 prevents raise @ source-time for natural-key path specifically.|V16
B65|2026-06-09|`.claude/skills/smoke-test/SKILL.md:40` "operational commands" enumeration lists `tag remove` ∧ `enrollment remove`; both verbs dropped from §I.cli per T91/T92 (replaced w/ `tag disable`/`enrollment disable`); §V.6 audit-recipe pre-amend scoped to `src/mailpilot/SKILL.md` only ∴ `.claude/skills/**/*.md` vocab drift evades mechanical check (4 skill dirs, 65 CLI-vocab refs @ detect time). Symptom: smoke-test operator copy-pastes `enrollment remove` from L40, hits `No such command 'remove'`. Recurrence class = SKILL-shaped doc carries CLI vocab → CLI surface change (verb retire ∨ flag rename) silently invalidates doc, parametric across operator-facing skill bodies. Distinct file scope from §B.47 (which closed §V.6 violation for `src/mailpilot/SKILL.md`). Detected by /sdd:check 2026-06-09 operator-review of cited tokens.|V6
B66|2026-06-09|`.claude/skills/smoke-test/SKILL.md:920-1000` C4.a CTE selects `n_warns`/`n_exceptions` on `agent.invoke` spans only; ⊥ counts `agent.tool_errors` warn spans (emitted @ `src/mailpilot/agent/invoke.py:703` per tool-error scan) ∧ ⊥ rollup-span `tool_error_count` attr (set @ `:708`); §V.70 ≤5% retry-rate ceiling toothless ∴ prior burst regressions (§B.54 12%, §B.57 25%, §B.58 209%) all silently passed C4.a despite §V.70 binding ratio @ ≤5%. Recurrence-class: invariant body specifies measurable contract w/ named gate site but consumer SQL/assertion omits the metric ∴ contract toothless silently. Detected by /sdd:check 2026-06-09 (V70 UNVERIFIABLE).|V70
