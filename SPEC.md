# SPEC

## §G GOAL

Agent-operated CRM. Gmail = comms layer. Claude Code = strategist; internal Pydantic AI agent = tactical executor (routing, auto-reply, follow-up).

## §C CONSTRAINTS

- Python 3.14. basedpyright strict. ruff. TDD per CLAUDE.md.
- PostgreSQL 18. psycopg, raw SQL via `psycopg.sql`. no ORM.
- IDs = UUIDv7 (`uuid.uuid7()` via `_new_id()`).
- Gmail scope = `gmail.modify` only. no additional Gmail scopes.
- Drive scope = `drive.readonly` only. Read-only KB grounding. no write/modify.
- Auth = service account & domain-wide delegation per §V.37. Credential source in {file path (`google_application_credentials` setting or `GOOGLE_APPLICATION_CREDENTIALS` env var), Application Default Credentials (e.g. GCE metadata server, Workload Identity, Cloud Run identity)}. Per-account impersonation: file creds -> `credentials.with_subject(email)`; ADC creds -> `service_account.Credentials(signer=iam.Signer(...), service_account_email=<sa>, subject=email, ...)`. no OAuth user login.
- Email body = plain text only (control chars stripped). no HTML body persistence. no embeddings, no vector store, no ingestion pipeline.
- KB files = `.md` in Drive folder named in `workflow.instructions`. no in-app PDF/Docs/HTML conversion.
- ASCII-only project artifacts (code, docs, CLI output). Agent-generated email body content exempt.
- Tool pattern: typed sig, DI deps, dict return, error dicts on failure (no raise to agent).

## §I INTERFACES

- cli: `mailpilot <noun> <verb> [args]` -> JSON on stdout, exit code 0 or 1, errors to stderr.
  - nouns: `account`, `company`, `contact`, `workflow`, `enrollment`, `task`, `email`, `activity`, `tag`, `note`, `template`.
  - verbs: `list|search|view|create|update|disable|add|reply|send|start|stop|cancel|retry|run|sync|export|import`.
  - top-level: `run` (sync & task loop), `status`, `config get|set`, global `--version|--debug|--completion|--skill`.
  - envelope: `list|search|sync|export|import` -> `{"<plural>": [...], "ok": true}`; `view|create|update|disable|add|reply|send|start|stop|cancel|retry` -> `{"<singular>": {...}, "ok": true}`; err -> `{"error": CODE, "message": TEXT, "ok": false}`.
  - email projection: `email view|list` ! project `route_method` in {`classified`, `thread_match`, `rfc_message_id_match`, `skipped_outside_window`, `skipped_no_workflows`, `skipped_predates_workflows`, `skipped_no_inbound_workflows`} so operator audits routing decision from CLI w/o Logfire.
  - contact projection: `contact list|search` ! project `title` + `company_domain` (company denorm per §V.5) so operator reads role + org from CLI w/o a separate company lookup.
  - `template` = read-only, code-defined (registry in `src/mailpilot/agent/templates.py` per §V.44). Verbs: `list [--direction inbound|outbound]`, `view NAME`. no create/update/delete — new template = code change + PR.
  - `workflow import` accepts `--file <path>`: `.json` workflow array (live-state dump) | `.toml` single-workflow catalog entry (§V.103) | directory (glob `*.toml`); stdin = JSON only.
- agent tools (`src/mailpilot/agent/tools.py`): `send_email`, `reply_email`, `search_emails`, `read_email`, `read_contact`, `read_company`, `list_enrollments`, `create_task`, `cancel_task`, `record_enrollment_outcome`, `disable_contact`, `list_drive_markdown`, `read_drive_markdown`, `search_drive_markdown`, `noop`. all tool -> typed sig, dict return, err dict on failure.
- pubsub (`src/mailpilot/pubsub.py`): topic `mailpilot-topic-dev`, sub `mailpilot-sub-dev` (defaults; per-env override via `MAILPILOT_GOOGLE_PUBSUB_TOPIC` / `..._SUBSCRIPTION`). `setup_pubsub()` idempotent. `start_subscriber(settings, callback)` streaming pull. `make_notification_callback(queue, wakeup_event)` decode -> enqueue -> `wakeup_event.set()`. `renew_watches()` refresh @ T-24h.
- config (`src/mailpilot/settings.py`): `database_url`, `anthropic_api_key`, `anthropic_model` (default `claude-sonnet-4-6`), `google_application_credentials`, `google_pubsub_topic`, `google_pubsub_subscription`, `logfire_token`, `logfire_environment` in {`development`, `production`}, `run_interval` (default 60s), `max_concurrent_tasks` (default 10).
- module: `src/mailpilot/gmail.py` -> `GmailClient`; `src/mailpilot/drive.py` -> `DriveClient`. Mirror shape.
- entrypoint: `mailpilot = "mailpilot.cli:main"`.

## §V INVARIANTS

V1: every CLI cmd loads settings first; DB + network init after (settings-first)
V2: cli.py module-level imports = click only; heavy deps lazy-import inside cmd fns
V3: stdout = strict JSON only (all flags, incl --debug); operator lifecycle + errors -> stderr; Logfire console exporter ! target stderr (ConsoleOptions output=sys.stderr), never stdout — output unset defaults stdout so console lines corrupt JSON envelope
V4: every cmd output ! match §I.cli envelope; error path -> {"error","message","ok":false} + exit 1
V5: list/view rows carry parent denorm joined @ fetch — workflow rows + account_email; enrollment rows + workflow_name + contact_email + contact_name; contact list/search rows + company_domain (LEFT JOIN company ON company_id, NULL when company_id NULL)
V7: EmailSummary projection ! include gmail_thread_id, is_routed, route_method — operator audits routing from CLI w/o Logfire
V8: contact/company views inline <= 10 latest notes (_INLINE_NOTES_CAP, 2 queries, no JOIN) + total count; agent read_contact/read_company route through load_contact_view/load_company_view -> agent + CLI context byte-identical; _BASE protocol carries personalize-via-notes directive
V10: tag disable = soft — disabled_reason set marks terminal row; disabled_reason IS NULL gate blocks double-disable; emits tag_disabled activity; list hides disabled unless --include-disabled
V11: status payload = fixed envelope {version, schema, sync_loop, accounts, tasks, config, counts}; tasks block carries pending, failed_24h, scheduled_future, oldest_pending_age_seconds, max_attempt_count_pending
V12: IDs minted client-side via _new_id() -> UUIDv7; enrollment addressed by scalar id, composite (workflow_id, contact_id) retired from signatures
V13: tag + note target = XOR — exactly one of {contact_id, company_id} set (schema CHECK)
V14: activity + note append-only — INSERT only, no update/delete fns
V15: enrollment.status in {active, paused, disabled}; disabled = terminal operator exit, requires non-empty disabled_reason (CHECK) + enrollment_disabled activity; outcomes live on activity timeline via record_enrollment_outcome (accepts only completed|failed), enrollment row untouched
V16: race-safe create — UNIQUE-bearing create_X uses ON CONFLICT DO NOTHING -> None to race loser, exactly 1 row persists; bulk variants converge to shared ids; CLI surfaces duplicate_key envelope
V17: activity targets >= 1 of {contact_id, company_id}, both allowed (multi-target); list_activities enforces same
V18: schema drift — metadata row/table missing or hash mismatch -> status schema.drift=true + warn, never silent
V19: schema hash = sha256 over normalized schema.sql (strip -- comments, collapse whitespace)
V20: email.route_method NULL or in 7-value enum (schema CHECK, set per §I.cli); non-NULL -> is_routed=TRUE; NULL + is_routed=TRUE = pipeline ran, no match ("unrouted" = span-only label)
V21: background loops wake on events not timers — wakeup_event set by Pub/Sub notify + pg NOTIFY task_pending (INSERT + retry-UPDATE triggers); run_interval tick = fallback only
V22: <= 1 routing.route_email span lifecycle per email_id — History-API re-delivery + repeat sync sweep never trigger second route pass (is_routed gate); duplicate route spans inflate metrics + mask classifier regressions
V23: task drain = bounded pool <= max_concurrent_tasks; each worker owns its psycopg.Connection; atomic claim blocks re-dispatch of in-flight tasks; each worker roots its own trace — drain worker detaches the dispatching tick's sync.loop.iteration OTel context before run.execute_task (py3.14 ThreadPoolExecutor.submit propagates the active span) so co-tick agent.invoke don't inherit one trace_id — trace_id 1:1 w/ an agent.invoke (closes §B.82)
V24: main loop never blocks on task futures — reaper collects on later ticks + emits task.drain
V25: advisory locks 2-tier — coarse (workflow_id, contact_id) + task-scoped (task_id split-half CRC32 pair); lock acquired before agent.invoke span opens (loser -> None, no span); contention -> reschedule w/o attempt_count bump, scheduled_at push fires task_pending_trigger
V26: agent.invoke span trigger attr in {enrollment_run, enrollment_schedule, task, email, manual} = caller path
V27: routing pipeline order: thread match -> RFC message-id match -> LLM classify, all account-scoped; classifier = single-turn, no tools, body truncated @ 16384 chars, hallucinated workflow_id coerced to None, zero active inbound workflows -> no LLM call; every outcome marks is_routed=TRUE w/ distinct route_method
V28: task.enrollment_id NOT NULL; workflow_id + contact_id denorm retained for filters; enrollment guaranteed @ route time via _ensure_enrollment — ON CONFLICT once, enrollment_added activity on first insert only
V29: trigger email body inlined once under "New inbound email:"; excluded from email_history — no prompt duplicate
V30: prompt framing follows trigger — first-reach-out (enrollment_run + enrollment_schedule byte-identical) vs deferred-task vs inbound; inbound email present -> email framing wins; no synthesized task_description
V31: protocol branches on trigger — trigger='task' -> terminal-outcome instruction (record_enrollment_outcome); other triggers -> initial-send-only
V32: enrollment_schedule = distinct trigger label (observability split from enrollment_run); --scheduled-at -> pending first-touch task (email_id NULL), idempotent, rejected for inbound workflows
V33: enrollment self-loop rejected — contact.email == workflow account email, case-insensitive
V34: every Drive call carries Shared-Drive flags — corpora="allDrives", supportsAllDrives=True, includeItemsFromAllDrives=True
V35: Drive KB isolation = per-account impersonation (DWD with_subject); account reads only files its identity can read; list/search filter mimeType text/markdown + trashed=false; content decoded UTF-8 errors=replace
V37: auth = service account + domain-wide delegation; file creds -> with_subject(email); ADC -> iam.Signer credentials w/ subject=email; no OAuth user login
V38: Drive tools registered sequential=True — serializes parallel dispatch (shared httplib2.Http thread-unsafe, max concurrent transport = 1); transport faults (HttpError, TimeoutError, OSError) -> structured drive_unavailable dict, never bare raise
V39: agent tool failure -> error dict {error, message}, never exception to agent; agent re-drafts via tool-error path
V40: protocol fragment naming tools ! name >= 2 distinct tools, never exactly 1
V41: KB grounding rules (search-first, 2-search budget then single list, read top >= 3 hits, per-target search budget on compare) live in the workflow definition's `instructions` field (§V.103), not a code-defined template protocol fragment; inbound-google-drive template binds the Drive tool set but carries no grounding fragment
V42: outbound body format lint — >= 3 consecutive spec-shape lines (short label + whitespace + value) w/o |---| separator -> format_check rejection; ASCII rule-lines (---, ===, ___) not separators; _BASE protocol fragment MUST explicitly mandate pipe-table for spec rows (model numbers, flow rates, dimensions, capacities) so format-lint is backstop not primary enforcement — permissive "may use Markdown tables" alone is insufficient
V44: agent shape owned by code-defined template registry — TEMPLATES keys == WorkflowTemplateName members; WorkflowTemplate frozen; every template carries non-empty protocol + tools + description; workflow.template + type immutable post-create (update raises ValueError), type derived from template
V45: protocol composed in canonical fragment order _BASE -> trigger branch -> _DECLINE -> _NO_FABRICATION; every fragment workflow-neutral or email-universal — workflow-specific behavioral instructions (KB-grounding discipline, folder-specific protocol) live in workflow.instructions (§V.103), never a template fragment; template owns tool set + fragment protocol, no per-workflow fragment override; agent-facing prompt text carries zero SPEC citation — no fragment string nor model-visible tool description embeds `§V/§T/§B.<n>` (runtime agent has no SPEC.md; cite the governing invariant in an adjacent comment/docstring, not the prompt); grep `§[VTB]\.[0-9]+` over composed protocol + registered tool descriptions -> zero hits (closes §B.79)
V46: template name = <direction>-<data-system>; prefix == direction field
V47: Anthropic calls set anthropic_cache_instructions=True + anthropic_cache_tool_definitions=True (classifier + workflow agent); agent.invoke span carries cache_read_input_tokens + cache_creation_input_tokens
V48: Anthropic HTTP timeout = 240s (4x httpx default); APITimeoutError + httpx.ReadTimeout classified terminal not transient — mid-turn tool side-effects make retry unsafe
V49: bounded auto-retry — 4 attempts, execute_task per-task classification, manual retry = failed/cancelled only — retry matrix → .claude/check-extras.md §V.49
V51: every logfire.exception site reachable from `mailpilot run` ! paired operator_event("error", source=..., message=...); contract test sweeps run-reachable modules for logfire.exception sites — each ! operator_event("error") in same except block
V52: logfire.configure(environment=settings.logfire_environment) -> spans carry deployment_environment; cloud queries filter by env
V53: agent tool spans come from logfire.instrument_pydantic_ai() (gen_ai.tool.name attr); no logfire.span inside agent tools; agents carry explicit names mailpilot.classifier + mailpilot.workflow
V54: every CLI mutation = logfire.span("<noun>.<verb>") + paired operator_event w/ changed-field list; psycopg constraint -> envelope code {UniqueViolation: duplicate_key, ForeignKeyViolation: foreign_key_violation, NotNullViolation: not_null_violation, CheckViolation: check_violation}; other psycopg.Error -> database_error; pydantic ValidationError -> validation_error; logfire.exception + operator_event("error") fire before envelope; non-psycopg exceptions re-raise w/o envelope
V55: tool_response span attr exempt from Logfire scrubbing; all other attrs scrubbed
V57: test-google-drive burst payload composed from the qa_pairs corpus via qa.py pick — in-scope / out-of-scope / compare mix exercises the three classifier branches under burst; aggregate-only oracle, reply content not graded per-message (structural health measured by C4, not content fidelity)
V59: /test-google-drive = Drive-KB burst-load oracle (smoke Scenario C), phase-numbered body (Phase 0 Setup .. Phase 5 Teardown) mirrored across 2 variants from source outbound@lab5.ca — variant-prod -> hello@lab5.ca deployment_environment='production' warm/non-destructive (no make clean, demo-workflow pre-exists), variant-dev -> inbound@lab5.ca deployment_environment='development' full Phase 0 setup (make clean + accounts/contacts/demo-workflow); arg [dev|prod|both] default dev — no-arg validates development only (prod opt-in, both = prod-then-dev warm ordering); Phases 1-4 byte-shared across variants, only Phase 0 + Phase 5 (dev-only teardown) + 3 params diverge; burst (payload-gen + P=4 fire + self-heal + round-trip poll) owned by scripts/burst.py (--mode {local|gmail} absorbs the prod/dev poll divergence), not inlined bash; subject [TGD-<HHMMSS>-<i>] fresh-randomized; PASS = aggregate C4 Logfire gates + zero error/warn scoped to each variant's deployment_environment; every run (PASS or FAIL) emits a chat-only span-free Gate report per variant — one row per C4 gate (pass/fail mark + measured vs threshold + owning §V), no spans/email_id/trace_id/timeline; on FAIL the skill auto-launches the Logfire investigation over the current-run window (no manual /logfire:debug), classifies the breach -> "## Next" /sdd:spec-ready remedy (breach -> cause -> recurrence-class -> proposed §V/§B + paste-ready /sdd:spec line), span detail FAIL-only; C4 read-count attribution (G.a compare classifier, G.c Drive-race scan, investigate.md per-invoke timeline) rolls up per agent.invoke via parent-span lineage (read/tool span -> agent run -> agent.invoke), never keyed on trace_id — under §V.23 concurrent drain co-tick invokes share one trace_id (inherited from a common run.execute_task parent) so trace_id is not 1:1 w/ an invocation; reads already parented under each invoke's agent-run span so gate-side per-invoke rollup suffices (rooting each agent.invoke in its own trace = optional belt-and-suspenders); C4 gate logic + per-variant verdict behavior-invariant (gates alone decide); Gate report + remedy advisory + chat-only (no .md artifact, §V.57 aggregate-only intact) — gate SQL → references/gates.md, FAIL-only investigation SQL + remedy template → references/investigate.md
V61: reply-latency verdict from Logfire agent.invoke spans, never the CLI poll (round-trip check only); /test-google-drive judges latency aggregate over the burst window per §V.69, scoped per variant deployment_environment; two-budget split sla_agent_seconds + sla_delivery_seconds attributes a breach to agent vs delivery — thresholds → .claude/check-extras.md §V.61
V62: /release extends /gh:release — post-tag: push main + v<x.y.z>, uv build asserts dist/mailpilot-<x.y.z>-py3-none-any.whl exists, gh release create v<x.y.z> --verify-tag --notes-from-tag attaches wheel; confirm-before-mutate gate covers push + upload; version source = pyproject.toml [project].version; deploy = published wheel asset — tag-only release not deployable
V63: declarative export/import — import accepts JSON array (live-state dump, DB<->JSON) + TOML catalog (per §V.103, dispatched by `.toml` ext / dir); export = JSON only; stdin = JSON only (TTY w/o --file -> validation_error); per-row errors continue batch w/ per-row error entries; upsert keyed on natural unique fields; round-trip (JSON) idempotent; export order deterministic (workflows by name)
V67: persisted outbound in_reply_to + references_header mirror wire MIME headers exactly
V68: pre-send fact-check — numeric tokens in body appear in read_drive_markdown ledger; table vs prose doc scoping; empty ledger skips; mismatch -> fact_check_mismatch — recipe → .claude/check-extras.md §V.68
V69: tick classifying >= 1 inbound -> next tick forces full sweep + wakeup_event set; test-google-drive N=4 per-variant burst T_delivery <= 75s
V70: burst retry-rate contract — N=4 gate per-branch (flat 5% ratio void at small sample: 1 error = 25%, false-fails lone compare invoke): non-compare invokes strict (sum tool_error_count == 0), compare invoke tolerates <= 2 self-correcting §V.68 fact-check re-drafts; compare exhausting §V.71 cap-3 emits cap_reached warn -> already fails C4 n_warns==0 so per-compare floor can't mask non-self-correcting agent; ratio <= 5% ceiling governs larger N (P<=8, N<=25); prod + dev measured separately; breach -> §V.41/§V.42/§V.68 investigation — measurement detail → .claude/check-extras.md §V.70
V71: per-task reply-rejection counter (reply_rejection_scope) — format_check + fact_check rejections share cap 3; past cap both checks bypass; outside scope checks always enforce
V72: company.profile JSONB validated vs CompanyProfile — required {summary, products, target_customers, sources} non-empty; timezone optional, null on multi-zone; malformed -> validation_error
V73: skill-body-embedded Workflow snippet runnable as authored — (a) zero free vars; (b) args-as-collection guard; (c) prose-matches-dispatch; (d) saved-workflow byte-identical — recipe → .claude/check-extras.md §V.73
V74: CSV ingestion uses RFC-4180 parser (csv module / csv.DictReader); redirect resolution = hop-agnostic curl -sL; scope .claude/skills/**; .claude/workflows/*.js excluded — recipe → .claude/check-extras.md §V.74
V75: sync incremental via History API from gmail_history_id checkpoint; history 404 -> full INBOX re-sync; checkpoint snapshot pre-fetch, last_synced_at post-store; message 404 mid-batch -> skip (deleted)
V76: routing eligibility window — received_at older than 7 days, zero active workflows, or predates earliest active workflow -> is_routed=TRUE w/ matching skipped_* route_method, no LLM call
V77: outbound email row persists only after Gmail accepts send — Gmail failure -> no orphan row
V78: outbound MIME stamped X-MailPilot-Version always + X-MailPilot-Account-Id when account-bound; replies set In-Reply-To + References, References defaults to in_reply_to
V79: send/reply guards — disabled contact blocks send + reply; cold-send cooldown 30 days per (account, contact, workflow); reply requires original gmail_thread_id + contact_id (typed errors); reply subject gets "Re: " prefix unless already prefixed, case-insensitive
V80: bounce handling — sender local-part in {mailer-daemon, postmaster} (case-insensitive) or label contains "BOUNCE" -> most recent outbound in same thread + account marked bounced + contact disabled w/ "bounced:" reason prefix; unsubscribe path uses "unsubscribed:" prefix
V81: agent run ! call >= 1 tool; noop(reason) = explicit no-op escape; zero tool calls -> AgentDidNotUseToolsError
V82: agent email history scoped to (account_id, contact_id, workflow_id) — other workflows' mail excluded
V83: execute_task pre-flight cancels task when workflow inactive/missing, contact disabled/missing, enrollment missing or status != active
V84: pubsub notification callback acks unconditionally (decode error + missing emailAddress included); sets wakeup_event when supplied
V85: settings precedence kwargs > MAILPILOT_* env > ~/.mailpilot/config.json > defaults; config file auto-created on first load
V86: secret settings (anthropic_api_key, logfire_token, database_url) redacted as '***' in telemetry; config.set event logs key + changed flag
V87: cross-account isolation — thread + RFC message-id lookups scoped to account_id; agent read_email cross-account -> None (prompt-injection guard)
V88: entity enums enforced by schema CHECK — workflow.template/type/status, enrollment.status, email.direction/status/route_method, task.status, activity.type; value sets authoritative in schema.sql
V89: singleton rows — schema_metadata id=1, sync_status id='singleton'
V90: natural-key UNIQUE — account.email, company.domain, contact.email, workflow(account_id, name), enrollment(workflow_id, contact_id), email.gmail_message_id nullable-unique; tag names unique per owner among active rows (partial index excludes disabled)
V91: tag/note mutation + its activity row commit in one transaction — both or neither
V92: email render = Markdown -> HTML inline styles only, no stylesheet; THEMES = {blue, green, orange, purple, red, slate}; None/unknown theme -> blue fallback
V93: operator_event -> stderr single line "HH:MM:SS event=NAME k=v ..."; newlines collapsed to space; whitespace values double-quoted, inner quotes escaped
V94: CLI FK validation precedes mutation — referenced entity missing -> error envelope, no partial write
V95: contact lead-metadata = flat columns not JSONB — contact.title TEXT NULL (role label); contact.email_confidence INT NULL, schema CHECK email_confidence BETWEEN 0 AND 100; NULL = Bouncer unknown (unbilled, unverified) = high risk; email_confidence = sole email-risk score (low = high risk); operator review filter contact list --max-email-confidence N surfaces email_confidence <= N OR IS NULL so admit-all (§V.96) never drops unknowns — SQL inequality alone (NULL excluded) is the trap; no ContactProfile model
V96: lead-contacts discovery — discover set = company.profile IS NOT NULL and contact-count < 5; <= 5 contacts/company/run; admit-all — every discovered+verified email -> contact row, low/NULL email_confidence flags risk in run summary never gates admission; persistence memoizes via contact.email UNIQUE §V.90 -> idempotent re-run skips existing, no re-discovery of known-bad addresses
V97: lead-companies + lead-contacts run-summary completeness — batch gate capping below stale-count -> run summary carries deferred = stale-count - processed (rows left profile/contact-NULL for a follow-up run); all stale processed -> deferred 0 or omitted; bare created|enriched|seeded counts never the sole remainder signal
V98: lead-companies seed collision visibility — resolved-apex duplicate_key recorded in run-summary collapsed when incoming CSV display name diverges from the owning company's name; fires intra-batch AND onto a previously-seeded row; same-name re-seed stays silent existing; bare existing:N never the sole entity-merge signal
V99: every path a `.claude/skills/**` body cites resolves on disk — script refs (`uv run python .claude/skills/**/scripts/*.py`) + source-of-truth dirs named in prose or a runtime recovery gate ! exist; cited-but-absent = recovery instruction that errors when the operator needs it. Scope = grep skill bodies `\.claude/skills/\S+\.py` + backticked dir refs -> each ! resolve
V100: skill-body progressive-disclosure — `.claude/skills/**/SKILL.md` keeps live procedure inline (gates, step sequence, scenario flow); run-end-only or rarely-loaded material (report-format templates, derivation SQL + scan checklists, timing tables) lives in `references/*.md` cited not inlined; near-verbatim prose shared across sibling skills (shared conventions, batch-gate logic, output-block format) likewise lives in one shared `references/*.md` both cite, never per-skill copies aligned by hand; body past skill-creator ~500-line budget = procedure buried among templates -> extract. Scope = `.claude/skills/**/SKILL.md`
V101: skill-body obligation vocabulary — `.claude/skills/**` prose marks a hard requirement w/ an explicit word (`MUST` / `required`), never bare ` ! ` as must (the telegraph must-glyph reads as negation in code so a model executing the skill can invert the constraint); backticked shell (`[ ! -f ]`, `!=`) + SPEC.md/telegraph register exempt. Scope = `.claude/skills/**/*.md` prose, grep ` ! ` -> zero must-sense hits — recipe → .claude/check-extras.md §V.101
V102: project-skill frontmatter hygiene — every `.claude/skills/**/SKILL.md` sets scoped `allowed-tools` + `argument-hint` (safety rail + invocation shape; broad allowed-tools defensible only w/ a stated reason); `description` field = triggering-intent signal (the always-in-context trigger text), vendor names + full pipeline-stage detail belong in the body not the metadata budget. Scope = `.claude/skills/**/SKILL.md` frontmatter
V103: file-based workflow defs = `workflows/*.toml`, 1 workflow/file, pure TOML (stdlib tomllib, no new dep); fields = Workflow row 1:1 {name, template, theme, objective, instructions}, instructions = TOML multi-line literal string; `workflow import --file X.toml` parses -> row byte-identical to JSON-array import + shared validation (malformed/missing-required -> validation_error per §V.54, no partial write §V.94); `--file <dir>` globs `*.toml` (batch catalog apply, per-row errors continue §V.63); `workflows/` = private git submodule kborovik/workflows (dev/test bench here, prod imports same catalog); any skill-cited `workflows/` path needs `git submodule update --init` (resolves §V.99 only post-init); root `workflows/*.toml` (CRM defs) distinct from `.claude/workflows/*.js` (Claude Code scripts §V.73-74)

## §T TASKS

id|status|task|cites
T109|x|impl §V.51(+) per §B.71 — pair connect-fail logfire.exception w/ operator_event("error") + sweep contract test for all run-reachable logfire.exception sites|V51,B71
T110|x|add contact.title + contact.email_confidence columns w/ CHECK; Contact model + CLI --title/--email-confidence flags; contact list --max-email-confidence filter; TDD|V95
T111|x|rename lead-encreach -> lead-companies skill dir + workflow file + frontmatter; update all intra-skill refs; cite-DAG sweep|V73,B72
T112|x|new lead-contacts skill + lead-contacts-find.js + contact-finder agent — Hunter -> TheOrg -> Bouncer pipeline, seed <= 5 contacts/company; workflow byte-identical to skill-mirror per §V.73|V96,V73
T113|x|impl §V.3(+) per §B.73 — ConsoleOptions(output=sys.stderr) in configure_logging + recurrence-guard test (schema-drift path)|V3,B73
T114|x|impl §V.97(+) per §B.74 — add deferred field to lead-companies + lead-contacts Run summary when batch gate caps; skill-prose only|V97,B74
T115|x|impl §V.98(+) per §B.75 — seed_companies.py: name-divergent duplicate_key -> collapsed (not bare existing); Run summary prose mirrors|V98,B75
T116|x|impl §V.5(+): ContactSummary + search_contacts add title + company_domain (LEFT JOIN); --min/max-email-confidence, --company-domain, --title filters; TDD|V5,V95
T117|x|impl §V.95(+) per §B.76 — list_contacts OR IS NULL disjunct for NULL email_confidence + docstring fix + recurrence-guard test|V95,B76
T118|x|impl §V.99(+) per §B.77 — smoke-test SKILL.md dead KB-sync pointers: Scripts intro (:52) + `kb-docs/`/`sync_kb_to_drive.py` entries (:63-64) + Phase 0 missing-compare-target recovery gate (:109) + compare-target note (:348) cite a script + dir that never existed (git history clean; scripts/ holds only qa.py, qa_pairs.json, generate_qa_pairs.py). Rewrite to the real KB-maintenance path: demo Drive folder `1IUuPinOopUv_YWOZyFpt2ZX8Hd8bpZat` is hand-maintained — drop the dead script+dir refs, replace the gate recovery command w/ the manual Drive-upload path (impersonated kb@lab5.ca). Skill-prose only, no src/ change. Verify: `rg -n 'sync_kb_to_drive|kb-docs' .claude/skills/` -> zero hits.|V99,B77
T119|x|impl §V.100(+) — split smoke-test SKILL.md (1254 lines, ~2.5x the 500-line budget): extract Phase 5 report format (`### §1 Execution`/`### §2 Bugs`/`### §3 Invariants`/severity+routing/`### Spec hand-off block`, ~:1098-1228) -> `references/report-format.md`; Derivation aids SQL + 13-category scan checklist (`### Derivation aids`, ~:1041-1097) -> `references/derivation-aids.md`; Timing table (`## Timing`, ~:1229-end) -> reference or trim. Replace each extracted span w/ a one-line "load references/<f>.md at Phase 5" pointer; live gate/scenario procedure stays inline. Skill-prose only, no src/ change. Verify: `wc -l .claude/skills/smoke-test/SKILL.md` << 1254 + `ls .claude/skills/smoke-test/references/` shows report-format.md + derivation-aids.md|V100
T120|x|add §V.73 drift-guard — lead-companies + lead-contacts embed the full `.claude/workflows/*.js` body as the spec-of-record mirror, kept byte-identical to the saved file by hand (in-sync now, audited); add a mechanical test asserting each skill-body embedded snippet == saved `lead-companies-enrich.js`/`lead-contacts-find.js` body below `meta` per §V.73 — drift fails CI not review. Scope = grep `rg -n 'spec-of-record mirror' .claude/skills/` -> each mirror block carries a byte-identical test. Failing test first per TDD.|V73
T121|x|impl §V.101(+) — replace must-sense ` ! ` w/ `MUST` across `.claude/skills/**` (5 sites: release SKILL.md:218,:222; lead-contacts:115; lead-companies:92,:173); SPEC.md + telegraph register untouched. Scope = grep `rg -n ' ! ' .claude/skills/` -> zero must-sense hits post-edit. Skill-prose only, no src/ change.|V101
T122|x|fix stale scenario count — smoke-test SKILL.md:12 "Two scenarios share one Phase 0 setup" -> "Three" (Scenarios A/B/C; :23 already says "All three"); leftover from before Scenario C landed. Scope = grep `rg -n 'Two scenarios' .claude/skills/smoke-test/SKILL.md` -> zero hits. Skill-prose only.|-
T123|x|impl §V.100(+) sibling-DRY — extract near-verbatim lead-companies/lead-contacts shared prose (Conventions: envelope/stdout-parsing/ASCII; batch-gate logic; Next-block format) -> `references/lead-pipeline-conventions.md` both skills cite, not per-skill copies. Scope = grep `rg -n 'Conventions|batch gate|Next block' .claude/skills/lead-companies/SKILL.md .claude/skills/lead-contacts/SKILL.md` -> shared block replaced w/ references pointer. Skill-prose only, no src/ change.|V100
T124|x|add demo-test cross-skill dep note — demo-test calls `.claude/skills/smoke-test/scripts/qa.py` (:63,:171); Prerequisites (:37) never flags the coupling so demo-test silently breaks if smoke-test is moved/packaged alone (the §V.99-resolved path goes stale). Add a one-line Prerequisites note naming the smoke-test-owned dependency. Scope = grep `rg -n 'smoke-test/scripts' .claude/skills/demo-test/SKILL.md` -> Prerequisites declares the dependency. Skill-prose only.|V99
T125|x|impl §V.102(+) frontmatter — add scoped `allowed-tools` + `argument-hint` to demo-test + smoke-test SKILL.md (both set neither; smoke-test runs `make clean` + sends real email so a scoped allowed-tools is a cheap safety rail). Scope = grep `rg -n 'allowed-tools|argument-hint' .claude/skills/demo-test/SKILL.md .claude/skills/smoke-test/SKILL.md` -> both keys present. Skill-prose only.|V102
T126|x|impl §V.102(+) description trim — lead-companies + lead-contacts `description:` pack vendor names + full pipeline stages into the always-in-context trigger text; trim to triggering intent (enrich companies / find contacts), vendor/stage detail -> body. Scope = grep `rg -n '^description:' .claude/skills/lead-companies/SKILL.md .claude/skills/lead-contacts/SKILL.md` -> description = trigger-intent, no vendor roster. Skill-prose only.|V102
T127|x|impl §V.101(+) audit recipe — add "## §V.101 — must-sense ` ! ` ban" mechanical-grep recipe to .claude/check-extras.md mirroring §V.74 "Mechanical greps (manual judgment on hits)" shape: trigger on .claude/skills/**/*.md change; grep ` ! ` over .claude/skills/**/*.md, manual-judgment exempt backticked shell (`[ ! -f ]`, `!=`) + SPEC.md/telegraph register; build EXECUTE step-3 PUBLISHED probe auto-fires it on future skill edits. Scope = grep `rg -n ' ! ' .claude/skills/` -> zero must-sense hits. Audit-only, no src/.|V101
T128|x|impl §V.42(+) _BASE pipe-table mandate — replace _BASE "Email bodies may use Markdown formatting (headers, bold, tables)" in src/mailpilot/agent/templates.py w/ prescriptive: when reply body carries product specs (model numbers, flow rates, dimensions, capacities), use GFM pipe table w/ header row + |---| separator; space-aligned spec blocks rejected by §V.42 lint. Add test asserting _BASE carries the mandate. Scope = grep `rg -n 'pipe.table\|may use Markdown' src/mailpilot/agent/templates.py` -> mandate present|V42
T129|x|reorganize test skills -> single `.claude/skills/test-google-drive/` — merge demo-test + smoke-test, keep ONLY Scenario C (burst-load oracle) parameterized by {target_account, env}; run 2 variants, source outbound@lab5.ca: variant-prod -> hello@lab5.ca env='production' warm-state (no make clean / no workflow create, demo-test posture); variant-dev -> inbound@lab5.ca env='development' full Phase 0 setup (make clean + accounts/contacts/demo-workflow, smoke-test posture); each variant fires Scenario-C burst (N=4 per the reduction this row supersedes) + C4 aggregate Logfire gates filtered on its own deployment_environment; drop Scenarios A/B + per-message qa.py correctness grading (aggregate-only); retain qa.py burst-payload gen (move scripts/qa.py + qa_pairs.json under test-google-drive). Verify: `ls .claude/skills/test-google-drive/SKILL.md` + `rg -l 'demo-test|smoke-test' .claude/skills/` -> zero stale refs.|V52,V57,V59,V61,V69,V70
T130|x|impl §V.45(+) strip SPEC cite from agent prompt — drop `(§V.42)` from _BASE in src/mailpilot/agent/templates.py (keep the behavioral mandate + "rejected by the outbound format lint" motivation, drop the §-numbering); add guard test: every build_protocol() output + registered tool description carries no `§[VTB]\.[0-9]+` token. Scope = grep `§[VTB]\.[0-9]+` over composed protocol + registered tool descriptions -> zero hits|V45
T131|x|impl §V.103(+) + §V.63(∆) — TOML workflow-def standard: add `workflows/` git submodule (kborovik/workflows); `workflow import --file` parses `.toml` via stdlib tomllib + globs `*.toml` on dir -> row byte-identical to JSON path (failing test first, TDD); migrate tests/fixtures/workflows-inbound.json -> `workflows/demo-lab5-mailpilot.toml` (instructions = multi-line literal), drop orphaned workflows-outbound.json; repoint test-google-drive/SKILL.md:117-119 `--file` + add `git submodule update --init` to make + Phase 0; CLAUDE.md 1-line `workflows/` vs `.claude/workflows/` note. Verify: `rg 'tests/fixtures/workflows-' .claude/ tests/ src/` -> zero hits|V103,V63,V99,V59
T132|x|impl §V.41(∆) + §V.45(∆) — move workflow-specific grounding out of templates.py into the workflow def: delete `_DRIVE_GROUNDING` fragment, set inbound-google-drive protocol_post = _DECLINE + _NO_FABRICATION (keep _DRIVE tool binding); fold grounding discipline (search-first 2-search budget, read top >=3, per-target search on compare) + verbatim-citation/no-unit-conversion guidance into workflows/demo-lab5-mailpilot.toml `instructions`; update tests/test_agent_drive_concurrency.py + protocol-cite guard. Scope = grep `rg -n '_DRIVE_GROUNDING' src/` -> zero hits. TDD.|V41,V45,V103,V68
T133|x|impl §V.59(∆) post-mortem report — add a "Post-mortem / performance report" section to .claude/skills/test-google-drive/SKILL.md emitted every run (PASS or FAIL) per variant after the C4 gates: (a) breach attribution — per breached C4 gate name the failing span(s) + owning §V (sla_delivery->§V.69, tool_error->§V.70 branch, overlap->§V.23, drive-dur->§V.38), flag self-heal-timing artifact vs real regression; (b) run-over-run trend — Logfire query over a widened same-deployment_environment window comparing this run's sla_agent/sla_delivery/retry_rate/tokens vs prior runs; (c) per-invoke tool-call timeline from records (search_drive_markdown/read_drive_markdown/chat per trace); chat-only, no .md artifact (§V.57 aggregate-only intact), advisory-only (never changes PASS/FAIL); report-format templates + trend SQL live in .claude/check-extras.md §V.59 not inlined (§V.100). Verify: `rg -n 'post.?mortem|performance report' .claude/skills/test-google-drive/SKILL.md` -> section present|V59,V57,V100,V69,V70,V23,V38
T134|x|impl §V.59(∆) Report simplification — in .claude/skills/test-google-drive/SKILL.md + .claude/check-extras.md §V.59: rename "## Post-mortem / performance report" section -> "## Report"; drop part (b) run-over-run trend (the widened-window 14-day trend query + prose); relabel remaining parts (a) breach attribution + (b) per-invoke tool-call timeline; rewrite "## On FAIL" so the skill auto-investigates the current-run Logfire records (drop the manual "/logfire:debug operator next move" posture) and emits a "## Next" section of suggested remedies derived from the breach; keep chat-only + advisory (never alters verdict). Verify: `rg -n 'Post-mortem|run-over-run|last 14 days' .claude/skills/test-google-drive/SKILL.md .claude/check-extras.md` -> zero hits; `rg -n '## Report|## Next' .claude/skills/test-google-drive/SKILL.md` -> both present|V59,V57,V100,V69,V70,V23,V38
T135|x|refactor test-google-drive per designs/test-google-drive-phase-split.md — phase-split SKILL.md body (## Phase 0..5) + prod/dev param-block mirror + arg [dev|prod|both] default dev (no-arg = development only) + scripts/burst.py owning B1-B3 (payload-gen + P=4 fire + self-heal + round-trip poll, --mode {local|gmail}) so Phase 1 body <= ~15 lines; migrate C4 gate SQL -> references/gates.md + FAIL-only investigation SQL + /sdd:spec-ready remedy template -> references/investigate.md, repoint §V.59 pointer off .claude/check-extras.md (sub-blocks removed, other §-blocks untouched); simplify every-run output to a span-free per-gate Gate report (no email_id/trace_id/timeline); on FAIL auto-launch the Logfire investigation -> ## Next /sdd:spec-ready remedy; C4 gate logic + per-variant verdict behavior-invariant; cite-DAG sweep after repoint (§B.72 class). Edits run through /skill-creator. Verify: `rg '## Phase [0-5]' .claude/skills/test-google-drive/SKILL.md` present; `ls .claude/skills/test-google-drive/scripts/burst.py .claude/skills/test-google-drive/references/gates.md .claude/skills/test-google-drive/references/investigate.md`; `rg '§V\.59' .claude/check-extras.md` -> zero; Gate report carries no email_id/trace_id token; /sdd:check cite-DAG clean|V59,V100,V57
T136|x|impl §V.59(∆) per §B.81 — re-key C4 read attribution off trace_id onto per-invoke parent-span lineage: in .claude/skills/test-google-drive/references/gates.md rewrite G.a read_counts CTE + LEFT JOIN + G.c Drive-race scan to roll read_drive_markdown up per agent.invoke (read/tool span -> agent run invoke_agent span -> agent.invoke) not GROUP BY trace_id; same fix for investigate.md per-invoke timeline (join tool spans via parent-span chain, not r.trace_id = i.trace_id); co-tick invokes sharing one trace_id under §V.23 must not smear per-trace aggregates. Verify: `rg -n 'GROUP BY trace_id\|rc.trace_id = r.trace_id\|trace_id = i.trace_id' .claude/skills/test-google-drive/references/` -> zero read/tool-attribution hits; a burst where >1 inbound co-tick yields 3nc/1c|V59,V23,V57
T137|x|impl §V.23(∆) per §B.82 — root each drained task in its own trace: in src/mailpilot/sync.py `_execute_task_in_worker` attach a fresh empty opentelemetry.context.Context() before execute_task + detach in finally, so run.execute_task -> agent.invoke open as a new trace root not a child of the dispatching sync.loop.iteration span; trace_id 1:1 w/ an agent.invoke. Test: a >1 co-tick burst yields a distinct trace_id per agent.invoke span (no shared root). Verify: `rg -n 'context.attach\|Context\(\)' src/mailpilot/sync.py` -> fresh-root detach present; co-tick burst -> N distinct trace_ids|V23,V52,B82
T138|x|rename test-google-drive sub-gate labels C4.a/b/c -> G.a/b/c per designs/test-google-drive-gate-labels.md — strip stale positional `4` (C1-C3/C5-C7 siblings absorbed by §T.135 phase-split); G.{a,b,c} = non-positional enumeration of the 3-gate set, drift-proof. s/C4\.([abc])/G.\1/g across .claude/skills/test-google-drive/references/gates.md (## Gate headers + "same CTE as C4.a" cross-refs + Phase-3 Gate-report "from C4.a/b/c" mapping), references/investigate.md (breach->gate map + "matches C4.a is_compare"), SKILL.md ("run gates C4.a/C4.b/C4.c" + "(C4.b/C4.a/C4.c gates)"); retain C4-AND verdict token + "C4 gates"/"measured by C4" family brand (Q2 declined — C4 lives on as non-positional family/verdict brand); sequence before §T.136 build so the trace_id re-key inherits G.a/G.c. Verify: `rg -n 'C4\.[abc]' .claude/skills/test-google-drive/` -> zero & `rg -n 'C4-AND' .claude/skills/test-google-drive/SKILL.md` -> present & /sdd:check cite-DAG clean|V59,V57,V23

## §B BUGS

id|date|cause|fix
B71|2026-06-12|`initialize_database` connect-fail path (`database.py:151`): logfire.exception w/o paired operator_event("error"), reachable from `mailpilot run` startup — operator stderr silent on DB-connect failure|V51
B72|2026-06-12|SPEC rebuild Phase 1+2 sweep excluded `.claude/skills/**` citers — 6 operative §V rows dropped, 13 cites dangled; surfaced by /sdd:check cite-DAG whole-repo scan|V22,V59,V62,V70,V73,V74
B73|2026-06-13|configure_logging (cli.py:66): ConsoleOptions output unset -> defaults stdout; logfire.warn lines print stdout ahead of JSON envelope, violating V3 — json.load over `mailpilot company list` fails "Extra data: line 1 column 2"|V3
B74|2026-06-13|lead-companies on theirstack.csv (stale=23): operator chose First-10 at the batch gate -> 13 rows left profile IS NULL, but run-summary envelope carries no deferred count -> operator cannot see rows need a follow-up run; symmetric in lead-contacts|V97
B75|2026-06-13|lead-companies seed collapsed accumulator catches only intra-batch apex merges; a CSV row redirecting onto a previously-seeded apex w/ a divergent name lands silent in existing — confirmed: whitecapsupply.com row named "National Concrete Accessories", surfaced only as existing:2|V98
B76|2026-06-13|lead-contacts seeded 35 contacts all email_confidence NULL (Bouncer unknown); `contact list --max-email-confidence 70` returned 0/35 — `email_confidence <= N` excludes NULL (SQL three-valued logic) + docstring codified the exclusion contradicting §V.96 NULL-flags-risk; operator ships unverified addresses|V95
B77|2026-06-13|smoke-test SKILL.md documents `sync_kb_to_drive.py` + `kb-docs/` as the KB-maintenance path (Scripts :52,:63-64) + Phase 0 KB-visibility gate (:109) references the same nonexistent script — recovery command errors when KB-drift triggers it; 4th dead ref at :348|V99
B78|2026-06-14|_BASE "may use Markdown tables" (permissive) w/o pipe-table mandate; V42 lint reactive not preventive; agent generates space-aligned spec block first, lint rejects, retry — systematic 7/8 invocations under burst, retry_rate 0.875 >> §V.70 0.05 ceiling|V42
B79|2026-06-14|_BASE protocol fragment (agent/templates.py:90) embeds literal SPEC cite `(§V.42)` — runtime agent has no SPEC.md, so the §-numbering is dead authoring metadata leaking into every reply-agent system prompt|V45
B80|2026-06-14|§V.70 flat retry-rate ratio <=5% void at N=4 (1 err=25%) + no compare carve-out: lone compare invoke structurally re-drafts on §V.68 fact-check (cross-datasheet unit conversion 5gph->120gpd, 100gpd->4.17gph; prose-buried 1.25/300) then self-corrects clean, yet retry_rate=0.5 false-failed test-google-drive dev — distinct cause from B78 (format-lint)|V70
B81|2026-06-14|test-google-drive C4 gates attribute read_drive_markdown by trace_id (G.a compare classifier read_counts GROUP BY trace_id + LEFT JOIN, G.c Drive-race trace-window scan, investigate.md timeline JOIN on trace_id) — under §V.23 concurrent drain >1 inbound co-tick share one trace_id (inherited from a common run.execute_task parent), smearing per-trace read counts across co-tick invokes -> branch-split false-FAIL (measured 1nc/3c, true 3nc/1c via agent-run->agent.invoke parent lineage); not a §V.27 agent regression|V59
B82|2026-06-15|§V.23 drain worker inherits the dispatching tick's sync.loop.iteration OTel context (py3.14 ThreadPoolExecutor.submit propagates the active span into workers), so every task drained in one tick opens run.execute_task -> agent.invoke under that tick's trace -> all co-tick invokes share one trace_id (not 1:1 w/ an agent.invoke) — instrumentation root cause behind §B.81's gate-side read smear; regressed when the sync.loop.iteration span (PR #77) began wrapping the drain dispatch|V23
