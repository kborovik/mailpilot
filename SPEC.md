# SPEC

## §G GOAL

Agent-operated CRM. Gmail = comms layer. Claude Code = strategist; internal Pydantic AI agent = tactical executor (routing, auto-reply, follow-up).

## §C CONSTRAINTS

- Python 3.14. basedpyright strict. ruff. TDD per CLAUDE.md.
- PostgreSQL 18. psycopg, raw SQL via `psycopg.sql`. ⊥ ORM.
- IDs = UUIDv7 (`uuid.uuid7()` via `_new_id()`).
- Gmail scope = `gmail.modify` only. ⊥ additional Gmail scopes.
- Drive scope = `drive.readonly` only. Read-only KB grounding. ⊥ write/modify.
- Auth = service account & domain-wide delegation per §V.42. Credential source ∈ {file path (`google_application_credentials` setting ∨ `GOOGLE_APPLICATION_CREDENTIALS` env var), Application Default Credentials (e.g. GCE metadata server, Workload Identity, Cloud Run identity)}. Per-account impersonation: file creds → `credentials.with_subject(email)`; ADC creds → `service_account.Credentials(signer=iam.Signer(...), service_account_email=<sa>, subject=email, ...)`. ⊥ OAuth user login.
- Email body = plain text only (control chars stripped). ⊥ HTML body persistence. ⊥ embeddings, ⊥ vector store, ⊥ ingestion pipeline.
- KB files = `.md` in Drive folder named in `workflow.instructions`. ⊥ in-app PDF/Docs/HTML conversion.
- ASCII-only project artifacts (code, docs, CLI output). Agent-generated email body content exempt.
- Tool pattern: typed sig, DI deps, dict return, error dicts on failure (⊥ raise to agent).

## §I INTERFACES

- cli: `mailpilot <noun> <verb> [args]` → JSON on stdout, exit code 0 ∨ 1, errors to stderr.
  - nouns: `account`, `company`, `contact`, `workflow`, `enrollment`, `task`, `email`, `activity`, `tag`, `note`, `template`.
  - verbs: `list|search|view|create|update|disable|add|remove|reply|send|start|stop|cancel|retry|run|sync|export|import`.
  - top-level: `run` (sync & task loop), `status`, `config get|set`, global `--version|--debug|--completion|--skill`.
  - envelope: `list|search|sync|export|import` → `{"<plural>": [...], "ok": true}`; `view|create|update|disable|add|remove|reply|send|start|stop|cancel|retry` → `{"<singular>": {...}, "ok": true}`; err → `{"error": CODE, "message": TEXT, "ok": false}`.
  - `template` = read-only, code-defined (registry in `src/mailpilot/agent/templates.py` per §V.32). Verbs: `list [--direction inbound|outbound]`, `view NAME`. ⊥ create/update/delete — new template = code change + PR.
- agent tools (`src/mailpilot/agent/tools.py`): `send_email`, `reply_email`, `search_emails`, `read_email`, `read_contact`, `read_company`, `list_enrollments`, `create_task`, `cancel_task`, `record_enrollment_outcome`, `disable_contact`, `list_drive_markdown`, `read_drive_markdown`, `search_drive_markdown`, `noop`. ∀ tool → typed sig, dict return, err dict on failure.
- pubsub (`src/mailpilot/pubsub.py`): topic `mailpilot-topic-dev`, sub `mailpilot-sub-dev` (defaults; per-env override via `MAILPILOT_GOOGLE_PUBSUB_TOPIC` / `..._SUBSCRIPTION`). `setup_pubsub()` idempotent. `start_subscriber(settings, callback)` streaming pull. `make_notification_callback(queue, wakeup_event)` decode → enqueue → `wakeup_event.set()`. `renew_watches()` refresh @ T-24h.
- config (`src/mailpilot/settings.py`): `database_url`, `anthropic_api_key`, `anthropic_model` (default `claude-sonnet-4-6`), `google_application_credentials`, `google_pubsub_topic`, `google_pubsub_subscription`, `logfire_token`, `logfire_environment` ∈ {`development`, `production`}, `run_interval` (default 60s).
- module: `src/mailpilot/gmail.py` → `GmailClient`; `src/mailpilot/drive.py` → `DriveClient`. Mirror shape.
- entrypoint: `mailpilot = "mailpilot.cli:main"`.

## §V INVARIANTS

V1: ∀ Settings consumer → receive `Settings` instance. ⊥ pass API keys / config values as separate fn args. CLI loads via `get_settings()` & forwards.
V2: `cli.py` module top-level imports → only `click`. Heavy deps (`logfire`, `psycopg`, `pydantic`, `mailpilot.*`) ! lazy inside fn bodies. Why: `--help` ~50ms.
V3: `mailpilot run` main loop ! `wait()` on shared `wakeup_event` set by Pub/Sub callback / `LISTEN/NOTIFY` / signal handlers. Clear `wakeup_event` BEFORE processing → mid-iter events re-trigger next wait. Periodic timer = upper-bound fallback only. Canonical: `start_sync_loop` in `src/mailpilot/sync.py`.
V4: ∀ JSON-yielding single-shot CLI command → stdout strict-JSON. ⊥ preceded ∨ interleaved by operator-log lines. `operator_event(...)` → stderr always. Long-running `mailpilot run` exempt (operator console).
V5: CLI envelope shape: `list|search|sync|export|import` → `{"<plural>": [...], "ok": true}`; `view|create|update|disable|add|remove|reply|send|start|stop|cancel|retry` → `{"<singular>": {...}, "ok": true}`; ⊥ inline entity fields at top level. Use `output_entity("<singular>", model)` & `output({"<plural>": [...]})`. `remove` ≡ inverse of `add` → same singular envelope; payload branches on row persistence: hard DELETE (row ⊥ retained) → composite-key (∨ natural-identifier) projection only; soft-delete (row retained w/ terminal-state sentinel, e.g. `status='removed'`) → removed entity. ⊥ echo full pre-delete `<Entity>`-model blob on hard DELETE — stale lifecycle fields (`enrollment.status`, etc.) mislead readers into believing remove failed (§B.33).
V6: ∀ list|search row projects via `<Entity>Summary` — fields ⊆ {id, natural identifier, filter fields exposed as `--flag`, timestamp ordered by, parent-entity natural identifier paired w/ already-included parent FK (e.g., `account_email` alongside `account_id`)}. ⊥ long-text (`body_text`, `instructions`, `objective`), ⊥ JSON blobs, ⊥ variable-cardinality lists, ⊥ Gmail bookkeeping. Why parent-NI clause: operator-facing list output often pivots on parent NI (account email, company name); raw FK alone forces round-trip via `<parent> view <fk>` → noisy ∧ slow. Denormalized parent-NI ≡ display-only ergonomic join-saver; paired-w/-FK constraint preserves FK ≡ canonical key per §V.51 (NI ⊥ writable, ⊥ filter target — ⊥ `--account-email` flag).
V7: ∀ ID = UUIDv7 via `_new_id()` (`uuid.uuid7()`). ⊥ uuid4, ⊥ serial.
V8: `tag`, `note` rows → exactly one of {`contact_id`, `company_id`} set (XOR). Enforced via schema CHECK on `tag` & `note` in `schema.sql`.
V9: `activity` & `note` append-only. ⊥ update fn.
V10: `enrollment.status` ∈ {`active`, `paused`} (schema CHECK on `enrollment.status` in `schema.sql`). Agent records terminal outcomes via `record_enrollment_outcome` → activity log; ⊥ mutate `enrollment.status` as terminal signal.
V11: `agent.invoke` span ! `trigger` attr ∈ {`enrollment_run`, `enrollment_schedule`, `task`, `email`, `manual`}, explicit caller-passed. ⊥ heuristic inference from arg presence. Span ! also carry `email_id` attr when invocation triggered by an inbound email (`trigger='email'`, ∨ `trigger='task'` w/ `task.email_id IS NOT NULL`). Why: conflated trigger labels mask operator-initiated retries as task drains, breaking Logfire regression detection; `enrollment_schedule` distinguishes CLI-scheduled first-touch (drained from task queue per §V.55) from operator-immediate `enrollment_run` (synchronous CLI) — agent behaviour identical, observability separates origin. `email_id` ≡ load-bearing dimension for per-message cost/latency analysis ∧ smoke-test concurrent-send regression gates (§B.37 — 2026-05-28 B7.5 gate queried `email_id` to disambiguate B75a vs B75b, found absent ∴ gate emitted false-zero rows).
V12: Routing pipeline: `thread_match` (Gmail thread_id) → RFC `In-Reply-To` / `References` match → LLM `_try_classify(candidates=active inbound workflows for account)` → unrouted.
V13: Inbound task creation filter (`create_tasks_for_routed_emails` in `database.py`): `direction='inbound' AND workflow_id IS NOT NULL AND contact_id IS NOT NULL AND e.created_at >= w.created_at AND NOT EXISTS (task WHERE email_id=e.id)`. Why: outbound mailbox seeing own send ⊥ create task; History re-delivery idempotent; ⊥ contact → ⊥ enrollment ∴ ⊥ task; `e.created_at` = DB insert time (⊥ Gmail `received_at`) excludes pre-workflow inbox traffic — emails Gmail-received before workflow exists but synced after ! ⊥ trigger retroactive auto-tasks.
V14: Drive list query: `mimeType='text/markdown' AND parents in '<folder_id>' AND trashed = false`. Flags ! `corpora="allDrives"` & `supportsAllDrives=True` & `includeItemsFromAllDrives=True`. `read_drive_markdown` ! `supportsAllDrives=True` on `files.get` & `files.get_media`. Why: KB folder ? lives in Shared Drive; default corpora excludes SD children → silent empty for impersonated SD member.
V15: ∀ agent tool failure → return `{"error": CODE, "message": TEXT}` dict. ⊥ raise to agent.
V16: ∀ agent run → ≥1 tool call. ∀ system-prompt fragment naming example tool sequence → ≥2 tool names (e.g. `reply_email` & `noop`). Why: single-tool example collapses to literal — agent emits exactly one tool named ∴ ⊥ generalises. Decline prose lives in shared `_DECLINE` fragment composed into every template's protocol per §V.33 — one edit point; same rule binds future fragments.
V17: Folder access = Drive permission of impersonated user. Folder ID ∉ secrets, ∉ access grants. Multi-tenant isolation delegated to Drive permission model.
V18: Race-safe email insert: `INSERT ... ON CONFLICT (...) DO NOTHING RETURNING *` → `Model | None`. `None` = concurrent worker won. History re-delivery idempotent.
V19: ∀ new logfire.exception site reachable from `mailpilot run` → paired `operator_event("error", source=<event_name>, message=str(exc))`. Why: keeps operator stderr stream complete under journald.
V20: ∀ new agent tool → unit tests cover {hit, no-hit, error}. ∀ new CLI cmd → tests patch `get_*` FK validators.
V21: `make check` ! green (ruff & basedpyright strict & pytest).
V22: Logfire spans split by `deployment_environment` ∈ {`development`, `production`} from `logfire_environment` setting. Project = `mailpilot` (token-scoped). MCP queries ! `WHERE deployment_environment = '<env>'`.
V23: `activity` row → ≥1 of {`contact_id`, `company_id`} set; both allowed. Enforced via schema CHECK on `activity` in `schema.sql`. Why: activity may attach to contact, company, or both (e.g., email sent to contact at a known company).
V24: Drive search query (`DriveClient.search_markdown`): `mimeType='text/markdown' AND '<folder_id>' in parents AND fullText contains '<query>' AND trashed = false`. Same Shared-Drive flags as §V.14 (`corpora="allDrives"`, `supportsAllDrives=True`, `includeItemsFromAllDrives=True`). Returns `[{"file_id", "name"}]` ranked by Drive native relevance. ⊥ snippet (Drive API ⊥ provide), ⊥ embeddings, ⊥ semantic match, ⊥ cross-folder. Why: KB grounding scales past full-folder enumeration; scope locked to workflow KB folder per §V.17.
V25: smoke-test out-scope QA verifier (`.claude/skills/smoke-test/scripts/qa.py:check`) ! exclude tokens present in original question before fabrication regex match. Why: agent decline that echoes "your question about X 50,000 ppm" trips verifier despite ⊥ fabrication. `details.declined: true` authoritative when ⊥ fabrication block detected outside echoed question.
V26: ∀ `running tool` span → structured `gen_ai.tool.name` attribute set (OTel GenAI semantic convention, populated by `logfire.instrument_pydantic_ai()` in `src/mailpilot/cli.py`). Logfire dashboards & alerts ! query `attributes->>'gen_ai.tool.name'`; ⊥ parse free-text `message` ("running tool: <name>"). Why: convention-aligned attribute already exists upstream; inventing sibling `tool_name` duplicates data & drifts from OTel.
V27: ∀ email → ≤1 `routing.route_email` span lifecycle per `email_id`. History-API re-delivery & repeat sync sweep ⊥ trigger second route pass. Why: duplicate route spans inflate metrics & mask classifier regressions (e.g., A4 single delivery → 2 `skipped_no_workflows` spans observed).
V28: ∀ KB-grounded reply ∧ `search_drive_markdown` ≥2 hits → agent ! `read_drive_markdown` on top ≥3 (∨ all hits if <3) before composing reply. Why: model-number queries (e.g., `WS36-600-2`) match both general & model-specific docs; first-hit grounding picks wrong doc → wrong specs. Rule lives in `_DRIVE_GROUNDING` fragment bound only to `inbound-google-drive` template per §V.33; `outbound-general` & `inbound-general` templates physically cannot trigger this path. Out-of-scope decline path (§V.16) unaffected.
V29: ∀ `reply_email` ∨ `send_email` invocation w/ body containing key-value cluster (≥3 consecutive lines each matching `^[^|]{1,80}\s+\S` — short line ≤80 chars, label + whitespace + value, ⊥ pipe on line; whitespace count ≥1 regardless of size) ∧ ⊥ `|---|` (∨ `| --- |`) pipe-table separator anywhere in body → tool ! return `{"error": "format", "message": "spec body MUST use Markdown pipe-table w/ |---| header separator"}` → agent retries per §V.15. Heuristic ! match regardless of value shape (numeric, text, units, ranges); ASCII rule-line runs (`-{6,}`, `={6,}`, `_{6,}` on standalone line) ⊥ count as pipe-table separator. Why: §B.5, §B.9, §B.14, §B.36 = same prompt-fidelity drift recurring after prompt-only fix (§T.7), numeric-only lint (§T.9), ∧ 2-space lint (§T.31); §B.14 KDF rows non-numeric (`Mesh Size  20×50`) ∴ pre-T.31 regex missed them; §B.36 single-space `**label** value` (smoke-test 2026-05-28 B4 ∧ B7) slipped pre-T.62 `\s{2,}` regex ∴ widened to `\s+`. Line-length bound `{1,80}` preserves prose immunity (multi-clause prose lines typically >80 chars). ASCII rule-lines = workaround surface. Decline path (§V.16) unaffected — decline bodies ⊥ contain spec rows.
V30: SUPERSEDED by §V.32 / §V.33 — Drive tool gating now structural via template registry (`_DRIVE` tuple bound only to `inbound-google-drive`); ⊥ regex sniff. Original wording retained for §B.10 trace: "Drive tools ! conditionally registered in agent toolset — included only when `workflow.instructions` contains Drive folder ID pattern (`\b[A-Za-z0-9_-]{25,}\b`). Structural exclusion = single source of truth; LLM cannot call a tool that isn't bound."
V31: smoke-test in-scope KB grounding gate (Scenario B, gate B4) ! operator-judged. Load source doc via `qa.py source --id ID` (DriveClient impersonating `inbound@lab5.ca` against `MailPilot Demo` folder) → operator emits structured JSON verdict {`qa_id`, `question`, `source_file`, `source_file_alts: list[str]` (default `[]`), `answers_question`, `every_factual_claim_supported_by_source`, `cites_source_file`, `unsupported_claims: [...]`, `verdict`}. `verdict='pass'` ↔ all 3 booleans true & `unsupported_claims=[]`. `cites_source_file=true` ↔ agent's citation ∈ {`source_file`} ∪ `source_file_alts` (set union — admits cross-source identifier collisions e.g. model `WS36-600-2` ∈ both `pure-aqua-industrial-water-softener.md` ∧ `pure-aqua-sf-100s-industrial-water-softener.md` w/ divergent specs per §B.40). ⊥ token-substring match against curated `expected_tokens`. ⊥ free-form rating — `unsupported_claims` ! name each unsupported claim verbatim (structural defence against LLM-judge sycophancy). Why: substring match false-negatives on phrasing variation (`0.48 mm` vs `0.48mm`); §V.25 digit-echo exclusion = 2nd regex band-aid for same brittleness class ∴ regex approach at limit; per-Q token curation = unbounded maintenance debt as KB grows. Out-scope decline (B6) keeps script verifier — regex appropriate for shape detection (vendor-near-digits fabrication, decline-phrase presence). `source_file_alts` admits inherent KB shape (cross-source identifier collisions ⊥ author-error) ∴ ⊥ regrade by hand each run.
V32: `workflow.template` ∈ {`outbound-general`, `inbound-general`, `inbound-google-drive`}. NOT NULL & schema CHECK matching `WorkflowTemplateName` Literal (`src/mailpilot/agent/templates.py`). `workflow.type` populated server-side from `TEMPLATES[template].direction` at `create_workflow`; ⊥ caller-passed `type` mismatch. `update_workflow` ! reject `template` change. Why: template change = tool-list change = destabilizing; force delete & recreate. Existing reads of `wf.type` in `routing.py`, `sync.py`, `cli.py`, `schema.sql` unchanged.
V33: Agent shape owned by template, ⊥ workflow. ∀ Agent instantiation → `Agent(name=..., deps_type=..., tools=list(TEMPLATES[wf.template].tools), instructions=TEMPLATES[wf.template].protocol + wf.instructions)`. ⊥ per-workflow tool override, ⊥ per-workflow protocol override. New tool surface (SQL, Confluence, …) → new template constant; ⊥ bolt onto workflow. Protocol = ordered concat of named fragments: `_BASE → _DEFERRED_TASK → [overlay]? → _DECLINE → _NO_FABRICATION`; `wf.instructions` appended last by the instantiation rule above. Template-specific overlays (e.g. `_DRIVE_GROUNDING` ∈ `inbound-google-drive`) slot between `_DEFERRED_TASK` & `_DECLINE` — guardrails (`_DECLINE` + `_NO_FABRICATION`) ! remain final two framing fragments so they bind regardless of overlay. Each rule lives in exactly one fragment (§V.16 → `_DECLINE`; §V.28 → `_DRIVE_GROUNDING`). Why: single edit point for invariants; structural tool gate; auditable via `WHERE template = ...`; canonical order pinned ⊥ drift between templates ∧ ⊥ accidental overlay-shadows-guardrail reorder.
V34: ∀ template name = `<direction>-<data-system>` (direction ∈ {`outbound`, `inbound`}; data-system = concrete binding: `general`, `google-drive`, future `postgres`, `confluence`). Naming by binding ⊥ abstract concept. Why: tools (`_DRIVE` tuple) & protocol fragment (`_DRIVE_GROUNDING`, mentions `search_drive_markdown` explicitly) = Google-Drive-specific; abstract names (`inbound-knowledge-base`) hide binding & create ambiguity when parallel data-source templates land.
V35: ∀ inbound `agent.invoke` prompt → trigger email body inlined at most once. `_build_user_prompt` (`src/mailpilot/agent/invoke.py`) ! exclude `email.id` from `email_history` before passing to `_format_email_history`. Fresh-thread invocation w/ trigger as only contact email → render `No prior email history with this contact.`; ⊥ `Email history (1 messages):` w/ trigger as sole entry. Why: `email_history` query (`database.list_emails(contact_id, account_id, workflow_id)`) returns trigger row, `_format_trigger` re-inlines same body under `New inbound email:` → body printed twice. `_BASE` system-prompt fragment ("When a trigger email is included in your prompt, its full body is already provided -- do not call read_email to fetch it again") already assumes single inclusion.
V36: ∀ `agent.invoke` → user-prompt trigger block ! match span's `trigger` attribute (§V.11). `_format_trigger` (`src/mailpilot/agent/invoke.py`) ! receive explicit `trigger` arg & branch: `email` (∃ email) → `New inbound email:` block; `task` (∃ persisted task row) → `Deferred task:` block w/ description & context; `enrollment_run` ∨ `enrollment_schedule` → first-reach-out framing (e.g. `First reach-out for this enrollment. Compose the initial outbound message per workflow objective & instructions.`) — identical text for both since both signify "first outbound message; ⊥ prior context"; `manual` ∨ outbound fallback → existing `This is an outbound invocation. ...` text. ⊥ `Deferred task:` block when `trigger ≠ task`. Why: §V.11 enforces span-attribute correctness, but prompt framing carries the actual signal the model reads — `enrollment_run` initial reach-out framed as resumed deferred work risks resumption-shaped reasoning (look for prior context that ⊥ exists, call `list_enrollments` to "see where it left off"). Synthesising `task_description="manual <type> run"` at the caller is a code smell hiding the real `trigger` from the prompt builder.
V37: ∀ Anthropic LLM call (workflow agent in `src/mailpilot/agent/invoke.py` ∧ classifier in `src/mailpilot/agent/classify.py`) → request ! set Anthropic `cache_control` breakpoint on stable prefix (system prompt + tool definitions) so multi-turn invocations re-bill prefix as `cache_read_input_tokens` ⊥ as fresh input. `agent.invoke` rollup span attrs ! include `cache_read_input_tokens` ∧ `cache_creation_input_tokens` summed across child `chat` spans. Why: stable ~1500-token prefix repeats verbatim every turn; uncached re-billing inflates inbound multi-turn input tokens ~2x (smoke-run 2026-05-05 B4: 6 chat calls × ~1500-token prefix = ~9000 redundant tokens); rollup-level attrs avoid per-turn-span join for cost reporting & enable single-row cache-hit-rate alerting.
V38: `/release` project-local slash cmd extends `/gh:release` semantics. After local SemVer tag (`/gh:release` Procedure §1–§12), `/release` ! (a) `git push origin main && git push origin v<x.y.z>`, (b) `uv build` → assert `dist/mailpilot-<x.y.z>-py3-none-any.whl` ∃, (c) `gh release create v<x.y.z> --verify-tag --notes-from-tag dist/mailpilot-<x.y.z>-py3-none-any.whl`. Confirm-before-mutate gate (per `/gh:release` §9) covers push ∧ asset upload. Manifest = `pyproject.toml [project].version`; build backend = `uv_build` ∴ wheel name pattern fixed. Why: deploy = install of published `.whl` asset reachable via `gh release download`; tag-only release ⊥ deployable. `/gh:release` plugin cmd unchanged — covers tag-only repos.
V39: `workflow export` / `workflow import` = declarative round-trip on (`account_id`, `name`). Export → `{"workflows": [{name, template, objective, instructions, theme}, ...], "ok": true}` per `--account-id`. Import → JSON from `--file` ∨ stdin; ∀ row: absent → `create_workflow` (activation = side-effect of objective ∧ instructions both non-empty per existing create-path rule); present → `update_workflow` for changed fields ⊥ `template`; `template` differs → per-row `{"error": "template_immutable"}` per §V.32, batch continues. Import ! ⊥ write `workflow.status`. Round-trip on unchanged DB ≡ ⊥ row mutations 2nd run (idempotent upsert). Why: `make clean` wipes workflow rows; declarative replay = source-of-truth ⊥ operational state ∴ status ∉ payload; template change = §V.32 destabilising → row-error, ⊥ batch abort.
V40: smoke-test final report (`.claude/skills/smoke-test/SKILL.md` Phase 5) ≡ 3 sections, actionable-only: §1 **Execution** — phase-results matrix (current Part A) + ≤3-bullet Logfire signal summary (top span by volume, error/warn count, cache-hit ratio on multi-turn invokes); one screen max. §2 **Bugs** — observable failures, each = single paragraph + `Spec action:` line per existing severity matrix (`bug` ∨ `bug+invariant` ∨ `code-only`); Critical/High w/ `bug`∨`bug+invariant` auto-invoke `/sdd:spec`; Medium/Low print-only. §3 **Invariants** — under-specified contracts surfaced by inspection (⊥ concrete failure this run), each = single paragraph + `Spec action: propose-invariant` invocation; print-only, operator pastes. Spec hand-off block unchanged. Auto-write `./smoke-test-<YYYY-MM-DD>-<HHMMSS>.md` unchanged. ⊥ Notes section, ⊥ `none`-routed Suggestions, ⊥ `propose-task` Suggestions, ⊥ Part B SQL transcript ∈ output, ⊥ Part C 13-cat narrative ∈ output. Logfire SQL queries ∧ 13-cat scan ≡ derivation aids ∈ procedure body, ⊥ rendered as report sections. Why: prior 5-part report inflated output ~3× w/ FYI prose ⊥ acted on; operator's actual need = queue of `/sdd:spec` inputs ∴ report ≡ queue, ⊥ narrative.
V41: `mailpilot --skill` → stdout = packaged `src/mailpilot/SKILL.md` body verbatim, exit 0, ⊥ JSON envelope (matches `--version` / `--help` shape; §V.4 binds noun-verb cmds only). Source resolved via `importlib.resources.files("mailpilot").joinpath("SKILL.md")` ∴ survives wheel install. `pyproject.toml` ! include `SKILL.md` as package data (uv-build equivalent of `force-include`). Skill content audience = external LLM agent w/ `mailpilot` installed as CLI dep; scope = (a) noun-verb grammar + envelope shape per §V.5 + exit codes, (b) common task recipes (enrollment, send, route query, export/import), (c) settings keys per §I.config + `mailpilot config set` usage. ⊥ contain DB schema, ⊥ Pydantic AI / template internals (§V.32-§V.34 = package-internal). Test asserts `mailpilot --skill` stdout byte-equal to packaged file ∧ `importlib.resources` path resolves post-install. Why: external agents discover capability via `--help` (single vector); FS-path discovery brittle across install methods (pip vs pipx vs uv tool vs vendored).
V42: ∀ Google API client construction (Gmail, Drive, Pub/Sub) → credential source resolved via fallback chain `google_application_credentials` setting → `GOOGLE_APPLICATION_CREDENTIALS` env var → Application Default Credentials (ADC). DWD impersonation: file creds → `creds.with_subject(email)`; ADC creds → `service_account.Credentials(signer=iam.Signer(Request(), source_creds, sa_email), service_account_email=sa_email, subject=email, scopes=..., token_uri="https://oauth2.googleapis.com/token")` via IAM Credentials API (signs JWTs remotely; no local private key). New Google client modules ! route through `gmail.build_delegated_credentials(scopes, subject)` (impersonated) ∨ `gmail.build_default_credentials(scopes)` (non-impersonated). `mailpilot.sync` Pub/Sub & watch-renewal gates ! use `gmail.has_google_credentials()` (file ∨ ADC reachable), ⊥ raw `settings.google_application_credentials` truthiness. Project-ID resolution → `gmail.resolve_project_id()` (JSON `project_id` field ∨ ADC default project). Required IAM in ADC mode: instance SA holds `roles/iam.serviceAccountTokenCreator` on itself; IAM Credentials API enabled. Why: pre-fix code raised SystemExit when both setting & env empty → ADC blocked entirely → GCE deploy of `mailpilot-1.lab5-mailpilot-prd1` crash-looped 46+ iters until §B.15 fix; centralised helpers ≡ single audit point so a new client (Calendar, Sheets, …) cannot re-introduce the gap.
V43: `AnthropicModel` constructor in `src/mailpilot/agent/invoke.py` (`_build_agent`) ∧ `src/mailpilot/agent/classify.py` (`_get_model`) ! pass an `httpx.Client(timeout=httpx.Timeout(240.0))` (∨ Pydantic-AI-equiv `http_client` / `model_settings`) — explicit 240s read-timeout, 4× default 60s. ⊥ retry on `httpx.ReadTimeout` ∨ `anthropic.APITimeoutError`: a tool call mid-turn (e.g. `send_email`, `reply_email`, Drive writes) cannot be safely re-driven — idempotency ⊥ guaranteed across SMTP / Drive side-effects. ∴ timeout → bubble to `run.task.agent_failed` per §V.19, operator replay. Why: 2 prod TimeoutError events (§B.16) hit at exactly the default 60s read window during long-context multi-turn invocations (post-`read_drive_markdown` of 13KB doc → next chat completion stalled); 60s ⊥ sufficient under model load, 180s borderline (events near deploy window) ∴ 240s = headroom. Test: httpx-mock asserts client constructed w/ 240s read-timeout on `AnthropicModel(...)` in both modules; assert ⊥ retry wrapper around chat call (bare except @ `run.py:127` is the intended terminal handler).
V44: Task retry classification ≡ exception-class + status-code allow-list ∈ `src/mailpilot/agent/retry.py`. Allow-list: Drive/Gmail `googleapiclient.errors.HttpError` w/ status ∈ {500, 502, 503, 504, 429}; Anthropic `APIStatusError` w/ status ∈ {502, 503, 529}; `socket.timeout`/`TimeoutError` raised inside Drive tool spans. ∉ allow-list: `anthropic.APITimeoutError`, `httpx.ReadTimeout` — §V.43 exclusion preserved (mid-turn timeout ⊥ safely re-driven across SMTP/Drive side-effects). Max attempts = 4 (1 initial + 3 retries). Backoff schedule = [30s, 120s, 300s] + jitter, hardcoded (⊥ `Settings` field — YAGNI). Retry path: `attempt_count += 1`, `scheduled_at = now() + backoff`, `status` stays `pending`, `result` updated w/ last-failure summary; budget exhausted → `complete_task(status="failed", result={"reason": ..., "attempt_count": N, "terminal": "max_attempts"})`. `task.attempt_count INTEGER NOT NULL DEFAULT 0` added to schema; `task_pending_trigger` extended `AFTER INSERT OR UPDATE OF status, scheduled_at` (drain loop wakes on retry reschedule; terminal-status wakes accepted as benign noise). `Task` ∧ `TaskSummary` models gain `attempt_count: int`. `mailpilot task retry --task-id <ID>` manual cmd allowed on `failed`/`cancelled` rows only — refuses `completed` (tools already fired → retry = duplicate side-effects) ∧ `pending` (already queued, no-op); resets `status='pending'`, `attempt_count=0`, `scheduled_at=now()`, fires `pg_notify`. Drive socket-timeout cap via `httplib2.Http(timeout=...)` on `build_drive_service` bounds 60s Drive-side stall window so classifier sees `socket.timeout` quickly. ∀ retry path → paired `operator_event("task.retry", task_id, attempt, exc=type(exc).__name__)` per §V.19 pattern. Why: §B.16 — failed task row blocks any re-processing of same email forever (`create_tasks_for_routed_emails` ∧ `get_unprocessed_inbound_email` both filter `NOT EXISTS (task WHERE email_id=e.id)`); transient API blip ≠ terminal failure; only Anthropic *server-side status codes* (502/503/529) retry since those are returned before any tool call fires ∴ §V.43 idempotency rule honored, ⊥ amendment needed. `socket.timeout`/`TimeoutError` allow-list entry assumes real Drive-side stall (transient, independent across retry attempts); local concurrency race (§B.34) violates assumption — all 4 retries hung deterministically; §V.61 prevents recurrence @ registration ∴ allow-list assumption holds post-§V.61.
V45: `.claude/skills/demo-test/SKILL.md` ≡ liveness probe of public demo address `hello@lab5.ca` against deployed production instance. Source acct = `outbound@lab5.ca` (local CLI sender); target = `hello@lab5.ca` (processed by prod deploy, ⊥ local run loop). Setup: ⊥ `make clean`, ⊥ account create, ⊥ workflow import — assume warm state; pre-flight verifies `mailpilot account list` ∋ `outbound@lab5.ca`; pre-flight fails → exit `FAIL` w/ missing-prereq line ⊥ send. Scope: single in-scope question via `qa.py pick --type inscope` (reuse `.claude/skills/smoke-test/scripts/qa.py` verbatim, ⊥ new scripts); ⊥ out-of-scope arm; ⊥ Scenario A outbound flow. Subject = `[DEMO-<HHMMSS>] <topic>` (distinct prefix from `[ST-...]` per smoke-test) — fresh-randomised per run. Send via `mailpilot email send --account-id <OUTBOUND_ACCOUNT_ID> --to hello@lab5.ca --subject ... --body <question>`; capture `TEST_START` ISO before send. Gates (ALL ! pass for `PASS`): G1 reply round-trip — query Gmail directly (impersonate `outbound@lab5.ca` per §V.42; `GmailClient.list_messages(query="from:hello@lab5.ca after:<TEST_START_EPOCH>", label_ids=["INBOX"])` invoked via inline `uv run python -c '...'` ∨ thin CLI verb wrapping same — ⊥ `mailpilot email list` per §V.46) ≤12 attempts × 5s; match unique `<topic>` tail (Gmail prepends `Re: ` ∴ substring); reply present within 60s. G2 groundedness — operator-judged per §V.31, `qa.py source --id ID` loads source doc; structured JSON verdict ({`answers_question`, `every_factual_claim_supported_by_source`, `cites_source_file`, `unsupported_claims`, `verdict`}); `verdict='pass'` required. G3 Logfire production env — window `[TEST_START, now]`, filter `WHERE deployment_environment = 'production'` per §V.22; required spans present {≥1 `agent.invoke` `trigger='task'` per §V.11, ≥1 `running tool` `attributes->>'gen_ai.tool.name' = 'search_drive_markdown'` per §V.26, ≥1 `gmail.send_message`}; zero spans w/ `is_exception = true` ∨ `level ∈ {'error','warn'}` in window. Output: stdout = single line `PASS` ∨ `FAIL: <one-line reason>` + 3-bullet Logfire summary (top span by volume, error/warn count, `cache_read_input_tokens` ratio per §V.37). ⊥ phase matrix, ⊥ §1/§2/§3 sections per §V.40, ⊥ auto-write `.md` to disk, ⊥ auto-invoke `/sdd:spec`. Why: `/smoke-test` ≡ destructive 2-scenario regression suite (`make clean`, ~10min) ∴ unfit for routine "is public demo alive?" probe; `/demo-test` ≡ binary liveness check against published `lab5.ca/mailpilot/` promise running on prod deploy, exits <2min, fit for post-deploy spot-check.
V46: liveness probes (skills under `.claude/skills/<name>/SKILL.md` whose explicit purpose ≡ "is deployed system alive?" — e.g. `/demo-test` per §V.45) ! query authoritative production-facing surface directly (`GmailClient`, `DriveClient`, Logfire MCP) — ⊥ via local-mailpilot-DB reads (`mailpilot email list`, `mailpilot contact list`, etc.) that depend on a separately-started `mailpilot run` ∨ explicit sync step to be fresh. Implementation: probe SKILL.md procedure ! invoke source-of-truth API via inline `uv run python -c '...'` against `mailpilot.gmail` / `mailpilot.drive` ∨ thin `mailpilot` CLI verb wrapping same. Out-scope: full regression suites (`/smoke-test`) that own their own sync lifecycle ∧ ⊥ probes. Why: probe nominally tests deployed system; local DB only mirrors Gmail/Drive after sync; probe procedure forbids local loop (§V.45 setup) ∧ ⊥ trigger sync → local-DB poll = checking the wrong surface → false-FAIL on a healthy production (§B.23, 2026-05-11 `/demo-test` run: G1 polled 60s ∧ found nothing while production had replied <5s after send, surfaced post-hoc by manual `mailpilot account sync`).
V47: ∀ operator-initiated CRM config mutation via CLI = noun ∈ {account, company, contact, workflow, enrollment, tag, note} ∧ verb ∈ {create, update, disable, add, remove, start, stop, cancel, import} → CLI handler ! wrap mutation in `logfire.span("<noun>.<verb>", entity_id?, account_id?, ...attrs)`; on success → `operator_event("<noun>.<verb>", entity_id=..., account_id=..., changed=[<field>...])`; on raise → `logfire.exception(...)` + paired `operator_event("error", source="<noun>.<verb>", message=str(exc))` (same pairing shape as §V.19, extended to CLI surface — ⊥ only `mailpilot run`). Span placement = CLI handler layer (`src/mailpilot/cli.py`) ∴ self-discriminates operator from (a) sync-driven auto-creation (e.g. `create_or_get_contact_by_email` during inbound sync — DB fn, ⊥ CLI handler), (b) agent-tool mutations already covered by §V.26 `gen_ai.tool.name`. `changed` semantics: `create` → field set populated at insert; `update`/`disable` → field names whose values actually mutated (pre-vs-post row diff via paired `get_<noun>` reads inside the span); `add`/`remove` → relational key set; `start`/`stop`/`cancel` → terminal status name; `import` emits per-row events ∴ idempotent re-run ≡ empty `changed` lists per row (verifiable audit per §V.39). Test contract: per §V.20 `capfire: CaptureLogfire` — (a) span name + attrs on success, (b) `operator_event` emission w/ correct `changed`, (c) `logfire.exception` + paired error event on raise. Why: CRM config mutations = operator-curated identity state w/ zero observability today; CLI-layer placement = natural seam separating operator from sync ∧ agent paths; one uniform invariant binds the surface ∴ future nouns / verbs inherit obligation by convention ⊥ per-entity invariants.
V48: ∀ §-cite in repo prose (code comments, docstrings, SPEC.md body, CLAUDE.md, `.claude/skills/**/*.md`, `tests/`) ! dotted form `§[VTB]\.[0-9]+`. Dotless `§V<n>` / `§T<n>` / `§B<n>` ⊥ allowed. Bare-form `V<n>` / `T<n>` / `B<n>` (no `§` prefix) as spec citation in prose ⊥ allowed even though bare-form ∉ auditor grep scope (collision risk per `V32` as CSS class ∨ version label) — prose-intent = §-cite ∴ ! carry `§` prefix. Why: `/sdd:check` cite-resolution regex grep ≡ `§[VTB]\.[0-9]+` (explicit dot ∧ § prefix) ∴ dotless ∨ bare evades auditor; §B.22 (skills) + §B.24 (code-dotless) + §B.26 (tests) + §B.30 (code-bare) ≡ same sub-class (cite-shape malformation) recurring 4×. Bare-form ∧ wrong-section prose drift (§B.22 sub-class a) ⊥ regex-catchable — operator review only.
V49: outbound-template agent (`outbound-general`) ⊥ call `record_enrollment_outcome` inside `agent.invoke` w/ `trigger ∈ {enrollment_run, enrollment_schedule, manual}` — tool reserved for `trigger=task` invocations on an enrollment w/ prior outbound email already in thread. `enrollment_schedule` joins the no-record-outcome set because it shares first-touch semantics w/ `enrollment_run` per §V.36 (drained from task queue but agent context ≡ "first outbound message"). Structural gate options (impl-side): (a) omit `record_enrollment_outcome` from outbound agent's bound tool list when `trigger ≠ task`, ∨ (b) `_DEFERRED_TASK` protocol fragment branches on `trigger` per §V.36 — `trigger=enrollment_run` → fragment says "Send the initial email & stop; do not call record_enrollment_outcome; outcome will be assessed when a reply arrives". Why: §B.25 — `_DEFERRED_TASK` text "After completing the workflow objective for a contact, call record_enrollment_outcome" (`src/mailpilot/agent/templates.py:69-74`) treats first-reach-out send as objective-completion → premature `enrollment_completed` activity row; arrival of decline-reply later → second terminal `enrollment_failed` row for same enrollment (two terminal activities per single enrollment). Per-fixture `workflow.instructions` appended after `_DEFERRED_TASK` per §V.33 ordering ⊥ reliably override template-fragment instruction in model reasoning ∴ gate ! be structural ∨ trigger-aware at protocol layer, ⊥ at fixture layer.
V50: Logfire content scrubbing ⊥ redact `tool_response` field on Pydantic-AI `running tool` spans (covers ∀ agent tool outputs — currently `read_drive_markdown` content the only sizeable body, rule binds future tools w/ structured `tool_response` payload uniformly). Impl: `logfire.configure(..., scrubbing=ScrubbingOptions(callback=<f>))` ∈ `cli.configure_logging`; `<f>(match: ScrubMatch)` returns `match.value` when `match.path[:2] == ("attributes", "tool_response")`; else returns `None` (delegate to default scrubber). Why: 2026-05-12 smoke-run B4/B6 — default substring matcher matched `"auth"` inside KB markdown (likely from `"authorized"` in `watts-smartstream-uv-disinfection.md`) → `tool_response.content` replaced w/ `"[Scrubbed due to 'auth']"` → trace structurally ⊥ show what KB content the agent grounded against ∴ §V.31 grounding regressions ⊥ verifiable from traces alone. KB markdown ≡ non-sensitive product documentation; agent tool outputs ⊥ carry secrets by design (no credentials, tokens, user PII flow through tool returns). Test: `capfire: CaptureLogfire` per §V.20 asserts `running tool` span whose `attributes.tool_response.content` contains substring `"authorized"` retains literal content (⊥ placeholder) post-scrub.
V51: ∀ `<Entity>Summary` (list-view projection) ! ∋ (a) FK columns the row's `<Entity>` schema declares as FK to other entity tables (e.g., `activity.workflow_id`, `activity.email_id`, `activity.task_id`), ∧ (b) external-system join keys the entity carries to participate in pipelines outside the DB (e.g., `email.gmail_thread_id` used by routing pipeline thread_match per §V.12). Why: FK columns ∧ external-system join keys ≡ canonical join surface for downstream reporting; omitting them from list responses forces consumers to parse `detail` JSONB display labels (`detail.workflow_name` ≡ display label, ⊥ FK), fall back to raw SQL, ∨ per-row round-trip via `view`. External-system keys ≡ same recurrence class as DB FKs: §B.39 — smoke-test 2026-05-28 A4 needed `gmail_thread_id` to confirm threading, found absent from `EmailSummary`, forced round-trip via `email view` per row. §V.6 enumerates allowed Summary fields w/o explicitly binding FK-inclusion — V51 binds it. `TaskSummary` (`src/mailpilot/models.py:273-283`) already complies; `ActivitySummary` precedent (§B.27, fixed §T.46); `EmailSummary` (`src/mailpilot/models.py:225-238`) ≡ sole external-key violator @ amend time per §B.39.
V52: ∀ `agent.invoke` w/ `input_tokens ≥ 1024` (Anthropic prompt-cache eligibility floor) → rollup span ! carry `cache_creation_input_tokens` ∧ `cache_read_input_tokens` attrs (value ≥ 0 admitted; presence asserts §V.37 plumbing intact). Strict `> 0` first-invoke outcome ⊥ unit-testable w/o live Anthropic endpoint ∴ behaviour-binding deferred to smoke-test operator inspection per §V.40 Phase 5 §1 Logfire summary. Why: prior wording bound `> 0` on first invoke per `(workflow_id, trigger)`; 2026-05-12 smoke A3 (`trigger=enrollment_run`, outbound, 4483 input_tokens) → `cache_creation_input_tokens=0`; A7 next outbound → `cache_creation_input_tokens=2282`; inbound B4 ∧ B6 wrote caches normally ∴ Anthropic-side ⊥ at fault, breakpoint silently absent on outbound first-invoke path (§B.28). /sdd:check 2026-05-27 flagged strict form UNVERIFIABLE — unit tests ⊥ assert `> 0` w/o live LLM call; attr-presence form aligns audit infra (`tests/test_agent_invoke.py` capfire spans) w/ invariant. §B.28 first-invoke skip remains observable via smoke-test Logfire summary; automated recurrence guard moves from unit-test layer to operator inspection at smoke time.
V53: CLI `contact view` ∧ `company view` envelope inner-dict ≡ agent tool `read_contact` ∧ `read_company` return (byte-identical modulo §V.5 wrap). Single helper `load_contact_view(conn, id) → ContactView` ∧ `load_company_view(conn, id) → CompanyView` renders both surfaces. `ContactView` ≡ `Contact` + `notes: list[Note]` (cap N=10, ORDER BY `created_at` DESC) + `notes_total: int` + `company_notes: list[Note]` (parent company's notes when `contact.company_id IS NOT NULL`, else `[]`) + `company_notes_total: int`. `CompanyView` ≡ `Company` + `notes` + `notes_total` (⊥ `company_notes` — root entity). Full `Note.body` verbatim, ⊥ preview. `ContactView` ∧ `CompanyView` ≡ view-only projections (separate from DB-row models, mirroring `<Entity>Summary` precedent). Cap = hardcoded `_INLINE_NOTES_CAP = 10` in `database.py`. ⊥ `Settings` field — YAGNI. `_BASE` protocol fragment (`src/mailpilot/agent/templates.py`, §V.33) gains one-liner: `"If read_contact or read_company returns notes or company_notes, treat them as context for personalizing your response. Never invent facts about a contact or company that aren't supported by their notes."` Why: single rendering path = single test contract; operator ∧ agent see identical context ∴ personalization observable from CLI without invoking agent; §V.6 Summary rule unaffected (binds list/search only, view exempt); same fabrication-class guard §V.16 / §V.31 / §V.50 already enforce for KB, extended to CRM facts.
V54: ∀ CLI cmd reading from stdin (fallback path when `--file` omitted) → ! `sys.stdin.isatty()` guard BEFORE `sys.stdin.read()`; `isatty() == True` → `output_error("no input: provide --file PATH or pipe JSON via stdin", "validation_error")` (SystemExit per §V.4 error envelope) ⊥ proceed to `read()`. Sites @ amend time: `src/mailpilot/cli.py:632` (`company import`), `:920` (`contact import`), `:2249` (`workflow import`). Why: `sys.stdin.read()` on a TTY blocks indefinitely waiting for EOF → indistinguishable from a frozen process (⊥ prompt, ⊥ error, ⊥ recovery signal); operator runs `mailpilot <noun> import` interactively expecting a `--file`-or-error response, gets a silent hang instead (§B.29). Future stdin-fallback cmds inherit obligation by convention — invariant binds the surface uniformly, ⊥ per-cmd guard duplication.
V55: `mailpilot enrollment add` accepts optional `--scheduled-at <ISO>` flag. Present → CLI inserts a `task` row w/ that `scheduled_at`, `description="scheduled first reach-out"`, `context={"trigger": "enrollment_schedule"}`, `email_id` NULL after `create_enrollment`; existing drain (`execute_task` ∈ `src/mailpilot/run.py`) picks up when `scheduled_at <= now()` per §V.3 ∧ invokes the workflow agent w/ `trigger="enrollment_schedule"` per §V.11 (read from `task.context.trigger`, default `"task"`). Absent → behavior unchanged (enrollment row only, no task; manual `mailpilot enrollment run` required). Restricted to outbound workflows: inbound workflow + `--scheduled-at` → `output_error("--scheduled-at only valid for outbound workflows", "invalid_state")` per §V.5 (inbound is reactive ∴ scheduled first-touch nonsensical). Idempotency: re-running w/ `--scheduled-at` on enrollment that already has a pending first-touch task (filter: `task.email_id IS NULL ∧ task.workflow_id = WF ∧ task.contact_id = CT ∧ task.status = 'pending'`) returns existing enrollment, ⊥ duplicate task insert. CLI envelope unchanged (`{"enrollment": {...}, "ok": true}` per §V.5); side-effect task surfaces in `mailpilot task list`. Operator-event extended per §V.47: `enrollment.add` event carries `scheduled_at` field when flag set, `changed` list adds `"scheduled_first_send"`. Why: outbound first-send today = manual-only (`enrollment run`, `src/mailpilot/cli.py:2437`); operators want fire-and-forget scheduling (campaign sends, time-zone-aware delivery, batched enrollment w/ staggered first-touch). Lightweight surface — task queue + drain loop already handle deferred execution per §V.3 ∴ this row binds (a) operator-facing CLI entry, (b) trigger semantics so agent ⊥ read drained task as resumed deferred work (§B.25 recurrence class). Orthogonal to §V.13 (inbound auto-task creation, different surface: routing pipeline ⊥ CLI handler, different lifecycle: reactive ⊥ proactive).
V56: `mailpilot enrollment add` ! reject when `contact.email` (case-insensitive ASCII compare) ≡ workflow's owning account email. Handler resolves account via `get_account(connection, workflow.account_id)` after existing FK validation of workflow ∧ contact; match → `output_error(f"cannot enroll contact {contact.email} in workflow {workflow.name}: contact email matches workflow's account email", "self_loop")` per §V.5; ⊥ call `create_enrollment`, ⊥ enter `cli_mutation` span ∴ ⊥ `enrollment.add` operator event ∧ ⊥ `enrollment_added` activity row. Why: contact-email ≡ account-email creates semantic self-loop — inbound workflow notionally processes mail it sent to itself; outbound notionally addresses own mailbox; either direction degenerate. Orthogonal to §V.55 (`--scheduled-at` flag mechanics, outbound-only, scheduling-class) — §V.56 unconditional, direction-agnostic, validation-class. Reject ≡ minimal point of intervention per issue #127 author reasoning (contacts global, constraint per-workflow); broader `contact create` rejection deferred (separable surface).
V57: `mailpilot contact disable --contact-id <id> --reason <text>` soft-disables a contact by writing `disabled_reason`. Handler ! validate `reason.strip() ≠ ""` (else `output_error("reason cannot be empty", "validation_error")` per §V.5); ! read pre-row via `before = get_contact(connection, contact_id)` (`before is None` → `output_error(f"contact not found: {contact_id}", "not_found")`); under `cli_mutation("contact", "disable", entity_id=contact_id)` span → `updated = database.disable_contact(connection, contact_id, reason)` (added by §T.47) → `changed = ["disabled_reason"] if before.disabled_reason != updated.disabled_reason else []` per §V.47 `update`/`disable` semantics → `operator_event("contact.disable", entity_id=contact_id, changed=changed)` ∴ first disable ∨ reason-change → `["disabled_reason"]`; idempotent re-disable w/ same `reason` → `[]`. Envelope = singular per §V.5: `{"contact": {<full Contact w/ disabled_reason set>}, "ok": true}` — row retained ∴ ⊥ composite-key projection (distinct from hard-delete `remove` per amended §V.5). ⊥ `contact_disabled` activity row emission (consistency w/ existing `contact create`/`update` handlers ∧ agent-tool `disable_contact` (`src/mailpilot/agent/tools.py:274`) — operator-event ∈ §V.47 IS the audit trail). Surfaces existing `database.disable_contact` ∧ agent-tool `disable_contact` to operator-CLI ∴ symmetric w/ agent-runtime. Hard-delete `contact remove` deferred per issue #128 author "(1) could be added later" — separable surface. Why: §B.32 surfaced contact-lifecycle gap during issue #127 rollback (operator stuck w/ orphan contact row; only cleanup path = raw `DELETE FROM contact WHERE id = ...` bypassing future referential-integrity guards); `disable` ≡ cleaner default per issue #128 author reasoning (preserves audit history, mirrors existing agent tool, uses existing `disabled_reason` column + `idx_contact_active` partial index from §T.47). Orthogonal to §V.55 / §V.56 (`enrollment add` per-cmd rules, different cmd/noun) ∧ §V.47 (meta-instrumentation contract — §V.57 binds per-cmd CLI surface, §V.47 binds telemetry shape).
V58: `schema_metadata` row ! recorded w/ current `mailpilot_version` + normalized `schema_hash` @ schema apply time (probe-empty branch of `initialize_database` ∈ `src/mailpilot/database.py`). Subsequent connections (probe-non-empty branch) ! compare current normalized hash against recorded; mismatch ∨ row-missing → `logfire.warn("schema drift detected", recorded_version, current_version, recorded_hash, current_hash)` + paired `operator_event("schema.drift", recorded_version, current_version, recorded_hash, current_hash)` (extends §V.19 pattern to warn-class event since drift ⊥ exception). ⊥ SystemExit — operator owns remediation via `make clean` (data-destructive, prod = scheduled window). Version-only diff w/ matching hash ≡ silent (patch release w/o schema touch). `mailpilot status` envelope ! surface `schema: {hash, applied_at, drift}` block (rename of prior `schema_metadata`; `mailpilot_version` promoted to top-level `status.version` per §V.60 ∴ ⊥ duplicated inside `schema`) ∴ drift state pull-able alongside push warning. Table shape: `id INT PRIMARY KEY DEFAULT 1 CHECK (id=1)` + `mailpilot_version TEXT NOT NULL` + `schema_hash TEXT NOT NULL` + `applied_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` ∴ row count ∈ {0, 1}. No backfill (`make clean` recreates per §T.13 / §T.34 / §T.47 precedent). Why: prod wheel-install runs against persisted DB; `initialize_database` skips schema apply when probe finds `account` table (deliberate per `task_pending_trigger` AccessExclusiveLock vs sync-loop RowExclusiveLock deadlock) ∴ schema additions silently absent on prod until operator runs `make clean`; first write hitting missing col raises opaque `psycopg.errors.UndefinedColumn` ∴ drift class invisible; warn @ startup binds drift class explicitly. No fail-fast — `mailpilot run` ≡ HA-critical, warn-loud > hard-outage.
V59: `schema_hash` ≡ sha256 of normalized `schema.sql`: (1) strip `--`-line comments via `re.sub(r"--[^\n]*", "", sql)`, (2) collapse whitespace runs via `re.sub(r"\s+", " ", normalized).strip()`, (3) `hashlib.sha256(normalized.encode("utf-8")).hexdigest()`. `_compute_schema_hash(sql: str) -> str` helper ∈ `src/mailpilot/database.py`. Why: trivial reformatting (blank line shuffle, comment add) else flips raw-bytes hash → false-positive drift warning floods logs; normalization scopes hash sensitivity to SQL-meaningful changes.
V60: `mailpilot status` envelope inner payload ≡ block layout for LLM-agent troubleshooting. Top-level blocks under `{"status": {...}, "ok": true}` envelope (per §V.5): `version` (mailpilot package version str — top-level "what binary am I running?"), `schema` per §V.58 (`{hash, applied_at, drift}`), `sync_loop` (`{running, pid, started_at, heartbeat_at, heartbeat_age_seconds}`; null when ⊥ running), `accounts[]` (per-row `{id, email, disabled, last_synced_at, last_synced_age_seconds, gmail_history_id, watch_expiration, watch_expires_in_hours}`; `disabled: bool` ≡ `disabled_reason IS NOT NULL`; `last_synced_age_seconds` ∧ `watch_expires_in_hours` server-computed via SQL `EXTRACT(EPOCH FROM (now() - col))::int` ∴ ⊥ Python clock skew; null timestamp → null age), `tasks` (`{pending, failed_24h, scheduled_future, oldest_pending_age_seconds, max_attempt_count_pending}`; `failed_24h` ≡ `WHERE status='failed' ∧ completed_at >= now() - interval '24 hours'`; `scheduled_future` ≡ pending w/ `scheduled_at > now()`; `oldest_pending_age_seconds` ≡ `now() - min(scheduled_at) WHERE status='pending' ∧ scheduled_at <= now()`), `config` (`{anthropic_api_key_set, anthropic_model, logfire_environment, logfire_token_set, google_pubsub_topic, google_pubsub_subscription, database_url}`; secrets surfaced as `*_set: bool` only — ⊥ echo values; `database_url` scrubbed to `<scheme>://<host>[:<port>]/<db>` via `urllib.parse.urlsplit` reconstruction ∴ ⊥ leak creds into agent transcripts / Logfire spans), `counts` (`{companies, contacts, workflows, emails, activities, tags, notes}` — demoted sanity tail; `accounts` dropped, derivable as `len(status.accounts)`). Backwards compat ⊥ retained — operator confirmed no scripts rely on prior shape; release-note bump in same commit per §T.29 / §T.54 precedent. Why: prior payload ≡ entity-counts dump + sync-running bit ∴ ⊥ discriminating for "what's wrong?"; reshape surfaces staleness (sync_loop ∧ account ages), queue health (tasks), config sanity (secrets-as-bool, scrubbed `database_url`), schema state per §V.58 — counts demoted to sanity tail; secrets-as-bool + db-url scrub prevent credential leak into agent transcripts ∧ Logfire spans.
V61: ∀ agent tool whose underlying client ⊥ thread-safe (e.g. `httplib2.Http`-backed `googleapiclient.discovery.Resource`, ∨ any shared mutable transport across calls) → `Tool(...)` ! `sequential=True` on registration ∈ `src/mailpilot/agent/templates.py`. Pydantic AI dispatcher serializes that tool's invocations even when model emits in parallel; thread-safe peer tools retain parallel dispatch. @ amend time binds `_DRIVE` tuple ∈ `inbound-google-drive` template (`list_drive_markdown`, `read_drive_markdown`, `search_drive_markdown`). Contract test enumerates `Tool` entries per template ∴ regression caught when (a) a Drive `Tool` drops the kwarg, (b) Pydantic AI removes the flag, (c) new non-thread-safe binding lands w/o it (future Gmail-tool expansion, Calendar ∨ Sheets via `google-api-python-client`). Why: `googleapiclient.discovery.build(...)` hard-wired to `httplib2` — modern Cloud Client Libraries ⊥ cover Drive ∨ Gmail; `httplib2.Http` carries one connection-pool dict w/o locks ∴ 2 threads sharing one instance race connection state; Pydantic AI dispatches sync tools via `asyncio.to_thread` ∴ Anthropic-emitted parallel calls land on separate threads against same transport. §B.34 — `read_drive_markdown` emitted ×2 parallel, one returned 1.12s ∧ sibling hung 60.83s @ socket timeout, killed agent run. §V.44 transient classifier correctly returned `is_transient=True` but race ≡ deterministic ∴ all 4 retries hung. Structural gate @ registration ≡ single point of intervention: per-call `Http` rebuild eats DNS+TLS handshake ∧ ⊥ speak Pydantic AI's contract; async/httpx Drive transport ≡ reimpl Drive REST against httpx (large YAGNI hit).
V62: Task drain MUST dispatch ready tasks via bounded concurrent worker pool. `_drain_pending_tasks` (`src/mailpilot/sync.py:418-431`) ! replace sequential `for task in pending: execute_task(...)` w/ `concurrent.futures.ThreadPoolExecutor(max_workers=settings.max_concurrent_tasks)` ∧ `submit` per pending task; futures tracked via `as_completed` for `task.drain` operator_event timing aggregation. New `Settings.max_concurrent_tasks: int = 4` (`src/mailpilot/settings.py`) bounds pool size ∴ production deploy can tune per-instance. Per-task exception handling preserves §V.44 retry classification (transient → reschedule, terminal → fail) — uncaught exceptions ∈ one worker ! ⊥ poison sibling futures. Worker isolation: each future opens its own `psycopg.Connection` via `settings.connection()` ∴ ⊥ shared cursor / transaction races across concurrent agent runs. Pool size = 1 (regression-mode) preserves strict sequential behavior. Why: smoke-test 2026-05-28 B7.5 — 2 concurrently-arrived inbound emails on demo workflow ran `agent.invoke` spans strictly sequential (span#1 22s 15:14:36→15:14:58, span#2 30s 15:14:58→15:15:29, 3ms gap, zero overlap) → 2nd msg stalled full duration of 1st (~30s here, up to 60s near SLA) → lab5.ca/mailpilot/ 60s promise eaten under traffic burst. Drive `Tool(... sequential=True)` (§V.61) ⊥ sufficient — intra-invocation only (one agent.invoke, multiple tool calls); inter-invocation serialization @ drain layer ≡ orthogonal recurrence class. Bounded pool also prevents traffic-burst from self-DoSing Anthropic rate limit ∧ Gmail send quota.
V63: smoke-test ∧ demo-test gates w/ span-backed observables (`agent.invoke`, `running tool`, `gmail.send_message`, etc.) ! derive primary verdict from Logfire spans, ⊥ wall-clock + CLI poll. Latency SLA = `agent.invoke.end_timestamp - T_SEND` (server-computed via Logfire query), ⊥ poll-attempt count × cadence. CLI polls (e.g., `mailpilot email list`) reserved for side-effect verification only — "did the reply round-trip back to the inbox at all?" — w/ generous cap (≥120s) since reply-arrived signal ⊥ latency-budget gate. Why: §B.38 — smoke-run 2026-05-28 B7 measured agent span 51s but 5s poll first observed reply @ attempt 11 (~64s polled) → borderline gate verdict needed Logfire cross-check; B7.5 graded latency from `T_SEND` wall-clock w/o subtracting unavoidable sync-tick lag (~12s observed: T_SEND_B75=15:14:24Z, first `agent.invoke` start=15:14:36Z) → inflated measured latency above pure agent budget. Logfire ≡ authoritative source for span data (already binds §V.22 telemetry contract, §V.26 tool-name attr, §V.45 G3 demo-test gates). Orthogonal to §V.46: §V.46 binds liveness probes' data source (regression suites explicit out-scope); §V.63 binds gate-verdict source within regression suites — different scope, complementary rule.
V64: Pub/Sub-notify-driven sync ! ⊥ serialize w/ in-flight `agent.invoke`. When `pubsub.notify` for account X arrives while ≥1 `agent.invoke` span(s) mid-execution (typically dispatched via §V.62 worker pool), main run loop ! reach `sync.account.run(X)` ≤ 5s post-notify regardless of in-flight `agent.invoke(s)` duration. ∴ Pub/Sub → sync → routing → task-create pipeline (upstream of drain) ⊥ blocked on drain completion. Impl-side: `_drain_pending_tasks` (`src/mailpilot/sync.py`) ! ⊥ block main loop on `as_completed` over worker-pool futures past one main-loop tick budget; main loop returns from `_drain_pending_tasks` ∧ continues `wakeup_event.wait()` per §V.3 while futures complete ∈ background. Why: smoke-test 2026-05-28 B7 (62s end-to-end vs 60s SLA; 31s `agent.invoke` + 32s pre-agent overhead) ∧ B7.5 (LATENCY_B75b=72s; both emails delivered to Gmail @ 15:01:25, first sync picked up only B75a, agent.invoke for B75a held loop 15:01:34→15:02:05 (31s), during entire 31s window ⊥ `sync.account.run` fired on inbound account, B75b sat in Gmail 44s before routing → second sync fired 0.1s after agent.invoke ended @ 15:02:05) — both root-caused to sync-blocked-by-drain serialization. §V.62 (drain pool) ≡ necessary ⊥ sufficient: pool dispatches concurrent agent.invoke for tasks that ALREADY exist; new-email task creation lives upstream ∴ blocked separately. lab5.ca/mailpilot/ 60s SLA per §V.45/§V.63 budgets agent reasoning time ∴ any sync-blocking overhead between Pub/Sub notify ∧ agent.invoke start consumes that budget directly. Orthogonal to §V.62 (intra-drain concurrency) ∧ §V.3 (event-driven wake) — §V.64 binds the sync-vs-drain inter-layer contract.

## §T TASKS

id|status|task|cites
T1|x|spec ratified — capture future work as new §T rows; backprop bugs via `/sdd:spec bug:`|-
T2|x|pair the 3 unpaired logfire.exception sites in `run.py:199` (run.sync.account_failed), `pubsub.py:203` (pubsub.notification.decode_error), `pubsub.py:286` (pubsub.watch.renewal_failed) w/ `operator_event("error", source=<event_name>, message=str(exc))`. Add span-contract tests asserting paired emission per site|V19
T3|x|impl `search_drive_markdown` agent tool. Add `DriveClient.search_markdown(folder_id, query)` (`src/mailpilot/drive.py`), tool wrapper (`src/mailpilot/agent/tools.py`), register in `agent/invoke.py`, system-prompt mention as preferred entry when KB folder large. Tests cover {hit, no-hit, 404 folder, drive 5xx}. Smoke-test scenario B uplift KB to ≥10 docs to demonstrate search vs full enumeration|V14,V15,V20,V24
T4|x|harden smoke-test out-scope QA verifier per §V.25 — strip tokens shared w/ original question before fabrication regex; treat `details.declined=true` w/ ⊥ fabrication-outside-echo as pass|V25
T5|x|superseded — amended §V.26 cites `gen_ai.tool.name` (OTel GenAI convention) already populated by `logfire.instrument_pydantic_ai()` in `src/mailpilot/cli.py:59`. ⊥ code work. See §B.6|V26
T6|x|superseded — investigation done (revised §B.4): no duplicate routing exists. `_store_inbound_message` (`sync.py:769`) & `route_email` (`routing.py:87`) already enforce ≤1 span per email_id. ⊥ code work|V27
T7|x|tighten demo workflow instructions in `.claude/skills/smoke-test/SKILL.md` to require pipe-table Markdown for spec sheets|-
T8|x|SUBSUMED by §T.15 — "Read top ≥3 hits" rule moves from `_SYSTEM_PREFIX` into `_DRIVE_GROUNDING` fragment of `inbound-google-drive` template. Demo workflow B1 prompt alignment & `read_drive_markdown` ≥3 agent test still in scope, lands w/ T15|V20,V28,V33
T9|x|impl `_check_spec_table(body)` heuristic in `src/mailpilot/agent/tools.py` — return `{"error": "format", ...}` when body has ≥3 spec-shape rows ∧ ⊥ `|---|` separator. Wire into `send_email` (`tools.py:41`) & `reply_email` (`tools.py:81`) before SMTP submit. Tests cover {pure prose pass, prose & valid pipe-table pass, spec-shape ⊥ table reject, decline body ⊥ specs pass, mixed body w/ ≥1 separator pass}. Smoke-test verifier extension: assert reply_email span returns no `error=format` for B4|V15,V20,V29
T10|x|SUPERSEDED by §T.15 — `_workflow_uses_kb` regex predicate replaced by structural template gating per §V.32 / §V.33. Drive tools now bound only to `inbound-google-drive` template's `_DRIVE` tuple; non-KB workflows physically cannot register Drive tools|V32,V33
T11|x|impl §V.31 — add `qa.py source --id ID` subcommand (`.claude/skills/smoke-test/scripts/qa.py`): DriveClient w/ `inbound@lab5.ca` impersonation, list & find file by `source_file` name in folder `1IUuPinOopUv_YWOZyFpt2ZX8Hd8bpZat`, print `content` to stdout, exit ≠0 if missing (KB drift signal). Fence `qa.py check` inscope branch → exit 2 w/ "inscope grading is now operator-judged in B4; see SKILL.md". patch `.claude/skills/smoke-test/SKILL.md` Scripts section (lines 51-55), gate B4 (lines 377-385), gate B6 (lines 425-437) per V31 contract. Retain `qa_pairs.json.expected_tokens` field for historical-run repro but ⊥ gate consumption|V31
T12|x|impl template registry — `src/mailpilot/agent/templates.py`: `WorkflowTemplate` frozen dataclass (`name`, `direction`, `description`, `protocol`, `tools`); fragment constants `_DECLINE`, `_DEFERRED_TASK`, `_NO_FABRICATION`, `_DRIVE_GROUNDING`; tool tuples `_CORE`, `_DRIVE`; `TEMPLATES: dict[WorkflowTemplateName, WorkflowTemplate]` w/ 3 entries. Define `WorkflowTemplateName = Literal["outbound-general", "inbound-general", "inbound-google-drive"]`. Tests parametrize universal invariants over `TEMPLATES.values()` (non-empty protocol, non-empty tools, name prefix matches `direction`, registry agrees w/ Literal); named per-template contract tests (`test_outbound_general_excludes_drive_tools`, `test_inbound_google_drive_includes_drive_tools`, `test_inbound_google_drive_protocol_carries_grounding`)|V20,V32,V33,V34
T13|x|schema — add `workflow.template TEXT NOT NULL` & CHECK constraint enumerating `WorkflowTemplateName` values (`src/mailpilot/schema.sql`). No backfill (`make clean` recreates DB)|V32
T14|x|`database.create_workflow` — accepts `template: WorkflowTemplateName`, populates `workflow.type` from `TEMPLATES[template].direction`; rejects caller-passed `type` mismatch. `update_workflow` rejects `template` field. `Workflow` / `WorkflowSummary` models gain `template: WorkflowTemplateName` field. `list_workflows` accepts optional `template` filter|V32,V33
T15|x|refactor `_build_agent` (`src/mailpilot/agent/invoke.py`) — drop `_TOOLS` list & `_SYSTEM_PREFIX` prose; lookup template via `workflow.template` → `Agent(model, tools=template.tools, system_prompt=template.protocol + workflow.instructions)`. Drop `_workflow_uses_kb` predicate (§T.10 path) once added. Tests assert exact tool tuple bound per template & integration test `test_build_agent_binds_template_tools`|V20,V32,V33,V34
T16|x|CLI — `workflow create` drops `--type`, requires `--template <outbound-general|inbound-general|inbound-google-drive>` (no default). `workflow update` rejects `--template`. `workflow list` adds `--template` filter alongside `--type`. `workflow view` exposes `template`. New `template` noun (read-only, code-defined): `template list [--direction inbound|outbound]` → `{"templates": [{name, direction, description, tool_count}, ...], "ok": true}`; `template view NAME` → `{"template": {name, direction, description, tools: [tool_name, ...], protocol}, "ok": true}`. Both skip `initialize_database()`. New models `WorkflowTemplateSummary` & `WorkflowTemplateRecord` (`models.py`). Tests cover envelope, `--direction` filter, unknown-name → `not_found`|V5,V6,V20,V32
T17|x|drop §V.30 path code (`_workflow_uses_kb` & regex sniff) & related tests once T15 lands; §V.30 already marked SUPERSEDED in SPEC|V32,V33
T18|x|smoke-test wiring — Phase 0 `mailpilot workflow create` invocations swap to `--template outbound-general` (Scenario A) & `--template inbound-google-drive` (Scenario B). Add Scenario A regression assertion via Logfire MCP: zero `search_drive_markdown` spans during outbound A window (§B.10 cannot recur). patch `.claude/skills/smoke-test/SKILL.md` Phase 0 & gate sections|V20,V32,V33
T19|x|sweep prompt fragments in `src/mailpilot/agent/templates.py` for single-tool examples — extend `_DECLINE` "(e.g. reply_email)" → ≥2 tools (e.g. `reply_email` & `noop`); audit `_BASE`, `_DEFERRED_TASK`, `_NO_FABRICATION`, `_DRIVE_GROUNDING` for same pattern. Tests assert ≥2 tool names mentioned wherever a fragment lists an example sequence|V16
T20|x|filter trigger email from `email_history` in `_build_user_prompt` (`src/mailpilot/agent/invoke.py:545-552`) — exclude row where `id == email.id` before formatting. Tests: (a) inbound trigger w/ otherwise-empty history → prompt contains `No prior email history with this contact.` & 1 occurrence of trigger body; (b) inbound trigger w/ N prior emails → prompt `Email history` count = N (excluding trigger) & body appears exactly once total; (c) outbound (⊥ trigger) prompt unchanged. Smoke-test Logfire assertion: ∀ inbound `chat` span → trigger body substring count = 1 in `gen_ai.input.messages[0].parts[0].content`|V20,V35
T21|x|thread `trigger` arg through `_build_user_prompt` (`src/mailpilot/agent/invoke.py:377`) → `_format_trigger` (`invoke.py:356`); add `enrollment_run` branch ("First reach-out for this enrollment. Compose the initial outbound message per workflow objective & instructions."); drop synthesised `task_description = f"manual {wf.type} run"` at `cli.py:1841` — pass `trigger="enrollment_run"` & ⊥ task_description on that path. `run.py:123` (real task drain) keeps passing `task_description=task.description` w/ `trigger="task"`. Tests: (a) `enrollment_run` outbound (⊥ email) → prompt contains `First reach-out` & ⊥ `Deferred task:`; (b) `task` trigger w/ persisted task description → prompt renders `Deferred task:` block (regression guard); (c) `enrollment_run` w/ ∃ email (continued enrollment) → email-branch wins (existing precedence preserved); (d) `manual` trigger ⊥ email ⊥ task_description → outbound fallback prose. Smoke-test Logfire assertion: A3 first `chat` span's user prompt ⊥ contain `Deferred task:` substring|V11,V20,V36
T22|x|impl §V.37 — set `AnthropicModelSettings(anthropic_cache_tool_definitions=True, anthropic_cache_instructions=True)` (∨ equivalent Pydantic AI cache-control surface for current version) on `AnthropicModel` in `src/mailpilot/agent/invoke.py` (`_build_agent` model construction) ∧ `src/mailpilot/agent/classify.py` (`_get_model`). Bubble `cache_read_input_tokens` ∧ `cache_creation_input_tokens` from per-turn `chat` span usage → `agent.invoke` rollup span. Tests: (a) httpx-mock assertion — outbound Anthropic request body contains `cache_control` block after tool definitions for both agent ∧ classifier; (b) span-contract test (`capfire`) — `agent.invoke` rollup span carries `cache_read_input_tokens` ∧ `cache_creation_input_tokens` attrs (≥0 OK, presence required) summed from child chat spans. Smoke-test follow-up: re-run `/smoke-test` → confirm `cache_read_input_tokens > 0` on B4 multi-turn invocation; ⊥ regression on KB grounding gates B4/B6|V20,V37
T23|x|impl `.claude/commands/release.md` (thin dispatcher) + `.claude/skills/release/SKILL.md` (procedure). Inherit `/gh:release` Procedure verbatim through §12 (manifest detect → `pyproject.toml`; bump derivation; annotated tag w/ `--cleanup=verbatim`; baseline & retag-baseline modes; confirm-before-mutate). Extend post-§12: §13 push (`git push origin main && git push origin v<x.y.z>`), §14 build (`uv build`; assert `dist/mailpilot-<x.y.z>-py3-none-any.whl` ∃ — ⊥ → bail), §15 publish (`gh release create v<x.y.z> --verify-tag --notes-from-tag dist/mailpilot-<x.y.z>-py3-none-any.whl`). Confirm gate (§9) shows {target tag, push refs, wheel path, gh release create cmd}. Argument-hint mirrors `/gh:release` (`[patch|minor|major|x.y.z|retag-baseline]`).|V38
T24|x|impl `workflow export --account-id <ID>` cmd (`src/mailpilot/cli.py`). List envelope per §V.5/§V.39, fields = {name, template, objective, instructions, theme}. Tests: envelope shape, account-not-found error, deterministic ordering (e.g. `ORDER BY name`)|V4,V5,V20,V39
T25|x|impl `workflow import --account-id <ID> [--file PATH]` cmd. JSON from `--file` ∨ stdin. Upsert keyed `(account_id, name)` per §V.39. Tests: create path (activates iff objective ∧ instructions both non-empty), update path (status untouched, only changed fields written), `template` differs → per-row `template_immutable` error & batch continues, stdin vs `--file`, malformed JSON → top-level error envelope|V5,V20,V32,V39
T26|x|round-trip integration test: seed N workflows → `workflow export` → truncate `workflow` table for account → `workflow import` → diff ∅ on (name, template, objective, instructions, theme). Re-run import on unchanged DB → 0 mutations (assert via row `updated_at` unchanged ∨ `update_workflow` not called)|V20,V39
T27|x|smoke-test wiring — Phase 0 swaps 3 imperative `workflow create` per account → 1 `workflow import --file tests/fixtures/workflows-<scenario>.json` per account. Add fixtures `tests/fixtures/workflows-outbound.json` (Scenario A) ∧ `tests/fixtures/workflows-inbound.json` (Scenario B). patch `.claude/skills/smoke-test/SKILL.md` Phase 0 section|V20,V39
T28|x|migrate `company export`/`import` (`cli.py:486-522`) ∧ `contact export`/`import` (`cli.py:674-719`) to §V.5 list envelope. Export: stdout `{"companies"|"contacts": [...], "ok": true}`; `--file PATH` optional (when set: file = same JSON, stdout still emits envelope per §V.4). Import: read JSON from `--file` ∨ stdin; emit `{"companies"|"contacts": [...], "ok": true}` w/ per-row errors per §V.39 import pattern. Drop `{"exported"|"imported": N, "file": F}` shape. Update tests + any caller / shell script (`make` targets, smoke-test). Align shape across (`company`, `contact`, `workflow`) export/import|V4,V5,V20,V39
T29|x|migrate `tag remove` (`cli.py:1304`) ∧ `enrollment remove` (`cli.py:2150`) to §V.5 singular envelope per amended V5. `tag remove` → `output_entity("tag", removed_tag_record)` w/ {name, owner_type, owner_id} fields; `enrollment remove` → `output_entity("enrollment", removed_enrollment)` w/ {workflow_id, contact_id, status_at_removal}. Update `test_cli.py` assertions for both. ⊥ silent shape change — bump in same commit as v0.x release notes|V5,V20
T30|x|rewrite `.claude/skills/smoke-test/SKILL.md` Phase 5 per §V.40 — collapse Parts A/B/C → §1 Execution (phase matrix + ≤3-bullet Logfire summary; SQL queries & 13-cat scan retained inline as derivation aids before §1, ⊥ rendered as separate output sections). Part D → §2 Bugs (severity matrix & auto-invoke rules unchanged). Part E `propose-invariant` Suggestions → §3 Invariants. Drop Notes, `none`-routed Suggestions, `propose-task` Suggestions, `code-only` Defects (operator already has prior `smoke-test-*.md` files to grep). Spec hand-off block ∧ auto-write to disk unchanged. Update worked-example block in SKILL.md|V40
T31|x|widen `_check_spec_table` (`src/mailpilot/agent/tools.py:50`) per amended §V.29 — replace `_SPEC_ROW_RE` w/ `^[^|]+?\s{2,}\S` (drop numeric-value requirement); ASCII rule-line runs (`-{6,}`, `={6,}`, `_{6,}` on standalone line) ⊥ count as pipe-table separator. Tests cover {numeric specs reject (regression for §B.9), non-numeric specs reject (regression for §B.14), mixed numeric+text reject, ASCII rule-line ⊥ pass-through, valid pipe-table pass, decline body pass, prose w/ incidental 2-space alignment ⊥ false-positive}. Smoke-test verifier extension: assert reply_email span returns no `error=format` for B4 grounded reply|V20,V29
T32|x|impl `mailpilot --skill` global flag (`src/mailpilot/cli.py`) reading packaged `src/mailpilot/SKILL.md` via `importlib.resources`; configure `pyproject.toml` to ship `SKILL.md` as package data; author `src/mailpilot/SKILL.md` per §V.41 scope (noun-verb grammar, envelope, recipes, settings); tests cover {flag prints body verbatim, package-data resolves via importlib.resources after `uv build` install, missing-file = hard fail not silent}|V5,V20,V41
T33|x|impl §V.43 — pass `http_client=httpx.Client(timeout=httpx.Timeout(240.0))` to `AnthropicProvider(...)` in `_build_anthropic_model` (`src/mailpilot/agent/invoke.py:351`) ∧ `_get_model` (`src/mailpilot/agent/classify.py:63`). Add `httpx` to `pyproject.toml` deps if not already direct (currently transitive via `anthropic`). Tests: (a) httpx-mock asserts the AnthropicProvider's client carries `timeout.read == 240.0` in both modules; (b) assert ⊥ retry wrapper exists around `agent.run()` / `agent.run_sync()` call sites — bare `except Exception` @ `run.py:127` is the intended terminal handler per V43 (idempotency: tool-call mid-turn ⊥ safely re-driven). ⊥ regression on V37 cache_control settings (both ctors retain `AnthropicModelSettings(anthropic_cache_tool_definitions=True, anthropic_cache_instructions=True)`)|V20,V37,V43
T34|x|impl §V.44 — bounded auto-retry for transient agent task failures per issue #122. (a) new `src/mailpilot/agent/retry.py` w/ `is_transient(exc)` classifier ∧ constants `MAX_ATTEMPTS=4`, `BACKOFF_SECONDS=[30, 120, 300]`; allow-list per V44. (b) `src/mailpilot/schema.sql` — `task.attempt_count INTEGER NOT NULL DEFAULT 0`; extend `task_pending_trigger` → `AFTER INSERT OR UPDATE OF status, scheduled_at`. (c) `src/mailpilot/database.py` — `reschedule_task_for_retry(connection, task_id, backoff_seconds, exc)` ∧ `manual_retry_task(connection, task_id)` (status-restricted); FK validators per CLAUDE.md gotcha. (d) `src/mailpilot/models.py` — `Task` ∧ `TaskSummary` gain `attempt_count: int`. (e) `src/mailpilot/run.py:127` — swap bare `except Exception`: transient ∧ budget left → `reschedule_task_for_retry` + `operator_event("task.retry", ...)`; else terminal `failed` (existing path). (f) `src/mailpilot/cli.py` — `mailpilot task retry --task-id <ID>` cmd. (g) `src/mailpilot/drive.py` — `httplib2.Http(timeout=...)` cap on `build_drive_service`. Tests: classifier parametrized over (exception class, status) per §V.20; retry path (transient + budget → row `pending` w/ bumped `attempt_count`; budget exhausted → terminal `failed`); §V.43 exclusion regression (`anthropic.APITimeoutError` ∧ `httpx.ReadTimeout` stay terminal); CLI cmd FK-validator patches; `operator_event("task.retry", ...)` paired emission. ⊥ regression on §V.43 (`AnthropicProvider` 240s read-timeout retained) ∨ §V.37 (`anthropic_cache_*` settings retained). PR: #123|V19,V20,V37,V43,V44
T35|x|drop non-canonical `run_loop` (`src/mailpilot/run.py:188-218`) ∧ its 3 tests in `tests/test_run.py` (`test_run_loop_single_iteration`, `test_run_loop_sync_error_continues`, `test_run_loop_keyboard_interrupt_not_error_span`). `mailpilot run` ! routes through `start_sync_loop` per §V.3 (`cli.py:218`); `run_loop` uses `time.sleep(run_interval)` ∧ ⊥ `wakeup_event` ∴ violates the event-driven contract if ever wired back in. Keep `execute_task`, `_handle_agent_failure`, `_sync_all_accounts` (still called by `sync.py:423`, `start_sync_loop` plumbing). Run `make check` after delete — green required. Surfaced by `/sdd:check --all` on 2026-05-11 as a non-canonical-loop observation alongside §B.17|V3,V20,V21
T36|x|reshape `mailpilot account sync` output (`src/mailpilot/cli.py:421`) to §V.5 plural envelope — drop top-level `total_stored` (caller-side aggregation from per-row `stored`); rename `results` → entity-typed plural key (e.g. `accounts: [{account_id, email, stored, error?}, ...]`). Tests assert envelope = `{"accounts": [...], "ok": true}` w/ ⊥ inline top-level non-list fields. Update smoke-test ∧ any `make` target consuming current shape|V5,V20
T37|x|delete orphan `_sync_all_accounts` (`src/mailpilot/run.py:185-204`) ∧ its now-unused imports from `src/mailpilot/run.py` (`get_account`, `list_accounts`, `GmailClient`, `sync_account`). Sole caller (`run_loop`) removed by §T.35; `sync.py` has its own `_sync_all_accounts` @ `sync.py:388` ∴ run.py copy = dead code. `make check` green required; basedpyright `reportUnusedFunction` warning count drops by 1|V21
T38|x|impl `.claude/skills/demo-test/SKILL.md` per §V.45. Reuse `.claude/skills/smoke-test/scripts/qa.py` (`pick`, `source`) verbatim, ⊥ new scripts. Local CLI surface: `mailpilot account list`, `mailpilot email send`, `mailpilot email list`. Logfire MCP queries against project=`mailpilot` per §V.22. Skill description triggers: "demo test", "is the demo alive?", "check lab5.ca/mailpilot/", "demo liveness". Update CLAUDE.md "Always-on skills" section: append `/demo-test` line alongside `/smoke-test`. ⊥ ship as `mailpilot --skill` package-data (skill ≡ Claude-Code-local, requires operator-judged G2)|V20,V22,V26,V31,V37,V40,V45
T39|x|fix `.claude/skills/smoke-test/SKILL.md` cite drift per §B.22 — L37 prefix `(SPEC §V13)` → `(SPEC §V.5)` (correct target + canonical shape; same line later cites `§V.5 plural envelope` correctly); L161 `§V12 / §T18 / §B2` → `§V.12 / §T.18 / §B.2`; L272 `§V12 / §T18` → `§V.12 / §T.18`. Skill prose only — `make check` ⊥ required; re-run `/sdd:check` post-edit → §-cite drift ↓1|-
T40|x|impl §V.46 + §V.45 G1 amend. Replace `mailpilot email list` poll in `.claude/skills/demo-test/SKILL.md` Step 5 w/ direct Gmail API call: inline `uv run python -c "import sys, json; from mailpilot.gmail import GmailClient, build_delegated_credentials; ..."` against `GmailClient.list_messages(query="from:hello@lab5.ca after:<EPOCH>", label_ids=["INBOX"])` impersonating `outbound@lab5.ca` per §V.42. Subject-match logic stays (substring on unique `<topic>` tail). Preserve §V.45 "⊥ new scripts" line — route via inline Python ∨ `mailpilot` CLI, ⊥ `.claude/skills/demo-test/scripts/`. Verify post-fix: dry-run `/demo-test` against current prod → G1 passes within real 60s window w/o manual sync|V42,V45,V46
T41|x|impl §V.47 — instrument CRM config CLI mutations across (noun, verb) matrix at CLI handler layer (`src/mailpilot/cli.py`). Per-handler obligations: `logfire.span("<noun>.<verb>", entity_id?, account_id?, ...)` wrapping mutation; on success → `operator_event("<noun>.<verb>", entity_id=..., account_id=..., changed=[...])`; on raise → `logfire.exception` + paired `operator_event("error", source="<noun>.<verb>", message=str(exc))`. Concrete matrix from current `cli.py` handlers: `account` {create, update}; `company` {create, update, import}; `contact` {create, update, import}; `workflow` {create, update, start, stop, import}; `enrollment` {add, remove, update}; `tag` {add, remove}; `note` {add}. `update` handlers ! read pre-row via `get_<noun>(...)`, perform mutation, read post-row, compute `changed` = [k for k, (a, b) in zip(field_names, paired_values) if a != b]. `import` handlers emit per-row events ∴ re-run on unchanged DB → empty `changed` lists (regression guard for §V.39). Tests under `tests/test_cli_telemetry.py` (new) via `capfire: CaptureLogfire` parametrized over the matrix assert (a) span name + attrs on success, (b) `operator_event` emission w/ correct `changed` per verb semantics, (c) `logfire.exception` + paired `operator_event("error", source="<noun>.<verb>", ...)` on raise (simulate via injected `psycopg.OperationalError` patch on `database.<fn>`). ⊥ regression on (i) §V.26 agent-tool `gen_ai.tool.name` spans (operator-CLI events live at handler layer, ⊥ DB ∴ agent paths untouched), (ii) §V.39 workflow/contact/company import idempotency. PR: #125|V19,V20,V26,V39,V47
T42|x|patch dotless §-cites in code per §B.24 + §V.48 — `src/mailpilot/cli.py:79` `§V13` → `§V.5` (wrong-section: envelope shape governed by §V.5, ⊥ §V.13 inbound-task filter); `src/mailpilot/agent/invoke.py:523` `§V11` → `§V.11` (canonical dotted form). Sweep `rg '§[VTB][0-9]+(?:[^.]|$)' src/ .claude/ CLAUDE.md SPEC.md` for any dotless residue. Doc prose only — `make check` ⊥ required.|V48
T43|x|impl §V.49 — `_DEFERRED_TASK` fragment becomes `trigger`-aware in `src/mailpilot/agent/templates.py`. Split into `_DEFERRED_TASK_TASK` (current text, `trigger='task'`) ∧ `_DEFERRED_TASK_INITIAL` (new: "Send the initial email & stop; do not call record_enrollment_outcome; outcome will be assessed when a reply arrives", `trigger ∈ {enrollment_run, manual}`). `WorkflowTemplate` gains `build_protocol(trigger) -> str` composing per-invocation; `_build_agent` (`src/mailpilot/agent/invoke.py`) calls `template.build_protocol(trigger) + workflow.instructions` ⊥ `template.protocol + workflow.instructions`. Canonical fragment order per §V.33 preserved. Tests: (a) `build_protocol('enrollment_run')` ⊥ contain `record_enrollment_outcome`, `build_protocol('task')` contains it; (b) outbound agent run w/ `trigger='enrollment_run'` → `record_enrollment_outcome` ⊥ called (mock assert); (c) outbound agent run w/ `trigger='task'` + decline reply → `record_enrollment_outcome` called w/ `outcome='failed'` (regression); (d) `inbound-*` templates unchanged. Smoke-test Logfire: A3 first-reach-out span ⊥ produce `enrollment_completed` activity row.|V20,V33,V36,V49
T44|x|impl §V.50 — `logfire.configure(scrubbing=ScrubbingOptions(callback=<f>))` ∈ `src/mailpilot/cli.py:configure_logging`. `<f>(match: ScrubMatch)` returns `match.value` when `match.path[:2] == ("attributes", "tool_response")`; else returns `None` (delegate to default scrubber). Tests: `tests/test_logfire_scrubbing.py` (new) via `capfire: CaptureLogfire` per §V.20 — (a) `running tool` span emitted w/ `attributes.tool_response.content` containing substring `"authorized"` retains literal content; (b) default scrubber path still active on other fields (e.g. `attributes.gen_ai.system_prompt` w/ `"password=xyz"` → placeholder). Regression: smoke-test B4 / B6 traces show `read_drive_markdown` `tool_response.content` populated end-to-end (⊥ `[Scrubbed due to ...]` placeholder).|V20,V50
T45|x|patch dotless §-cites in `tests/` per §B.26 + §V.48 — `tests/test_cli.py:5817` `§V13` → `§V.5` (correct target: envelope shape; dotted); `tests/test_agent_invoke_trigger.py:3` `§V12` → `§V.11` (correct target: trigger span attr; dotted); `tests/test_pubsub.py:255` `§T13` → drop SPEC.md cite (no §T row covers "pubsub.notification single span, no duplicate debug log" — regression-guard test ⊥ load-bearing on §-cite). Sweep `rg '§[VTB][0-9]+(?:[^.]|$)' src/ tests/ .claude/ CLAUDE.md SPEC.md` post-patch for residue; verify ∅. Test prose only — `make check` ⊥ required.|V48
T46|x|impl §V.51 — add `workflow_id`, `email_id`, `task_id` fields to `ActivitySummary` (`src/mailpilot/models.py:323-331`); extend SELECT in `list_activities` (`src/mailpilot/database.py:2308-2313`) to include those columns. Tests: (a) `mailpilot activity list` JSON output for `enrollment_added` / `email_sent` / `enrollment_failed` rows exposes `workflow_id` populated; (b) `email_id` populated on `email_*` activities; (c) `task_id` field present (null when absent). Smoke-test follow-up: A8 ∧ B7 gates pass via CLI alone ⊥ `psql` fallback.|V20,V51
T47|x|strip `company` ∧ `contact` to identity-minimum + collapse status → disabled_reason + atomic `--note STR` on create cmds. `src/mailpilot/models.py`: `Company` drop {domain_aliases, linkedin, industry, employee_count, founded_year, products_services, locations, company_type, recent_activity, qualification_notes, profile_summary}, `CompanySummary` drop {industry, employee_count}, `Contact` drop {domain, email_type, position, seniority, department, profile_summary, linkedin, status, status_reason} ∧ add `disabled_reason: str | None`, `ContactSummary` rename `status` → `disabled_reason` ∧ drop `domain`. `src/mailpilot/schema.sql`: column drops, drop `idx_contact_domain` index, drop `contact.status` CHECK + status/status_reason cols, add `disabled_reason TEXT`, add `CREATE INDEX idx_contact_active ON contact (id) WHERE disabled_reason IS NULL`. `src/mailpilot/database.py`: narrow `create_company` (sig unchanged), `create_contact` (drop `domain` param), `create_or_get_contact_by_email` (drop email-domain split), `list_companies`/`search_companies`/`list_contacts` SELECTs narrow, add `disable_contact(connection, contact_id, reason) -> Contact`, `list_contacts` default filters `WHERE disabled_reason IS NULL` w/ `--include-disabled` override. `src/mailpilot/cli.py`: `company create` add `--note STR` + atomic note insert under same `cli_mutation("company","create")` span (§V.47) w/ `changed` extended w/ `"note"`; `contact create` same; `contact list` add `--include-disabled` flag. `src/mailpilot/agent/tools.py`: `disable_contact` body swaps `status`/`status_reason` writes → `disabled_reason` write (sig unchanged). `company export`/`import` ∧ `contact export`/`import` payload shape narrows (contact: drop `domain`, `status`, `status_reason`; add nullable `disabled_reason`; company: drop research-blob cols); idempotency preserved per §V.39 (note ∉ payload — interactive-only). ⊥ migration (`make clean` recreates per §T.13 / §T.34 precedent). Tests cover narrowed Summary shapes per §V.6, `--note` atomic insert + telemetry per §V.47, disabled-default-filter, agent-tool swap, export/import round-trip per §V.39|V5,V6,V8,V9,V20,V39,V47,V51
T48|x|impl §V.53. (a) `src/mailpilot/models.py` add `ContactView` ∧ `CompanyView` Pydantic projections (ContactView ≡ Contact fields + `notes: list[Note]` + `notes_total: int` + `company_notes: list[Note]` + `company_notes_total: int`; CompanyView ≡ Company fields + `notes` + `notes_total`). (b) `src/mailpilot/database.py` add `_INLINE_NOTES_CAP = 10` constant ∧ `load_contact_view(connection, contact_id) → ContactView | None` ∧ `load_company_view(connection, company_id) → CompanyView | None` helpers (single txn; 3 queries for company, 4 for contact w/ `company_id` set; ⊥ JOIN — separate `LIMIT 10 ORDER BY created_at DESC` ∧ `COUNT(*)` queries). (c) `src/mailpilot/cli.py` `contact view` ∧ `company view` handlers swap `get_*` → `load_*_view`; `output_entity("contact"|"company", view)`; not-found path → 404 envelope unchanged. (d) `src/mailpilot/agent/tools.py` `read_contact` ∧ `read_company` route through helpers; `view.model_dump(mode="json")` return; §V.15 error path on not-found ∧ DB error preserved. (e) `src/mailpilot/agent/templates.py` `_BASE` append one-liner per §V.53. Tests per §V.20: (i) `load_contact_view` returns notes DESC + correct total + `company_notes` when contact has `company_id` set + empty arrays + zero totals when orphan; (ii) cap enforced at exactly 10 when DB has >10 notes (total reflects real count); (iii) CLI `contact view` JSON inner-dict ≡ `read_contact` `model_dump(mode="json")` byte-identical modulo `{"contact": ..., "ok": true}` wrap; (iv) `load_company_view` returns notes DESC + total; (v) full `body` verbatim ⊥ preview-style truncation; (vi) agent tool not-found ∧ DB error paths return error dict per §V.15; (vii) `_BASE` text contains the new directive ∧ ⊥ duplicate after re-append (regression guard). Smoke-test follow-up: A3 gate `body_text contains both $CONTACT_NOTE_TOKEN ∧ $COMPANY_NOTE_TOKEN` passes (drafted but ⊥ executable until this lands); A8 gate `note_added` activity rows unaffected (notes added in A1a, read-path only).|V5,V6,V8,V9,V15,V20,V33,V53
T49|x|impl §V.54 isatty-guard sweep — patch 3 stdin-fallback import cmds in `src/mailpilot/cli.py` (sweep scope grep: `rg -n "sys\.stdin\.read\(\)" src/mailpilot/cli.py` → cli.py:632 `company import`, cli.py:920 `contact import`, cli.py:2249 `workflow import`): wrap stdin branch w/ `if sys.stdin.isatty(): output_error("no input: provide --file PATH or pipe JSON via stdin", "validation_error")` BEFORE `sys.stdin.read()`. Tests per §V.20: parametrize over (`company import`, `contact import`, `workflow import`) — (a) TTY stdin (monkeypatch `sys.stdin.isatty` → `True`) → `validation_error` envelope on stderr, SystemExit, ⊥ `read()` attempt; (b) piped stdin (default test runner, `isatty=False`) → existing happy path unchanged; (c) `--file` path → existing path unchanged (isatty bypassed).|V20,V54
T50|x|patch bare-form `V<n>` cites in code prose to canonical `§V.<n>` form per §B.30 + amended §V.48. Sweep scope grep pattern: `rg -n '\b[VTB][0-9]+\b' src/ tests/ .claude/ CLAUDE.md` — broad pattern (FPs in identifiers like `V32` as version label, var name); operator-review each hit, patch only prose-intent spec-citations. Sites @ amend time (12 prose cites): `src/mailpilot/agent/invoke.py` (`:328` "per V49", `:344` "V37 cache_control + V43 read-timeout", `:402` "V36:", `:451` "V35:", `:632` "V37:"); `src/mailpilot/agent/templates.py` (`:12-13` "SPEC.md sections V32 ... V33 ... V34", `:53` "per V33", `:65` "per V49"); `src/mailpilot/agent/classify.py` (`:60` "V37:", `:64` "V43:"); `src/mailpilot/agent/retry.py` (`:92` "V44 allow-list", `:94` "V43 exclusion"). Code prose only — `make check` ⊥ required. Post-patch canonical-form grep `rg -n '§[VTB]\.[0-9]+' src/` should show ≥12 new dotted hits; bare-form grep should drop to identifier-only matches (operator confirms ∅ prose-intent residue).|V48
T51|x|patch bare-form `V31` cite in `.claude/skills/smoke-test/scripts/qa.py:16` per §B.31 + §V.48 — `Outscope-only post-V31` → `Outscope-only post-§V.31`. Sweep scope grep: `rg -n '\b[VTB][0-9]+\b' .claude/skills/smoke-test/scripts/qa.py` — operator per-line review distinguishes SPEC §V/§T/§B citations (patch to dotted §-form) from smoke-test internal gate IDs (B1-B9 / A1-A9 phase labels at L12, L18, L163, L221, leave). Post-patch verify: re-run sweep grep → only gate-ID hits remain, ⊥ spec-citation residue. Doc prose only — `make check` ⊥ required.|V48
T52|x|impl §V.55. (a) `src/mailpilot/cli.py` `enrollment add` — add `--scheduled-at <ISO>` Click option (ISO 8601 parse w/ tz-aware fallback); present + outbound wf → after `create_enrollment`, check idempotency filter (existing pending first-touch task for `(workflow_id, contact_id)` w/ `email_id IS NULL`); ⊥ existing → call `create_task(workflow_id, contact_id, description="scheduled first reach-out", scheduled_at=<iso>, context={"trigger": "enrollment_schedule"}, email_id=None)`; inbound wf + `--scheduled-at` → `output_error("--scheduled-at only valid for outbound workflows", "invalid_state")` per §V.5. (b) `src/mailpilot/run.py` `execute_task` — read `trigger = task.context.get("trigger", "task")`, pass to `invoke_workflow_agent(...)` instead of hardcoded `trigger="task"`. (c) `src/mailpilot/agent/invoke.py` `_format_trigger` — extend `enrollment_run` branch to also match `enrollment_schedule` (alias, identical framing per §V.36). (d) `src/mailpilot/agent/templates.py` `WorkflowTemplate.build_protocol(trigger)` — extend `_DEFERRED_TASK_INITIAL` mapping to also match `enrollment_schedule` (no-record-outcome set per §V.49). (e) `src/mailpilot/cli.py` enrollment.add `cli_mutation` call-site — pass `scheduled_at` field when flag set + `"scheduled_first_send"` ∈ `changed` per §V.47. Tests per §V.20: (i) `enrollment add --scheduled-at <T>` on outbound wf inserts task row w/ correct fields; idempotent re-run returns existing enrollment, ⊥ 2nd task; (ii) `--scheduled-at` on inbound wf → `invalid_state` error envelope; (iii) drain w/ `context={"trigger":"enrollment_schedule"}` → `invoke_workflow_agent(..., trigger="enrollment_schedule")` (mock assert); (iv) drain w/ `context={}` → falls back to `trigger="task"` (regression guard); (v) `_format_trigger(trigger="enrollment_schedule")` returns first-reach-out framing byte-identical to `enrollment_run`; (vi) outbound agent run w/ `trigger="enrollment_schedule"` → `record_enrollment_outcome` ⊥ called (§V.49 regression); (vii) `operator_event("enrollment.add", ...)` carries `scheduled_at` field when flag set; (viii) `capfire` span-contract test asserts `agent.invoke` span carries `trigger="enrollment_schedule"` attr per §V.11. Smoke-test follow-up: extend Scenario A0 to exercise `--scheduled-at <near-future>` path; verify `agent.invoke trigger='enrollment_schedule'` span appears in Logfire window per §V.45 G3 pattern.|V3,V11,V20,V36,V47,V49,V55
T53|x|impl §V.56 — `enrollment_add` (`src/mailpilot/cli.py:2422`) gains pre-`cli_mutation` self-loop check: after `get_workflow` ∧ `get_contact` resolve (both already FK-validated), fetch `account = get_account(connection, workflow.account_id)`; `account ≠ None ∧ account.email.lower() == contact.email.lower()` → `output_error(f"cannot enroll contact {contact.email} in workflow {workflow.name}: contact email matches workflow's account email", "self_loop")` per §V.5; ⊥ proceed to `create_enrollment`, ⊥ enter `cli_mutation` span. Check fires after `--scheduled-at` inbound-reject (§V.55) so error-class precedence stays input-validation → state-validation → semantic-validation. Tests per §V.20: (a) outbound wf + contact email ≡ account email → `self_loop` envelope, ⊥ enrollment row, ⊥ `enrollment.add` operator event, ⊥ `enrollment_added` activity; (b) inbound wf same → `self_loop`; (c) case-insensitive: contact `HELLO@lab5.ca` vs account `hello@lab5.ca` → reject; (d) distinct emails → existing happy path unchanged (regression); (e) §B.32 reproducer end-to-end: `account create --email X` + `contact create --email X` + `enrollment add --workflow-id <wf-on-X> --contact-id <c>` → `self_loop`; (f) patch sites per CLAUDE.md "Patching gotcha" — existing `enrollment_add` tests gain `get_account` patch returning distinct-email account.|V5,V20,V56
T54|x|sweep `remove`-verb handlers ∈ `src/mailpilot/cli.py` (scope grep: `@[a-z_]+\.command\("remove"\)`) → enforce amended §V.5 hard-DELETE branch ∧ §V.47 `changed` semantics. ∀ match: (a) swap `output_entity("<noun>", deleted)` → composite-key (∨ natural-identifier) projection — `enrollment remove` → `{"enrollment": {"workflow_id": wid, "contact_id": cid}, "ok": true}`; `tag remove` → `{"tag": {"name": n, "owner_type": t, "owner_id": id}, "ok": true}`; (b) `operator_event("<noun>.remove", ..., changed=[<relational-key-fields>])` per §V.47 — drop `["status"]` (enrollment) ∧ `["name"]` (tag). Patch `tests/test_cli.py:test_enrollment_remove` ∧ `test_tag_remove` payload assertions. ⊥ silent shape change — bump v0.x release note in same commit per §T.29 precedent.|V5,V20,V47
T55|x|impl §V.57 — add `contact disable` Click cmd in `src/mailpilot/cli.py` between `contact_update` (cli.py:769-821) ∧ `contact_search`. Click registration: `@contact.command("disable")` w/ `@click.argument("contact_id")` ∧ `@click.option("--reason", required=True, help="Explanation written to disabled_reason.")`. Handler: `reason.strip() == ""` → `output_error("reason cannot be empty", "validation_error")` per §V.5; `before = get_contact(connection, contact_id)`; `before is None` → `output_error(f"contact not found: {contact_id}", "not_found")`; under `cli_mutation("contact", "disable", entity_id=contact_id)`: `updated = database.disable_contact(connection, contact_id, reason)`; `changed = ["disabled_reason"] if before.disabled_reason != updated.disabled_reason else []`; `operator_event("contact.disable", entity_id=contact_id, changed=changed)`; `output_entity("contact", updated)`. Amend §V.5 ∧ §V.47 verb sets to include `disable` (mechanical inserts; place after `update` for mutation-group ordering — already applied in same commit). Tests under `tests/test_cli.py` ∧ `tests/test_cli_telemetry.py` per §V.20 `capfire`: (a) happy path — singular envelope w/ Contact (`disabled_reason` non-null) + `cli_mutation` span (`name="contact.disable"`, `entity_id=<id>` attr) + `operator_event("contact.disable", entity_id=<id>, changed=["disabled_reason"])`; (b) empty `--reason` (whitespace-only) → `validation_error` envelope, ⊥ span, ⊥ operator event; (c) missing `--reason` → Click error (`required=True`); (d) non-existent `contact_id` → `not_found` envelope, ⊥ span, ⊥ operator event; (e) idempotent re-disable w/ same `reason` → `changed=[]` ∈ event ∧ DB row unchanged modulo `updated_at` bump (per `database.disable_contact` body); (f) re-disable w/ new `reason` → `changed=["disabled_reason"]` ∧ DB `disabled_reason` reflects new text; (g) DB raise (patch `database.disable_contact` → `psycopg.OperationalError`) → `logfire.exception` + paired `operator_event("error", source="contact.disable", message=...)` per §V.47; (h) `mailpilot contact --help` lists `disable` alongside {create, update, search, list, view, export, import} (Click cmd registration regression guard). ⊥ patches required on existing contact-cmd tests (handler self-contained). PR: link to issue #128.|V5,V20,V47,V57
T56|x|impl §V.6 parent-NI clause for `Workflow` surface — extend `Workflow` (`src/mailpilot/models.py:93-106`) ∧ `WorkflowSummary` (`:109-118`) w/ `account_email: str` field. Patch `get_workflow` (`src/mailpilot/database.py:852`) ∧ `list_workflows` (`:874`) SQL to `JOIN account a ON workflow.account_id = a.id` ∧ project `a.email AS account_email` (INNER JOIN safe per `account_id NOT NULL REFERENCES account(id)` ∈ schema). `list_workflows_full` (`:923`) ⊥ touched — feeds `workflow export` (§V.39) declarative payload keyed on `(account_id, name)` ∴ `account_email` ∉ export shape (round-trip purity). Tests per §V.20: (a) `workflow view` envelope `{"workflow": {..., "account_id": <id>, "account_email": <email>}, "ok": true}`; (b) `workflow list` envelope `{"workflows": [{..., "account_id", "account_email"}, ...], "ok": true}`; (c) `workflow export` payload ⊥ contain `account_email` (regression guard for §V.39); (d) `workflow create` / `update` accept ⊥ `account_email` field (model write-side unaffected — denormalized join column, ⊥ workflow-table column); (e) `workflow list --account-id X` filter unchanged (regression guard — NI ⊥ filter target).|V5,V6,V20,V39
T57|x|schema — add `schema_metadata` table to `src/mailpilot/schema.sql` per §V.58 shape (single-row, `id INT PRIMARY KEY DEFAULT 1 CHECK (id=1)`, `mailpilot_version TEXT NOT NULL`, `schema_hash TEXT NOT NULL`, `applied_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP`). No backfill (`make clean` recreates DB per §T.13 / §T.34 / §T.47 precedent)|V58
T58|x|impl §V.58 + §V.59 — (a) `_compute_schema_hash(sql: str) -> str` helper in `src/mailpilot/database.py` (sha256 of normalized form per §V.59); (b) `SchemaMetadata` Pydantic model in `src/mailpilot/models.py` (`mailpilot_version: str`, `schema_hash: str`, `applied_at: datetime`); (c) `_read_schema_metadata(connection) -> SchemaMetadata | None` in `database.py` (returns `None` when table ∨ row missing — both ≡ legacy / drift); (d) `initialize_database` probe-empty branch — `INSERT INTO schema_metadata (mailpilot_version, schema_hash) VALUES (%s, %s)` after `connection.execute(schema_sql)`; (e) `initialize_database` probe-non-empty branch — read + compare + warn + operator_event (paired per §V.19 pattern); (f) `get_status_counts` extends return dict w/ `schema_metadata` block per §V.58 envelope; CLI `status` handler envelope unchanged otherwise; (g) curated-event registry — add `schema.drift` to `src/mailpilot/operator_log.py` curated set ∧ CLAUDE.md "Curated events" listing; (h) lift `_MAILPILOT_VERSION` module-level constant (currently `gmail.py:62` `importlib.metadata.version("mailpilot")`) ∨ import shared. Tests per §V.20 + `capfire: CaptureLogfire`: (i) write-on-apply — fresh DB → row w/ current version + hash present after init; (ii) drift-trip — pre-seed row w/ stale hash → init → `logfire.warn` + `operator_event("schema.drift", ...)` emitted, ⊥ SystemExit; (iii) normalization-stable — schema.sql w/ added `--` comment ∨ extra whitespace → hash unchanged → ⊥ drift; (iv) normalization-detects-real — schema.sql w/ column-added → hash differs → drift; (v) version-only-silent — same hash, different version → ⊥ warn, ⊥ event; (vi) legacy-DB-drift — drop `schema_metadata` table on initialized DB → init → drift detected (row-missing branch); (vii) status envelope — `mailpilot status` JSON ∋ `schema_metadata` block; drift state ↔ `drift: true` + `current_*` fields|V19,V20,V58,V59
T59|x|impl §V.60 + §V.58(+) status payload reshape. (a) `src/mailpilot/database.py` — rename `get_status_counts` → `get_status_payload(connection, settings) -> dict[str, object]`; queries: `schema_metadata` fetch (existing), `sync_status` row (existing), per-account `SELECT id, email, (disabled_reason IS NOT NULL) AS disabled, gmail_history_id, watch_expiration, last_synced_at, EXTRACT(EPOCH FROM (now() - last_synced_at))::int AS last_synced_age_seconds, (EXTRACT(EPOCH FROM (watch_expiration - now()))::int / 3600) AS watch_expires_in_hours FROM account ORDER BY email`, task aggregates (single CTE: pending count, failed_24h count, scheduled_future count, oldest_pending_age_seconds, max attempt_count pending), entity counts (existing minus `accounts`). (b) `_scrub_database_url(url: str) -> str` helper via `urllib.parse.urlsplit` → reconstruct `scheme://host[:port]/db` (drop netloc userinfo). (c) `src/mailpilot/cli.py` `status` handler loads `Settings` via `get_settings()`, passes to `get_status_payload`, emits `{"status": payload}` per §V.5. Tests per §V.20: (i) fresh DB → `accounts: []` ∧ zero task aggregates; (ii) account `last_synced_at = now() - 5min` → `last_synced_age_seconds` ∈ [298, 302]; (iii) `disabled_reason` set → `disabled: true`; (iv) pending past → `oldest_pending_age_seconds` non-null; (v) pending future → counted ∈ `scheduled_future`, ⊥ ∈ `oldest_pending_age_seconds`; (vi) failed `completed_at = now() - 23h` → `failed_24h: 1`; same row aged to 25h → `0`; (vii) `anthropic_api_key` set → `anthropic_api_key_set: true`; unset → `false`; (viii) `database_url = "postgresql://user:pass@host:5432/db"` → echoed `"postgresql://host:5432/db"`; (ix) pre-seeded stale hash → `schema.drift: true` ∧ top-level `version` ≡ `_MAILPILOT_VERSION`. ⊥ backwards compat — release-note bump in same commit per §T.29 / §T.54 precedent.|V5,V20,V58,V60
T60|x|impl §V.61 + §V.44 amend + §B.34 fix per `designs/drive-parallel-tool-race.md`. (a) `src/mailpilot/agent/templates.py:156-158` — annotate `_DRIVE` Tool entries w/ `sequential=True` (`list_drive_markdown`, `read_drive_markdown`, `search_drive_markdown`); dispatcher serializes parallel emissions on these tools, non-Drive tools keep parallel dispatch. (b) `src/mailpilot/agent/tools.py:419-514` — broaden catch envelopes on all 3 Drive tools to `(HttpError, socket.timeout, TimeoutError, OSError)` returning `{"error": "drive_unavailable", "message": str(exc)}`; preserve existing `not_found` branch on `HttpError` 404 in `read_drive_markdown`. (c) Contract test (`tests/test_agent_templates.py` ∨ new `tests/test_agent_drive_concurrency.py`) — assert every Drive `Tool` instance in `TEMPLATES["inbound-google-drive"].tools` carries `sequential is True`; anchored to §V.61. (d) Behavioral test — stub `httplib2.Http` whose `request()` raises `RuntimeError("concurrent http reuse")` when invoked while another `request()` is in flight on same instance; bind `DriveClient.from_service` against stub; run `agent.run` w/ forced parallel `read_drive_markdown` fan-out; pre-fix `RuntimeError` (race repro), post-fix both calls succeed (dispatcher serializes). (e) `/smoke-test` extension — `.claude/skills/smoke-test/SKILL.md` adds gate **B6.5 (concurrent-fanout)** between B6 ∧ B7: Phase 0 generates `SUBJECT_B3` ∧ verifies all 4 subjects (`SUBJECT_A`, `SUBJECT_B1`, `SUBJECT_B2`, `SUBJECT_B3`) distinct; scenario B sends 3 sequential emails (B1 in-scope single-source, B2 out-of-scope decline, B3 in-scope multi-source compare-and-contrast). `.claude/skills/smoke-test/scripts/qa_pairs.json` — add `multi-source` pair type w/ `source_files: [a.md, b.md]`; ≥1 compare-and-contrast pair across 2 existing answer-bearing seeds (e.g. `pure-aqua-commercial-ro-systems.md` ∧ `pure-aqua-industrial-water-softener.md`). `.claude/skills/smoke-test/scripts/qa.py pick --type multisource` selector + `.claude/skills/smoke-test/scripts/generate_qa_pairs.py` extension for compare-and-contrast generation. Gate B6.5 strict-overlap assertion (≡ #130 regression signal): Logfire shows ≥2 `read_drive_markdown` tool spans within same `agent.invoke` whose wall-clock `[start, end]` overlap (`start(span_2) < end(span_1)`); pre-fix gate fails (one span hangs ≥60s), post-fix both spans <1s w/ overlap. Logfire review for Scenario B — expected `read_drive_markdown` count = 2+ for B6.5; single-call result = Bug. PR: #130|V20,V43,V44,V61
T61|x|impl §V.62 — replace sequential drain in `_drain_pending_tasks` (`src/mailpilot/sync.py:418-431`) w/ concurrent worker pool. (a) `src/mailpilot/settings.py` — add `max_concurrent_tasks: int = 4`. (b) `src/mailpilot/sync.py:_drain_pending_tasks` — refactor: `with concurrent.futures.ThreadPoolExecutor(max_workers=settings.max_concurrent_tasks) as pool: futures = [pool.submit(_execute_task_in_worker, settings, task) for task in pending]; for fut in concurrent.futures.as_completed(futures): fut.result()`. (c) `_execute_task_in_worker(settings, task) -> None` — opens its own `psycopg.Connection` via `settings.connection()` context manager, calls `execute_task(connection, settings, task)`. (d) Per-task exceptions caught ∧ logged so sibling futures continue; classification path stays ∈ `execute_task` (§V.44 retry preserved). (e) `task.drain` operator_event aggregates over ∀ futures (`drained=len(futures)`, `duration_ms` from outer-pool wall-clock). Tests per §V.20: (i) `capfire`: 2 pending tasks → 2 `agent.invoke` spans w/ overlapping wall-clock intervals (`start(later) < end(earlier)`); (ii) regression-guard `max_concurrent_tasks=1` → strict sequential preserved (span#2.start >= span#1.end); (iii) 5 pending tasks, max_workers=4 → 4 concurrent + 1 queued, `task.drain` event aggregates correctly; (iv) per-worker `psycopg.Connection` isolation — 2 workers ∈ same drain ⊥ share transaction state (concurrent INSERT into `activity` from each → ⊥ FK violations, both rows persist). Smoke-test follow-up: re-run B7.5 → both `agent.invoke` spans overlap ∧ both replies arrive within 60s SLA from `T_SEND_B75`.|V20,V44,V61,V62
T62|x|widen `_check_spec_table` (`src/mailpilot/agent/tools.py:50`) per amended §V.29 — replace cluster-row regex `^[^|]+?\s{2,}\S` → `^[^|]{1,80}\s+\S.*$` (drop `\s{2,}` floor → `\s+`; add line-length cap `{1,80}`). Tests cover {single-space `**label** value` cluster reject (regression for §B.36), original 2-space `key  value` cluster still reject (regression for §B.9 / §B.14), numeric specs reject, non-numeric specs reject (KDF `Mesh Size  20×50`-shape), prose paragraph w/ incidental short line ⊥ false-positive, prose w/ multi-clause line ≥80 chars ∧ multiple spaces ⊥ trigger, valid pipe-table pass, decline body pass, ASCII rule-line ⊥ pass-through as separator}. Smoke-test follow-up: re-run B4 / B7 → `reply_email` span returns `error=format` if agent reverts to single-space cluster render; verifier extension flags.|V20,V29
T63|x|impl §V.11(+) — extend `agent.invoke` span construction (`src/mailpilot/agent/invoke.py:540-545`) to `set_attribute("email_id", ...)` when `trigger='email'` (∃ `email` arg) ∨ `trigger='task'` ∧ `task.email_id IS NOT NULL`. Wire from existing caller args (`email: Email \| None`, `task: Task \| None` ∈ `invoke_workflow_agent` sig). Tests per §V.20 via `capfire`: (a) `trigger='email'` w/ email arg → span `email_id == email.id`; (b) `trigger='task'` w/ task.email_id non-null → span `email_id == task.email_id`; (c) `trigger='task'` w/ task.email_id null → span ⊥ carry `email_id` attr; (d) `trigger='enrollment_run'` → span ⊥ carry `email_id`; (e) regression: existing trigger / workflow_id / contact_id / workflow_type attrs unchanged. Smoke-test follow-up: re-run B7.5 → 2 `agent.invoke` spans each w/ distinct `email_id` ∴ gate SQL `GROUP BY email_id` returns 2 rows|V11,V20
T64|x|impl §V.63 — refactor test-skill gates w/ span-backed latency observables to derive primary verdict from Logfire MCP. Scope vocab table: {smoke-test B4 in-scope reply latency, smoke-test B7 out-scope decline latency, smoke-test B7.5 concurrent-send dual-reply latency, demo-test G1 production reply round-trip}. Per gate: (a) replace `mailpilot email list` poll-loop as latency source w/ Logfire `query_run` against `agent.invoke` span filtered by `email_id=<id>` (post §T.63) ∧ `deployment_environment='<env>'` per §V.22; (b) latency = `end_timestamp - T_SEND` (T_SEND captured pre-`email send`); (c) CLI poll retained as `did-round-trip?` check w/ cap raised to ≥120s; (d) SLA threshold stays 60s per existing prose. Update `.claude/skills/smoke-test/SKILL.md` ∧ `.claude/skills/demo-test/SKILL.md` gate prose. ⊥ regression on §V.45 G2 (operator-judged) ∨ G3 (Logfire-verified)|V20,V22,V45,V63
T65|x|impl §V.51(+) — add `gmail_thread_id: str \| None` field to `EmailSummary` (`src/mailpilot/models.py:225-238`); extend `list_emails` SELECT (`src/mailpilot/database.py`) to project `gmail_thread_id`. Tests per §V.20: (a) `mailpilot email list` JSON envelope rows ∋ `gmail_thread_id` (populated for synced inbound/sent, null for queued-not-yet-sent outbound); (b) regression: `email view` shape unchanged; `ContactSummary` / `TaskSummary` unchanged. Smoke-test follow-up: rewrite A4 to read `gmail_thread_id` from `email list` output directly ⊥ round-trip via `email view`|V20,V51
T66|x|impl §V.31(+) — extend `qa_pairs.json` schema w/ optional `source_file_alts: list[str]` on `in_scope` entries; update `.claude/skills/smoke-test/scripts/qa.py source --id ID` to resolve primary `source_file` ∨ fall back to first alt when absent; update `.claude/skills/smoke-test/scripts/generate_qa_pairs.py` to detect cross-source identifier collisions ∧ populate `source_file_alts`. Add explicit `source_file_alts: ["pure-aqua-industrial-water-softener.md", "pure-aqua-sf-100s-industrial-water-softener.md"]` entry to qa-in-022 (WS36-600-2). Update B4 verifier prose in `.claude/skills/smoke-test/SKILL.md` to compute `cites_source_file` over set union per amended §V.31. Tests: (a) `qa.py source --id qa-in-022` returns ≥1 source's content; (b) verdict schema accepts `source_file_alts` field; (c) regression: pairs w/o alts default `[]`|V20,V31
T67|.|impl §V.64 — decouple `_drain_pending_tasks` from main-loop tick. `src/mailpilot/sync.py:_drain_pending_tasks` (lines 420-445) currently uses `with concurrent.futures.ThreadPoolExecutor(...) as pool: futures = [pool.submit(...)]; for fut in concurrent.futures.as_completed(futures): fut.result()` — `with` block exits via `pool.shutdown(wait=True)` ∴ main loop blocks until all in-flight `agent.invoke` futures complete. Refactor: hoist `ThreadPoolExecutor(max_workers=settings.max_concurrent_tasks)` ownership to `start_sync_loop` (`src/mailpilot/sync.py`) ∴ pool outlives a single drain tick; `_drain_pending_tasks(connection, settings, pool, in_flight)` accepts injected pool ∧ `in_flight: set[Future]` tracker, submits new futures, returns immediately w/o `as_completed`. Between sync ticks, main loop opportunistically reaps completed futures from `in_flight` via non-blocking `Future.done()` check ∧ emits `task.drain` operator_event w/ aggregated `drained` ∧ `duration_ms`. Pool lifecycle: created in `start_sync_loop` setup, shut down on SIGTERM w/ `wait=True` so in-flight futures drain cleanly. Per-worker `psycopg.Connection` isolation (§V.62) preserved — `_execute_task_in_worker` unchanged. Wall-clock contract per §V.64: post-`pubsub.notify` → `sync.account.run.start` ≤ 5s regardless of in-flight `agent.invoke` count. Tests per §V.20: (a) `capfire` — N pending tasks dispatched, main loop returns from `_drain_pending_tasks` < 100ms regardless of agent.invoke duration (mock long-running `execute_task`); (b) `pubsub.notify` arrival during in-flight drain → next `sync.account.run` starts ≤ 5s from notify; (c) SIGTERM during in-flight drain → all futures complete before process exit (no orphan threads); (d) `task.drain` operator_event still emitted w/ accurate `drained` ∧ `duration_ms` (aggregation point shifted to lazy reaper); (e) regression: `max_concurrent_tasks=1` preserves bounded concurrency ∧ per-worker connection isolation unchanged. Smoke-test follow-up: re-run B7.5 → 2 `agent.invoke` spans overlap (currently strictly sequential per §B.41) ∧ LATENCY_B75b ≤ 60s; re-run B7 → pre-agent overhead ≤ 15s ∴ end-to-end ≤ 60s.|V20,V62,V64

## §B BUGS

id|date|cause|fix
B1|2026-05-01|3 logfire.exception sites in `run.py:199`, `pubsub.py:203`, `pubsub.py:286` reachable from `mailpilot run` ⊥ paired w/ `operator_event("error", ...)` → operator stderr stream under journald has gaps where exceptions occur. Surfaced by `/sdd:check` post-rebuild on 2026-05-01|V19
B2|2026-05-02|smoke-test out-scope QA verifier flags digits from echoed user question as fabrications (qa-out-005 Veolia OPUS II — agent declined politely w/ ⊥ specs, verifier still failed gate B6). Surfaced by smoke run|V25
B3|2026-05-02|`running tool` span ⊥ structured `tool_name` attribute; identity only in free-text `message`. Logfire dashboards ! parse prose. Surfaced by smoke run|V26
B4|2026-05-02|smoke-run A observed 2 `skipped_no_workflows` spans during A's window. Initial diagnosis ("duplicate routing pass") wrong. Logfire evidence 2026-05-03: spans came from 2 distinct `email_id`s — `019deb87-f336-...` (pre-existing inbox msg picked up by initial sync sweep post `make clean`) & `019deb88-4382-...` (A4 delivery). Both routed exactly once: `_store_inbound_message` (`sync.py:769`) uses `INSERT ON CONFLICT DO NOTHING RETURNING *` → duplicate inserts exit early; `route_email` (`routing.py:87`) short-circuits on `is_routed=True`. ∴ §V.27 already holds; §T.6 superseded|V27
B5|2026-05-02|demo workflow B1 reply rendered spec sheet w/ asterisks instead of pipe-table despite "plain Markdown" instructions — prompt-fidelity drift on output style|V29
B6|2026-05-03|B3 misdiagnosed: `running tool` span did expose structured tool identity — Pydantic AI's `instrument_pydantic_ai()` populates `gen_ai.tool.name` (OTel GenAI convention). Smoke-test Logfire query asked for `attributes->>'tool_name'` (null) & missed `gen_ai.tool.name` (populated on all 13 spans). ∴ V26 amended to cite canonical attribute|V26
B7|2026-05-03|smoke-run B4 in-scope question targeted `WS36-600-2` in `pure-aqua-sf-100s-industrial-water-softener.md` (65 GPM). Agent grounded to broader `pure-aqua-industrial-water-softener.md` (110 GPM) — both files match search query, agent read only top hit. Cause: `_SYSTEM_PREFIX` (`src/mailpilot/agent/invoke.py:350`) "Read the top matches" & demo workflow instr "Pick the top relevant hit" → singular collapse, N=1 read, wrong doc grounded|V28
B8|2026-05-03|smoke-test B4 in-scope grounding gate over-relied on substring match of `expected_tokens` from `qa_pairs.json` — passed only when agent phrasing matched curated tokens char-for-char, false-negatives on semantically correct replies (e.g., `0.48mm` vs curated `0.48 mm`). §V.25 digit-echo exclusion = 2nd regex band-aid for same brittleness class. Cause: gate confused 'tokens-present' (structural) w/ 'reply-grounded-in-source' (semantic). ∴ B4 inscope grading delegated to operator w/ source doc loaded & structured verdict naming unsupported claims; B6 outscope decline kept script-based (regex fits shape-detection)|V31
B9|2026-05-03|smoke-run B4 (qa-in-026) reply rendered spec sheet as 5 space-separated `<label>  <number>` lines (`Continuous Flow Rate 110 GPM` …) ⊥ pipe-table syntax despite explicit `| Specification | Value | / |---|---|` instruction in `.claude/skills/smoke-test/SKILL.md:321`. Same drift as §B.5 — prompt-only fix (§T.7) ⊥ hold. ∴ §V.29 = tool-level lint required|V29
B10|2026-05-03|smoke-run A3 first attempt: outbound workflow `Outbound Smoke A` (instructions ⊥ Drive folder ref) triggered agent to call `search_drive_markdown` 5x then refuse to send — `tool_call_count=5` wasted, agent reasoning "No knowledge base documents were found … unable to fabricate". Cause: Drive tools unconditionally registered in `_TOOLS` (`src/mailpilot/agent/invoke.py:331-333`); `_SYSTEM_PREFIX:345` "if workflow references…" prompt-only gate ⊥ structural. Workaround: amended workflow.instructions to explicit "do NOT call list_drive_markdown / search_drive_markdown / read_drive_markdown". ∴ §V.30 = structural tool-registration gate|V30
B11|2026-05-04|smoke-run 2026-05-04 confirmed inbound trigger email inlined twice in agent prompt across A7 (chat 873a8544df5f2b3d), B4 (chat e2c9c7f53aa1efa5), B6 (chat 28e3a6609321f4b3) — same `email_id` & full body in both `Email history` & `New inbound email:` sections. Cause: `_build_user_prompt` (`src/mailpilot/agent/invoke.py:545-552`) passes unfiltered `email_history` from `database.list_emails(contact_id, account_id, workflow_id)` (which includes the trigger row) alongside the trigger to `_format_trigger`; ⊥ dedupe step. Token waste scales w/ body length & thread depth; misleading `Email history (1 messages)` count on fresh threads; risk of model treating same email as 2 distinct events|V35
B12|2026-05-04|smoke-run 2026-05-04 A3 first chat 6a8a97c4bf1878f6: prompt rendered `Deferred task:\nDescription: manual outbound run` despite invocation being operator-initiated `mailpilot enrollment run` (foreground synchronous initial reach-out, not drained task). Cause: `cli.py:1841` synthesises `description = f"manual {wf.type} run"` & passes as `task_description`; `_format_trigger` (`src/mailpilot/agent/invoke.py:356-374`) branches on `task_description` truthiness w/ ⊥ awareness of caller's `trigger` label (`enrollment_run` vs `task`). Span `trigger` attribute (§V.11) correctly = `enrollment_run` ∴ prompt-side framing alone out of step w/ span-side label. Behaviour benign on this run but framing risks resumption-shaped reasoning at scale|V36
B13|2026-05-08|`company export` (`cli.py:486`), `company import` (`cli.py:505`), `contact export` (`cli.py:674`), `contact import` (`cli.py:693`) emit `{"exported"|"imported": N, "file": F}` envelope ⊥ list envelope `{"<plural>": [...], "ok": true}` per §V.5. Pre-existing — cmds shipped before §V.5 enumerated `export|import` verbs. Surfaced by `/sdd:check --all` post issue-119 amendment 2026-05-08 (V5 expanded to bind `export|import` → list envelope). Recurrence-class caught by amended §V.5 itself — ⊥ new invariant required|V5
B14|2026-05-09|smoke-run 2026-05-09 B4 (email `019e0a94-0695-7005-825d-abcfae801ec8`, `kdf-process-filtration-media.md`): reply rendered specs as paragraphs of space-aligned `<label>  <value>` rows separated by `------------------------------` ASCII rule-lines, ⊥ Markdown pipe-table. §V.29 lint silent — `_SPEC_ROW_RE` (`src/mailpilot/agent/tools.py:46`) required numeric-shape values (`[\$]?[\d,]+`); KDF spec rows include non-numeric values (`Mesh Size  20×50`, `Type  Granular`) ∴ ⊥ matched. Same drift class as §B.5, §B.9 post prompt-only fix (§T.7) ∧ post numeric-tier lint (§T.9). ∴ §V.29 widened: any `key  value` cluster + reject ASCII rule-line faux-separator|V29
B15|2026-05-10|`mailpilot.service` on GCE instance `mailpilot-1.lab5-mailpilot-prd1` crash-restart-looped 46+ iters after deploy w/ ⊥ `google_application_credentials` setting (operator expected GCE-attached SA via ADC). `gmail.resolve_credentials_path` raised SystemExit when both setting & `GOOGLE_APPLICATION_CREDENTIALS` env empty → ADC never tried; `GmailClient(email)` ctor → `build_gmail_service` → exit. Same blind spot in `drive.py:build_drive_service` (used same resolver), `pubsub.py:_load_credentials` & `_resolve_project_id` (read JSON file directly), `sync.py` Pub/Sub start gate & watch-renewal gate (raw setting truthiness). Fix removed `resolve_credentials_path`, added `gmail.build_delegated_credentials` (file → `with_subject` OR ADC + `iam.Signer` JWT signing), `gmail.build_default_credentials` (file OR ADC), `gmail.has_google_credentials()`, `gmail.resolve_project_id()`; rerouted Drive, Pub/Sub, sync gates through helpers|V42
B16|2026-05-10|2 prod `TimeoutError` events ("The read operation timed out", 60s read window) on `agent.invoke` (`src/mailpilot/agent/invoke.py` → `run.py:117`) bubbled to `run.task.agent_failed` (`run.py:128`) → `complete_task(... status="failed")` (`run.py:134`); event A: 04:18 UTC, event B: 04:45 UTC on task `019e1034-6375-710d-a223-61bde1d8cfb8` (inbound workflow `019e0e49-ea2c-72d4-a675-f96dba4e83bd`, 6248 input + 308 output tokens, $0.0198, 1344-char prompt). Event B trace: `search_drive_markdown` → `read_drive_markdown` (13KB `pure-aqua-industrial-water-softener.md`) → 60s stall on next chat completion → timeout. Bare `except Exception` @ `run.py:127` ⊥ retry, ⊥ classify exception → transient LLM API flake = permanently lost work, manual replay only. Default `httpx` read-timeout 60s intersects long-context Anthropic latency under load. Co-observed (⊥ backpropped): 407 pre-§V.42 SystemExit (§B.15, fixed by 04:30 UTC daemon restart), 2 dev `psycopg.OperationalError`, 2 dev `pydantic_ai.exceptions.ContentFilterError`|V43,V44
B17|2026-05-11|§V.44 mandated `mailpilot task retry --task-id <ID>` (impl `cli.py:2389`) but §I.cli verb enumeration `{list|search|view|create|update|add|remove|reply|send|start|stop|cancel|run|export|import}` ⊥ updated to include `retry` when V44 row landed. EXTRA-class drift (code-exposes-verb-not-in-§I) surfaced by `/sdd:check --all` on 2026-05-11. Same recurrence class as §B.13 (pre-§V.5 export/import verbs absent from §I before §V.5 amendment enumerated them): §V row adds verb-shape obligation but §I list-shape ⊥ synced. ∴ recurrence caught by §I list-shape audit sub-recipe (enumerate every list-field in §I, set-diff against code symbol table); ⊥ new invariant required|V5
B18|2026-05-11|`mailpilot account sync` cmd exposed (`src/mailpilot/cli.py:355`); verb `sync` ∉ §I.cli verb pool. EXTRA-class drift surfaced by `/sdd:check --all` on 2026-05-11 — first catch by §B.17.fix's named "§I list-shape audit sub-recipe (enumerate every list-field in §I, set-diff against code symbol table)". Same recurrence class as §B.13 (pre-§V.5 export/import absent from §I) & §B.17 (retry absent from §I when §V.44 landed): code adds a verb, §I list-field ⊥ synced. ⊥ new invariant required — recurrence already attributed to §V.5 verb-pool obligation; fix = amend §I.cli verb list to include `sync`|V5
B19|2026-05-11|`mailpilot account sync` (`src/mailpilot/cli.py:421`) emits `{"results": [...], "total_stored": N, "ok": true}`; `total_stored` is an inline top-level entity-field forbidden by §V.5 ("⊥ inline entity fields at top level"). Pre-existing — cmd shipped before §V.5 enumerated `sync` as plural-envelope verb. Surfaced by `/sdd:check --all` cascade after `amend §I.cli + §V.5(+): add sync verb` (commit e5058f4) on 2026-05-11. Same recurrence-class as §B.13 (pre-§V.5 export/import shape drift surfaced by §V.5 verb-pool expansion): code envelope diverges from §V.5 ∴ verb-pool amend reveals violation. Recurrence-class caught by amended §V.5 itself — ⊥ new invariant required|V5
B20|2026-05-11|`_sync_all_accounts` (`src/mailpilot/run.py:185`) orphan post-§T.35 closure. §T.35 task text directed "Keep `_sync_all_accounts` (still called by `sync.py:423`, `start_sync_loop` plumbing)" — but `sync.py:423` calls `execute_task`, not `_sync_all_accounts`; `sync.py` has its own `_sync_all_accounts` @ `sync.py:388`. T35 author conflated two same-named fns ∴ run.py copy retained as dead code. basedpyright `reportUnusedFunction` warning surfaced but treated as pre-existing noise class during T35 cascade. Surfaced by `/sdd:check --all` cascade after T35 closure on 2026-05-11. Recurrence class = mis-attributed caller text in §T "keep" lists; catch = grep-verify caller list before retaining fn names; basedpyright already surfaces orphan post hoc. ⊥ new invariant required (warning-class observation, ⊥ runtime breach)|-
B21|2026-05-11|smoke-test skill (`.claude/skills/smoke-test/SKILL.md`) gates A8 ∧ B7 prose claim `enrollment_added.detail.workflow_id == <WORKFLOW_ID>` ∧ cite 'ADR-08' for `enrollment_*` activity vocabulary. Runtime `activity.detail` for `enrollment_added` rows stores `{"workflow_name": "<wf_name>"}` — no `workflow_id` field; `find . -iname 'ADR-08*'` returns no results; ⊥ `docs/` dir ∈ repo. Smoke-run 2026-05-11 (`smoke-test-2026-05-11-150600.md`) Bug 1: gate passed strict-form check (type exists) but field-shape ∧ ADR-08 citation = stale prose written against architecture never committed. Recurrence class = spec-adjacent doc text drifts from runtime when ⊥ §V binds the claim (same shape as §B.20 warning-class observation). Same report's proposed Invariant 2 ("activity.detail MUST carry workflow_id / email_id") premised on factual error — `activity` schema (`src/mailpilot/schema.sql:103`) has dedicated FK columns `email_id`, `workflow_id`, `task_id` at row level; ∀ emit site (`email_ops.py:84`, `cli.py:2069`, `sync.py:791`) passes them as top-level columns. `detail` JSONB ≡ display-label payload (workflow_name, subject) by design; FK columns ≡ canonical join surface. Fix = (a) drop ADR-08 references from skill prose, (b) redirect gate prose → check `activity.workflow_id` (FK column, correct join surface) ⊥ `detail.workflow_id` (would be redundant duplication of column-as-JSONB-key), (c) ⊥ new §V required (warning-class doc drift + schema reality already supports stable joins). Surfaced by `/smoke-test` Phase 5 inspection; refined by `/sdd:check --all` AMEND-gate fact-check 2026-05-11|-
B22|2026-05-11|smoke-test skill (`.claude/skills/smoke-test/SKILL.md`) L37 introduces envelope-shape rule prefixed `(SPEC §V13)` — §V.13 governs inbound task-creation filter, ⊥ envelope shape (§V.5; same line later cites `§V.5 plural envelope` correctly ∴ writer-intent = §V.5). L37, L161, L272 use dotless `§V13 / §V12 / §T18 / §B2`; `/sdd:check` §-cite regex `§[VTB]\.[0-9]+` silently skips dotless → audit blind-spot. Two recurrence sub-classes: (a) spec-adjacent prose drifts from §V semantics (= §B.21 stale ADR-08, §B.20 mis-attributed text); (b) cite-shape malformation evades auditor regex. Surfaced by `/sdd:check` cite-resolution pass on 2026-05-11. Fix = (a) L37 `§V13` → `§V.5`; (b) L161 `§V12 / §T18 / §B2` → `§V.12 / §T.18 / §B.2`; (c) L272 `§V12 / §T18` → `§V.12 / §T.18`. ⊥ new §V — sub-class (a) ≡ warning-class doc drift (§B.21 precedent); sub-class (b) ≡ check-skill regex tooling concern, ⊥ project-level invariant|-
B23|2026-05-11|`/demo-test` G1 (§V.45) polled `mailpilot email list --account-id <OUTBOUND> --direction inbound --since <TEST_START>` for 60s ∧ found nothing; §V.45 setup explicitly forbids `mailpilot run` ∧ G1 procedure ⊥ trigger sync → local DB stayed empty full poll window even though production replied promptly (manual `mailpilot account sync` post-poll surfaced reply immediately; G3 production-env Logfire spans all green for same window: `agent.invoke trigger='task'`=1, `gen_ai.tool.name='search_drive_markdown'`=1, `gmail.send_message`=1, errors=0). Probe nominally checked "did production reply?" but G1 implementation routed through local mirror state ∴ false-FAIL on healthy prod. Recurrence class: liveness probes checking production-facing claims via local mirror DB. Fix = §V.46 + amend §V.45 G1 (query Gmail directly via `GmailClient.list_messages` impersonating `outbound@lab5.ca` per §V.42) + §T.40 impl|V46
B24|2026-05-12|`/sdd:check` cite-resolution pass 2026-05-12 found §B.22 recurrence in CODE: `src/mailpilot/cli.py:79` docstring cites `§V13` for envelope shape (governed by §V.5 ∴ wrong-section) ∧ dotless form evades auditor regex; `src/mailpilot/agent/invoke.py:523` cites `§V11` (target §V.11 semantically correct, dotless only). §B.22 fix (§T.39) patched skill prose only — code-side cites slipped. Same two sub-classes as §B.22: (a) prose-drift wrong-section, (b) cite-shape malformation. §V.48 (+) binds sub-class (b) at project level so future recurrence catchable mechanically.|V48
B25|2026-05-12|smoke-run 2026-05-12 A8: outbound workflow agent called `record_enrollment_outcome` inside `agent.invoke(trigger=enrollment_run)` @ 12:20:32 UTC (reason "Initial outbound email about polemarch inflected successfully sent... Awaiting reply to determine interest.") then again inside `agent.invoke(trigger=task)` @ 12:21:26 UTC (reason "Contact explicitly declined") → 2 terminal activity rows (`enrollment_completed` + `enrollment_failed`) for 1 enrollment (contact `019e1c20-cb5b-724a-921a-2042ee9cd66c`, workflow `019e1c21-20a8-733a-86da-c0cf7f196fa8`). Cause: `_DEFERRED_TASK` protocol fragment (`src/mailpilot/agent/templates.py:69-74`) "After completing the workflow objective for a contact, call record_enrollment_outcome with outcome='completed'" ⊥ distinguish first-reach-out (no reply yet, outcome ⊥ knowable) from reply-handling (drained task, outcome IS knowable); agent treats "sent requested email" as objective-completion ∴ premature outcome. Fixture text "When you receive a reply...call record_enrollment_outcome" appended after `_DEFERRED_TASK` per §V.33 ordering ⊥ override template fragment in practice. Recurrence class = template-protocol fragment over-fires terminal tool when `trigger` semantics ignored; per-fixture override ⊥ reliable ∴ gate ! structural at protocol/template layer|V49
B26|2026-05-12|`/sdd:check --all` 2026-05-12 found §B.24 recurrence in `tests/`: §T.42 sweep scope (`src/ .claude/ CLAUDE.md SPEC.md`) omitted `tests/`; §V.48 binds "code comments, docstrings" ∴ `tests/` ∈ scope. 3 dotless §-cites: `tests/test_cli.py:5817` `§V13` (wrong-section: envelope shape ≡ §V.5, ⊥ §V.13 inbound-task filter), `tests/test_agent_invoke_trigger.py:3` `§V12` (wrong-section: trigger span attr ≡ §V.11, ⊥ §V.12 routing pipeline), `tests/test_pubsub.py:255` `§T13` (UNRESOLVED: §T.13 = workflow.template schema task, no §T row covers Logfire row-collapse claim). Same shape as §B.24 (post-§T.39 skill-only patch) — sub-classes (a) wrong-section + (b) dotless. Recurrence caught by existing §V.48 ∴ ⊥ new invariant; fix = code-only patch (test prose) + §T.45 extends sweep scope to `tests/`.|V48
B27|2026-05-12|smoke-run 2026-05-12 A8 + B7: `mailpilot activity list` JSON omits `workflow_id`, `email_id`, `task_id`, ∧ `detail` columns. Cause: `list_activities` (`src/mailpilot/database.py:2308-2313`) SELECT projects only `id, contact_id, company_id, type, summary, created_at` → `ActivitySummary` (`src/mailpilot/models.py:323-331`) ⊥ declare FK columns ∴ CLI list response strips them; DB rows populate FKs correctly (`create_activity` `src/mailpilot/database.py:2213-2262` ∧ all callers pass FKs). `Activity` model docstring (`models.py:306-308`) states FK columns exist "to let reports join activity to source records without parsing detail JSON" — but the only public list endpoint forces fallback to parsing `detail.workflow_name` (display label, ⊥ FK). Recurrence class = `<Entity>Summary` projection drops FK columns the schema declares as join surface; §V.6 enumerates allowed Summary fields w/o explicit FK-binding ∴ §V.51(+). Smoke-test gates A8 ∧ B7 fall back to `psql` for `activity.workflow_id` assertions, contradicting model docstring contract. `EmailSummary` ∧ `TaskSummary` already include FKs ∴ `ActivitySummary` ≡ sole regression at amend time.|V51
B28|2026-05-12|smoke-run 2026-05-12 A3: outbound first-invoke `agent.invoke` (`trigger=enrollment_run`, workflow `019e1eb4-e5c5-705d-8e96-f70561ae427c`, 4483 input_tokens) emitted `cache_creation_input_tokens=0` ∧ `cache_read_input_tokens=0` (cold + ⊥ write). A7 next outbound invoke (same workflow, `trigger=task`, reply-handling) emitted `cache_creation_input_tokens=2282`. Inbound B4 ∧ B6 (`inbound-google-drive` template) wrote ∧ read caches normally (B4 cache_read=9987/41809, B6 cache_read=9543/12966) ∴ Anthropic-side ⊥ at fault. Config @ `src/mailpilot/agent/invoke.py:367-370` sets `AnthropicModelSettings(anthropic_cache_tool_definitions=True, anthropic_cache_instructions=True)` per §V.37; rollup-span bubble @ `invoke.py:635-636` records both attrs. ∴ violation cause ∈ runtime path between Pydantic-AI ∧ Anthropic, ⊥ missing config. Root cause TBD — candidates: (a) `outbound-general` cacheable prefix (instructions + tool definitions, ⊥ user prompt) falls below Anthropic's 1024-token write threshold on A3's first-touch path → silent skip vs A7's slightly different prefix shape from prior turn machinery; (b) `_format_trigger` `trigger=enrollment_run` branch (§V.36) writes "First reach-out" framing into a prompt position that bypasses cache_control breakpoint placement; (c) Pydantic-AI `anthropic_cache_instructions=True` interacts w/ zero-history first-invoke state differently than multi-turn task-driven invokes. §V.52 (+) binds telemetric outcome → future smoke runs catch class mechanically. Fix: reproduce locally w/ request-body capture, pick remedy from (a/b/c); ⊥ §T row yet — investigation, ⊥ behaviour change|V52
B29|2026-05-14|`mailpilot contact import` (`src/mailpilot/cli.py:920`) invoked w/o `--file` from interactive shell hangs silently on `sys.stdin.read()` waiting for EOF — ⊥ prompt, ⊥ error, indistinguishable from a frozen process. Root cause: `raw = pathlib.Path(file).read_text() if file else sys.stdin.read()` lacks `sys.stdin.isatty()` guard ∴ TTY stdin → block on read. Same shape at `cli.py:632` (`company import`) ∧ `cli.py:2249` (`workflow import`) → recurrence-class across all 3 stdin-fallback import cmds. Surfaced by operator running `mailpilot contact import` interactively 2026-05-14. Recurrence caught by §V.54(+) isatty-guard obligation binding the surface uniformly; sweep impl per §T.49.|V54
B30|2026-05-15|`/sdd:check --all` 2026-05-15 found §V.48 sub-class (b) recurrence in code prose: 12 bare-form `V<n>` cites (no § prefix) in `src/mailpilot/agent/invoke.py:328` ("per V49"), `:344` ("V37 cache_control + V43 read-timeout"), `:402` ("V36:"), `:451` ("V35:"), `:632` ("V37:"); `src/mailpilot/agent/templates.py:12-13` ("SPEC.md sections V32 ... V33 ... V34"), `:53` ("per V33"), `:65` ("per V49"); `src/mailpilot/agent/classify.py:60` ("V37:"), `:64` ("V43:"); `src/mailpilot/agent/retry.py:92` ("V44 allow-list"), `:94` ("V43 exclusion"). Same shape as §B.22/§B.24/§B.26 sub-class (b) "cite-shape malformation evades auditor regex" — §V.48 strictly bans dotless `§V<n>` (with §) but bare-form `V<n>` (no §) is the next workaround surface. Inconsistent within single file: `src/mailpilot/agent/retry.py` uses canonical `§V.43`/`§V.44` at lines 1, 8, 44, 51, 76, 86 but bare `V43`/`V44` at 92, 94. Recurrence caught by §V.48 amend (bare-form clause) ∴ ⊥ new §V; fix = code-only prose patch (§T.48) + §V.48 wording widened.|V48
B31|2026-05-15|`/sdd:check --all` 2026-05-15 (post §T.48 closure cascade) found §V.48 sub-class (b) recurrence @ `.claude/skills/smoke-test/scripts/qa.py:16` — docstring `Outscope-only post-V31` carries bare-form `V31` SPEC §V.31 cite. §T.48 sweep grep `rg -n '\b[VTB][0-9]+\b' .claude/` surfaced this line but operator-review misclassified the cluster of qa.py hits as smoke-test gate IDs (surrounding L12, L18, L163, L221 use `B4` as smoke-test phase label, ⊥ SPEC §B.4 cite) ∧ missed that L16 specifically cites §V.31. Sub-class (b) 5th occurrence (§B.22 skills, §B.24 code-dotless, §B.26 tests, §B.30 code-bare). Recurrence-cause = operator-review blind-spot bias when sweep returns mixed-class hits (gate-ID + spec-cite intermingled in same file); §V.48 already acknowledges "operator review only" ∴ ⊥ new invariant required. Fix = code-only prose patch (§T.49) + operator lesson: per-line review when adjacent hits ∈ different namespace.|V48
B32|2026-05-26|`mailpilot enrollment add` accepted enrollment where `contact.email == workflow.account.email` → semantic self-loop, ⊥ warning. Observed on mailpilot 0.5.2 building smoke wf on `hello@lab5.ca`: contact `email='hello@lab5.ca'` enrolled into wf owned by same-email account, `enrollment list` showed self-loop `active`. Handler `enrollment_add` (`src/mailpilot/cli.py:2422`) FK-validates workflow ∧ contact independently but ⊥ cross-entity compare across `workflow.account_id → account.email` FK chain. Operator workaround: `enrollment remove --workflow-id <wid> --contact-id <cid>` post-hoc; orphan contact row persists (no `contact remove` verb — separable issue, operator-accessible contact lifecycle). Recurrence class = cross-entity identity self-reference admitted at CRM mutation seam (workflow-account-contact triangle); class binds to `enrollment add` only per author "minimal point of intervention" reasoning (contacts global, self-loop constraint per-workflow). Reported via GitHub issue #127 by kborovik, surfaced via `/sdd:build §T.T-2` against freshly-rebuilt DB.|V56
B33|2026-05-26|`mailpilot enrollment remove` echoed pre-delete `Enrollment` blob via `output_entity("enrollment", deleted)` (`cli.py:2632`) → response `{"enrollment": {..., "status": "active", ...}, "ok": true}` after successful hard DELETE; operator-event line claimed `changed=["status"]` (row deleted ∴ ⊥ field mutation). Same handler violates §V.5 hard-delete branch (composite-key projection required) ∧ §V.47 (`add`/`remove` → relational key set ∴ `changed=["workflow_id", "contact_id"]`, ⊥ `["status"]`). `tag remove` (`cli.py:1518`) shares class — echoes pre-delete `Tag` row ∧ emits `changed=["name"]`. Recurrence class = `remove`-verb handler picks "echo pre-delete entity" path on hard DELETE → leaks pre-delete lifecycle state into CLI response → scripts checking `.enrollment.status` see `"active"` post-remove → may retry ∨ alert. Reported via GitHub issue #129 by kborovik; surfaced during T-2 rollback on #127 enrollment self-loop smoke wf.|V5,V47
B34|2026-05-28|prod task `019e6b46-66ea-75c2-846b-f02acf19b321` (inbound `inbound-google-drive` wf) emitted ×2 `read_drive_markdown` calls parallel; one returned 1.12s, sibling hung 60.83s @ socket timeout. `DriveClient` (`src/mailpilot/drive.py:53-57`) shares one `httplib2.Http(timeout=60)` across `agent.invoke`; `httplib2.Http` ⊥ thread-safe (single connection-pool dict, no locks); Pydantic AI dispatches sync tools via `asyncio.to_thread` ∴ Anthropic-emitted parallel calls land on separate threads against same transport. `read_drive_markdown` (`src/mailpilot/agent/tools.py:501-514`) catches `HttpError` only ∴ bare `TimeoutError` escaped wrapper ∧ killed `agent.invoke`. §V.44 transient classifier returned `is_transient=True` but race ≡ deterministic ∴ all 4 retries hung ~22 min → terminal `failed`. Root cause structural: legacy `google-api-python-client` only supports non-thread-safe `httplib2` transport (Drive ∧ Gmail ∉ modern Cloud Client Libraries); Google guidance ≡ build-per-thread ∨ serialize-calls. Reported via GitHub issue #130. Recurrence class = any agent tool whose underlying client ⊥ thread-safe → §V.61 binds `sequential=True` on `Tool(...)` registration; §V.44 `Why:` clarifier links classifier allow-list to §V.61 ∴ future readers ⊥ re-debate carve-out.|V44,V61
B35|2026-05-28|`_drain_pending_tasks` (`src/mailpilot/sync.py:427`) iterates pending tasks sequentially (`for task in pending: execute_task(...)`) → 2 concurrently-arrived inbound emails on same demo workflow ran `agent.invoke` spans strictly sequential. Smoke-test 2026-05-28 gate B7.5 (new concurrent-dual-send gate added same date): T_SEND_B75 = 15:14:24Z (both sends ~ms-apart via Bash `&`); span#1 ran 15:14:36.461 → 15:14:58.583 (22s, trigger=task), span#2 ran 15:14:58.587 → 15:15:29.543 (30s, trigger=task); 3ms gap, zero wall-clock overlap. Both replies eventually arrived (both grounded correctly, both routed `classified` to demo wf, no `is_exception` ∨ `level=warn`). Recurrence class = inter-invocation drain-layer serialization: §V.61 `sequential=True` is intra-invocation (one agent.invoke, multiple tool calls) ∴ ⊥ helps inter-invocation case (orthogonal concept per fold-first audit 2026-05-28). End-user impact: traffic burst stalls 2nd msg by full 1st-msg duration → lab5.ca/mailpilot/ 60s promise consumed under any concurrent inbound load. Surfaced ≡ new gate's intended fail signal.|V62
B36|2026-05-28|§V.29 `_check_spec_table` regex `^[^|]+?\s{2,}\S` required ≥2 whitespace between label ∧ value; smoke-test 2026-05-28 B4 (qa-in-010, 28F1855MM) ∧ B7 (qa-cmp-011, ROPV vs PAPV) replies rendered specs as `**Peak Flow Rate** *26.6 GPM*`-style lines (single space + bold) → regex ⊥ matched → format-lint silently passed despite no `|---|` separator. B4: 5 such lines separated by `------------------------------` ASCII rule-lines, no `|`; B7: same shape across 2 product sections. Both replies factually correct ∧ grounded ∧ cite source ∴ regression presentation-only (Medium severity). Same recurrence class as §B.5, §B.9, §B.14 — 4th instance — agent learns to render around whatever regex tier is current. End-user impact: external recipients see ad-hoc spec layout that ⊥ render as clean table in Gmail's HTML view. ∴ §V.29 widened `\s{2,}` → `\s+` ∧ added line-length cap `{1,80}` to preserve prose immunity per §T.62.|V29
B37|2026-05-28|`agent.invoke` span lacked `email_id` attribute. Smoke-test 2026-05-28 B7.5 gate prose specified "exactly 2 `agent.invoke` spans each w/ its own `email_id` attribute matching B75a / B75b" but actual attrs were `{contact_id, workflow_id, trigger, workflow_type, input_tokens, cache_read_input_tokens, cache_creation_input_tokens, tool_call_count, prompt_length, model, result, status, agent_reasoning, output_tokens, total_tokens, llm_requests}` — no `email_id`. Operator pasting the gate's literal SQL got zero rows ∧ concluded the spans were missing. Cause: span construction (`src/mailpilot/agent/invoke.py:540-545`) sets `{workflow_id, contact_id, workflow_type, trigger}` but omits `email_id` from the rollup attrs even when `email` ∨ `task.email_id` is in scope. Recurrence class = span attr surface drifts from downstream-consumer expectations (gates, dashboards, per-message cost rollups). Fix = §V.11(+) binds `email_id` as required attr when invocation triggered by email; impl §T.63.|V11
B38|2026-05-28|smoke-test gates B4 / B7 / B7.5 graded reply latency against `T_SEND` (operator wall-clock) + 5s CLI poll, conflating three sources of delay: (a) unavoidable sync-tick lag (~12s observed: T_SEND_B75=15:14:24Z, first `agent.invoke` start=15:14:36Z), (b) agent span duration (~22-30s for the concurrent pair), (c) poll-cadence granularity (5s buckets first observed B7's reply at attempt 11 ≈ 64s when the agent span was actually 51s). Result: borderline gate verdicts that needed manual Logfire cross-check; inflated latency measurements above the pure agent budget. Cause: gates encoded their pass/fail predicate against the operator's wall-clock view rather than Logfire span timestamps. Recurrence class = test-skill gate verdicts derived from out-of-band measurements when an in-band span observable exists. Fix = §V.63(+) binds Logfire-first gate-verdict source; impl §T.64.|V63
B39|2026-05-28|gate A4 needed `gmail_thread_id` to confirm Gmail threading worked; field is present on the `email view` payload but absent from the `list` projection (`EmailSummary`). Operator forced per-row round-trip from `list` → `view` to read the field. Cause: `EmailSummary` (`src/mailpilot/models.py:225-238`) projects `{id, account_id, contact_id, workflow_id, direction, subject, sender, status, is_routed, sent_at, received_at}` — covers in-DB FKs but omits the Gmail-side join key the routing pipeline (§V.12 thread_match) depends on. Recurrence class = Summary projection drops external-system join keys downstream consumers need; same shape as the original §V.51 (FK-drop in `ActivitySummary`, §B.27) but w/ external-system identifier rather than DB FK. Fix = §V.51(+) extends required-fields clause to cover external-system join keys; impl §T.65.|V51
B40|2026-05-28|smoke-run B75a (qa-in-022, WS36-600-2 industrial softener) found two KB files describing the same model w/ divergent specs: `pure-aqua-industrial-water-softener.md` (110 GPM continuous / 125 GPM peak) ∧ `pure-aqua-sf-100s-industrial-water-softener.md` (65 GPM continuous / 120 GPM peak). Agent flagged the cross-file discrepancy in its reply ∧ grounded against the SF-100S doc. Operator graded PASS for reply quality but `cites_source_file=true` was ambiguous since the agent's citation (SF-100S) didn't match the qa pair's pinned `source_file` (the general industrial-softener doc). Phase 0 KB-visibility gate didn't catch the collision. Cause: `qa_pairs.json` schema treats `source_file` as singular; KB has legitimate cross-source identifier collisions that no schema-level alias allows. Recurrence class = KB cross-source identifier collision admitted by Drive but rejected by single-source qa schema. Fix = §V.31(+) admits optional `source_file_alts` list; impl §T.66.|V31
B41|2026-05-28|smoke-test 2026-05-28 B7 (compare-and-contrast, qa-cmp-003 Birm vs Pyrolox) measured 62s end-to-end vs §V.45/§V.63 60s SLA — `agent.invoke` ran 31s (15:14:24→15:14:55 by Logfire), pre-agent overhead consumed 32s (T_SEND→agent.invoke.start: 4s `sync.account.run` + 3s `agent.classify_email` + 1s task enqueue + 24s loop-block window because demo workflow's prior `agent.invoke` held the run-loop tick). B7.5 (concurrent dual-send qa-in-017 ∧ qa-in-020) confirmed structural cause: both emails delivered to Gmail @ 15:01:25 (`received_at` identical); first sync @ 15:01:33 picked up only B75a (`new=1`); `agent.invoke` for B75a held loop 15:01:34→15:02:05 (31s); during entire 31s window ⊥ `sync.account.run` fired on inbound account despite `pubsub.notify` having queued for B75b; second sync fired @ 15:02:05 (0.1s after agent.invoke ended); sync for B75b @ 15:02:09 (`new=1 duplicates=1`); `agent.invoke` for B75b @ 15:02:10→15:02:37 (LATENCY_B75b=72s, second SLA breach in same run). Cause: main run loop blocks on `_drain_pending_tasks` → synchronous `execute_task` → `invoke_workflow_agent` → `agent.invoke`; while drain ∈ progress, `pubsub.notify` events queue (wakeup_event set per §V.3) but loop ⊥ ticks back to `sync.account.run` until drain returns. §V.62 (drain pool) ⊥ sufficient: addresses inter-drain concurrency (multiple agent.invoke overlap once tasks exist) but ⊥ task-create-upstream-of-drain sync stall — task for B75b ⊥ created during B75a's drain ∴ pool ⊥ help. Recurrence class = sync-vs-drain serialization @ main-loop layer. End-user impact: every concurrent inbound mail under load eats up to one full `agent.invoke` duration (30s nominal) of its own SLA budget. Fix = §V.64(+) binds Pub/Sub-notify → sync.account.run wall-clock contract; impl decouples worker pool from main-loop progression.|V64
