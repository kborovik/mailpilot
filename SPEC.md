# SPEC

## §G GOAL

Agent-operated CRM. Gmail = comms layer. Claude Code = strategist; internal Pydantic AI agent = tactical executor (routing, auto-reply, follow-up).

## §C CONSTRAINTS

- Python 3.14. basedpyright strict. ruff. TDD per CLAUDE.md.
- PostgreSQL 18. psycopg, raw SQL via `psycopg.sql`. ⊥ ORM.
- IDs = UUIDv7 (`uuid.uuid7()` via `_new_id()`).
- Gmail scope = `gmail.modify` only. ⊥ additional Gmail scopes.
- Drive scope = `drive.readonly` only. Read-only KB grounding. ⊥ write/modify.
- Auth = service account & domain-wide delegation per §V.42. Credential source ∈ {file path (`google_application_credentials` setting ∨ `GOOGLE_APPLICATION_CREDENTIALS` env var), Application Default Credentials (e.g. GCE metadata server, Workload Identity, Cloud Run identity)}. Per-account impersonation: file creds → `credentials.with_subject(email)`; ADC creds → `service_account.Credentials(signer=iam.Signer(...), service_account_email=<sa>, subject=email, ...)`. ⊥ OAuth user login.
- Email body = plain text only (control chars stripped). ⊥ HTML body persistence. ⊥ embeddings, ⊥ vector store, ⊥ ingestion pipeline.
- KB files = `.md` in Drive folder named in `workflow.instructions`. ⊥ in-app PDF/Docs/HTML conversion.
- ASCII-only project artifacts (code, docs, CLI output). Agent-generated email body content exempt.
- Tool pattern: typed sig, DI deps, dict return, error dicts on failure (⊥ raise to agent).

## §I INTERFACES

- cli: `mailpilot <noun> <verb> [args]` → JSON on stdout, exit code 0 ∨ 1, errors to stderr.
  - nouns: `account`, `company`, `contact`, `workflow`, `enrollment`, `task`, `email`, `activity`, `tag`, `note`, `template`.
  - verbs: `list|search|view|create|update|add|remove|reply|send|start|stop|cancel|retry|run|sync|export|import`.
  - top-level: `run` (sync & task loop), `status`, `config get|set`, global `--version|--debug|--completion|--skill`.
  - envelope: `list|search|sync|export|import` → `{"<plural>": [...], "ok": true}`; `view|create|update|add|remove|reply|send|start|stop|cancel|retry` → `{"<singular>": {...}, "ok": true}`; err → `{"error": CODE, "message": TEXT, "ok": false}`.
  - `template` = read-only, code-defined (registry in `src/mailpilot/agent/templates.py` per §V.32). Verbs: `list [--direction inbound|outbound]`, `view NAME`. ⊥ create/update/delete — new template = code change + PR.
- agent tools (`src/mailpilot/agent/tools.py`): `send_email`, `reply_email`, `search_emails`, `read_email`, `read_contact`, `read_company`, `list_enrollments`, `create_task`, `cancel_task`, `record_enrollment_outcome`, `disable_contact`, `list_drive_markdown`, `read_drive_markdown`, `search_drive_markdown`, `noop`. ∀ tool → typed sig, dict return, err dict on failure.
- pubsub (`src/mailpilot/pubsub.py`): topic `mailpilot-topic-dev`, sub `mailpilot-sub-dev` (defaults; per-env override via `MAILPILOT_GOOGLE_PUBSUB_TOPIC` / `..._SUBSCRIPTION`). `setup_pubsub()` idempotent. `start_subscriber(settings, callback)` streaming pull. `make_notification_callback(queue, wakeup_event)` decode → enqueue → `wakeup_event.set()`. `renew_watches()` refresh @ T-24h.
- config (`src/mailpilot/settings.py`): `database_url`, `anthropic_api_key`, `anthropic_model` (default `claude-sonnet-4-6`), `google_application_credentials`, `google_pubsub_topic`, `google_pubsub_subscription`, `logfire_token`, `logfire_environment` ∈ {`development`, `production`}, `run_interval` (default 60s).
- module: `src/mailpilot/gmail.py` → `GmailClient`; `src/mailpilot/drive.py` → `DriveClient`. Mirror shape.
- entrypoint: `mailpilot = "mailpilot.cli:main"`.

## §V INVARIANTS

V1: ∀ Settings consumer → receive `Settings` instance. ⊥ pass API keys / config values as separate fn args. CLI loads via `get_settings()` & forwards.
V2: `cli.py` module top-level imports → only `click`. Heavy deps (`logfire`, `psycopg`, `pydantic`, `mailpilot.*`) ! lazy inside fn bodies. Why: `--help` ~50ms.
V3: `mailpilot run` main loop ! `wait()` on shared `wakeup_event` set by Pub/Sub callback / `LISTEN/NOTIFY` / signal handlers. Clear `wakeup_event` BEFORE processing → mid-iter events re-trigger next wait. Periodic timer = upper-bound fallback only. Canonical: `start_sync_loop` in `src/mailpilot/sync.py`.
V4: ∀ JSON-yielding single-shot CLI command → stdout strict-JSON. ⊥ preceded ∨ interleaved by operator-log lines. `operator_event(...)` → stderr always. Long-running `mailpilot run` exempt (operator console).
V5: CLI envelope shape: `list|search|sync|export|import` → `{"<plural>": [...], "ok": true}`; `view|create|update|add|remove|reply|send|start|stop|cancel|retry` → `{"<singular>": {...}, "ok": true}`; ⊥ inline entity fields at top level. Use `output_entity("<singular>", model)` & `output({"<plural>": [...]})`. `remove` ≡ inverse of `add` → same singular envelope; payload = removed entity (∨ its composite-key fields when the row is not retained post-delete).
V6: ∀ list|search row projects via `<Entity>Summary` — fields ⊆ {id, natural identifier, filter fields exposed as `--flag`, timestamp ordered by}. ⊥ long-text (`body_text`, `instructions`, `objective`), ⊥ JSON blobs, ⊥ variable-cardinality lists, ⊥ Gmail bookkeeping.
V7: ∀ ID = UUIDv7 via `_new_id()` (`uuid.uuid7()`). ⊥ uuid4, ⊥ serial.
V8: `tag`, `note` rows → exactly one of {`contact_id`, `company_id`} set (XOR). Enforced via schema CHECK on `tag` & `note` in `schema.sql`.
V9: `activity` & `note` append-only. ⊥ update fn.
V10: `enrollment.status` ∈ {`active`, `paused`} (schema CHECK on `enrollment.status` in `schema.sql`). Agent records terminal outcomes via `record_enrollment_outcome` → activity log; ⊥ mutate `enrollment.status` as terminal signal.
V11: `agent.invoke` span ! `trigger` attr ∈ {`enrollment_run`, `task`, `email`, `manual`}, explicit caller-passed. ⊥ heuristic inference from arg presence. Why: conflated trigger labels mask operator-initiated retries as task drains, breaking Logfire regression detection.
V12: Routing pipeline: `thread_match` (Gmail thread_id) → RFC `In-Reply-To` / `References` match → LLM `_try_classify(candidates=active inbound workflows for account)` → unrouted.
V13: Inbound task creation filter (`create_tasks_for_routed_emails` in `database.py`): `direction='inbound' AND workflow_id IS NOT NULL AND contact_id IS NOT NULL AND e.created_at >= w.created_at AND NOT EXISTS (task WHERE email_id=e.id)`. Why: outbound mailbox seeing own send ⊥ create task; History re-delivery idempotent; ⊥ contact → ⊥ enrollment ∴ ⊥ task; `e.created_at` = DB insert time (⊥ Gmail `received_at`) excludes pre-workflow inbox traffic — emails Gmail-received before workflow exists but synced after ! ⊥ trigger retroactive auto-tasks.
V14: Drive list query: `mimeType='text/markdown' AND parents in '<folder_id>' AND trashed = false`. Flags ! `corpora="allDrives"` & `supportsAllDrives=True` & `includeItemsFromAllDrives=True`. `read_drive_markdown` ! `supportsAllDrives=True` on `files.get` & `files.get_media`. Why: KB folder ? lives in Shared Drive; default corpora excludes SD children → silent empty for impersonated SD member.
V15: ∀ agent tool failure → return `{"error": CODE, "message": TEXT}` dict. ⊥ raise to agent.
V16: ∀ agent run → ≥1 tool call. ∀ system-prompt fragment naming example tool sequence → ≥2 tool names (e.g. `reply_email` & `noop`). Why: single-tool example collapses to literal — agent emits exactly one tool named ∴ ⊥ generalises. Decline prose lives in shared `_DECLINE` fragment composed into every template's protocol per §V.33 — one edit point; same rule binds future fragments.
V17: Folder access = Drive permission of impersonated user. Folder ID ∉ secrets, ∉ access grants. Multi-tenant isolation delegated to Drive permission model.
V18: Race-safe email insert: `INSERT ... ON CONFLICT (...) DO NOTHING RETURNING *` → `Model | None`. `None` = concurrent worker won. History re-delivery idempotent.
V19: ∀ new logfire.exception site reachable from `mailpilot run` → paired `operator_event("error", source=<event_name>, message=str(exc))`. Why: keeps operator stderr stream complete under journald.
V20: ∀ new agent tool → unit tests cover {hit, no-hit, error}. ∀ new CLI cmd → tests patch `get_*` FK validators.
V21: `make check` ! green (ruff & basedpyright strict & pytest).
V22: Logfire spans split by `deployment_environment` ∈ {`development`, `production`} from `logfire_environment` setting. Project = `mailpilot` (token-scoped). MCP queries ! `WHERE deployment_environment = '<env>'`.
V23: `activity` row → ≥1 of {`contact_id`, `company_id`} set; both allowed. Enforced via schema CHECK on `activity` in `schema.sql`. Why: activity may attach to contact, company, or both (e.g., email sent to contact at a known company).
V24: Drive search query (`DriveClient.search_markdown`): `mimeType='text/markdown' AND '<folder_id>' in parents AND fullText contains '<query>' AND trashed = false`. Same Shared-Drive flags as §V.14 (`corpora="allDrives"`, `supportsAllDrives=True`, `includeItemsFromAllDrives=True`). Returns `[{"file_id", "name"}]` ranked by Drive native relevance. ⊥ snippet (Drive API ⊥ provide), ⊥ embeddings, ⊥ semantic match, ⊥ cross-folder. Why: KB grounding scales past full-folder enumeration; scope locked to workflow KB folder per §V.17.
V25: smoke-test out-scope QA verifier (`.claude/skills/smoke-test/scripts/qa.py:check`) ! exclude tokens present in original question before fabrication regex match. Why: agent decline that echoes "your question about X 50,000 ppm" trips verifier despite ⊥ fabrication. `details.declined: true` authoritative when ⊥ fabrication block detected outside echoed question.
V26: ∀ `running tool` span → structured `gen_ai.tool.name` attribute set (OTel GenAI semantic convention, populated by `logfire.instrument_pydantic_ai()` in `src/mailpilot/cli.py`). Logfire dashboards & alerts ! query `attributes->>'gen_ai.tool.name'`; ⊥ parse free-text `message` ("running tool: <name>"). Why: convention-aligned attribute already exists upstream; inventing sibling `tool_name` duplicates data & drifts from OTel.
V27: ∀ email → ≤1 `routing.route_email` span lifecycle per `email_id`. History-API re-delivery & repeat sync sweep ⊥ trigger second route pass. Why: duplicate route spans inflate metrics & mask classifier regressions (e.g., A4 single delivery → 2 `skipped_no_workflows` spans observed).
V28: ∀ KB-grounded reply ∧ `search_drive_markdown` ≥2 hits → agent ! `read_drive_markdown` on top ≥3 (∨ all hits if <3) before composing reply. Why: model-number queries (e.g., `WS36-600-2`) match both general & model-specific docs; first-hit grounding picks wrong doc → wrong specs. Rule lives in `_DRIVE_GROUNDING` fragment bound only to `inbound-google-drive` template per §V.33; `outbound-general` & `inbound-general` templates physically cannot trigger this path. Out-of-scope decline path (§V.16) unaffected.
V29: ∀ `reply_email` ∨ `send_email` invocation w/ body containing `key  value` cluster (≥3 lines `^[^|]+?\s{2,}\S` — label, 2+ spaces, any non-whitespace value, ⊥ pipe on line) ∧ ⊥ `|---|` (∨ `| --- |`) pipe-table separator anywhere in body → tool ! return `{"error": "format", "message": "spec body MUST use Markdown pipe-table w/ |---| header separator"}` → agent retries per §V.15. Heuristic ! match regardless of value shape (numeric, text, units, ranges); ASCII rule-line runs (`-{6,}`, `={6,}`, `_{6,}` on standalone line) ⊥ count as pipe-table separator. Why: §B.5, §B.9, §B.14 = same prompt-fidelity drift recurring after prompt-only fix (§T.7) ∧ numeric-only lint (§T.9); §B.14 KDF rows non-numeric (`Mesh Size  20×50`) ∴ pre-amend regex missed them; ASCII rule-lines = next observed workaround surface. Decline path (§V.16) unaffected — decline bodies ⊥ contain spec rows.
V30: SUPERSEDED by §V.32 / §V.33 — Drive tool gating now structural via template registry (`_DRIVE` tuple bound only to `inbound-google-drive`); ⊥ regex sniff. Original wording retained for §B.10 trace: "Drive tools ! conditionally registered in agent toolset — included only when `workflow.instructions` contains Drive folder ID pattern (`\b[A-Za-z0-9_-]{25,}\b`). Structural exclusion = single source of truth; LLM cannot call a tool that isn't bound."
V31: smoke-test in-scope KB grounding gate (Scenario B, gate B4) ! operator-judged. Load source doc via `qa.py source --id ID` (DriveClient impersonating `inbound@lab5.ca` against `MailPilot Demo` folder) → operator emits structured JSON verdict {`qa_id`, `question`, `source_file`, `answers_question`, `every_factual_claim_supported_by_source`, `cites_source_file`, `unsupported_claims: [...]`, `verdict`}. `verdict='pass'` ↔ all 3 booleans true & `unsupported_claims=[]`. ⊥ token-substring match against curated `expected_tokens`. ⊥ free-form rating — `unsupported_claims` ! name each unsupported claim verbatim (structural defence against LLM-judge sycophancy). Why: substring match false-negatives on phrasing variation (`0.48 mm` vs `0.48mm`); §V.25 digit-echo exclusion = 2nd regex band-aid for same brittleness class ∴ regex approach at limit; per-Q token curation = unbounded maintenance debt as KB grows. Out-scope decline (B6) keeps script verifier — regex appropriate for shape detection (vendor-near-digits fabrication, decline-phrase presence).
V32: `workflow.template` ∈ {`outbound-general`, `inbound-general`, `inbound-google-drive`}. NOT NULL & schema CHECK matching `WorkflowTemplateName` Literal (`src/mailpilot/agent/templates.py`). `workflow.type` populated server-side from `TEMPLATES[template].direction` at `create_workflow`; ⊥ caller-passed `type` mismatch. `update_workflow` ! reject `template` change. Why: template change = tool-list change = destabilizing; force delete & recreate. Existing reads of `wf.type` in `routing.py`, `sync.py`, `cli.py`, `schema.sql` unchanged.
V33: Agent shape owned by template, ⊥ workflow. ∀ Agent instantiation → `Agent(name=..., deps_type=..., tools=list(TEMPLATES[wf.template].tools), instructions=TEMPLATES[wf.template].protocol + wf.instructions)`. ⊥ per-workflow tool override, ⊥ per-workflow protocol override. New tool surface (SQL, Confluence, …) → new template constant; ⊥ bolt onto workflow. Protocol = ordered concat of named fragments: `_BASE → _DEFERRED_TASK → [overlay]? → _DECLINE → _NO_FABRICATION`; `wf.instructions` appended last by the instantiation rule above. Template-specific overlays (e.g. `_DRIVE_GROUNDING` ∈ `inbound-google-drive`) slot between `_DEFERRED_TASK` & `_DECLINE` — guardrails (`_DECLINE` + `_NO_FABRICATION`) ! remain final two framing fragments so they bind regardless of overlay. Each rule lives in exactly one fragment (§V.16 → `_DECLINE`; §V.28 → `_DRIVE_GROUNDING`). Why: single edit point for invariants; structural tool gate; auditable via `WHERE template = ...`; canonical order pinned ⊥ drift between templates ∧ ⊥ accidental overlay-shadows-guardrail reorder.
V34: ∀ template name = `<direction>-<data-system>` (direction ∈ {`outbound`, `inbound`}; data-system = concrete binding: `general`, `google-drive`, future `postgres`, `confluence`). Naming by binding ⊥ abstract concept. Why: tools (`_DRIVE` tuple) & protocol fragment (`_DRIVE_GROUNDING`, mentions `search_drive_markdown` explicitly) = Google-Drive-specific; abstract names (`inbound-knowledge-base`) hide binding & create ambiguity when parallel data-source templates land.
V35: ∀ inbound `agent.invoke` prompt → trigger email body inlined at most once. `_build_user_prompt` (`src/mailpilot/agent/invoke.py`) ! exclude `email.id` from `email_history` before passing to `_format_email_history`. Fresh-thread invocation w/ trigger as only contact email → render `No prior email history with this contact.`; ⊥ `Email history (1 messages):` w/ trigger as sole entry. Why: `email_history` query (`database.list_emails(contact_id, account_id, workflow_id)`) returns trigger row, `_format_trigger` re-inlines same body under `New inbound email:` → body printed twice. `_BASE` system-prompt fragment ("When a trigger email is included in your prompt, its full body is already provided -- do not call read_email to fetch it again") already assumes single inclusion.
V36: ∀ `agent.invoke` → user-prompt trigger block ! match span's `trigger` attribute (§V.11). `_format_trigger` (`src/mailpilot/agent/invoke.py`) ! receive explicit `trigger` arg & branch: `email` (∃ email) → `New inbound email:` block; `task` (∃ persisted task row) → `Deferred task:` block w/ description & context; `enrollment_run` → first-reach-out framing (e.g. `First reach-out for this enrollment. Compose the initial outbound message per workflow objective & instructions.`); `manual` ∨ outbound fallback → existing `This is an outbound invocation. ...` text. ⊥ `Deferred task:` block when `trigger ≠ task`. Why: §V.11 enforces span-attribute correctness, but prompt framing carries the actual signal the model reads — `enrollment_run` initial reach-out framed as resumed deferred work risks resumption-shaped reasoning (look for prior context that ⊥ exists, call `list_enrollments` to "see where it left off"). Synthesising `task_description="manual <type> run"` at the caller is a code smell hiding the real `trigger` from the prompt builder.
V37: ∀ Anthropic LLM call (workflow agent in `src/mailpilot/agent/invoke.py` ∧ classifier in `src/mailpilot/agent/classify.py`) → request ! set Anthropic `cache_control` breakpoint on stable prefix (system prompt + tool definitions) so multi-turn invocations re-bill prefix as `cache_read_input_tokens` ⊥ as fresh input. `agent.invoke` rollup span attrs ! include `cache_read_input_tokens` ∧ `cache_creation_input_tokens` summed across child `chat` spans. Why: stable ~1500-token prefix repeats verbatim every turn; uncached re-billing inflates inbound multi-turn input tokens ~2x (smoke-run 2026-05-05 B4: 6 chat calls × ~1500-token prefix = ~9000 redundant tokens); rollup-level attrs avoid per-turn-span join for cost reporting & enable single-row cache-hit-rate alerting.
V38: `/release` project-local slash cmd extends `/gh:release` semantics. After local SemVer tag (`/gh:release` Procedure §1–§12), `/release` ! (a) `git push origin main && git push origin v<x.y.z>`, (b) `uv build` → assert `dist/mailpilot-<x.y.z>-py3-none-any.whl` ∃, (c) `gh release create v<x.y.z> --verify-tag --notes-from-tag dist/mailpilot-<x.y.z>-py3-none-any.whl`. Confirm-before-mutate gate (per `/gh:release` §9) covers push ∧ asset upload. Manifest = `pyproject.toml [project].version`; build backend = `uv_build` ∴ wheel name pattern fixed. Why: deploy = install of published `.whl` asset reachable via `gh release download`; tag-only release ⊥ deployable. `/gh:release` plugin cmd unchanged — covers tag-only repos.
V39: `workflow export` / `workflow import` = declarative round-trip on (`account_id`, `name`). Export → `{"workflows": [{name, template, objective, instructions, theme}, ...], "ok": true}` per `--account-id`. Import → JSON from `--file` ∨ stdin; ∀ row: absent → `create_workflow` (activation = side-effect of objective ∧ instructions both non-empty per existing create-path rule); present → `update_workflow` for changed fields ⊥ `template`; `template` differs → per-row `{"error": "template_immutable"}` per §V.32, batch continues. Import ! ⊥ write `workflow.status`. Round-trip on unchanged DB ≡ ⊥ row mutations 2nd run (idempotent upsert). Why: `make clean` wipes workflow rows; declarative replay = source-of-truth ⊥ operational state ∴ status ∉ payload; template change = §V.32 destabilising → row-error, ⊥ batch abort.
V40: smoke-test final report (`.claude/skills/smoke-test/SKILL.md` Phase 5) ≡ 3 sections, actionable-only: §1 **Execution** — phase-results matrix (current Part A) + ≤3-bullet Logfire signal summary (top span by volume, error/warn count, cache-hit ratio on multi-turn invokes); one screen max. §2 **Bugs** — observable failures, each = single paragraph + `Spec action:` line per existing severity matrix (`bug` ∨ `bug+invariant` ∨ `code-only`); Critical/High w/ `bug`∨`bug+invariant` auto-invoke `/sdd:spec`; Medium/Low print-only. §3 **Invariants** — under-specified contracts surfaced by inspection (⊥ concrete failure this run), each = single paragraph + `Spec action: propose-invariant` invocation; print-only, operator pastes. Spec hand-off block unchanged. Auto-write `./smoke-test-<YYYY-MM-DD>-<HHMMSS>.md` unchanged. ⊥ Notes section, ⊥ `none`-routed Suggestions, ⊥ `propose-task` Suggestions, ⊥ Part B SQL transcript ∈ output, ⊥ Part C 13-cat narrative ∈ output. Logfire SQL queries ∧ 13-cat scan ≡ derivation aids ∈ procedure body, ⊥ rendered as report sections. Why: prior 5-part report inflated output ~3× w/ FYI prose ⊥ acted on; operator's actual need = queue of `/sdd:spec` inputs ∴ report ≡ queue, ⊥ narrative.
V41: `mailpilot --skill` → stdout = packaged `src/mailpilot/SKILL.md` body verbatim, exit 0, ⊥ JSON envelope (matches `--version` / `--help` shape; §V.4 binds noun-verb cmds only). Source resolved via `importlib.resources.files("mailpilot").joinpath("SKILL.md")` ∴ survives wheel install. `pyproject.toml` ! include `SKILL.md` as package data (uv-build equivalent of `force-include`). Skill content audience = external LLM agent w/ `mailpilot` installed as CLI dep; scope = (a) noun-verb grammar + envelope shape per §V.5 + exit codes, (b) common task recipes (enrollment, send, route query, export/import), (c) settings keys per §I.config + `mailpilot config set` usage. ⊥ contain DB schema, ⊥ Pydantic AI / template internals (§V.32-§V.34 = package-internal). Test asserts `mailpilot --skill` stdout byte-equal to packaged file ∧ `importlib.resources` path resolves post-install. Why: external agents discover capability via `--help` (single vector); FS-path discovery brittle across install methods (pip vs pipx vs uv tool vs vendored).
V42: ∀ Google API client construction (Gmail, Drive, Pub/Sub) → credential source resolved via fallback chain `google_application_credentials` setting → `GOOGLE_APPLICATION_CREDENTIALS` env var → Application Default Credentials (ADC). DWD impersonation: file creds → `creds.with_subject(email)`; ADC creds → `service_account.Credentials(signer=iam.Signer(Request(), source_creds, sa_email), service_account_email=sa_email, subject=email, scopes=..., token_uri="https://oauth2.googleapis.com/token")` via IAM Credentials API (signs JWTs remotely; no local private key). New Google client modules ! route through `gmail.build_delegated_credentials(scopes, subject)` (impersonated) ∨ `gmail.build_default_credentials(scopes)` (non-impersonated). `mailpilot.sync` Pub/Sub & watch-renewal gates ! use `gmail.has_google_credentials()` (file ∨ ADC reachable), ⊥ raw `settings.google_application_credentials` truthiness. Project-ID resolution → `gmail.resolve_project_id()` (JSON `project_id` field ∨ ADC default project). Required IAM in ADC mode: instance SA holds `roles/iam.serviceAccountTokenCreator` on itself; IAM Credentials API enabled. Why: pre-fix code raised SystemExit when both setting & env empty → ADC blocked entirely → GCE deploy of `mailpilot-1.lab5-mailpilot-prd1` crash-looped 46+ iters until §B.15 fix; centralised helpers ≡ single audit point so a new client (Calendar, Sheets, …) cannot re-introduce the gap.
V43: `AnthropicModel` constructor in `src/mailpilot/agent/invoke.py` (`_build_agent`) ∧ `src/mailpilot/agent/classify.py` (`_get_model`) ! pass an `httpx.Client(timeout=httpx.Timeout(240.0))` (∨ Pydantic-AI-equiv `http_client` / `model_settings`) — explicit 240s read-timeout, 4× default 60s. ⊥ retry on `httpx.ReadTimeout` ∨ `anthropic.APITimeoutError`: a tool call mid-turn (e.g. `send_email`, `reply_email`, Drive writes) cannot be safely re-driven — idempotency ⊥ guaranteed across SMTP / Drive side-effects. ∴ timeout → bubble to `run.task.agent_failed` per §V.19, operator replay. Why: 2 prod TimeoutError events (§B.16) hit at exactly the default 60s read window during long-context multi-turn invocations (post-`read_drive_markdown` of 13KB doc → next chat completion stalled); 60s ⊥ sufficient under model load, 180s borderline (events near deploy window) ∴ 240s = headroom. Test: httpx-mock asserts client constructed w/ 240s read-timeout on `AnthropicModel(...)` in both modules; assert ⊥ retry wrapper around chat call (bare except @ `run.py:127` is the intended terminal handler).
V44: Task retry classification ≡ exception-class + status-code allow-list ∈ `src/mailpilot/agent/retry.py`. Allow-list: Drive/Gmail `googleapiclient.errors.HttpError` w/ status ∈ {500, 502, 503, 504, 429}; Anthropic `APIStatusError` w/ status ∈ {502, 503, 529}; `socket.timeout`/`TimeoutError` raised inside Drive tool spans. ∉ allow-list: `anthropic.APITimeoutError`, `httpx.ReadTimeout` — §V.43 exclusion preserved (mid-turn timeout ⊥ safely re-driven across SMTP/Drive side-effects). Max attempts = 4 (1 initial + 3 retries). Backoff schedule = [30s, 120s, 300s] + jitter, hardcoded (⊥ `Settings` field — YAGNI). Retry path: `attempt_count += 1`, `scheduled_at = now() + backoff`, `status` stays `pending`, `result` updated w/ last-failure summary; budget exhausted → `complete_task(status="failed", result={"reason": ..., "attempt_count": N, "terminal": "max_attempts"})`. `task.attempt_count INTEGER NOT NULL DEFAULT 0` added to schema; `task_pending_trigger` extended `AFTER INSERT OR UPDATE OF status, scheduled_at` (drain loop wakes on retry reschedule; terminal-status wakes accepted as benign noise). `Task` ∧ `TaskSummary` models gain `attempt_count: int`. `mailpilot task retry --task-id <ID>` manual cmd allowed on `failed`/`cancelled` rows only — refuses `completed` (tools already fired → retry = duplicate side-effects) ∧ `pending` (already queued, no-op); resets `status='pending'`, `attempt_count=0`, `scheduled_at=now()`, fires `pg_notify`. Drive socket-timeout cap via `httplib2.Http(timeout=...)` on `build_drive_service` bounds 60s Drive-side stall window so classifier sees `socket.timeout` quickly. ∀ retry path → paired `operator_event("task.retry", task_id, attempt, exc=type(exc).__name__)` per §V.19 pattern. Why: §B.16 — failed task row blocks any re-processing of same email forever (`create_tasks_for_routed_emails` ∧ `get_unprocessed_inbound_email` both filter `NOT EXISTS (task WHERE email_id=e.id)`); transient API blip ≠ terminal failure; only Anthropic *server-side status codes* (502/503/529) retry since those are returned before any tool call fires ∴ §V.43 idempotency rule honored, ⊥ amendment needed.
V45: `.claude/skills/demo-test/SKILL.md` ≡ liveness probe of public demo address `demo@lab5.ca` against deployed production instance. Source acct = `outbound@lab5.ca` (local CLI sender); target = `demo@lab5.ca` (processed by prod deploy, ⊥ local run loop). Setup: ⊥ `make clean`, ⊥ account create, ⊥ workflow import — assume warm state; pre-flight verifies `mailpilot account list` ∋ `outbound@lab5.ca`; pre-flight fails → exit `FAIL` w/ missing-prereq line ⊥ send. Scope: single in-scope question via `qa.py pick --type inscope` (reuse `.claude/skills/smoke-test/scripts/qa.py` verbatim, ⊥ new scripts); ⊥ out-of-scope arm; ⊥ Scenario A outbound flow. Subject = `[DEMO-<HHMMSS>] <topic>` (distinct prefix from `[ST-...]` per smoke-test) — fresh-randomised per run. Send via `mailpilot email send --account-id <OUTBOUND_ACCOUNT_ID> --to demo@lab5.ca --subject ... --body <question>`; capture `TEST_START` ISO before send. Gates (ALL ! pass for `PASS`): G1 reply round-trip — query Gmail directly (impersonate `outbound@lab5.ca` per §V.42; `GmailClient.list_messages(query="from:demo@lab5.ca after:<TEST_START_EPOCH>", label_ids=["INBOX"])` invoked via inline `uv run python -c '...'` ∨ thin CLI verb wrapping same — ⊥ `mailpilot email list` per §V.46) ≤12 attempts × 5s; match unique `<topic>` tail (Gmail prepends `Re: ` ∴ substring); reply present within 60s. G2 groundedness — operator-judged per §V.31, `qa.py source --id ID` loads source doc; structured JSON verdict ({`answers_question`, `every_factual_claim_supported_by_source`, `cites_source_file`, `unsupported_claims`, `verdict`}); `verdict='pass'` required. G3 Logfire production env — window `[TEST_START, now]`, filter `WHERE deployment_environment = 'production'` per §V.22; required spans present {≥1 `agent.invoke` `trigger='task'` per §V.11, ≥1 `running tool` `attributes->>'gen_ai.tool.name' = 'search_drive_markdown'` per §V.26, ≥1 `gmail.send_message`}; zero spans w/ `is_exception = true` ∨ `level ∈ {'error','warn'}` in window. Output: stdout = single line `PASS` ∨ `FAIL: <one-line reason>` + 3-bullet Logfire summary (top span by volume, error/warn count, `cache_read_input_tokens` ratio per §V.37). ⊥ phase matrix, ⊥ §1/§2/§3 sections per §V.40, ⊥ auto-write `.md` to disk, ⊥ auto-invoke `/sdd:spec`. Why: `/smoke-test` ≡ destructive 2-scenario regression suite (`make clean`, ~10min) ∴ unfit for routine "is public demo alive?" probe; `/demo-test` ≡ binary liveness check against published `lab5.ca/demo` promise running on prod deploy, exits <2min, fit for post-deploy spot-check.
V46: liveness probes (skills under `.claude/skills/<name>/SKILL.md` whose explicit purpose ≡ "is deployed system alive?" — e.g. `/demo-test` per §V.45) ! query authoritative production-facing surface directly (`GmailClient`, `DriveClient`, Logfire MCP) — ⊥ via local-mailpilot-DB reads (`mailpilot email list`, `mailpilot contact list`, etc.) that depend on a separately-started `mailpilot run` ∨ explicit sync step to be fresh. Implementation: probe SKILL.md procedure ! invoke source-of-truth API via inline `uv run python -c '...'` against `mailpilot.gmail` / `mailpilot.drive` ∨ thin `mailpilot` CLI verb wrapping same. Out-scope: full regression suites (`/smoke-test`) that own their own sync lifecycle ∧ ⊥ probes. Why: probe nominally tests deployed system; local DB only mirrors Gmail/Drive after sync; probe procedure forbids local loop (§V.45 setup) ∧ ⊥ trigger sync → local-DB poll = checking the wrong surface → false-FAIL on a healthy production (§B.23, 2026-05-11 `/demo-test` run: G1 polled 60s ∧ found nothing while production had replied <5s after send, surfaced post-hoc by manual `mailpilot account sync`).
V47: ∀ operator-initiated CRM config mutation via CLI = noun ∈ {account, company, contact, workflow, enrollment, tag, note} ∧ verb ∈ {create, update, add, remove, start, stop, cancel, import} → CLI handler ! wrap mutation in `logfire.span("<noun>.<verb>", entity_id?, account_id?, ...attrs)`; on success → `operator_event("<noun>.<verb>", entity_id=..., account_id=..., changed=[<field>...])`; on raise → `logfire.exception(...)` + paired `operator_event("error", source="<noun>.<verb>", message=str(exc))` (same pairing shape as §V.19, extended to CLI surface — ⊥ only `mailpilot run`). Span placement = CLI handler layer (`src/mailpilot/cli.py`) ∴ self-discriminates operator from (a) sync-driven auto-creation (e.g. `create_or_get_contact_by_email` during inbound sync — DB fn, ⊥ CLI handler), (b) agent-tool mutations already covered by §V.26 `gen_ai.tool.name`. `changed` semantics: `create` → field set populated at insert; `update` → field names whose values actually mutated (pre-vs-post row diff via paired `get_<noun>` reads inside the span); `add`/`remove` → relational key set; `start`/`stop`/`cancel` → terminal status name; `import` emits per-row events ∴ idempotent re-run ≡ empty `changed` lists per row (verifiable audit per §V.39). Test contract: per §V.20 `capfire: CaptureLogfire` — (a) span name + attrs on success, (b) `operator_event` emission w/ correct `changed`, (c) `logfire.exception` + paired error event on raise. Why: CRM config mutations = operator-curated identity state w/ zero observability today; CLI-layer placement = natural seam separating operator from sync ∧ agent paths; one uniform invariant binds the surface ∴ future nouns / verbs inherit obligation by convention ⊥ per-entity invariants.
V48: ∀ §-cite in repo prose (code comments, docstrings, SPEC.md body, CLAUDE.md, `.claude/skills/**/*.md`) ! dotted form `§[VTB]\.[0-9]+`. Dotless `§V<n>` / `§T<n>` / `§B<n>` ⊥ allowed. Why: `/sdd:check` cite-resolution regex grep ≡ `§[VTB]\.[0-9]+` (explicit dot) ∴ dotless evades auditor; §B.22 (skills) + §B.24 (code) ≡ same sub-class (cite-shape malformation) recurring twice. Wrong-section prose drift (§B.22 sub-class a) ⊥ regex-catchable — operator review only.
V49: outbound-template agent (`outbound-general`) ⊥ call `record_enrollment_outcome` inside `agent.invoke` w/ `trigger ∈ {enrollment_run, manual}` — tool reserved for `trigger=task` invocations on an enrollment w/ prior outbound email already in thread. Structural gate options (impl-side): (a) omit `record_enrollment_outcome` from outbound agent's bound tool list when `trigger ≠ task`, ∨ (b) `_DEFERRED_TASK` protocol fragment branches on `trigger` per §V.36 — `trigger=enrollment_run` → fragment says "Send the initial email & stop; do not call record_enrollment_outcome; outcome will be assessed when a reply arrives". Why: §B.25 — `_DEFERRED_TASK` text "After completing the workflow objective for a contact, call record_enrollment_outcome" (`src/mailpilot/agent/templates.py:69-74`) treats first-reach-out send as objective-completion → premature `enrollment_completed` activity row; arrival of decline-reply later → second terminal `enrollment_failed` row for same enrollment (two terminal activities per single enrollment). Per-fixture `workflow.instructions` appended after `_DEFERRED_TASK` per §V.33 ordering ⊥ reliably override template-fragment instruction in model reasoning ∴ gate ! be structural ∨ trigger-aware at protocol layer, ⊥ at fixture layer.

## §T TASKS

id|status|task|cites
T1|x|spec ratified — capture future work as new §T rows; backprop bugs via `/sdd:spec bug:`|-
T2|x|pair the 3 unpaired logfire.exception sites in `run.py:199` (run.sync.account_failed), `pubsub.py:203` (pubsub.notification.decode_error), `pubsub.py:286` (pubsub.watch.renewal_failed) w/ `operator_event("error", source=<event_name>, message=str(exc))`. Add span-contract tests asserting paired emission per site|V19
T3|x|impl `search_drive_markdown` agent tool. Add `DriveClient.search_markdown(folder_id, query)` (`src/mailpilot/drive.py`), tool wrapper (`src/mailpilot/agent/tools.py`), register in `agent/invoke.py`, system-prompt mention as preferred entry when KB folder large. Tests cover {hit, no-hit, 404 folder, drive 5xx}. Smoke-test scenario B uplift KB to ≥10 docs to demonstrate search vs full enumeration|V14,V15,V20,V24
T4|x|harden smoke-test out-scope QA verifier per §V.25 — strip tokens shared w/ original question before fabrication regex; treat `details.declined=true` w/ ⊥ fabrication-outside-echo as pass|V25
T5|x|superseded — amended §V.26 cites `gen_ai.tool.name` (OTel GenAI convention) already populated by `logfire.instrument_pydantic_ai()` in `src/mailpilot/cli.py:59`. ⊥ code work. See §B.6|V26
T6|x|superseded — investigation done (revised §B.4): no duplicate routing exists. `_store_inbound_message` (`sync.py:769`) & `route_email` (`routing.py:87`) already enforce ≤1 span per email_id. ⊥ code work|V27
T7|x|tighten demo workflow instructions in `.claude/skills/smoke-test/SKILL.md` to require pipe-table Markdown for spec sheets|-
T8|x|SUBSUMED by §T.15 — "Read top ≥3 hits" rule moves from `_SYSTEM_PREFIX` into `_DRIVE_GROUNDING` fragment of `inbound-google-drive` template. Demo workflow B1 prompt alignment & `read_drive_markdown` ≥3 agent test still in scope, lands w/ T15|V20,V28,V33
T9|x|impl `_check_spec_table(body)` heuristic in `src/mailpilot/agent/tools.py` — return `{"error": "format", ...}` when body has ≥3 spec-shape rows ∧ ⊥ `|---|` separator. Wire into `send_email` (`tools.py:41`) & `reply_email` (`tools.py:81`) before SMTP submit. Tests cover {pure prose pass, prose & valid pipe-table pass, spec-shape ⊥ table reject, decline body ⊥ specs pass, mixed body w/ ≥1 separator pass}. Smoke-test verifier extension: assert reply_email span returns no `error=format` for B4|V15,V20,V29
T10|x|SUPERSEDED by §T.15 — `_workflow_uses_kb` regex predicate replaced by structural template gating per §V.32 / §V.33. Drive tools now bound only to `inbound-google-drive` template's `_DRIVE` tuple; non-KB workflows physically cannot register Drive tools|V32,V33
T11|x|impl §V.31 — add `qa.py source --id ID` subcommand (`.claude/skills/smoke-test/scripts/qa.py`): DriveClient w/ `inbound@lab5.ca` impersonation, list & find file by `source_file` name in folder `1IUuPinOopUv_YWOZyFpt2ZX8Hd8bpZat`, print `content` to stdout, exit ≠0 if missing (KB drift signal). Fence `qa.py check` inscope branch → exit 2 w/ "inscope grading is now operator-judged in B4; see SKILL.md". patch `.claude/skills/smoke-test/SKILL.md` Scripts section (lines 51-55), gate B4 (lines 377-385), gate B6 (lines 425-437) per V31 contract. Retain `qa_pairs.json.expected_tokens` field for historical-run repro but ⊥ gate consumption|V31
T12|x|impl template registry — `src/mailpilot/agent/templates.py`: `WorkflowTemplate` frozen dataclass (`name`, `direction`, `description`, `protocol`, `tools`); fragment constants `_DECLINE`, `_DEFERRED_TASK`, `_NO_FABRICATION`, `_DRIVE_GROUNDING`; tool tuples `_CORE`, `_DRIVE`; `TEMPLATES: dict[WorkflowTemplateName, WorkflowTemplate]` w/ 3 entries. Define `WorkflowTemplateName = Literal["outbound-general", "inbound-general", "inbound-google-drive"]`. Tests parametrize universal invariants over `TEMPLATES.values()` (non-empty protocol, non-empty tools, name prefix matches `direction`, registry agrees w/ Literal); named per-template contract tests (`test_outbound_general_excludes_drive_tools`, `test_inbound_google_drive_includes_drive_tools`, `test_inbound_google_drive_protocol_carries_grounding`)|V20,V32,V33,V34
T13|x|schema — add `workflow.template TEXT NOT NULL` & CHECK constraint enumerating `WorkflowTemplateName` values (`src/mailpilot/schema.sql`). No backfill (`make clean` recreates DB)|V32
T14|x|`database.create_workflow` — accepts `template: WorkflowTemplateName`, populates `workflow.type` from `TEMPLATES[template].direction`; rejects caller-passed `type` mismatch. `update_workflow` rejects `template` field. `Workflow` / `WorkflowSummary` models gain `template: WorkflowTemplateName` field. `list_workflows` accepts optional `template` filter|V32,V33
T15|x|refactor `_build_agent` (`src/mailpilot/agent/invoke.py`) — drop `_TOOLS` list & `_SYSTEM_PREFIX` prose; lookup template via `workflow.template` → `Agent(model, tools=template.tools, system_prompt=template.protocol + workflow.instructions)`. Drop `_workflow_uses_kb` predicate (§T.10 path) once added. Tests assert exact tool tuple bound per template & integration test `test_build_agent_binds_template_tools`|V20,V32,V33,V34
T16|x|CLI — `workflow create` drops `--type`, requires `--template <outbound-general|inbound-general|inbound-google-drive>` (no default). `workflow update` rejects `--template`. `workflow list` adds `--template` filter alongside `--type`. `workflow view` exposes `template`. New `template` noun (read-only, code-defined): `template list [--direction inbound|outbound]` → `{"templates": [{name, direction, description, tool_count}, ...], "ok": true}`; `template view NAME` → `{"template": {name, direction, description, tools: [tool_name, ...], protocol}, "ok": true}`. Both skip `initialize_database()`. New models `WorkflowTemplateSummary` & `WorkflowTemplateRecord` (`models.py`). Tests cover envelope, `--direction` filter, unknown-name → `not_found`|V5,V6,V20,V32
T17|x|drop §V.30 path code (`_workflow_uses_kb` & regex sniff) & related tests once T15 lands; §V.30 already marked SUPERSEDED in SPEC|V32,V33
T18|x|smoke-test wiring — Phase 0 `mailpilot workflow create` invocations swap to `--template outbound-general` (Scenario A) & `--template inbound-google-drive` (Scenario B). Add Scenario A regression assertion via Logfire MCP: zero `search_drive_markdown` spans during outbound A window (§B.10 cannot recur). patch `.claude/skills/smoke-test/SKILL.md` Phase 0 & gate sections|V20,V32,V33
T19|x|sweep prompt fragments in `src/mailpilot/agent/templates.py` for single-tool examples — extend `_DECLINE` "(e.g. reply_email)" → ≥2 tools (e.g. `reply_email` & `noop`); audit `_BASE`, `_DEFERRED_TASK`, `_NO_FABRICATION`, `_DRIVE_GROUNDING` for same pattern. Tests assert ≥2 tool names mentioned wherever a fragment lists an example sequence|V16
T20|x|filter trigger email from `email_history` in `_build_user_prompt` (`src/mailpilot/agent/invoke.py:545-552`) — exclude row where `id == email.id` before formatting. Tests: (a) inbound trigger w/ otherwise-empty history → prompt contains `No prior email history with this contact.` & 1 occurrence of trigger body; (b) inbound trigger w/ N prior emails → prompt `Email history` count = N (excluding trigger) & body appears exactly once total; (c) outbound (⊥ trigger) prompt unchanged. Smoke-test Logfire assertion: ∀ inbound `chat` span → trigger body substring count = 1 in `gen_ai.input.messages[0].parts[0].content`|V20,V35
T21|x|thread `trigger` arg through `_build_user_prompt` (`src/mailpilot/agent/invoke.py:377`) → `_format_trigger` (`invoke.py:356`); add `enrollment_run` branch ("First reach-out for this enrollment. Compose the initial outbound message per workflow objective & instructions."); drop synthesised `task_description = f"manual {wf.type} run"` at `cli.py:1841` — pass `trigger="enrollment_run"` & ⊥ task_description on that path. `run.py:123` (real task drain) keeps passing `task_description=task.description` w/ `trigger="task"`. Tests: (a) `enrollment_run` outbound (⊥ email) → prompt contains `First reach-out` & ⊥ `Deferred task:`; (b) `task` trigger w/ persisted task description → prompt renders `Deferred task:` block (regression guard); (c) `enrollment_run` w/ ∃ email (continued enrollment) → email-branch wins (existing precedence preserved); (d) `manual` trigger ⊥ email ⊥ task_description → outbound fallback prose. Smoke-test Logfire assertion: A3 first `chat` span's user prompt ⊥ contain `Deferred task:` substring|V11,V20,V36
T22|x|impl §V.37 — set `AnthropicModelSettings(anthropic_cache_tool_definitions=True, anthropic_cache_instructions=True)` (∨ equivalent Pydantic AI cache-control surface for current version) on `AnthropicModel` in `src/mailpilot/agent/invoke.py` (`_build_agent` model construction) ∧ `src/mailpilot/agent/classify.py` (`_get_model`). Bubble `cache_read_input_tokens` ∧ `cache_creation_input_tokens` from per-turn `chat` span usage → `agent.invoke` rollup span. Tests: (a) httpx-mock assertion — outbound Anthropic request body contains `cache_control` block after tool definitions for both agent ∧ classifier; (b) span-contract test (`capfire`) — `agent.invoke` rollup span carries `cache_read_input_tokens` ∧ `cache_creation_input_tokens` attrs (≥0 OK, presence required) summed from child chat spans. Smoke-test follow-up: re-run `/smoke-test` → confirm `cache_read_input_tokens > 0` on B4 multi-turn invocation; ⊥ regression on KB grounding gates B4/B6|V20,V37
T23|x|impl `.claude/commands/release.md` (thin dispatcher) + `.claude/skills/release/SKILL.md` (procedure). Inherit `/gh:release` Procedure verbatim through §12 (manifest detect → `pyproject.toml`; bump derivation; annotated tag w/ `--cleanup=verbatim`; baseline & retag-baseline modes; confirm-before-mutate). Extend post-§12: §13 push (`git push origin main && git push origin v<x.y.z>`), §14 build (`uv build`; assert `dist/mailpilot-<x.y.z>-py3-none-any.whl` ∃ — ⊥ → bail), §15 publish (`gh release create v<x.y.z> --verify-tag --notes-from-tag dist/mailpilot-<x.y.z>-py3-none-any.whl`). Confirm gate (§9) shows {target tag, push refs, wheel path, gh release create cmd}. Argument-hint mirrors `/gh:release` (`[patch|minor|major|x.y.z|retag-baseline]`).|V38
T24|x|impl `workflow export --account-id <ID>` cmd (`src/mailpilot/cli.py`). List envelope per §V.5/§V.39, fields = {name, template, objective, instructions, theme}. Tests: envelope shape, account-not-found error, deterministic ordering (e.g. `ORDER BY name`)|V4,V5,V20,V39
T25|x|impl `workflow import --account-id <ID> [--file PATH]` cmd. JSON from `--file` ∨ stdin. Upsert keyed `(account_id, name)` per §V.39. Tests: create path (activates iff objective ∧ instructions both non-empty), update path (status untouched, only changed fields written), `template` differs → per-row `template_immutable` error & batch continues, stdin vs `--file`, malformed JSON → top-level error envelope|V5,V20,V32,V39
T26|x|round-trip integration test: seed N workflows → `workflow export` → truncate `workflow` table for account → `workflow import` → diff ∅ on (name, template, objective, instructions, theme). Re-run import on unchanged DB → 0 mutations (assert via row `updated_at` unchanged ∨ `update_workflow` not called)|V20,V39
T27|x|smoke-test wiring — Phase 0 swaps 3 imperative `workflow create` per account → 1 `workflow import --file tests/fixtures/workflows-<scenario>.json` per account. Add fixtures `tests/fixtures/workflows-outbound.json` (Scenario A) ∧ `tests/fixtures/workflows-inbound.json` (Scenario B). patch `.claude/skills/smoke-test/SKILL.md` Phase 0 section|V20,V39
T28|x|migrate `company export`/`import` (`cli.py:486-522`) ∧ `contact export`/`import` (`cli.py:674-719`) to §V.5 list envelope. Export: stdout `{"companies"|"contacts": [...], "ok": true}`; `--file PATH` optional (when set: file = same JSON, stdout still emits envelope per §V.4). Import: read JSON from `--file` ∨ stdin; emit `{"companies"|"contacts": [...], "ok": true}` w/ per-row errors per §V.39 import pattern. Drop `{"exported"|"imported": N, "file": F}` shape. Update tests + any caller / shell script (`make` targets, smoke-test). Align shape across (`company`, `contact`, `workflow`) export/import|V4,V5,V20,V39
T29|x|migrate `tag remove` (`cli.py:1304`) ∧ `enrollment remove` (`cli.py:2150`) to §V.5 singular envelope per amended V5. `tag remove` → `output_entity("tag", removed_tag_record)` w/ {name, owner_type, owner_id} fields; `enrollment remove` → `output_entity("enrollment", removed_enrollment)` w/ {workflow_id, contact_id, status_at_removal}. Update `test_cli.py` assertions for both. ⊥ silent shape change — bump in same commit as v0.x release notes|V5,V20
T30|x|rewrite `.claude/skills/smoke-test/SKILL.md` Phase 5 per §V.40 — collapse Parts A/B/C → §1 Execution (phase matrix + ≤3-bullet Logfire summary; SQL queries & 13-cat scan retained inline as derivation aids before §1, ⊥ rendered as separate output sections). Part D → §2 Bugs (severity matrix & auto-invoke rules unchanged). Part E `propose-invariant` Suggestions → §3 Invariants. Drop Notes, `none`-routed Suggestions, `propose-task` Suggestions, `code-only` Defects (operator already has prior `smoke-test-*.md` files to grep). Spec hand-off block ∧ auto-write to disk unchanged. Update worked-example block in SKILL.md|V40
T31|x|widen `_check_spec_table` (`src/mailpilot/agent/tools.py:50`) per amended §V.29 — replace `_SPEC_ROW_RE` w/ `^[^|]+?\s{2,}\S` (drop numeric-value requirement); ASCII rule-line runs (`-{6,}`, `={6,}`, `_{6,}` on standalone line) ⊥ count as pipe-table separator. Tests cover {numeric specs reject (regression for §B.9), non-numeric specs reject (regression for §B.14), mixed numeric+text reject, ASCII rule-line ⊥ pass-through, valid pipe-table pass, decline body pass, prose w/ incidental 2-space alignment ⊥ false-positive}. Smoke-test verifier extension: assert reply_email span returns no `error=format` for B4 grounded reply|V20,V29
T32|x|impl `mailpilot --skill` global flag (`src/mailpilot/cli.py`) reading packaged `src/mailpilot/SKILL.md` via `importlib.resources`; configure `pyproject.toml` to ship `SKILL.md` as package data; author `src/mailpilot/SKILL.md` per §V.41 scope (noun-verb grammar, envelope, recipes, settings); tests cover {flag prints body verbatim, package-data resolves via importlib.resources after `uv build` install, missing-file = hard fail not silent}|V5,V20,V41
T33|x|impl §V.43 — pass `http_client=httpx.Client(timeout=httpx.Timeout(240.0))` to `AnthropicProvider(...)` in `_build_anthropic_model` (`src/mailpilot/agent/invoke.py:351`) ∧ `_get_model` (`src/mailpilot/agent/classify.py:63`). Add `httpx` to `pyproject.toml` deps if not already direct (currently transitive via `anthropic`). Tests: (a) httpx-mock asserts the AnthropicProvider's client carries `timeout.read == 240.0` in both modules; (b) assert ⊥ retry wrapper exists around `agent.run()` / `agent.run_sync()` call sites — bare `except Exception` @ `run.py:127` is the intended terminal handler per V43 (idempotency: tool-call mid-turn ⊥ safely re-driven). ⊥ regression on V37 cache_control settings (both ctors retain `AnthropicModelSettings(anthropic_cache_tool_definitions=True, anthropic_cache_instructions=True)`)|V20,V37,V43
T34|x|impl §V.44 — bounded auto-retry for transient agent task failures per issue #122. (a) new `src/mailpilot/agent/retry.py` w/ `is_transient(exc)` classifier ∧ constants `MAX_ATTEMPTS=4`, `BACKOFF_SECONDS=[30, 120, 300]`; allow-list per V44. (b) `src/mailpilot/schema.sql` — `task.attempt_count INTEGER NOT NULL DEFAULT 0`; extend `task_pending_trigger` → `AFTER INSERT OR UPDATE OF status, scheduled_at`. (c) `src/mailpilot/database.py` — `reschedule_task_for_retry(connection, task_id, backoff_seconds, exc)` ∧ `manual_retry_task(connection, task_id)` (status-restricted); FK validators per CLAUDE.md gotcha. (d) `src/mailpilot/models.py` — `Task` ∧ `TaskSummary` gain `attempt_count: int`. (e) `src/mailpilot/run.py:127` — swap bare `except Exception`: transient ∧ budget left → `reschedule_task_for_retry` + `operator_event("task.retry", ...)`; else terminal `failed` (existing path). (f) `src/mailpilot/cli.py` — `mailpilot task retry --task-id <ID>` cmd. (g) `src/mailpilot/drive.py` — `httplib2.Http(timeout=...)` cap on `build_drive_service`. Tests: classifier parametrized over (exception class, status) per §V.20; retry path (transient + budget → row `pending` w/ bumped `attempt_count`; budget exhausted → terminal `failed`); §V.43 exclusion regression (`anthropic.APITimeoutError` ∧ `httpx.ReadTimeout` stay terminal); CLI cmd FK-validator patches; `operator_event("task.retry", ...)` paired emission. ⊥ regression on §V.43 (`AnthropicProvider` 240s read-timeout retained) ∨ §V.37 (`anthropic_cache_*` settings retained). PR: #123|V19,V20,V37,V43,V44
T35|x|drop non-canonical `run_loop` (`src/mailpilot/run.py:188-218`) ∧ its 3 tests in `tests/test_run.py` (`test_run_loop_single_iteration`, `test_run_loop_sync_error_continues`, `test_run_loop_keyboard_interrupt_not_error_span`). `mailpilot run` ! routes through `start_sync_loop` per §V.3 (`cli.py:218`); `run_loop` uses `time.sleep(run_interval)` ∧ ⊥ `wakeup_event` ∴ violates the event-driven contract if ever wired back in. Keep `execute_task`, `_handle_agent_failure`, `_sync_all_accounts` (still called by `sync.py:423`, `start_sync_loop` plumbing). Run `make check` after delete — green required. Surfaced by `/sdd:check --all` on 2026-05-11 as a non-canonical-loop observation alongside §B.17|V3,V20,V21
T36|x|reshape `mailpilot account sync` output (`src/mailpilot/cli.py:421`) to §V.5 plural envelope — drop top-level `total_stored` (caller-side aggregation from per-row `stored`); rename `results` → entity-typed plural key (e.g. `accounts: [{account_id, email, stored, error?}, ...]`). Tests assert envelope = `{"accounts": [...], "ok": true}` w/ ⊥ inline top-level non-list fields. Update smoke-test ∧ any `make` target consuming current shape|V5,V20
T37|x|delete orphan `_sync_all_accounts` (`src/mailpilot/run.py:185-204`) ∧ its now-unused imports from `src/mailpilot/run.py` (`get_account`, `list_accounts`, `GmailClient`, `sync_account`). Sole caller (`run_loop`) removed by §T.35; `sync.py` has its own `_sync_all_accounts` @ `sync.py:388` ∴ run.py copy = dead code. `make check` green required; basedpyright `reportUnusedFunction` warning count drops by 1|V21
T38|x|impl `.claude/skills/demo-test/SKILL.md` per §V.45. Reuse `.claude/skills/smoke-test/scripts/qa.py` (`pick`, `source`) verbatim, ⊥ new scripts. Local CLI surface: `mailpilot account list`, `mailpilot email send`, `mailpilot email list`. Logfire MCP queries against project=`mailpilot` per §V.22. Skill description triggers: "demo test", "is the demo alive?", "check lab5.ca/demo", "demo liveness". Update CLAUDE.md "Always-on skills" section: append `/demo-test` line alongside `/smoke-test`. ⊥ ship as `mailpilot --skill` package-data (skill ≡ Claude-Code-local, requires operator-judged G2)|V20,V22,V26,V31,V37,V40,V45
T39|x|fix `.claude/skills/smoke-test/SKILL.md` cite drift per §B.22 — L37 prefix `(SPEC §V13)` → `(SPEC §V.5)` (correct target + canonical shape; same line later cites `§V.5 plural envelope` correctly); L161 `§V12 / §T18 / §B2` → `§V.12 / §T.18 / §B.2`; L272 `§V12 / §T18` → `§V.12 / §T.18`. Skill prose only — `make check` ⊥ required; re-run `/sdd:check` post-edit → §-cite drift ↓1|-
T40|x|impl §V.46 + §V.45 G1 amend. Replace `mailpilot email list` poll in `.claude/skills/demo-test/SKILL.md` Step 5 w/ direct Gmail API call: inline `uv run python -c "import sys, json; from mailpilot.gmail import GmailClient, build_delegated_credentials; ..."` against `GmailClient.list_messages(query="from:demo@lab5.ca after:<EPOCH>", label_ids=["INBOX"])` impersonating `outbound@lab5.ca` per §V.42. Subject-match logic stays (substring on unique `<topic>` tail). Preserve §V.45 "⊥ new scripts" line — route via inline Python ∨ `mailpilot` CLI, ⊥ `.claude/skills/demo-test/scripts/`. Verify post-fix: dry-run `/demo-test` against current prod → G1 passes within real 60s window w/o manual sync|V42,V45,V46
T41|x|impl §V.47 — instrument CRM config CLI mutations across (noun, verb) matrix at CLI handler layer (`src/mailpilot/cli.py`). Per-handler obligations: `logfire.span("<noun>.<verb>", entity_id?, account_id?, ...)` wrapping mutation; on success → `operator_event("<noun>.<verb>", entity_id=..., account_id=..., changed=[...])`; on raise → `logfire.exception` + paired `operator_event("error", source="<noun>.<verb>", message=str(exc))`. Concrete matrix from current `cli.py` handlers: `account` {create, update}; `company` {create, update, import}; `contact` {create, update, import}; `workflow` {create, update, start, stop, import}; `enrollment` {add, remove, update}; `tag` {add, remove}; `note` {add}. `update` handlers ! read pre-row via `get_<noun>(...)`, perform mutation, read post-row, compute `changed` = [k for k, (a, b) in zip(field_names, paired_values) if a != b]. `import` handlers emit per-row events ∴ re-run on unchanged DB → empty `changed` lists (regression guard for §V.39). Tests under `tests/test_cli_telemetry.py` (new) via `capfire: CaptureLogfire` parametrized over the matrix assert (a) span name + attrs on success, (b) `operator_event` emission w/ correct `changed` per verb semantics, (c) `logfire.exception` + paired `operator_event("error", source="<noun>.<verb>", ...)` on raise (simulate via injected `psycopg.OperationalError` patch on `database.<fn>`). ⊥ regression on (i) §V.26 agent-tool `gen_ai.tool.name` spans (operator-CLI events live at handler layer, ⊥ DB ∴ agent paths untouched), (ii) §V.39 workflow/contact/company import idempotency. PR: #125|V19,V20,V26,V39,V47
T42|x|patch dotless §-cites in code per §B.24 + §V.48 — `src/mailpilot/cli.py:79` `§V13` → `§V.5` (wrong-section: envelope shape governed by §V.5, ⊥ §V.13 inbound-task filter); `src/mailpilot/agent/invoke.py:523` `§V11` → `§V.11` (canonical dotted form). Sweep `rg '§[VTB][0-9]+(?:[^.]|$)' src/ .claude/ CLAUDE.md SPEC.md` for any dotless residue. Doc prose only — `make check` ⊥ required.|V48
T43|x|impl §V.49 — `_DEFERRED_TASK` fragment becomes `trigger`-aware in `src/mailpilot/agent/templates.py`. Split into `_DEFERRED_TASK_TASK` (current text, `trigger='task'`) ∧ `_DEFERRED_TASK_INITIAL` (new: "Send the initial email & stop; do not call record_enrollment_outcome; outcome will be assessed when a reply arrives", `trigger ∈ {enrollment_run, manual}`). `WorkflowTemplate` gains `build_protocol(trigger) -> str` composing per-invocation; `_build_agent` (`src/mailpilot/agent/invoke.py`) calls `template.build_protocol(trigger) + workflow.instructions` ⊥ `template.protocol + workflow.instructions`. Canonical fragment order per §V.33 preserved. Tests: (a) `build_protocol('enrollment_run')` ⊥ contain `record_enrollment_outcome`, `build_protocol('task')` contains it; (b) outbound agent run w/ `trigger='enrollment_run'` → `record_enrollment_outcome` ⊥ called (mock assert); (c) outbound agent run w/ `trigger='task'` + decline reply → `record_enrollment_outcome` called w/ `outcome='failed'` (regression); (d) `inbound-*` templates unchanged. Smoke-test Logfire: A3 first-reach-out span ⊥ produce `enrollment_completed` activity row.|V20,V33,V36,V49

## §B BUGS

id|date|cause|fix
B1|2026-05-01|3 logfire.exception sites in `run.py:199`, `pubsub.py:203`, `pubsub.py:286` reachable from `mailpilot run` ⊥ paired w/ `operator_event("error", ...)` → operator stderr stream under journald has gaps where exceptions occur. Surfaced by `/sdd:check` post-rebuild on 2026-05-01|V19
B2|2026-05-02|smoke-test out-scope QA verifier flags digits from echoed user question as fabrications (qa-out-005 Veolia OPUS II — agent declined politely w/ ⊥ specs, verifier still failed gate B6). Surfaced by smoke run|V25
B3|2026-05-02|`running tool` span ⊥ structured `tool_name` attribute; identity only in free-text `message`. Logfire dashboards ! parse prose. Surfaced by smoke run|V26
B4|2026-05-02|smoke-run A observed 2 `skipped_no_workflows` spans during A's window. Initial diagnosis ("duplicate routing pass") wrong. Logfire evidence 2026-05-03: spans came from 2 distinct `email_id`s — `019deb87-f336-...` (pre-existing inbox msg picked up by initial sync sweep post `make clean`) & `019deb88-4382-...` (A4 delivery). Both routed exactly once: `_store_inbound_message` (`sync.py:769`) uses `INSERT ON CONFLICT DO NOTHING RETURNING *` → duplicate inserts exit early; `route_email` (`routing.py:87`) short-circuits on `is_routed=True`. ∴ §V.27 already holds; §T.6 superseded|V27
B5|2026-05-02|demo workflow B1 reply rendered spec sheet w/ asterisks instead of pipe-table despite "plain Markdown" instructions — prompt-fidelity drift on output style|V29
B6|2026-05-03|B3 misdiagnosed: `running tool` span did expose structured tool identity — Pydantic AI's `instrument_pydantic_ai()` populates `gen_ai.tool.name` (OTel GenAI convention). Smoke-test Logfire query asked for `attributes->>'tool_name'` (null) & missed `gen_ai.tool.name` (populated on all 13 spans). ∴ V26 amended to cite canonical attribute|V26
B7|2026-05-03|smoke-run B4 in-scope question targeted `WS36-600-2` in `pure-aqua-sf-100s-industrial-water-softener.md` (65 GPM). Agent grounded to broader `pure-aqua-industrial-water-softener.md` (110 GPM) — both files match search query, agent read only top hit. Cause: `_SYSTEM_PREFIX` (`src/mailpilot/agent/invoke.py:350`) "Read the top matches" & demo workflow instr "Pick the top relevant hit" → singular collapse, N=1 read, wrong doc grounded|V28
B8|2026-05-03|smoke-test B4 in-scope grounding gate over-relied on substring match of `expected_tokens` from `qa_pairs.json` — passed only when agent phrasing matched curated tokens char-for-char, false-negatives on semantically correct replies (e.g., `0.48mm` vs curated `0.48 mm`). §V.25 digit-echo exclusion = 2nd regex band-aid for same brittleness class. Cause: gate confused 'tokens-present' (structural) w/ 'reply-grounded-in-source' (semantic). ∴ B4 inscope grading delegated to operator w/ source doc loaded & structured verdict naming unsupported claims; B6 outscope decline kept script-based (regex fits shape-detection)|V31
B9|2026-05-03|smoke-run B4 (qa-in-026) reply rendered spec sheet as 5 space-separated `<label>  <number>` lines (`Continuous Flow Rate 110 GPM` …) ⊥ pipe-table syntax despite explicit `| Specification | Value | / |---|---|` instruction in `.claude/skills/smoke-test/SKILL.md:321`. Same drift as §B.5 — prompt-only fix (§T.7) ⊥ hold. ∴ §V.29 = tool-level lint required|V29
B10|2026-05-03|smoke-run A3 first attempt: outbound workflow `Outbound Smoke A` (instructions ⊥ Drive folder ref) triggered agent to call `search_drive_markdown` 5x then refuse to send — `tool_call_count=5` wasted, agent reasoning "No knowledge base documents were found … unable to fabricate". Cause: Drive tools unconditionally registered in `_TOOLS` (`src/mailpilot/agent/invoke.py:331-333`); `_SYSTEM_PREFIX:345` "if workflow references…" prompt-only gate ⊥ structural. Workaround: amended workflow.instructions to explicit "do NOT call list_drive_markdown / search_drive_markdown / read_drive_markdown". ∴ §V.30 = structural tool-registration gate|V30
B11|2026-05-04|smoke-run 2026-05-04 confirmed inbound trigger email inlined twice in agent prompt across A7 (chat 873a8544df5f2b3d), B4 (chat e2c9c7f53aa1efa5), B6 (chat 28e3a6609321f4b3) — same `email_id` & full body in both `Email history` & `New inbound email:` sections. Cause: `_build_user_prompt` (`src/mailpilot/agent/invoke.py:545-552`) passes unfiltered `email_history` from `database.list_emails(contact_id, account_id, workflow_id)` (which includes the trigger row) alongside the trigger to `_format_trigger`; ⊥ dedupe step. Token waste scales w/ body length & thread depth; misleading `Email history (1 messages)` count on fresh threads; risk of model treating same email as 2 distinct events|V35
B12|2026-05-04|smoke-run 2026-05-04 A3 first chat 6a8a97c4bf1878f6: prompt rendered `Deferred task:\nDescription: manual outbound run` despite invocation being operator-initiated `mailpilot enrollment run` (foreground synchronous initial reach-out, not drained task). Cause: `cli.py:1841` synthesises `description = f"manual {wf.type} run"` & passes as `task_description`; `_format_trigger` (`src/mailpilot/agent/invoke.py:356-374`) branches on `task_description` truthiness w/ ⊥ awareness of caller's `trigger` label (`enrollment_run` vs `task`). Span `trigger` attribute (§V.11) correctly = `enrollment_run` ∴ prompt-side framing alone out of step w/ span-side label. Behaviour benign on this run but framing risks resumption-shaped reasoning at scale|V36
B13|2026-05-08|`company export` (`cli.py:486`), `company import` (`cli.py:505`), `contact export` (`cli.py:674`), `contact import` (`cli.py:693`) emit `{"exported"|"imported": N, "file": F}` envelope ⊥ list envelope `{"<plural>": [...], "ok": true}` per §V.5. Pre-existing — cmds shipped before §V.5 enumerated `export|import` verbs. Surfaced by `/sdd:check --all` post issue-119 amendment 2026-05-08 (V5 expanded to bind `export|import` → list envelope). Recurrence-class caught by amended §V.5 itself — ⊥ new invariant required|V5
B14|2026-05-09|smoke-run 2026-05-09 B4 (email `019e0a94-0695-7005-825d-abcfae801ec8`, `kdf-process-filtration-media.md`): reply rendered specs as paragraphs of space-aligned `<label>  <value>` rows separated by `------------------------------` ASCII rule-lines, ⊥ Markdown pipe-table. §V.29 lint silent — `_SPEC_ROW_RE` (`src/mailpilot/agent/tools.py:46`) required numeric-shape values (`[\$]?[\d,]+`); KDF spec rows include non-numeric values (`Mesh Size  20×50`, `Type  Granular`) ∴ ⊥ matched. Same drift class as §B.5, §B.9 post prompt-only fix (§T.7) ∧ post numeric-tier lint (§T.9). ∴ §V.29 widened: any `key  value` cluster + reject ASCII rule-line faux-separator|V29
B15|2026-05-10|`mailpilot.service` on GCE instance `mailpilot-1.lab5-mailpilot-prd1` crash-restart-looped 46+ iters after deploy w/ ⊥ `google_application_credentials` setting (operator expected GCE-attached SA via ADC). `gmail.resolve_credentials_path` raised SystemExit when both setting & `GOOGLE_APPLICATION_CREDENTIALS` env empty → ADC never tried; `GmailClient(email)` ctor → `build_gmail_service` → exit. Same blind spot in `drive.py:build_drive_service` (used same resolver), `pubsub.py:_load_credentials` & `_resolve_project_id` (read JSON file directly), `sync.py` Pub/Sub start gate & watch-renewal gate (raw setting truthiness). Fix removed `resolve_credentials_path`, added `gmail.build_delegated_credentials` (file → `with_subject` OR ADC + `iam.Signer` JWT signing), `gmail.build_default_credentials` (file OR ADC), `gmail.has_google_credentials()`, `gmail.resolve_project_id()`; rerouted Drive, Pub/Sub, sync gates through helpers|V42
B16|2026-05-10|2 prod `TimeoutError` events ("The read operation timed out", 60s read window) on `agent.invoke` (`src/mailpilot/agent/invoke.py` → `run.py:117`) bubbled to `run.task.agent_failed` (`run.py:128`) → `complete_task(... status="failed")` (`run.py:134`); event A: 04:18 UTC, event B: 04:45 UTC on task `019e1034-6375-710d-a223-61bde1d8cfb8` (inbound workflow `019e0e49-ea2c-72d4-a675-f96dba4e83bd`, 6248 input + 308 output tokens, $0.0198, 1344-char prompt). Event B trace: `search_drive_markdown` → `read_drive_markdown` (13KB `pure-aqua-industrial-water-softener.md`) → 60s stall on next chat completion → timeout. Bare `except Exception` @ `run.py:127` ⊥ retry, ⊥ classify exception → transient LLM API flake = permanently lost work, manual replay only. Default `httpx` read-timeout 60s intersects long-context Anthropic latency under load. Co-observed (⊥ backpropped): 407 pre-§V.42 SystemExit (§B.15, fixed by 04:30 UTC daemon restart), 2 dev `psycopg.OperationalError`, 2 dev `pydantic_ai.exceptions.ContentFilterError`|V43,V44
B17|2026-05-11|§V.44 mandated `mailpilot task retry --task-id <ID>` (impl `cli.py:2389`) but §I.cli verb enumeration `{list|search|view|create|update|add|remove|reply|send|start|stop|cancel|run|export|import}` ⊥ updated to include `retry` when V44 row landed. EXTRA-class drift (code-exposes-verb-not-in-§I) surfaced by `/sdd:check --all` on 2026-05-11. Same recurrence class as §B.13 (pre-§V.5 export/import verbs absent from §I before §V.5 amendment enumerated them): §V row adds verb-shape obligation but §I list-shape ⊥ synced. ∴ recurrence caught by §I list-shape audit sub-recipe (enumerate every list-field in §I, set-diff against code symbol table); ⊥ new invariant required|V5
B18|2026-05-11|`mailpilot account sync` cmd exposed (`src/mailpilot/cli.py:355`); verb `sync` ∉ §I.cli verb pool. EXTRA-class drift surfaced by `/sdd:check --all` on 2026-05-11 — first catch by §B.17.fix's named "§I list-shape audit sub-recipe (enumerate every list-field in §I, set-diff against code symbol table)". Same recurrence class as §B.13 (pre-§V.5 export/import absent from §I) & §B.17 (retry absent from §I when §V.44 landed): code adds a verb, §I list-field ⊥ synced. ⊥ new invariant required — recurrence already attributed to §V.5 verb-pool obligation; fix = amend §I.cli verb list to include `sync`|V5
B19|2026-05-11|`mailpilot account sync` (`src/mailpilot/cli.py:421`) emits `{"results": [...], "total_stored": N, "ok": true}`; `total_stored` is an inline top-level entity-field forbidden by §V.5 ("⊥ inline entity fields at top level"). Pre-existing — cmd shipped before §V.5 enumerated `sync` as plural-envelope verb. Surfaced by `/sdd:check --all` cascade after `amend §I.cli + §V.5(+): add sync verb` (commit e5058f4) on 2026-05-11. Same recurrence-class as §B.13 (pre-§V.5 export/import shape drift surfaced by §V.5 verb-pool expansion): code envelope diverges from §V.5 ∴ verb-pool amend reveals violation. Recurrence-class caught by amended §V.5 itself — ⊥ new invariant required|V5
B20|2026-05-11|`_sync_all_accounts` (`src/mailpilot/run.py:185`) orphan post-§T.35 closure. §T.35 task text directed "Keep `_sync_all_accounts` (still called by `sync.py:423`, `start_sync_loop` plumbing)" — but `sync.py:423` calls `execute_task`, not `_sync_all_accounts`; `sync.py` has its own `_sync_all_accounts` @ `sync.py:388`. T35 author conflated two same-named fns ∴ run.py copy retained as dead code. basedpyright `reportUnusedFunction` warning surfaced but treated as pre-existing noise class during T35 cascade. Surfaced by `/sdd:check --all` cascade after T35 closure on 2026-05-11. Recurrence class = mis-attributed caller text in §T "keep" lists; catch = grep-verify caller list before retaining fn names; basedpyright already surfaces orphan post hoc. ⊥ new invariant required (warning-class observation, ⊥ runtime breach)|-
B21|2026-05-11|smoke-test skill (`.claude/skills/smoke-test/SKILL.md`) gates A8 ∧ B7 prose claim `enrollment_added.detail.workflow_id == <WORKFLOW_ID>` ∧ cite 'ADR-08' for `enrollment_*` activity vocabulary. Runtime `activity.detail` for `enrollment_added` rows stores `{"workflow_name": "<wf_name>"}` — no `workflow_id` field; `find . -iname 'ADR-08*'` returns no results; ⊥ `docs/` dir ∈ repo. Smoke-run 2026-05-11 (`smoke-test-2026-05-11-150600.md`) Bug 1: gate passed strict-form check (type exists) but field-shape ∧ ADR-08 citation = stale prose written against architecture never committed. Recurrence class = spec-adjacent doc text drifts from runtime when ⊥ §V binds the claim (same shape as §B.20 warning-class observation). Same report's proposed Invariant 2 ("activity.detail MUST carry workflow_id / email_id") premised on factual error — `activity` schema (`src/mailpilot/schema.sql:103`) has dedicated FK columns `email_id`, `workflow_id`, `task_id` at row level; ∀ emit site (`email_ops.py:84`, `cli.py:2069`, `sync.py:791`) passes them as top-level columns. `detail` JSONB ≡ display-label payload (workflow_name, subject) by design; FK columns ≡ canonical join surface. Fix = (a) drop ADR-08 references from skill prose, (b) redirect gate prose → check `activity.workflow_id` (FK column, correct join surface) ⊥ `detail.workflow_id` (would be redundant duplication of column-as-JSONB-key), (c) ⊥ new §V required (warning-class doc drift + schema reality already supports stable joins). Surfaced by `/smoke-test` Phase 5 inspection; refined by `/sdd:check --all` AMEND-gate fact-check 2026-05-11|-
B22|2026-05-11|smoke-test skill (`.claude/skills/smoke-test/SKILL.md`) L37 introduces envelope-shape rule prefixed `(SPEC §V13)` — §V.13 governs inbound task-creation filter, ⊥ envelope shape (§V.5; same line later cites `§V.5 plural envelope` correctly ∴ writer-intent = §V.5). L37, L161, L272 use dotless `§V13 / §V12 / §T18 / §B2`; `/sdd:check` §-cite regex `§[VTB]\.[0-9]+` silently skips dotless → audit blind-spot. Two recurrence sub-classes: (a) spec-adjacent prose drifts from §V semantics (= §B.21 stale ADR-08, §B.20 mis-attributed text); (b) cite-shape malformation evades auditor regex. Surfaced by `/sdd:check` cite-resolution pass on 2026-05-11. Fix = (a) L37 `§V13` → `§V.5`; (b) L161 `§V12 / §T18 / §B2` → `§V.12 / §T.18 / §B.2`; (c) L272 `§V12 / §T18` → `§V.12 / §T.18`. ⊥ new §V — sub-class (a) ≡ warning-class doc drift (§B.21 precedent); sub-class (b) ≡ check-skill regex tooling concern, ⊥ project-level invariant|-
B23|2026-05-11|`/demo-test` G1 (§V.45) polled `mailpilot email list --account-id <OUTBOUND> --direction inbound --since <TEST_START>` for 60s ∧ found nothing; §V.45 setup explicitly forbids `mailpilot run` ∧ G1 procedure ⊥ trigger sync → local DB stayed empty full poll window even though production replied promptly (manual `mailpilot account sync` post-poll surfaced reply immediately; G3 production-env Logfire spans all green for same window: `agent.invoke trigger='task'`=1, `gen_ai.tool.name='search_drive_markdown'`=1, `gmail.send_message`=1, errors=0). Probe nominally checked "did production reply?" but G1 implementation routed through local mirror state ∴ false-FAIL on healthy prod. Recurrence class: liveness probes checking production-facing claims via local mirror DB. Fix = §V.46 + amend §V.45 G1 (query Gmail directly via `GmailClient.list_messages` impersonating `outbound@lab5.ca` per §V.42) + §T.40 impl|V46
B24|2026-05-12|`/sdd:check` cite-resolution pass 2026-05-12 found §B.22 recurrence in CODE: `src/mailpilot/cli.py:79` docstring cites `§V13` for envelope shape (governed by §V.5 ∴ wrong-section) ∧ dotless form evades auditor regex; `src/mailpilot/agent/invoke.py:523` cites `§V11` (target §V.11 semantically correct, dotless only). §B.22 fix (§T.39) patched skill prose only — code-side cites slipped. Same two sub-classes as §B.22: (a) prose-drift wrong-section, (b) cite-shape malformation. §V.48 (+) binds sub-class (b) at project level so future recurrence catchable mechanically.|V48
B25|2026-05-12|smoke-run 2026-05-12 A8: outbound workflow agent called `record_enrollment_outcome` inside `agent.invoke(trigger=enrollment_run)` @ 12:20:32 UTC (reason "Initial outbound email about polemarch inflected successfully sent... Awaiting reply to determine interest.") then again inside `agent.invoke(trigger=task)` @ 12:21:26 UTC (reason "Contact explicitly declined") → 2 terminal activity rows (`enrollment_completed` + `enrollment_failed`) for 1 enrollment (contact `019e1c20-cb5b-724a-921a-2042ee9cd66c`, workflow `019e1c21-20a8-733a-86da-c0cf7f196fa8`). Cause: `_DEFERRED_TASK` protocol fragment (`src/mailpilot/agent/templates.py:69-74`) "After completing the workflow objective for a contact, call record_enrollment_outcome with outcome='completed'" ⊥ distinguish first-reach-out (no reply yet, outcome ⊥ knowable) from reply-handling (drained task, outcome IS knowable); agent treats "sent requested email" as objective-completion ∴ premature outcome. Fixture text "When you receive a reply...call record_enrollment_outcome" appended after `_DEFERRED_TASK` per §V.33 ordering ⊥ override template fragment in practice. Recurrence class = template-protocol fragment over-fires terminal tool when `trigger` semantics ignored; per-fixture override ⊥ reliable ∴ gate ! structural at protocol/template layer|V49
