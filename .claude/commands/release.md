---
description: Cut a SemVer release for this repo, build the .whl, push the tag, and publish a GitHub release with the wheel attached.
argument-hint: [patch|minor|major|x.y.z|retag-baseline]
---

Run the procedure in `.claude/skills/release/SKILL.md` with `$ARGUMENTS`.
