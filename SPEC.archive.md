# SPEC archive

Archived rows from `SPEC.md` per /sdd:compact prong 3 (window-vs-archive split). Sorted by id ascending. Verbatim preservation per archive-schema invariant. Eager-probed by `/sdd:check --all` cite-DAG sweep; excluded from `--incremental`.

## §T TASKS

id|status|task|cites
T1|x|spec ratified|-
T2|x|pair 3 unpaired logfire.exception sites in run.py / pubsub.py w/ operator_event|V51
T3|x|impl search_drive_markdown agent tool ∧ uplift smoke KB ≥10 docs|V34,V39,V64,V36
T4|x|harden smoke-test out-scope QA verifier — strip echoed-question tokens before fab regex|V56
T5|x|SUPERSEDED — §V.53 amend|V53
T6|x|SUPERSEDED — no duplicate routing|V22
T7|x|require pipe-table Markdown for spec sheets in demo workflow instr|-
T8|x|SUBSUMED by §T.15|V64,V41,V45
T9|x|impl _check_spec_table heuristic; reject spec-shape body w/o |---| separator|V39,V64,V42
T10|x|SUPERSEDED by §T.15|V44,V45
T11|x|impl §V.57 — qa.py source --id subcommand; B4 grading operator-judged|V57
T12|x|impl template registry (WorkflowTemplate, fragments, _CORE / _DRIVE)|V64,V44,V45,V46
T13|x|schema — add workflow.template NOT NULL + CHECK|V44
T14|x|database.create_workflow accepts template; rejects type mismatch ∧ template change|V44,V45
T15|x|refactor _build_agent — lookup template; drop _TOOLS ∧ _SYSTEM_PREFIX|V64,V44,V45,V46
T16|x|CLI — workflow create requires --template; new template noun (read-only)|V4,V5,V64,V44
T17|x|drop §V.43 path code (_workflow_uses_kb regex sniff)|V44,V45
T18|x|smoke-test wiring — Phase 0 swaps to --template; Scenario A regression for §B.10|V64,V44,V45
T19|x|sweep prompt fragments for single-tool examples — extend to ≥2 tools|V40
T20|x|filter trigger email from email_history in _build_user_prompt|V64,V29
T21|x|thread trigger arg through _build_user_prompt → _format_trigger|V26,V64,V30
T22|x|impl §V.47 — cache_control breakpoint + cache_*_input_tokens attrs on rollup|V64,V47
T23|x|impl /release skill — push, build wheel, gh release create|V62
T24|x|impl workflow export --account-id|V3,V4,V64,V63
T25|x|impl workflow import --account-id [--file]|V4,V64,V44,V63
T26|x|round-trip integration test for workflow export/import|V64,V63
T27|x|smoke-test Phase 0 swaps to workflow import w/ fixtures|V64,V63
T28|x|migrate company/contact export/import to §V.4 list envelope|V3,V4,V64,V63
T29|x|migrate tag remove ∧ enrollment remove to §V.4 singular envelope|V4,V64
T30|x|rewrite smoke-test Phase 5 per §V.58 — collapse to §1/§2/§3|V58
T31|x|widen _check_spec_table — drop numeric-value req; ASCII rule-lines ⊥ separator|V64,V42
T32|x|impl mailpilot --skill flag reading packaged SKILL.md|V4,V64,V6
T33|x|impl §V.48 — 240s httpx read-timeout on AnthropicProvider|V64,V47,V48
T34|x|impl §V.49 — bounded auto-retry for transient agent task failures|V51,V64,V47,V48,V49
T35|x|drop non-canonical run_loop ∧ its 3 tests|V21,V64,V65
T36|x|reshape mailpilot account sync output to §V.4 plural envelope|V4,V64
T37|x|delete orphan _sync_all_accounts in run.py|V65
T38|x|impl /demo-test skill per §V.59|V64,V52,V53,V57,V47,V58,V59
T39|x|fix smoke-test SKILL.md cite drift|-
T40|x|impl §V.60 + §V.59 G1 amend — Gmail-API poll, ⊥ local-DB|V37,V59,V60
T41|x|impl §V.54 — CRM CLI mutation telemetry matrix|V51,V64,V53,V63,V54
T42|x|patch dotless §-cites in src/ per §V.66|V66
T43|x|impl §V.31 — _DEFERRED_TASK trigger-aware|V64,V45,V30,V31
T44|x|impl §V.55 — logfire ScrubbingOptions; retain tool_response content|V64,V55
T45|x|patch dotless §-cites in tests/ per §V.66|V66
T46|x|impl §V.7 — add workflow_id/email_id/task_id to ActivitySummary|V64,V7
T47|x|strip company ∧ contact to identity-min; disabled_reason; --note STR atomic|V4,V5,V13,V14,V64,V63,V54,V7
T48|x|impl §V.8 — ContactView/CompanyView w/ inline notes|V4,V5,V13,V14,V39,V64,V45,V8
T49|x|impl §V.9 isatty-guard on stdin-fallback imports|V64,V9
T50|x|patch bare-form V<n> cites in code prose to §V.<n>|V66
T51|x|patch bare-form V31 cite in qa.py|V66
T52|x|impl §V.32 — enrollment add --scheduled-at; enrollment_schedule trigger|V21,V26,V64,V30,V54,V31,V32
T53|x|impl §V.33 — enrollment add rejects contact-email ≡ account-email|V4,V64,V33
T54|x|sweep remove handlers — hard-DELETE composite-key projection per §V.4|V4,V64,V54
T55|x|impl §V.10 — contact disable cmd|V4,V64,V54,V10
T56|x|impl §V.5 parent-NI — add account_email to Workflow / WorkflowSummary|V4,V5,V64,V63
T57|x|schema — add schema_metadata table per §V.18|V18
T58|x|impl §V.18 + §V.19 — schema-hash drift detection|V51,V64,V18,V19
T59|x|impl §V.11 — reshape status payload (version, schema, sync_loop, accounts, tasks, config, counts)|V4,V64,V18,V11
T60|x|impl §V.38 — _DRIVE tools sequential=True; broaden tool catches|V64,V48,V49,V38
T61|x|impl §V.23 — concurrent worker pool in _drain_pending_tasks|V64,V49,V38,V23
T62|x|widen _check_spec_table — \s+ floor + 80-char line cap|V64,V42
T63|x|impl §V.26(+) — agent.invoke span sets email_id attr|V26,V64
T64|x|impl §V.61 — gate-verdict source = Logfire spans, ⊥ CLI poll|V64,V52,V59,V61
T65|x|impl §V.7(+) — add gmail_thread_id to EmailSummary|V64,V7
T66|x|impl §V.57(+) — source_file_alts in qa_pairs.json|V64,V57
T67|x|impl §V.24 — decouple drain pool from main-loop tick|V64,V23,V24
T68|x|impl §V.25 — scope agent advisory lock to unit of work|V64,V23,V24,V25
T69|x|impl §V.41(+) — _DRIVE_GROUNDING 2-consecutive-miss fallback to list_drive_markdown|V64,V41
T70|x|impl §I(+) — persist route_method on email; project on Email ∧ EmailSummary|V64,V7
T71|x|impl §V.20 — email.route_method enum CHECK + is_routed coupling CHECK in schema.sql|V64,V20
T72|x|impl §V.18(+) — extend make clean to drop ∧ recreate mailpilot_test alongside mailpilot|V18,V64,V65
T73|x|impl §V.67 — outbound create_email ! pass in_reply_to=resolved_in_reply_to, references_header=resolved_references|V64,V67
T74|x|sweep 60s → 90s @ SLA call-sites per §V.59(+) — vocab table: `60s SLA`,`60s promise`,`60s ceiling`,`60s latency verdict`,`within 60s`,`> 60`,`60s reply window`,`(60s SLA)`,`~10-60s`,`~20-60s`; scope = SPEC.md §V.23 ∧ §V.24 Why-clauses ∪ `.claude/skills/smoke-test/SKILL.md` ∪ `.claude/skills/demo-test/SKILL.md`; out-of-scope = `run_interval (default 60s)` Settings field, httpx 60s read-window per §V.48, `12 attempts × 5s ~60s` polling cadence math, `_DRIVE_HTTP_TIMEOUT_SECONDS` per §V.34/§V.49|V59,V64
T75|x|impl §V.68 — `reply_email` pre-send fact-check: per-invocation read-ledger keyed by `file_id` (populated on each `read_drive_markdown` success); tokenize proposed reply body, assert candidate spec-value tokens ∈ ledger union, return structured `{"error": "fact_check_mismatch", "unsupported": [...]}` on miss; zero-ledger no-op; cover `reply_email` ∧ `send_email`; regression test: mock agent body fabricating WS36-600-2=65 → wrapper returns error, agent re-drafts|V64,V68
T76|x|sweep §V.6 — fix `src/mailpilot/SKILL.md` per audit recipe; vocab table: `--body-text` → `--body` (3 sites: email send/reply, note add); `mailpilot email reply <EMAIL_ID>` → `mailpilot email reply --account-id <ID> --email-id <EMAIL_ID> --body "..."`; `mailpilot account sync <ACCOUNT_ID>` → `mailpilot account sync [--account-id <ID>]` (optional, omit → all); `mailpilot enrollment run <ENROLLMENT_ID>` → `--workflow-id <WID> --contact-id <CID>` (same for `enrollment view|update|remove`); verb roster (add `sync, disable, retry, get, set`); settings list (add `max_concurrent_tasks: int (default 4)`; state `run_interval` default `60`); env-prefix paragraph (rewrite: env vars use `MAILPILOT_<UPPERCASE_KEY>` form, e.g. `MAILPILOT_DATABASE_URL`)|V64,V6
T77|x|impl scalar enrollment.id per designs/enrollment-id.md — schema: enrollment.id UUIDv7 PK + UNIQUE(workflow_id, contact_id), task.enrollment_id NOT NULL FK, activity.enrollment_id nullable FK; CLI: enrollment {run,view,update,remove} take positional <ENROLLMENT_ID>, add ∧ list keep flagged constructor pair; agent tools: record_enrollment_outcome(enrollment_id) signature, list_enrollments adds id field; populate task.enrollment_id ∈ create_tasks_for_routed_emails ∧ outbound first-touch ∈ enrollment add --scheduled-at; src/mailpilot/SKILL.md positional-form rewrite; §V.6 clause (v) carve-out delete; FK validators extend w/ get_enrollment_by_id; make clean recycles schema|V6,V12,V28,V32,V64
T78|x|diag + close §V.42 lint bypass — §B.48: lint fired B7-first (4ms span, format error) but ⊥ fired B4 / B7-second / B75a / B75b despite stored body_text replays returning {"error":"format"}; trace tool-wrapper → email_ops → sync.send_email path for body mutation between Tool dispatch ∧ _check_spec_table entry; add integration test posting a body w/ ≥3 consecutive spec-row lines ∧ no pipe separator to `agent_tools.reply_email` ∧ `agent_tools.send_email` — both ! return {"error":"format"}|V42,V64
T79|x|impl §V.5(+) — EnrollmentSummary adds `workflow_name` (JOIN workflow on list_enrollments query); Enrollment view loads `workflow_name` + `contact_email` + `contact_name` via JOIN; impl-choice between (a) row-promotion onto Enrollment model (Workflow.account_email precedent) ∨ (b) EnrollmentView projection (ContactView/CompanyView per §V.8) ≡ build-step decision; regression test asserts `enrollment view <id>` envelope inner-dict ⊇ `enrollment list` row at parent-denorm field set|V5,V64
T80|x|impl §V.23(+) atomic-claim clause — dispatcher `_drain_pending_tasks` ! claim each task before pool submission; impl-choice between (a) `list_pending_tasks` UPDATE-RETURNING `status='pending'→'running'`, (b) `FOR UPDATE SKIP LOCKED` + status flip ∈ same txn, (c) in-process in-flight task-id set excluding re-selection — build-step decision; new state ! reset on worker exception (paired w/ `_handle_agent_failure`); regression test asserts N parallel ticks against single pending row → 1 agent.invoke + zero post-completion re-dispatch|V23
T81|x|raise Settings.max_concurrent_tasks default 4→10 per §V.23(+) burst-load formula; src/mailpilot/settings.py:65; smoke C4.a at default settings MUST yield max_latency≤110s ∧ p95≤90s|V23
T82|x|impl §V.54(+) envelope-translation clause — extend `cli_mutation` (`src/mailpilot/operator_log.py:44`) to catch `psycopg.Error` after the observability pair, map constraint subclass → error code (`UniqueViolation`→`duplicate_key`, `ForeignKeyViolation`→`foreign_key_violation`, `NotNullViolation`→`not_null_violation`, `CheckViolation`→`check_violation`, else `database_error`), then call `output_error(str(exc), code)` instead of `raise`; `output_error` imported lazily inside the except branch to preserve §V.2 cli.py top-level-imports rule (operator_log.py ⊥ cli.py ∴ lazy import sidesteps the §V.2 scope but matches the spirit of slow-import deferral); regression test: 5 §B.52 recurrence-class sites empirically confirmed via re-run probe — (a) `account create --email <existing>` → `duplicate_key` (account_email_key), (b) `company create --domain <existing>` → `duplicate_key` (company_domain_key), (c) `contact create --email <existing>` → `duplicate_key` (contact_email_key), (d) `workflow create --account-id <A> --name <existing>` → `duplicate_key` (workflow_account_id_name_key, composite), (e) `contact update <id> --email <peer>` → `duplicate_key` (contact_email_key); ∀ 5 cases assert exit 1, stdout empty, stderr contains operator `event=error source=<noun>.<verb> ...` line ∧ JSON envelope `{"error":"duplicate_key", "message":"...", "ok":false}`, ⊥ `Traceback (most recent call last):` substring; defensive coverage for FK / NOT NULL / CHECK codes — currently pre-handler-guarded (Click `Choice` validator on workflow template/theme; pre-mutation `get_<noun>` FK guards on contact/workflow/enrollment) ∴ no live recurrence-class trigger, but `cli_mutation` translation MUST still map all 4 subclasses via direct unit test invoking the wrapper w/ a synthesized `psycopg.errors.{ForeignKeyViolation,NotNullViolation,CheckViolation}` raise inside the `with`-block (one assertion per code) — guards against future handler refactor that drops a pre-check exposing a constraint path|V2,V3,V54,V64
T83|x|impl §V.69 — after `_run_periodic_iteration` (`src/mailpilot/sync.py`) classifies N_new > 0 new inbound emails in a pass, immediately call `wakeup_event.set()` + set next-tick `do_full_sweep=True` before returning; regression test fires 10 emails at P=5 concurrency against a test loop w/ instrumented wakeup-count counter ∧ asserts at least 2 consecutive full-sweep ticks ∧ `max(task.created_at − email.received_at) ≤ 75s` (use `postgresql://localhost/mailpilot_test`)|V69,V23
T84|x|impl §V.41 per-target compare-and-contrast clause — extend `_DRIVE_GROUNDING` text (`src/mailpilot/agent/templates.py:122-135`) w/ per-target search-budget rule: reply addresses ≥2 distinct product targets → ∀ target ! ≥1 target-specific `search_drive_markdown` query before `list_drive_markdown` accepted as grounding source for that target, even when other targets already grounded; regression test asserts fragment text contains per-target rule substring (e.g. `"per target"` ∨ `"each target"`) ∧ `inbound-google-drive` template registers `_DRIVE_GROUNDING` per §V.45|V41,V64
T85|x|impl §V.68 pipe-table-row scoping — replace `union = "\n".join(read_ledger.values())` (`src/mailpilot/agent/tools.py:82`) w/ pipe-table-line-filtered union (`"\n".join(line for c in read_ledger.values() for line in c.splitlines() if "|" in line)`); update §T.75 regression test fixture: (a) source mock contains WS36-600-2 pipe-row w/ 65 cont / 120 peak / 30 backwash + prose lines `"Temperature: 35-110 degrees F"` ∧ `"Operating pressure: 25 - 125 psi"`; (b) body claiming `"110 GPM continuous"` → `fact_check_mismatch` (`"110"` ∉ any pipe-row line); (c) body claiming `"65 GPM continuous"` → passes; (d) body claiming `"125 GPM peak"` → `fact_check_mismatch` (`"125"` ∉ pipe-rows, only prose); regression-test coverage matches §B.56 column-context-blindness recurrence class|V68,V64
T86|x|impl §V.71 — per-invocation format-lint rejection counter in `reply_email` ∧ `send_email` wrappers (`src/mailpilot/agent/tools.py`) keyed by `contextvars.ContextVar` set @ `run.execute_task` entry, reset on task exit; each `_check_spec_table` hit (§V.42) increments counter; counter ≥ 3 → wrapper accepts body w/o lint ∧ emits `logfire.warn("reply_email.format_lint.cap_reached", attempt=N, workflow_id=..., email_id=...)` ∧ proceeds to normal `email_ops.{reply,send}_email` send path; regression test: 3 consecutive lint-failure invocations on same task → 3rd call bypasses lint ∧ delivers, warn span emitted on bypass|V42,V64,V71
T87|x|impl §V.59(+) ∧ §V.61(+) two-budget observability — smoke-test §C4.a query (`.claude/skills/smoke-test/SKILL.md`) ∧ demo-test G1/G3 (`.claude/skills/demo-test/SKILL.md`) project `sla_agent_seconds` ∧ `sla_delivery_seconds` server-computed columns over `agent.invoke` spans; smoke-test gate asserts `p95(sla_agent_seconds) ≤ 75s` (burst) ∧ `p95(sla_agent_seconds) ≤ 50s` (steady), reports `max/p95/p50(sla_delivery_seconds)` w/o gating; demo-test G1 still gates end-to-end ≤ 90s; G1 FAIL → 3-bullet summary ∋ decomposition `sla_agent`, `sla_delivery`, total|V59,V61
T88|x|impl §V.68(+) per-document scoping — replace uniform pipe-row union in `_fact_check_body` (`src/mailpilot/agent/tools.py:160-165`) w/ per-document contribution: `union_parts = []; for content in read_ledger.values(): lines = content.splitlines(); union_parts.extend(l for l in lines if "|" in l) if any("|" in l for l in lines) else union_parts.append(content); union = "\n".join(union_parts)`; update §T.75 regression test fixture: (a) prose-only source mock containing `"KDF 55 medium can remove over 99% of free chlorine"` + body citing `"55"`, `"99"` → passes (per-doc full-content admit); (b) mixed-ledger w/ prose source + table source containing WS36-600-2 65 cont / 120 peak pipe-row → table doc pipe-row-scoped, prose doc full-content; (c) §B.56 single-doc collision case (same doc has `"110"` in both prose `"35-110 degrees F"` AND CVP pipe-row, body claims `"110 GPM continuous"` for WS36-600-2) → still `fact_check_mismatch` (pipe-row scoping unchanged for table-bearing docs)|V68,V64
T89|x|extend §V.71 counter to cover `_fact_check_body` rejections — rename `_consume_format_lint_rejection` → `_consume_reply_rejection` in `src/mailpilot/agent/tools.py`; add `rejection_type: Literal["format","fact_check"]` param; call `_consume_reply_rejection` at both `_check_spec_table` ∧ `_fact_check_body` rejection sites in `reply_email` ∧ `send_email` wrappers; cap=3 unchanged; bypass at cap returns body w/o either check; rename `_FormatLintCounter` → `_ReplyRejectionCounter` ∧ `_FORMAT_LINT_REJECTIONS` ContextVar → `_REPLY_REJECTIONS`; rename `format_lint_scope` → `reply_rejection_scope` (update `src/mailpilot/run.py:18,59` import + entry); update warn span name → `"reply_email.reply_rejection.cap_reached"` w/ `rejection_type` attribute; extend §T.86 regression test: (a) 3 consecutive `fact_check_mismatch` hits on same task → 3rd call bypasses fact-check ∧ delivers, warn span emitted w/ `rejection_type="fact_check"`; (b) mixed sequence (1 format + 2 fact-check) → 3rd call (the 3rd rejection regardless of type) bypasses, warn emitted w/ rejection_type of the 3rd hit|V42,V68,V71
T90|x|sweep smoke-test SKILL.md compare-type sla_agent gate per §V.61(+) — vocab table: `sla_agent_seconds > 50` (B7 gate), `sla_agent_seconds near the 50s ceiling` (B7 prose), `sla_agent<=50s` (B7 timing-table row); scope = `.claude/skills/smoke-test/SKILL.md` only — B7 (compare) call-sites only, ⊥ B4/B6/B7.5 (single-source ∧ decline retain 50s); B7 gate rewrites to `sla_agent_seconds > 90s` (Critical) w/ `50s < sla_agent ≤ 90s` advisory; burst C4.a non-compare p95 ≤ 75s, compare p95 reported separately w/ advisory 120s; out-of-scope = §V.61 SPEC.md row (already amended), all B4/B6/B7.5 gates (single-source ∧ decline), demo-test G1 end-to-end (still 90s per §V.59)|V61,V64
T91|x|impl §V.10(+) enrollment coverage ∧ §V.15(+) — schema (`src/mailpilot/schema.sql`): extend `enrollment.status` CHECK to admit `'disabled'`, add `disabled_reason TEXT` column, add coupling CHECK `(status = 'disabled') = (disabled_reason IS NOT NULL AND TRIM(disabled_reason) <> '')`, extend `activity.type` CHECK w/ `'enrollment_disabled'`; database (`src/mailpilot/database.py:1625-1664`): drop `delete_enrollment`, add `disable_enrollment(connection, enrollment_id, reason) -> Enrollment \| None` (single txn: UPDATE enrollment SET status='disabled', disabled_reason=%s RETURNING * + INSERT activity row type='enrollment_disabled' carrying summary=reason, detail={"reason": reason}, enrollment_id, contact_id, workflow_id); models (`src/mailpilot/models.py`): `Enrollment` gains `disabled_reason: str \| None`; cli (`src/mailpilot/cli.py:2657-2677`): drop `enrollment_remove` Click cmd, add `enrollment_disable(enrollment_id, --reason)` handler per §V.10 (reason.strip() validation, pre-row `get_enrollment_by_id` read, `cli_mutation("enrollment", "disable", ...)` span, `operator_event("enrollment.disable", changed=[...])`, singular envelope w/ full Enrollment model); agent runtime + tools status-filter sweep — scope = grep pattern `enrollment\.status\b` ∈ {`src/mailpilot/agent/**/*.py`, `src/mailpilot/sync.py`, `src/mailpilot/routing.py`, `src/mailpilot/run.py`, `src/mailpilot/database.py`} (`list_enrollments` tool, routing-pipeline `_ensure_enrollment` guard, drain-loop filters, any `WHERE status = 'active'` ∨ `WHERE status IN (...)` clauses must exclude `'disabled'`); out-of-scope = `schema.sql` CHECK row, `models.py` field declaration; SKILL.md sweep — `src/mailpilot/SKILL.md`, vocab table: `enrollment remove <ENROLLMENT_ID>` → `enrollment disable <ENROLLMENT_ID> --reason "<text>"`, verb-roster `remove` → `remove, disable`; tests sweep — scope = grep pattern `test_enrollment_remove` ∈ `tests/` → rewrite per §V.10 enrollment-coverage surface (validation, not-found, idempotent re-disable, activity-row emit assertion, status-filter exclusion); make clean recycles schema per §T.77 — ⊥ migration script|V4,V10,V14,V15,V17,V31,V54,V64
T92|x|impl §V.10(+) tag coverage — schema (`src/mailpilot/schema.sql`): add `tag.disabled_reason TEXT` nullable, add coupling CHECK `(disabled_reason IS NULL OR TRIM(disabled_reason) <> '')`, extend partial unique indexes `idx_tag_contact_unique` ∧ `idx_tag_company_unique` to also scope `AND disabled_reason IS NULL`, extend `activity.type` CHECK w/ `'tag_disabled'` (retain `'tag_removed'`); database (`src/mailpilot/database.py`): DROP `delete_tag` (`:2840-2865`), `remove_contact_tag` (`:3068-3110`), `remove_company_tag` (`:3113-3149`); ADD `disable_contact_tag(connection, contact_id, name, reason) -> Tag \| None` + `disable_company_tag(connection, company_id, name, reason) -> Tag \| None` (single-txn UPDATE-RETURNING gated on `disabled_reason IS NULL` + activity INSERT per §V.10 (c)); models (`src/mailpilot/models.py`): `Tag` gains `disabled_reason: str \| None`; cli (`src/mailpilot/cli.py`): DROP `tag_remove` Click cmd (`:1487-1560`), ADD `tag_disable(--contact-id, --company-id, name, --reason)` handler per §V.10 shape (XOR validation, `reason.strip()` validation, pre-row `get_contact`/`get_company` FK lookup per §V.64 patch contract, `cli_mutation("tag", "disable", entity_id=name, owner_type=..., owner_id=...)` span, `operator_event("tag.disable", changed=[...])`, singular envelope w/ full Tag model); query-filter sweep — scope = grep pattern `^def (list_tags\|list_contacts_by_tag\|list_companies_by_tag\|search_tags)` ∈ `src/mailpilot/database.py` (4 fns) → add `include_disabled: bool = False` kwarg + `WHERE disabled_reason IS NULL` default filter; CLI-flag sweep — scope = grep `@tag\.command\("(list\|search)"\)` ∈ `src/mailpilot/cli.py` → add `--include-disabled` flag forwarding into kwarg; SKILL.md sweep — `src/mailpilot/SKILL.md` `tag add` example block (`:185-194`) gains `mailpilot tag disable --contact-id <CID> vip --reason "<text>"` peer line; tests sweep — scope = grep pattern `(test_tag_remove\|test_delete_tag\|test_remove_contact_tag\|test_remove_company_tag)` ∈ `tests/` → rewrite per §V.10 (c) (validation, not-found, idempotent re-disable returns None, activity-row emit assertion, query-filter exclusion w/ ∧ w/o `--include-disabled`); make clean recycles schema per §T.77 — ⊥ migration script|V4,V10,V13,V14,V17,V54,V64
T93|x|impl §V.16(+) sweep — convert ∀ bare-INSERT create_<noun> ∈ `src/mailpilot/database.py` w/ natural-unique-key constraint to `INSERT ... ON CONFLICT (<unique_cols>) DO NOTHING RETURNING *` ∧ return type `Model \| None`. Scope = grep pattern `rg -nE '^def create_(account\|company\|contact\|workflow)\b' src/mailpilot/database.py` → 4 sites: `create_account` (`:420`; conflict cols `(email)`), `create_company` (`:554`; `(domain)`), `create_contact` (`:715`; `(email)`), `create_workflow` (`:1054`; composite `(account_id, name)`). Caller updates: (a) CLI handlers — `cli.py:296+` (account), `:471+` (company), `:721+` (contact), `:1791+` ∧ `:2169+` (workflow) — branch on `None` → `output_error(f"<noun> with <natural_key>={value!r} already exists", "duplicate_key")` per §V.4 envelope; ⊥ raise; `cli_mutation` span outcome ≡ ok; `operator_event("<noun>.create", ..., changed=[])` per §V.54 idempotent re-invoke semantics. (b) Sync caller `create_or_get_contact_by_email` (`database.py:797-837`): `create_contact(...) is None` → re-fetch via `get_contact_by_email` ∧ return; backfill branch unchanged. (c) Import handlers `company_import` (`cli.py:610-690`) ∧ `contact_import` (`cli.py:920-1020`): per-row `create_X(...) is None` → existing `{"error": "duplicate", ...}` result row; pre-fetch `existing` set retained as optimization, ⊥ relied on for correctness; `operator_event("<noun>.import", ..., changed=[])` on duplicate branch. (d) Workflow composite-key envelope message: `f"workflow {name!r} already exists for account {account_id}"`. Tests sweep — scope = grep pattern `rg -n 'test_create_(account\|company\|contact\|workflow)\b' tests/` → add (e1) per-fn unit: `create_X` against existing natural key returns `None`, ⊥ raises, ⊥ `logfire.exception`; (e2) per-CLI: re-run cmd against existing row exits 1 w/ envelope `{"error":"duplicate_key","ok":false}` ∧ `capfire: CaptureLogfire` asserts span outcome=ok, ⊥ `.failed` suffix; (e3) sync TOCTOU: 2 concurrent threads call `create_or_get_contact_by_email` w/ same novel email → exactly 1 INSERT, both threads return same Contact row, ⊥ raise. §V.54 envelope tests for FK / NOT NULL / CHECK paths (§T.82 (d)) unchanged — those still raise + §V.54 catches. Manual regression: re-run `mailpilot company create --domain lab5.ca` post-fix → exit 1, stdout duplicate_key envelope, stderr operator-log error line, logfire span outcome=ok.|V16,V54,V64
T94|x|impl §V.68(+) list-item corpus-build per §B.60 — `_fact_check_body` (`src/mailpilot/agent/tools.py:149`) table-bearing-doc branch (`:177` `pipe_lines = [line for line in lines if "|" in line]`) extends admit set: union pipe-row lines w/ list-item lines matching `re.match(r"^\s*[-*]\s", line)`. Scope = grep `rg -n '"\|" in line' src/mailpilot/agent/tools.py` → 1 hit @ `:177` (fact-check branch); `:222` ∈ `_check_spec_table` excluded (different concern per §V.42). Prose-only-doc branch unchanged (full content already admitted per §T.88). §B.56 anti-collision preserved — bullet `Key:` labels context-anchor, ⊥ cross-product prose-line leakage class. Test sweep: scope = grep `rg -n 'test_fact_check\|_fact_check_body' tests/` → extend existing fact-check tests w/ table-bearing-doc fixture carrying `"- Specific Gravity: 2.0"` bullet line — assert numeric token `2.0` ∈ corpus union, body containing `2.0` ⊥ rejected by `_fact_check_body`. Detected by /sdd:check 2026-06-09 (V68 VIOLATE).|V68,V64
T95|x|impl §V.6(+) `.claude/skills/**/*.md` vocab-drift audit per §B.65 — extend `.claude/check-extras.md §V.6` recipe steps (i) ∧ (ii) to additionally scan file set `.claude/skills/**/*.md`. Scope = grep `rg -nE '\bmailpilot (account\|company\|contact\|workflow\|enrollment\|tag\|note\|task\|email\|activity\|template) ' .claude/skills/` → 65 hits across 4 skill dirs (demo-test, lead-encreach, release, smoke-test) @ amend time. Fix L40 site: `.claude/skills/smoke-test/SKILL.md:40` → drop `tag remove` ∧ `enrollment remove` from operational-commands enumeration (verify: `mailpilot {tag,enrollment} disable` returns singular envelope per §V.10 / §V.4 → bespoke-shape claim ⊥ holds, parenthetical removal preferred over verb substitution). Test sweep: extras-recipe ⊥ test-covered ∴ manual /sdd:check post-impl validates scan emits 0 hits.|V6,V64
T96|x|impl §V.70 burst retry-rate gate @ smoke-test C4.a per §B.66 — extend existing C4.a CTE (`.claude/skills/smoke-test/SKILL.md:~920-960`) to sum rollup-span `tool_error_count` attr already set @ `src/mailpilot/agent/invoke.py:708`: add `SUM(COALESCE((attributes->>'tool_error_count')::int, 0)) AS n_tool_errors` ∈ outer select over the `burst` CTE; compute `n_tool_errors::float / NULLIF(n_invokes::float, 0) AS retry_rate`. Add assertion to C4.a "Assertions" block: `retry_rate <= 0.05` (≤5% per §V.70 ceiling) — failure ⊢ "prompt-fidelity regression under load → investigate §V.41 (search-first ordering), §V.57 (KB coverage), §V.42 (format-lint sensitivity)" per §V.70 body. Scope = grep `rg -n 'C4\.a\|n_warns\|n_exceptions' .claude/skills/smoke-test/SKILL.md` → 1 C4.a CTE block ∧ 1 Assertions bullet list. Source-of-truth rationale: rollup-attr `tool_error_count` (single CTE, ⊥ extra LEFT JOIN over `agent.tool_errors` warn spans) preferred. Test sweep: smoke-test gate is operator-judged (manual run) ∴ ⊥ automated test; manual /sdd:check post-impl re-classifies V70 HOLD.|V70,V61,V64
T97|x|impl §V.72 schema migration — add `company.profile JSONB NULL` column to `src/mailpilot/schema.sql` `company` table; ⊥ index (operator-query selectivity = NULL/NOT NULL only @ v1 cardinality, partial-index unwarranted); ⊥ backfill — `make clean` recreates per §V.18 precedent. Scope = grep `rg -n 'CREATE TABLE company' src/mailpilot/schema.sql` → 1 hit. Test: schema-apply pytest fixture detects new column via `information_schema.columns` query; `mailpilot status` envelope renders fresh `schema.hash` post-`make clean` per §V.18.|V72,V18
T98|x|impl §V.72 `CompanyProfile` Pydantic model + `update_company` validation — add `class CompanyProfile(BaseModel)` to `src/mailpilot/models.py` w/ field signatures per §V.72 row body; `database.update_company` ∈ `src/mailpilot/database.py` accepts `profile: dict | None = None` kwarg → non-None branch validates via `CompanyProfile.model_validate(profile)` (raise `pydantic.ValidationError` on mismatch), persists `json.dumps(model.model_dump(exclude_unset=True))` via `UPDATE company SET profile = %s::jsonb ...`; `Company` ∈ `models.py` gains `profile: dict | None = None` reading JSONB column. CLI `company update` ∈ `src/mailpilot/cli.py` gains `--profile-json TEXT` flag → handler `json.loads(profile_json)` (catch `JSONDecodeError` → `output_error("invalid JSON: <e>", "validation_error")` per §V.4), forward dict to `update_company`; raised `ValidationError` caught by `cli_mutation` boundary per §V.54 → envelope `{"error": "validation_error", ...}`. Test sweep: extend `tests/test_database.py` ∧ `tests/test_cli_company.py` w/ fixtures {valid-full, valid-partial-no-timezone, invalid-missing-summary, invalid-non-list-products, invalid-empty-sources, invalid-json-text} — assert validation outcome ∧ envelope shape.|V72,V54,V64
T99|x|impl §V.72 `company list --no-profile | --has-profile` filter + `CompanySummary.has_profile` projection — `database.list_companies` ∈ `src/mailpilot/database.py` accepts `has_profile: bool | None = None` kwarg → `None` (default, no filter), `True` (`WHERE profile IS NOT NULL`), `False` (`WHERE profile IS NULL`); SELECT projects `(profile IS NOT NULL) AS has_profile` alongside existing columns. CLI `company list` ∈ `src/mailpilot/cli.py` gains mutually-exclusive `--has-profile` ∧ `--no-profile` flags → both set → `output_error("--has-profile and --no-profile are mutually exclusive", "validation_error")` per §V.72. `CompanySummary` ∈ `src/mailpilot/models.py` gains `has_profile: bool` field. Test sweep: extend `tests/test_database.py` w/ 3-row fixture (`profile=NULL`, `profile={...full}`, `profile={...partial}`) — assert filter behavior ∀ kwarg setting ∧ Summary projection accuracy; CLI XOR-flag test in `tests/test_cli_company.py`.|V72,V5,V64
T100|x|impl `lead-encreach` skill @ `.claude/skills/lead-encreach/SKILL.md` per `designs/lead-encreach.md` — sub-commands {`seed <domain>...`, `seed-file <path> [--column NAME]`, `list-stale [--limit N]`, `enrich <id|domain>`, `enrich-all [--limit N]`}; file ingestion source/format-agnostic — auto-detect CSV vs plain-text from first non-empty line, apex-domain-only extraction (CSV column-header sniff `[domain, website, company_url, url]` ∨ `--column NAME` override, plain-text per-line w/ `#` comments ignored), redirect resolution via `curl -sLI`, collision-on-resolved-apex skip+log; ⊥ pre-populate profile from CSV columns ∨ text-line annotations. Concurrent enrichment via Workflow `parallel()` w/ concurrency 3 dispatched as `agent(..., {agentType: "company-profiler"})`; AskUserQuestion batch gate @ >10 stale rows (options: First-10 (Recommended) / First-25 / All-N). Skill frontmatter: `model: opus`, `allowed-tools: Bash(mailpilot company *), Bash(curl *), Bash(lynx *), Read, Task, AskUserQuestion`. Skill body inherits `## OUTPUT — "Next" block` heading per §V.6(+) user-typeable-SKILL.md contract. Test sweep: skill-body audit ⊥ test-covered (operator-judged manual /sdd:check); embed mechanical smoke via `seed acme.com beta.io` no-op re-run idempotency check post-impl.|V72,V6
T101|x|impl `company-profiler` sub-agent @ `.claude/agents/company-profiler.md` per `designs/lead-encreach.md` enrichment-agent-prompt block — frontmatter: `name: company-profiler`, `description: <design line>`, `model: sonnet`, `tools: Bash, mcp__claude_ai_FireCrawl__firecrawl_scrape`. System prompt: fetch via `curl -sL --max-time 20 -A "Mozilla/5.0" "https://<DOMAIN>/" | lynx -dump -stdin -nolist -width=120`; empty / blocked / JS-shell / <500 chars → retry `www.<DOMAIN>` ∧ `<DOMAIN>/about` ∧ Firecrawl fallback; distill into `CompanyProfile` JSON; update canonical `name` via `mailpilot company update <ID> --name "..."` if differs from placeholder; persist via `mailpilot company update <ID> --profile-json '...'`; return JSON `{company_id, domain, status: "enriched|skipped|failed", reason}`. ASCII-only constraint per §C. Generic agent name (⊥ `lead-encreach-` prefix) preserves reusability across future skills (account-create, contact-enrichment).|V72
T102|x|impl §V.8(+) dynamic field forwarding @ `load_contact_view` ∧ `load_company_view` per §B.67 — replace explicit-field constructor list w/ `**row.model_dump()` splat (∨ equivalent dynamic forwarding). Scope = grep `rg -n 'return (Contact\|Company)View\(' src/mailpilot/database.py` → 2 hits @ `:3505` ∧ `:3535`; fix both. Tests: extend `tests/test_database.py` w/ company fixture carrying `profile={"summary":...,"products":[...],...}` — assert `load_company_view(...).profile == fixture_profile` (regression guard for V8 field-omission class); same shape covers ContactView when future columns land. Detected by /sdd:check 2026-06-10 (V8 VIOLATE post-T98 Company.profile addition).|V8,V64
T103|x|impl §V.73 skill-snippet executability fix @ `lead-encreach` enrich-all per §B.68 — `.claude/skills/lead-encreach/SKILL.md` enrich-all step-4 ```js snippet: bind `stale` explicitly (capture `mailpilot company list --no-profile` JSON → inline `const STALE = [...]` ∨ `JSON.parse(args)` w/ `typeof args === 'string'` guard); chunk dispatch to honor §V.72 "concurrency 3" (batch into groups of 3 → sequential `await parallel(batch.map(...))`). + author `.claude/check-extras.md §V.73` mechanical audit recipe (free-symbol scan + `args.`-w/o-`JSON.parse` + prose-vs-`parallel` divergence ∀ skill-body ```js block). Scope = grep `rg -n 'await parallel\(' .claude/skills/` → 1 hit @ `.claude/skills/lead-encreach/SKILL.md:113`. Test sweep: skill-body ⊥ test-covered (operator-judged manual /sdd:check) — post-impl `/lead-encreach` enrich-all dispatches >0 agents (regression guard vs 0-agent abort).|V73
T104|x|impl §V.74 RFC-4180-parser-mandate fix @ `lead-encreach` seed-file per §B.69 — `.claude/skills/lead-encreach/SKILL.md` seed-file steps 1-5 (L61-69): CSV mode ! mandate `csv.DictReader` (⊥ physical-line iteration of Read-tool output), add embedded-newline caveat; plain-text line-iteration unchanged. Replace resolve_apex (L148-150) HEAD-grep w/ `curl -sL -o /dev/null --max-time 12 -w '%{url_effective}'`. + author `.claude/check-extras.md §V.74` recipe (flag physical-line / `splitlines` CSV ingestion ∈ `.claude/skills/**/*.md` ∧ `src/**`). Scope = grep `rg -n 'first non-empty line|curl -sLI' .claude/skills/` → 2 hits @ `.claude/skills/lead-encreach/SKILL.md` (L62, L149). Test sweep: skill-body ⊥ test-covered — post-impl seed-file on multi-line-quoted CSV fixture yields one company per logical row (⊥ phantom rows).|V74
T105|x|impl §V.72(+) intent-driven pipeline @ `lead-encreach` skill — `.claude/skills/lead-encreach/SKILL.md`: drop sub-command dispatch {`seed`, `seed-file`, `list-stale`, `enrich`, `enrich-all`} → single implicit pipeline (classify free-form args: file path → ingest w/ CSV/plain-text auto-detect, domain/UUID tokens → seed, bare → stale queue; domain arg ≡ idempotent ensure-exists-∧-enriched; stale query absorbed as internal step; `--limit N` pre-answers batch gate). Frontmatter `argument-hint` → `[<domain>... | <file-path>] [--limit N]`. Preserve verbatim bug-bought recipes: `csv.DictReader` RFC-4180 extraction block ∧ `curl -w '%{url_effective}'` resolve_apex (§V.74); chunked concurrency-3 Workflow snippet w/ `JSON.parse(args)` guard (§V.73); domain-only ingestion (⊥ pre-populate from CSV columns). Retain `## OUTPUT — "Next" block` heading per §V.6 user-typeable contract — items rephrased free-form (⊥ sub-command verbs). Scope = grep `rg -n 'seed-file|list-stale|enrich-all|## Sub-commands' .claude/skills/lead-encreach/SKILL.md`. Test sweep: skill-body ⊥ test-covered (operator-judged manual /sdd:check) — post-impl bare `/lead-encreach` ∧ `/lead-encreach <csv-path>` ∧ `/lead-encreach <domain>` classify correctly; batch gate remains sole AskUserQuestion site.|V72,V73,V74,V6
T106|x|impl §V.72(+) name-carve-out @ `lead-encreach` ingest — `.claude/skills/lead-encreach/SKILL.md`: CSV mode ! extract a name-ish column (`company_name` / `name` / `company`, first match) alongside the domain column ∧ seed `company.name` from it (placeholder only, ⊥ profile field); plain-text mode → apex-domain placeholder unchanged; ingest step-5 narrow `⊥ pre-populate profile from CSV columns` to PROFILE JSONB body (drop `all non-domain content discarded`); Scope-line + frontmatter `External data sources contribute the apex domain only` → `apex domain + optional CSV display-name placeholder; profile fields agent-discovered`; seed-stage `--name <resolved>` → `--name <csv-name ∨ resolved>`. `company-profiler` agent placeholder_name note already tolerates non-apex names ∴ ⊥ agent change. Scope = grep `rg -n 'apex domain only|pre-populate profile|placeholder name = apex|--name <resolved>|non-domain content discarded' .claude/skills/lead-encreach/SKILL.md`. Test sweep: skill-body ⊥ test-covered (operator-judged /sdd:check) — post-impl `/lead-encreach <csv-w/-name-col>` seeds CSV names visible in `company list` pre-enrichment (⊥ apex placeholders).|V72
T107|x|impl §V.16(+) operator-log fix per §B.70 — strike success-named `operator_event("<noun>.create", ..., changed=[])` on race-safe-duplicate (`created is None`) branch ∈ CLI create handlers; duplicate observability ≡ `output_error` envelope (exit 1) only, logfire span outcome stays ok (⊥ reintroduce `.failed`). Scope = grep `rg -nPU 'operator_event\(\s*\n?\s*"[^"]*",[^)]*changed=\[\],?\s*\)\s*\n\s*output_error' src/mailpilot/cli.py` → 4 hits (account.create L308, company.create L493, contact.create L815, workflow.create L2051). Test sweep: extend `tests/test_cli_*` capfire (§V.64) — duplicate branch asserts ∅ `event=<noun>.create` operator line, only `duplicate_key` envelope + exit 1 ∧ span outcome ok.|V16,V54,V64
T108|x|impl §V.73(+) ∧ §V.74(+) audit-recipe extensions @ `.claude/check-extras.md` — §V.73 §: add check (d) body-byte-identical diff between `lead-encreach` embedded enrich snippet (SKILL.md js-fenced block, post-`meta` slice) ∧ `.claude/workflows/lead-encreach-enrich.js` (post-`meta` slice) — mechanical: extract both bodies @ first `\n}\n` after `export const meta`, assert equal; fail mode: divergence → saved-file unaudited drift. §V.74 §: widen trigger `.claude/skills/**/*.md ∨ src/**` → `+ .claude/skills/**/scripts/*.py` (grep scope `.claude/skills/ src/` already covers; trigger-glob misses `.py`). Scope = grep `rg -n '## §V.73|## §V.74' .claude/check-extras.md` → 2 section headers. Test sweep: check-recipe ⊥ test-covered (operator-judged /sdd:check) — post-impl a divergent saved-workflow body ∧ a split-on-newline-over-CSV regression in a script both surface.|V73,V74

## §B BUGS

id|date|cause|fix
B1|2026-05-01|3 logfire.exception sites unpaired w/ operator_event ∴ stderr gaps under journald|V51
B2|2026-05-02|out-scope QA verifier flagged echoed-question digits as fabrications|V56
B3|2026-05-02|running tool span ⊥ structured tool_name (misdiagnosis — see §B.6)|V53
B4|2026-05-02|2 skipped_no_workflows spans — 2 distinct email_ids each routed once; §V.22 already held|V22
B5|2026-05-02|demo B1 reply rendered specs w/ asterisks ⊥ pipe-table — prompt-fidelity drift|V42
B6|2026-05-03|B3 misdiagnosis — gen_ai.tool.name already populated by instrument_pydantic_ai|V53
B7|2026-05-03|B4 grounded to broader doc, ⊥ model-specific — top-hit singular collapse, N=1 read|V41
B8|2026-05-03|B4 grounding gate substring-matched expected_tokens ∴ false-negative on phrasing variance|V57
B9|2026-05-03|B4 rendered specs as space-separated `<label>  <number>` ⊥ pipe-table — prompt-only fix didn't hold|V42
B10|2026-05-03|outbound A3 called search_drive_markdown ×5 — Drive tools unconditionally registered|V43
B11|2026-05-04|inbound trigger email inlined twice (Email history + New inbound email)|V29
B12|2026-05-04|A3 prompt rendered Deferred task block for operator-initiated enrollment_run|V30
B13|2026-05-08|company/contact export/import emitted {exported|imported: N, file} ⊥ §V.4 list envelope|V4
B14|2026-05-09|B4 KDF specs rendered w/ ASCII rule-line faux-separators — §V.42 regex required numerics|V42
B15|2026-05-10|GCE crash-loop 46+ iters — ⊥ google_application_credentials → ADC never tried|V37
B16|2026-05-10|2 prod TimeoutError at default 60s httpx read window on long-context agent.invoke|V48,V49
B22|2026-05-11|smoke-test skill carried dotless §V13/§V12/§T18/§B2 cites — auditor regex skipped|V66
B23|2026-05-11|/demo-test G1 polled local-DB; §V.59 setup forbids run-loop ∴ false-FAIL on healthy prod|V60
B24|2026-05-12|code-side §-cites carried dotless V13 / V11 — §V.66(+) binds project-wide|V66
B25|2026-05-12|outbound called record_enrollment_outcome on enrollment_run ∴ 2 terminal activity rows / enrollment|V31
B27|2026-05-12|mailpilot activity list omits workflow_id / email_id / task_id ∴ smoke-test falls back to psql|V7
B28|2026-05-12|outbound first-invoke cache_creation_input_tokens=0 — runtime path issue, ⊥ config|V50
B29|2026-05-14|mailpilot contact import w/o --file from TTY hangs on sys.stdin.read() — ⊥ isatty guard|V9
B30|2026-05-15|12 bare-form V<n> cites (no §) in code prose — §V.66 next workaround surface|V66
B31|2026-05-15|qa.py:16 bare-form V31 missed by §T.48 sweep — operator-review misclassified as gate ID|V66
B32|2026-05-26|enrollment add accepted contact.email ≡ workflow.account.email — semantic self-loop|V33
B33|2026-05-26|enrollment remove echoed pre-delete Enrollment ∧ claimed changed=[status] post hard-DELETE|V4,V54
B34|2026-05-28|read_drive_markdown ×2 parallel — httplib2.Http ⊥ thread-safe; all 4 §V.49 retries hung|V49,V38
B35|2026-05-28|_drain_pending_tasks sequential — 2 concurrent inbound emails ran agent.invoke spans strictly serial|V23
B36|2026-05-28|B4/B7 rendered `**label** value` single-space cluster — §V.42 \s{2,} regex missed|V42
B37|2026-05-28|agent.invoke span lacked email_id attr — B7.5 gate SQL GROUP BY email_id returned 0 rows|V26
B38|2026-05-28|smoke-test gates graded latency from wall-clock + 5s poll ⊥ Logfire spans — borderline verdicts|V61
B39|2026-05-28|gate A4 needed gmail_thread_id; EmailSummary projection omitted external-system join key|V7
B40|2026-05-28|two KB files describe same model w/ divergent specs — single source_file schema ambiguous|V57
B41|2026-05-28|main loop blocked on _drain_pending_tasks ∴ pubsub.notify for 2nd msg stalled 31s|V24
B42|2026-05-28|agent advisory lock keyed on (workflow_id, contact_id) ∴ §V.23 concurrent dispatch undone|V25
B43|2026-06-02|make clean dropped only dev DB ∴ schema.sql shrink (commit 93cbf66) left mailpilot_test w/ stale contact.domain NOT-NULL col — pytest hit psycopg.errors.NotNullViolation|V18
B44|2026-06-03|outbound send (sync.py:1223-1240) ⊥ pass resolved_in_reply_to/resolved_references to create_email; DB row's in_reply_to/references_header NULL on classifier-driven mid-thread replies despite wire-correct threading|V67
B45|2026-06-03|smoke-run B7 (compare 2 datasheets, Watts UV-COM vs Trojan UVMax Pro) agent.invoke=64s, B7.5b (2nd of 2 concurrent in-scope, WS36-600-2 flow rates) agent.invoke=63s — both substantively correct (groundedness PASS) but breached then-60s §V.59 SLA; tool work <5s each, wall-clock dominated by compose; B7 input_tokens=34926; resolution = relax §V.59 SLA to 90s + sweep call-sites per §T.74 + update lab5.ca/mailpilot/ statement (out-of-repo)|V59
B46|2026-06-03|smoke-run B7.5b agent reply (qa-in-022, WS36-600-2 flow rates) cited continuous=65 GPM, peak=120 GPM, backwash=35 GPM as primary answer; both same-invocation `read_drive_markdown` returns (`industrial-water-softener.md`, `sf-100s-industrial-water-softener.md`) contain WS36-600-2 = 110/125/30; fabricated tokens appear nowhere in either return; agent acknowledged correct values in hedging "Note" within same body; trace 019e8ef503adf84513a3cbb6948f8f87; co-occurred w/ §V.59 SLA breach (63s) under N=2 concurrent load|V68
B47|2026-06-03|src/mailpilot/SKILL.md drift from live CLI surface: `--body-text` recipes (live cli.py @click = `--body`); positional id forms for `email reply` / `account sync` / `enrollment {run,view,update,remove}` (live = flagged form, enrollment ⊥ scalar id per composite PK); verb roster ⊥ ∋ `{sync, disable, retry, get, set}`; settings ⊥ ∋ `max_concurrent_tasks` (settings.py:65); env-prefix described as "uppercase of same name" (actual `MAILPILOT_*` per `SettingsConfigDict(env_prefix=...)` ∈ settings.py:54)|V6
B48|2026-06-05|smoke run 2026-06-05: §V.42 lint fired B7-first reply_email (one 4ms agent.tool_errors span, ⊥ gmail.send_message child) but ⊥ fired B4 / B7-second / B75a / B75b reply_email — _check_spec_table replay on all 4 stored body_text returns {"error":"format"}; strip_control_chars (sole transform between tool-wrapper entry ∧ body_text col on outbound write) preserves pipes ∴ DB body ≡ tool-wrapper input body ∴ lint should have caught all 4; impl skipped lint on those 4 invocations, mechanism unknown|V42
B49|2026-06-05|enrollment list returned id, workflow_id, contact_id, contact_email, contact_name, status, updated_at; enrollment view returned bare Enrollment row (workflow_id, contact_id raw FKs; status, reason, created_at, updated_at) — view ⊊ list on parent-denorm set; operator audit during CLI consistency review surfaced inversion class (peer surface account/company/contact/workflow all satisfy view ⊇ list, enrollment alone violates)|V5
B50|2026-06-05|volume run 25 trigger sends → 121 emails (96 agent replies ≈4x amplification): single task emitted 12 sequential `run.execute_task` spans w/ same task_id, each w/ successful nested `agent.invoke` + `complete_task` call; final DB row status=completed attempt_count=0; dispatcher non-atomic claim — `list_pending_tasks` SELECT ⊥ FOR UPDATE SKIP LOCKED ⊥ status flip; §V.25 lock catches concurrent ⊥ sequential re-pick post-completion|V23
B51|2026-06-06|smoke run 2026-06-06 burst 24-email: max_concurrent_tasks=4 default violates formula ceil(25×35/90)=10; ThreadPoolExecutor(max_workers=4) queues 20-of-24 tasks → 6 sequential waves × ~35s avg → max_latency=235s (>110s burst ceiling), p95=217s (>90s SLA); Drive + routing clean (drive max_dur=1.3s, 60 overlap_pairs, 0 exceptions)|V23
B52|2026-06-05|re-run `mailpilot account create --email outbound@lab5.ca --display-name "Outbound Smoke"` against existing row emitted curated `event=error source=account.create message="duplicate key ... account_email_key ..."` operator-log line per §V.54 observability pair then dumped raw 30-line `psycopg.errors.UniqueViolation` traceback to stderr because `cli_mutation` (`operator_log.py:64-67`) re-raises after pairing ∧ Click default handler has no `psycopg.Error` translation; stdout stayed JSON-empty (§V.3 single-shot contract intact on stdout) but stderr carried unstructured traceback alongside structured event line, violating envelope expectation §V.3 + §V.4 set for CLI error paths; recurrence class = ∀ §V.54 noun.verb CLI mutation whose underlying DB INSERT/UPDATE may hit unique / FK / NOT NULL / CHECK constraint (account/company/contact/workflow re-create, CSV/JSON re-import, etc.)|V54
B53|2026-06-06|smoke C burst 25-email max_concurrent_tasks=10: Gmail Pub/Sub delivered notifications in 5 waves (+15/+25/+52/+105/+122s from T_SEND_C); wave-4 (11 invocations) started +105s; compare-inclusive avg ~50s per invoke → last span ends ~160s; §V.23(+) pool formula ⊥ includes T_delivery_p95 term; max=160s>110s burst ceiling, p95=156s>90s SLA; §V.69 T_delivery contract violated|V61,V69
B54|2026-06-06|burst-25 max=175s>110s, p95=162s>90s via two compounding factors: (a) inter-batch Pub/Sub gaps persist despite §V.69 wakeup-on-new (§T.83) — 5 classification waves at +17/+52/+73/+86/+100-108s from T_SEND_C; §T.83 wakeup closes intra-batch T_delivery ⊥ inter-batch gap; (b) 3 tool-error retry chains (fact_check_mismatch×2, not_found×1) each inflate affected invocation +40-70s; 12% retry rate (3/25) exhausts burst-ceiling headroom independently of wave timing; §V.70 retry-rate contract violated|V61,V69,V70
B55|2026-06-06|`_DRIVE_GROUNDING` fragment (`src/mailpilot/agent/templates.py:122-135`) ⊥ contains §V.41 per-target compare-and-contrast clause; fragment carries global 2-miss list-fallback only; compare invocation w/ 2-miss for target A → `list_drive_markdown` surfaces target B doc w/o target B-specific search ever issued → recreates model-number-collision class §V.41 closed @ spec but open @ impl; qa-cmp-011 (Pure Aqua PAPV vs ROPV R8040B, smoke-run 2026-06-03 B7) precedent; /sdd:check 2026-06-06 surfaced via prompt-text inspection|V41
B56|2026-06-06|smoke-run B7 compare (qa-cmp-005) + B75a direct (qa-in-022) agent fabricated WS softener model flow rates across ≥6 models — WS36-600-2 → 110 cont / 125 peak (source: 65/120), WS24-240-2.5 → 115/160 (source: 80/170), WS30-300-2.5 → 140/190 (source: 80/170), WS30-300-3 → 165/230 (source: 120/270), WS42-900-2 → 112/133 (source: 65/120), claimed range 86-1,130 GPM (source max: 1,000); wrong 100-GPM recommendation followed (agent named WS24-240-2.5 @ 80 cont as clearing 100 cont; correct answer WS30-300-3 @ 120 cont); §V.68 fact-check silent — current source docs (`pure-aqua-industrial-water-softener.md`, `pure-aqua-sf-100s-industrial-water-softener.md`) post-upstream-update carry 65 cont / 120 peak for WS36-600-2 (§B.46 historical 110/125 ⊥ live values), fabricated tokens collided via prose-line matches in `_fact_check_body` plain `token in union` — `"110"` ∈ `"35-110 degrees F"` (operating temp), `"125"` ∈ `"25 - 125 psi"` (operating pressure), `"115/140/165"` ∈ CVP commercial-softener pipe-rows (same column index, wrong product family); column-context-blind substring match = implementation gap, §V.68 spec intent ≡ pipe-table-row grounding ⊥ prose-line collision admit; reproducible on single-source direct lookup ∴ ⊥ compare-context-specific; §T.85 carries pipe-table-row scoping fix|V68
B57|2026-06-06|smoke-run 2026-06-06 B7 compare (qa-cmp-001, Dow FilmTec ECO-440i vs Hydranautics CPA4) + Scenario C burst (25 emails @ P=8): §V.42 `_check_spec_table` lint fires per-tool-call w/ ⊥ retry ceiling ∴ agent loops issuing fresh `reply_email` calls until it accidentally finds a body that misses the ≥3 consecutive key-value heuristic; B7 invocation made 17 `reply_email` calls over 195s wall-clock (>90s SLA); C burst's worst invocation made 35 `reply_email` calls over 417s, max latency 495s, p95 362s, both > 110s burst ceiling ∧ 90s SLA per §V.61; 8 `agent.tool_errors` warn-level spans across full test (32 `agent.invoke`) = 25% retry rate (vs §V.70 5% cap); content groundedness intact ∀ replies (B7 cited both datasheets correctly, no fabrications) ∴ failure mode ≡ purely latency + token waste (1.95M input tokens for 25 burst replies, expected ~500k w/o loops); root cause = `_check_spec_table` ∈ `src/mailpilot/agent/tools.py:114-145` stateless per-call ∧ wrapper has no per-invocation rejection counter|V71
B58|2026-06-06|smoke-run 2026-06-06 B4 attempt 1 (qa-in-006, `kdf-process-filtration-media.md`): prose-only KB doc, 0 pipe-table-row lines → `_fact_check_body` (`src/mailpilot/agent/tools.py:142-186`) builds empty union via §T.85 pipe-row filter (`"|" in line` for `line in content.splitlines()`) → ∀ genuine numeric citations ("55", "85", "99", "98" echoing source phrases "KDF 55 medium can remove over 99% of free chlorine", "up to 98% of water-soluble cations") flagged unsupported; agent looped 6 `reply_email` fact_check_mismatch retries, eventually `record_enrollment_outcome(failed)`, 0 replies delivered to operator. C burst (25 emails @ P=8) amplified to 46 retries / 22 invokes = 209% retry rate vs §V.70 ≤5% cap; single `sync.loop.iteration` consumed 1.85M input tokens / 413s wall-clock (trace `019e9e408a37df02d77c8371722949c7`); 2 tasks pending @ C7. §V.68(+) pipe-row scoping (§T.85) intended to kill prose-line false positives in table-bearing docs (§B.56), but uniform pipe-row-only union over the WHOLE ledger inverts ground-truth on prose-only docs. Per-document scoping (pipe-row for table-bearing docs, full-content for prose-only docs) preserves §B.56 anti-collision intent ∧ restores prose-doc grounding; §B.56 single-doc collision class (token in both prose AND pipe-row of SAME doc) still rejected under per-doc scoping. §T.88 carries per-document scoping fix|V68
B59|2026-06-06|smoke-run 2026-06-06 B7 compare (qa-cmp-001, Dow FilmTec ECO-440i + Hydranautics CPA4) + C burst 5 compare questions: `_fact_check_body` (§V.68) rejected valid grounded test-condition values (150 psi, 225 psi, 22.7 m³/d, 1500 ppm, 6000 gpd) that appear in prose sections of both datasheets — Dow ECO-440i test conditions ("2,000 ppm NaCl, 150 psi (10.3 bar), 77°F (25°C), pH 8 and 15% recovery") live in `## Specification Notes` prose, Hydranautics CPA4 test conditions ("1500 PPM NaCl solution, 225 psi (1.55 MPa) Applied Pressure, 77 °F") live in `## Test Conditions` prose; §T.88 pipe-row-only scoping (§V.68(+)) excluded those prose lines from both docs' union because each doc carries OTHER pipe tables (Product Specs, Application Data) ∴ per-doc check picked pipe-row branch ∴ prose body of same doc dropped. §V.71 retry ceiling covered only `_check_spec_table` (format-lint) hits — `_fact_check_body` rejection path called `return fact_check_error` directly (`src/mailpilot/agent/tools.py:259,308`) bypassing `_consume_format_lint_rejection` → no bypass fired; B7: 9 `fact_check_mismatch` retries + 18 re-reads, sla_agent=265s (>50s steady ceiling); C burst: p95_sla_agent=225s, max=531s (5 compare questions × up to ~10 reply_email calls each). Agent exited loop only by omitting contested values (attempt 9 switched to "Per datasheet (see note below)") ∴ B7 grounding verdict degraded to fail (answers_question=false, CPA4 permeate flow 6000 gpd omitted despite being in source). Two compounding causes: (a) §V.68 pipe-row-only scoping too narrow for table-bearing docs that embed valid citable specs in prose (test conditions, SI-unit footnotes, performance notes); narrowing the scope is reasonable for §B.56 anti-collision but inverts ground-truth for prose-table mixed docs. (b) §V.71 ceiling ≡ format-lint-only ∴ `_fact_check_body` rejection loop uncapped — orthogonal-class gap closed by extending counter to all rejection paths. §T.89 carries the counter-extension fix; the §V.68 prose-vs-table-row scoping question remains open (could broaden §T.88 to admit prose sections under headers ≠ Product Specs / Application Data, ∨ accept that §V.71 cap is the right backstop ∧ §V.68 stays narrow for collision protection)|V71
B60|2026-06-07|smoke-run 2026-06-07 B7 compare (qa-cmp-003 Clack Birm® vs Pyrolox®) + Scenario C burst 5 compare Qs: `_fact_check_body` (§V.68) rejected Physical Properties bullet-list tokens verbatim present in both source docs — Birm `"- Specific Gravity: 2.0 gm/cc"`, `"- pH range: 6.8–9.0"` (under operation conditions), `"- Mesh Size: 12 x 50"`; Pyrolox `"- Specific Gravity: 3.8"`, `"- pH: 6.5-9.0"`, `"- Bed Depth: Suggested depth 18 inches"`; Birm constraints `"D.O. content must be equal to at least 15% of the iron and 29% of the manganese content"`, `"Backwash rate: 10–12 gpm/sq. ft."` Both docs carry Order Information pipe tables ∴ §T.88 per-doc check picked pipe-row branch ∴ bullet-list Physical Properties + operation-constraint lines excluded from union. 10 tokens spuriously rejected on first compare invocation (2.0, 3.8, 6.8, 9.0, 6.5, 18, 15, 29, 10) → §V.71 cap fired at attempt=3, send proceeded (correctness preserved, latency burned). All 5 compare invocations hit cap; B7 sla_agent=63s >§V.61(+) 50s steady ceiling; C p95_sla_agent=93.75s, max=122s >§V.61(+) 75s burst ceiling; §V.70 retry rate 7/32 = 21.9% >> 5% cap (5 `cap_reached` spans + 2 stranded `agent.tool_errors`). B7 grounding verdict=pass (cap ensured send w/ values intact) ∴ ⊥ §B.59 grounding-degradation class. Cognate w/ §B.59 (same pipe-row-only root, both surface after §T.88), distinct surface: §B.59 ≡ prose-paragraph spec citations under prose headers (`## Test Conditions`, `## Specification Notes`); §B.60 ≡ bullet-list spec lines (`"- Key: Value"`) under any header. Bullet-list lines carry explicit `Key:` labels ∴ context-anchored, ⊥ admit cross-product token leakage seen in §B.56 prose-line matches (`"110"` ∈ `"35-110 degrees F"`); safe to admit alongside pipe-row lines without re-opening §B.56 class. Fix: §V.68(+) Impl extended — for table-bearing docs, union admits lines matching `r"^\s*[-*]\s"` alongside `"|"` lines; full-prose paragraph lines remain excluded per §B.56|V68
B61|2026-06-07|smoke-run 2026-06-07 B7 compare (qa-cmp-010 Watts UV-COM vs Trojan UVMax Pro): agent.invoke=84s vs §V.61(+) 50s steady ceiling. §V.71 cap behaved correctly — 2 `_check_spec_table` format rejections @ ~0.002s each → 3rd `reply_email` succeeded — ⊥ pathological loop, ⊥ fact_check_mismatch retries. 84s ≡ intrinsic compare cost: 2 datasheets loaded (in_tok=61667 vs single-source 22351), tool_call_count=8 (search ×2 + read ×2 + reply ×3 + record ×1), multi-doc synthesis. 50s steady ceiling structurally under-estimates compare-type. Pattern recurs cleanly across runs: §B.45 64s (Watts UV-COM vs Trojan, post-§T.74 90s relax pre-50s reinstate), §B.60 63s (Clack Birm vs Pyrolox, post-§T.88), §B.61 84s. ⊥ surface symptom in §V.68/§V.71 layer (both behaved correctly); ceiling itself ≡ wrong for compare-type. Fix: §V.61(+) introduces differentiated ceiling — single-source ∧ decline retain 50s steady, compare-type (≥2 `read_drive_markdown` per invocation) gets 90s steady; burst non-compare p95 ≤ 75s, compare p95 separate w/ advisory 120s. §T.90 sweeps smoke-test SKILL.md B7 gate text|V61
B62|2026-06-07|smoke-run 2026-06-07 Scenario C burst (8 emails @ P=8, mix 4 in-scope / 2 out-of-scope / 2 compare): `p95(sla_agent)=118s`, `max(sla_agent)=118s` vs §V.61(+) 75s burst ceiling. `overlap_pairs=28` (max C(8,2)) confirms all 8 invocations concurrent ∴ breach ≡ intrinsic execution duration, ⊥ serialization. Two compare questions (qa-cmp-008 Clack Birm vs Pyrolox, qa-cmp-002 Pulsafeeder vs ProMinent) dominate tail; non-compare invocations (4 in-scope + 2 out-of-scope) ≤ 50s ∴ blended p95 inflated solely by compare-type. `n_exceptions=0`, `n_warns=0`, `avg_cache_hit_ratio=0.645`, `read_drive_markdown max_dur=0.74s` (§B.34 race signature absent). All correctness gates passed: C3 routing 100% classified, C5 +8/+8/+8 activities, C6 outbound-workflow quiet under load. Cognate w/ §B.61 (single-send compare 84s, same structural root) — §B.62 ≡ burst surface of same compare-type 50s-ceiling under-estimate; blended-p95 gate (pre-§V.61(+) amend) ≡ wrong gate shape since 2 compare invocations alone shift p95 above 75s regardless of pool/concurrency health. Fix: §V.61(+) compare-type clause already amended in §B.61 commit — burst non-compare `p95(sla_agent) ≤ 75s`, compare `p95(sla_agent)` reported separately w/ advisory ceiling 120s; smoke-test SKILL.md C4.a query gate rewrite tracked by §T.90|V61
B63|2026-06-08|`mailpilot enrollment remove "019ea725-ff8f-7444-afce-baa3deb37176"` raised `psycopg.errors.ForeignKeyViolation` on `activity_enrollment_id_fkey` ("update or delete on table enrollment violates foreign key constraint ..."); `delete_enrollment` (`src/mailpilot/database.py:1625-1664`) issues bare DELETE w/ ⊥ ON DELETE clause; two FKs reference `enrollment.id` w/ ⊥ cascade — `activity.enrollment_id` (nullable, `schema.sql:160`) populated by every lifecycle event (`enrollment_added` @ creation, plus `enrollment_completed`/`enrollment_failed`/`enrollment_paused`/`enrollment_resumed`), `task.enrollment_id` (NOT NULL, `schema.sql:117`, §V.28) populated for every enrollment that ever scheduled ∨ received a task. `remove` succeeds only on enrollments w/ zero activity ∧ zero tasks — effectively never (`enrollment_added` activity emitted @ creation per §V.17 multi-target). Append-only audit per §V.14 forbids both candidate fixes that preserve audit: cascade DELETE on activity destroys audit (violates §V.14 spirit), SET NULL via UPDATE-on-activity violates §V.14 fn-rule. Resolution: drop `enrollment remove` hard-delete CLI cmd, replace w/ terminal soft-disable extending §V.10 contact-disable surface to enrollment noun; storage = extends `enrollment.status` enum {`active`, `paused`} → {`active`, `paused`, `disabled`} per §V.15(+) (single live-state signal; agent-filter sites skip `disabled` same as `paused`); `enrollment.disabled_reason TEXT` column coupled to status via CHECK; `enrollment_disabled` activity row emitted by `disable_enrollment` (append per §V.14, joins `enrollment_*` family ∈ `activity.type` CHECK). Distinct from §B.33 (cosmetic envelope-projection bug on hard-delete path) — §B.63 ≡ fundamental hard-delete-vs-append-only shape incompatibility ∴ remove cmd structurally unfit, ⊥ envelope-tweak fix|V10,V14,V15
B64|2026-06-08|`company create --domain <dup>` re-run recorded a `.failed` logfire span on natural-key collision (expected operator-retry, ⊥ system error) → pollutes failed-span dashboards; §V.16 covered only `create_email` ∴ account/company/contact/workflow create non-race-safe (sync-path `create_or_get_contact_by_email` TOCTOU also extant)|V16
B65|2026-06-09|operator-facing skill body (`.claude/skills/smoke-test/SKILL.md:40`) named retired verbs `tag remove` / `enrollment remove` (both dropped from §I.cli); §V.6 audit scoped to `src/mailpilot/SKILL.md` only ∴ `.claude/skills/**/*.md` vocab drift evaded the mechanical check|V6
B66|2026-06-09|`.claude/skills/smoke-test/SKILL.md:920-1000` C4.a CTE selects `n_warns`/`n_exceptions` on `agent.invoke` spans only; ⊥ counts `agent.tool_errors` warn spans (emitted @ `src/mailpilot/agent/invoke.py:703` per tool-error scan) ∧ ⊥ rollup-span `tool_error_count` attr (set @ `:708`); §V.70 ≤5% retry-rate ceiling toothless ∴ prior burst regressions (§B.54 12%, §B.57 25%, §B.58 209%) all silently passed C4.a despite §V.70 binding ratio @ ≤5%. Recurrence-class: invariant body specifies measurable contract w/ named gate site but consumer SQL/assertion omits the metric ∴ contract toothless silently. Detected by /sdd:check 2026-06-09 (V70 UNVERIFIABLE).|V70
B67|2026-06-10|`load_company_view` (`src/mailpilot/database.py:3535-3543`) explicit-field constructor list omits `profile=company.profile` ∴ `company view` envelope ∧ `read_company` agent-tool return show `profile: None` despite row carrying column post-§V.72 (`CompanyView.profile` field declared `models.py:448`, `Company.profile` populated by `get_company`). `load_contact_view` (`database.py:3505-3518`) carries same explicit-field anti-pattern — silently drops any future `Contact` column added post-V8 authoring. Recurrence-class: view ⊇ row constructor mechanics ⊥ enforce field forwarding ∴ Model-column addition silently inverts the view ⊇ row contract until operator-discovered. Detected by /sdd:check 2026-06-10 (V8 VIOLATE post-T98 Company.profile addition).|V8
B68|2026-06-10|`/lead-encreach` live run: enrich-all Workflow snippet (`.claude/skills/lead-encreach/SKILL.md:113-120`) referenced unbound `stale` ∴ ReferenceError as authored; natural fix — feed list-stale JSON via Workflow `args` — crashed `stale.map is not a function` ∵ runtime delivers `args` as JSON string ⊥ parsed array ∴ workflow aborted @ dispatch w/ 0 agents (run wf_fdf12e33-c31, 9ms, agent_count=0). Workaround: inline rows as `const STALE = [...]` literal (run wf_f8a6acd5-925 → 10/10 enriched). Secondary: SKILL.md L105/L137 + §V.72 prose "concurrency 3 / Default 3" ⊥ honored by `parallel(stale.map(...))` (submits all N, bounded by runtime cap `min(16, cores-2)`). Recurrence-class: skill-body Workflow snippet carries copy-paste contract → free var ∨ args-as-string assumption ∨ prose-vs-code divergence silently breaks operator paste, parametric ∀ skill body embedding executable Workflow snippet. GitHub #133. Fix: §V.73(+) + §T.103.|V73
B69|2026-06-10|`/lead-encreach` live run: seed-file CSV mode (`.claude/skills/lead-encreach/SKILL.md:61-69`) framed line-oriented ("format detection from first non-empty line", per-row extraction over Read-tool output); `theirstack.csv` quoted multi-line `company_description` → 25 logical rows spanned 217 physical lines ∴ physical-line iteration would mis-seed (prose → phantom rows). Caught pre-impact ∵ operator used `csv.DictReader` (25 rows → domain col `company_url` → 23 unique apexes post-dedupe). Secondary: resolve_apex (L148-150) `curl -sLI | grep '^location:' | tail -1 | awk '{print $2}'` — HEAD answered differently by 403 origins (bramptonbrick.com, gastronomia.ca, starbuilding.ca, westgroupe.com), awk retains header CR; `curl -sL -o /dev/null -w '%{url_effective}'` robust (surfaced 3 real redirects). Recurrence-class: CSV ingestion via physical-line iteration breaks on RFC-4180 quoted multi-line / embedded-comma fields, parametric ∀ CSV ingestion site. GitHub #134. Fix: §V.74(+) + §T.104.|V74
B70|2026-06-10|`mailpilot company create --domain <dup>` re-run emits misleading operator-log line `event=company.create domain=<d> changed=[]` (stderr, `cli.py:493`) on race-safe `created is None` branch, before `duplicate_key` envelope — success-named operator event on failure path (exit 1, ∅ row created). `changed=[]` ⊥ disambiguates: no-op `update` legitimately emits `changed=[]` ∴ operator grep `event=company.create` sees phantom create. §V.16(+) clause (added §B.64 to drop `.failed` span) over-corrected — removed failure span but substituted success-shaped operator line. Recurrence-class: ∀ 4 race-safe create handlers (account/company/contact/workflow create) emit `operator_event(<noun>.create, changed=[])` on duplicate-failure branch. Fix: §V.16(+) + §T.107.|V16
