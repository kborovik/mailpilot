# SPEC archive

Archived rows from `SPEC.md` per /sdd:compact prong 3 (window-vs-archive split). Sorted by id ascending. Verbatim preservation per archive-schema invariant. Eager-probed by `/sdd:check --all` cite-DAG sweep; excluded from `--incremental`.

## §T TASKS

id|status|task|cites
T1|x|spec ratified|-
T2|x|pair 3 unpaired logfire.exception sites in run.py / pubsub.py w/ operator_event|V51
T3|x|impl search_drive_markdown agent tool ∧ uplift smoke KB ≥10 docs|V34,V39,V64,V36
T4|x|harden smoke-test out-scope QA verifier — strip echoed-question tokens before fab regex|V56
T5|x|SUPERSEDED — §V.53 amend|V53
T6|x|SUPERSEDED — no duplicate routing|V22
T7|x|require pipe-table Markdown for spec sheets in demo workflow instr|-
T8|x|SUBSUMED by §T.15|V64,V41,V45
T9|x|impl _check_spec_table heuristic; reject spec-shape body w/o |---| separator|V39,V64,V42
T10|x|SUPERSEDED by §T.15|V44,V45
T11|x|impl §V.57 — qa.py source --id subcommand; B4 grading operator-judged|V57
T12|x|impl template registry (WorkflowTemplate, fragments, _CORE / _DRIVE)|V64,V44,V45,V46
T13|x|schema — add workflow.template NOT NULL + CHECK|V44
T14|x|database.create_workflow accepts template; rejects type mismatch ∧ template change|V44,V45
T15|x|refactor _build_agent — lookup template; drop _TOOLS ∧ _SYSTEM_PREFIX|V64,V44,V45,V46
T16|x|CLI — workflow create requires --template; new template noun (read-only)|V4,V5,V64,V44
T17|x|drop §V.43 path code (_workflow_uses_kb regex sniff)|V44,V45
T18|x|smoke-test wiring — Phase 0 swaps to --template; Scenario A regression for §B.10|V64,V44,V45
T19|x|sweep prompt fragments for single-tool examples — extend to ≥2 tools|V40
T20|x|filter trigger email from email_history in _build_user_prompt|V64,V29
T21|x|thread trigger arg through _build_user_prompt → _format_trigger|V26,V64,V30
T22|x|impl §V.47 — cache_control breakpoint + cache_*_input_tokens attrs on rollup|V64,V47
T23|x|impl /release skill — push, build wheel, gh release create|V62
T24|x|impl workflow export --account-id|V3,V4,V64,V63
T25|x|impl workflow import --account-id [--file]|V4,V64,V44,V63
T26|x|round-trip integration test for workflow export/import|V64,V63
T27|x|smoke-test Phase 0 swaps to workflow import w/ fixtures|V64,V63
T28|x|migrate company/contact export/import to §V.4 list envelope|V3,V4,V64,V63
T29|x|migrate tag remove ∧ enrollment remove to §V.4 singular envelope|V4,V64
T30|x|rewrite smoke-test Phase 5 per §V.58 — collapse to §1/§2/§3|V58
T31|x|widen _check_spec_table — drop numeric-value req; ASCII rule-lines ⊥ separator|V64,V42
T32|x|impl mailpilot --skill flag reading packaged SKILL.md|V4,V64,V6
T33|x|impl §V.48 — 240s httpx read-timeout on AnthropicProvider|V64,V47,V48
T34|x|impl §V.49 — bounded auto-retry for transient agent task failures|V51,V64,V47,V48,V49
T35|x|drop non-canonical run_loop ∧ its 3 tests|V21,V64,V65
T36|x|reshape mailpilot account sync output to §V.4 plural envelope|V4,V64
T37|x|delete orphan _sync_all_accounts in run.py|V65
T38|x|impl /demo-test skill per §V.59|V64,V52,V53,V57,V47,V58,V59
T39|x|fix smoke-test SKILL.md cite drift|-
T40|x|impl §V.60 + §V.59 G1 amend — Gmail-API poll, ⊥ local-DB|V37,V59,V60
T41|x|impl §V.54 — CRM CLI mutation telemetry matrix|V51,V64,V53,V63,V54
T42|x|patch dotless §-cites in src/ per §V.66|V66
T43|x|impl §V.31 — _DEFERRED_TASK trigger-aware|V64,V45,V30,V31
T44|x|impl §V.55 — logfire ScrubbingOptions; retain tool_response content|V64,V55
T45|x|patch dotless §-cites in tests/ per §V.66|V66
T46|x|impl §V.7 — add workflow_id/email_id/task_id to ActivitySummary|V64,V7
T47|x|strip company ∧ contact to identity-min; disabled_reason; --note STR atomic|V4,V5,V13,V14,V64,V63,V54,V7
T48|x|impl §V.8 — ContactView/CompanyView w/ inline notes|V4,V5,V13,V14,V39,V64,V45,V8
T49|x|impl §V.9 isatty-guard on stdin-fallback imports|V64,V9
T50|x|patch bare-form V<n> cites in code prose to §V.<n>|V66
T51|x|patch bare-form V31 cite in qa.py|V66
T52|x|impl §V.32 — enrollment add --scheduled-at; enrollment_schedule trigger|V21,V26,V64,V30,V54,V31,V32
T53|x|impl §V.33 — enrollment add rejects contact-email ≡ account-email|V4,V64,V33
T54|x|sweep remove handlers — hard-DELETE composite-key projection per §V.4|V4,V64,V54
T55|x|impl §V.10 — contact disable cmd|V4,V64,V54,V10
T56|x|impl §V.5 parent-NI — add account_email to Workflow / WorkflowSummary|V4,V5,V64,V63
T57|x|schema — add schema_metadata table per §V.18|V18
T58|x|impl §V.18 + §V.19 — schema-hash drift detection|V51,V64,V18,V19
T59|x|impl §V.11 — reshape status payload (version, schema, sync_loop, accounts, tasks, config, counts)|V4,V64,V18,V11
T60|x|impl §V.38 — _DRIVE tools sequential=True; broaden tool catches|V64,V48,V49,V38
T61|x|impl §V.23 — concurrent worker pool in _drain_pending_tasks|V64,V49,V38,V23
T62|x|widen _check_spec_table — \s+ floor + 80-char line cap|V64,V42
T63|x|impl §V.26(+) — agent.invoke span sets email_id attr|V26,V64
T64|x|impl §V.61 — gate-verdict source = Logfire spans, ⊥ CLI poll|V64,V52,V59,V61
T65|x|impl §V.7(+) — add gmail_thread_id to EmailSummary|V64,V7
T66|x|impl §V.57(+) — source_file_alts in qa_pairs.json|V64,V57
T67|x|impl §V.24 — decouple drain pool from main-loop tick|V64,V23,V24
T68|x|impl §V.25 — scope agent advisory lock to unit of work|V64,V23,V24,V25
T69|x|impl §V.41(+) — _DRIVE_GROUNDING 2-consecutive-miss fallback to list_drive_markdown|V64,V41
T70|x|impl §I(+) — persist route_method on email; project on Email ∧ EmailSummary|V64,V7
T71|x|impl §V.20 — email.route_method enum CHECK + is_routed coupling CHECK in schema.sql|V64,V20
T72|x|impl §V.18(+) — extend make clean to drop ∧ recreate mailpilot_test alongside mailpilot|V18,V64,V65
T73|x|impl §V.67 — outbound create_email ! pass in_reply_to=resolved_in_reply_to, references_header=resolved_references|V64,V67
T74|x|sweep 60s → 90s @ SLA call-sites per §V.59(+) — vocab table: `60s SLA`,`60s promise`,`60s ceiling`,`60s latency verdict`,`within 60s`,`> 60`,`60s reply window`,`(60s SLA)`,`~10-60s`,`~20-60s`; scope = SPEC.md §V.23 ∧ §V.24 Why-clauses ∪ `.claude/skills/smoke-test/SKILL.md` ∪ `.claude/skills/demo-test/SKILL.md`; out-of-scope = `run_interval (default 60s)` Settings field, httpx 60s read-window per §V.48, `12 attempts × 5s ~60s` polling cadence math, `_DRIVE_HTTP_TIMEOUT_SECONDS` per §V.34/§V.49|V59,V64
T75|x|impl §V.68 — `reply_email` pre-send fact-check: per-invocation read-ledger keyed by `file_id` (populated on each `read_drive_markdown` success); tokenize proposed reply body, assert candidate spec-value tokens ∈ ledger union, return structured `{"error": "fact_check_mismatch", "unsupported": [...]}` on miss; zero-ledger no-op; cover `reply_email` ∧ `send_email`; regression test: mock agent body fabricating WS36-600-2=65 → wrapper returns error, agent re-drafts|V64,V68
T76|x|sweep §V.6 — fix `src/mailpilot/SKILL.md` per audit recipe; vocab table: `--body-text` → `--body` (3 sites: email send/reply, note add); `mailpilot email reply <EMAIL_ID>` → `mailpilot email reply --account-id <ID> --email-id <EMAIL_ID> --body "..."`; `mailpilot account sync <ACCOUNT_ID>` → `mailpilot account sync [--account-id <ID>]` (optional, omit → all); `mailpilot enrollment run <ENROLLMENT_ID>` → `--workflow-id <WID> --contact-id <CID>` (same for `enrollment view|update|remove`); verb roster (add `sync, disable, retry, get, set`); settings list (add `max_concurrent_tasks: int (default 4)`; state `run_interval` default `60`); env-prefix paragraph (rewrite: env vars use `MAILPILOT_<UPPERCASE_KEY>` form, e.g. `MAILPILOT_DATABASE_URL`)|V64,V6
T77|x|impl scalar enrollment.id per designs/enrollment-id.md — schema: enrollment.id UUIDv7 PK + UNIQUE(workflow_id, contact_id), task.enrollment_id NOT NULL FK, activity.enrollment_id nullable FK; CLI: enrollment {run,view,update,remove} take positional <ENROLLMENT_ID>, add ∧ list keep flagged constructor pair; agent tools: record_enrollment_outcome(enrollment_id) signature, list_enrollments adds id field; populate task.enrollment_id ∈ create_tasks_for_routed_emails ∧ outbound first-touch ∈ enrollment add --scheduled-at; src/mailpilot/SKILL.md positional-form rewrite; §V.6 clause (v) carve-out delete; FK validators extend w/ get_enrollment_by_id; make clean recycles schema|V6,V12,V28,V32,V64
T78|x|diag + close §V.42 lint bypass — §B.48: lint fired B7-first (4ms span, format error) but ⊥ fired B4 / B7-second / B75a / B75b despite stored body_text replays returning {"error":"format"}; trace tool-wrapper → email_ops → sync.send_email path for body mutation between Tool dispatch ∧ _check_spec_table entry; add integration test posting a body w/ ≥3 consecutive spec-row lines ∧ no pipe separator to `agent_tools.reply_email` ∧ `agent_tools.send_email` — both ! return {"error":"format"}|V42,V64
T79|x|impl §V.5(+) — EnrollmentSummary adds `workflow_name` (JOIN workflow on list_enrollments query); Enrollment view loads `workflow_name` + `contact_email` + `contact_name` via JOIN; impl-choice between (a) row-promotion onto Enrollment model (Workflow.account_email precedent) ∨ (b) EnrollmentView projection (ContactView/CompanyView per §V.8) ≡ build-step decision; regression test asserts `enrollment view <id>` envelope inner-dict ⊇ `enrollment list` row at parent-denorm field set|V5,V64
T80|x|impl §V.23(+) atomic-claim clause — dispatcher `_drain_pending_tasks` ! claim each task before pool submission; impl-choice between (a) `list_pending_tasks` UPDATE-RETURNING `status='pending'→'running'`, (b) `FOR UPDATE SKIP LOCKED` + status flip ∈ same txn, (c) in-process in-flight task-id set excluding re-selection — build-step decision; new state ! reset on worker exception (paired w/ `_handle_agent_failure`); regression test asserts N parallel ticks against single pending row → 1 agent.invoke + zero post-completion re-dispatch|V23
T81|x|raise Settings.max_concurrent_tasks default 4→10 per §V.23(+) burst-load formula; src/mailpilot/settings.py:65; smoke C4.a at default settings MUST yield max_latency≤110s ∧ p95≤90s|V23
T82|x|impl §V.54(+) envelope-translation clause — extend `cli_mutation` (`src/mailpilot/operator_log.py:44`) to catch `psycopg.Error` after the observability pair, map constraint subclass → error code (`UniqueViolation`→`duplicate_key`, `ForeignKeyViolation`→`foreign_key_violation`, `NotNullViolation`→`not_null_violation`, `CheckViolation`→`check_violation`, else `database_error`), then call `output_error(str(exc), code)` instead of `raise`; `output_error` imported lazily inside the except branch to preserve §V.2 cli.py top-level-imports rule (operator_log.py ⊥ cli.py ∴ lazy import sidesteps the §V.2 scope but matches the spirit of slow-import deferral); regression test: 5 §B.52 recurrence-class sites empirically confirmed via re-run probe — (a) `account create --email <existing>` → `duplicate_key` (account_email_key), (b) `company create --domain <existing>` → `duplicate_key` (company_domain_key), (c) `contact create --email <existing>` → `duplicate_key` (contact_email_key), (d) `workflow create --account-id <A> --name <existing>` → `duplicate_key` (workflow_account_id_name_key, composite), (e) `contact update <id> --email <peer>` → `duplicate_key` (contact_email_key); ∀ 5 cases assert exit 1, stdout empty, stderr contains operator `event=error source=<noun>.<verb> ...` line ∧ JSON envelope `{"error":"duplicate_key", "message":"...", "ok":false}`, ⊥ `Traceback (most recent call last):` substring; defensive coverage for FK / NOT NULL / CHECK codes — currently pre-handler-guarded (Click `Choice` validator on workflow template/theme; pre-mutation `get_<noun>` FK guards on contact/workflow/enrollment) ∴ no live recurrence-class trigger, but `cli_mutation` translation MUST still map all 4 subclasses via direct unit test invoking the wrapper w/ a synthesized `psycopg.errors.{ForeignKeyViolation,NotNullViolation,CheckViolation}` raise inside the `with`-block (one assertion per code) — guards against future handler refactor that drops a pre-check exposing a constraint path|V2,V3,V54,V64
T83|x|impl §V.69 — after `_run_periodic_iteration` (`src/mailpilot/sync.py`) classifies N_new > 0 new inbound emails in a pass, immediately call `wakeup_event.set()` + set next-tick `do_full_sweep=True` before returning; regression test fires 10 emails at P=5 concurrency against a test loop w/ instrumented wakeup-count counter ∧ asserts at least 2 consecutive full-sweep ticks ∧ `max(task.created_at − email.received_at) ≤ 75s` (use `postgresql://localhost/mailpilot_test`)|V69,V23
T84|x|impl §V.41 per-target compare-and-contrast clause — extend `_DRIVE_GROUNDING` text (`src/mailpilot/agent/templates.py:122-135`) w/ per-target search-budget rule: reply addresses ≥2 distinct product targets → ∀ target ! ≥1 target-specific `search_drive_markdown` query before `list_drive_markdown` accepted as grounding source for that target, even when other targets already grounded; regression test asserts fragment text contains per-target rule substring (e.g. `"per target"` ∨ `"each target"`) ∧ `inbound-google-drive` template registers `_DRIVE_GROUNDING` per §V.45|V41,V64
T85|x|impl §V.68 pipe-table-row scoping — replace `union = "\n".join(read_ledger.values())` (`src/mailpilot/agent/tools.py:82`) w/ pipe-table-line-filtered union (`"\n".join(line for c in read_ledger.values() for line in c.splitlines() if "|" in line)`); update §T.75 regression test fixture: (a) source mock contains WS36-600-2 pipe-row w/ 65 cont / 120 peak / 30 backwash + prose lines `"Temperature: 35-110 degrees F"` ∧ `"Operating pressure: 25 - 125 psi"`; (b) body claiming `"110 GPM continuous"` → `fact_check_mismatch` (`"110"` ∉ any pipe-row line); (c) body claiming `"65 GPM continuous"` → passes; (d) body claiming `"125 GPM peak"` → `fact_check_mismatch` (`"125"` ∉ pipe-rows, only prose); regression-test coverage matches §B.56 column-context-blindness recurrence class|V68,V64
T86|x|impl §V.71 — per-invocation format-lint rejection counter in `reply_email` ∧ `send_email` wrappers (`src/mailpilot/agent/tools.py`) keyed by `contextvars.ContextVar` set @ `run.execute_task` entry, reset on task exit; each `_check_spec_table` hit (§V.42) increments counter; counter ≥ 3 → wrapper accepts body w/o lint ∧ emits `logfire.warn("reply_email.format_lint.cap_reached", attempt=N, workflow_id=..., email_id=...)` ∧ proceeds to normal `email_ops.{reply,send}_email` send path; regression test: 3 consecutive lint-failure invocations on same task → 3rd call bypasses lint ∧ delivers, warn span emitted on bypass|V42,V64,V71
T87|x|impl §V.59(+) ∧ §V.61(+) two-budget observability — smoke-test §C4.a query (`.claude/skills/smoke-test/SKILL.md`) ∧ demo-test G1/G3 (`.claude/skills/demo-test/SKILL.md`) project `sla_agent_seconds` ∧ `sla_delivery_seconds` server-computed columns over `agent.invoke` spans; smoke-test gate asserts `p95(sla_agent_seconds) ≤ 75s` (burst) ∧ `p95(sla_agent_seconds) ≤ 50s` (steady), reports `max/p95/p50(sla_delivery_seconds)` w/o gating; demo-test G1 still gates end-to-end ≤ 90s; G1 FAIL → 3-bullet summary ∋ decomposition `sla_agent`, `sla_delivery`, total|V59,V61
T88|x|impl §V.68(+) per-document scoping — replace uniform pipe-row union in `_fact_check_body` (`src/mailpilot/agent/tools.py:160-165`) w/ per-document contribution: `union_parts = []; for content in read_ledger.values(): lines = content.splitlines(); union_parts.extend(l for l in lines if "|" in l) if any("|" in l for l in lines) else union_parts.append(content); union = "\n".join(union_parts)`; update §T.75 regression test fixture: (a) prose-only source mock containing `"KDF 55 medium can remove over 99% of free chlorine"` + body citing `"55"`, `"99"` → passes (per-doc full-content admit); (b) mixed-ledger w/ prose source + table source containing WS36-600-2 65 cont / 120 peak pipe-row → table doc pipe-row-scoped, prose doc full-content; (c) §B.56 single-doc collision case (same doc has `"110"` in both prose `"35-110 degrees F"` AND CVP pipe-row, body claims `"110 GPM continuous"` for WS36-600-2) → still `fact_check_mismatch` (pipe-row scoping unchanged for table-bearing docs)|V68,V64
T89|x|extend §V.71 counter to cover `_fact_check_body` rejections — rename `_consume_format_lint_rejection` → `_consume_reply_rejection` in `src/mailpilot/agent/tools.py`; add `rejection_type: Literal["format","fact_check"]` param; call `_consume_reply_rejection` at both `_check_spec_table` ∧ `_fact_check_body` rejection sites in `reply_email` ∧ `send_email` wrappers; cap=3 unchanged; bypass at cap returns body w/o either check; rename `_FormatLintCounter` → `_ReplyRejectionCounter` ∧ `_FORMAT_LINT_REJECTIONS` ContextVar → `_REPLY_REJECTIONS`; rename `format_lint_scope` → `reply_rejection_scope` (update `src/mailpilot/run.py:18,59` import + entry); update warn span name → `"reply_email.reply_rejection.cap_reached"` w/ `rejection_type` attribute; extend §T.86 regression test: (a) 3 consecutive `fact_check_mismatch` hits on same task → 3rd call bypasses fact-check ∧ delivers, warn span emitted w/ `rejection_type="fact_check"`; (b) mixed sequence (1 format + 2 fact-check) → 3rd call (the 3rd rejection regardless of type) bypasses, warn emitted w/ rejection_type of the 3rd hit|V42,V68,V71
T90|x|sweep smoke-test SKILL.md compare-type sla_agent gate per §V.61(+) — vocab table: `sla_agent_seconds > 50` (B7 gate), `sla_agent_seconds near the 50s ceiling` (B7 prose), `sla_agent<=50s` (B7 timing-table row); scope = `.claude/skills/smoke-test/SKILL.md` only — B7 (compare) call-sites only, ⊥ B4/B6/B7.5 (single-source ∧ decline retain 50s); B7 gate rewrites to `sla_agent_seconds > 90s` (Critical) w/ `50s < sla_agent ≤ 90s` advisory; burst C4.a non-compare p95 ≤ 75s, compare p95 reported separately w/ advisory 120s; out-of-scope = §V.61 SPEC.md row (already amended), all B4/B6/B7.5 gates (single-source ∧ decline), demo-test G1 end-to-end (still 90s per §V.59)|V61,V64
T91|x|impl §V.10(+) enrollment coverage ∧ §V.15(+) — schema (`src/mailpilot/schema.sql`): extend `enrollment.status` CHECK to admit `'disabled'`, add `disabled_reason TEXT` column, add coupling CHECK `(status = 'disabled') = (disabled_reason IS NOT NULL AND TRIM(disabled_reason) <> '')`, extend `activity.type` CHECK w/ `'enrollment_disabled'`; database (`src/mailpilot/database.py:1625-1664`): drop `delete_enrollment`, add `disable_enrollment(connection, enrollment_id, reason) -> Enrollment \| None` (single txn: UPDATE enrollment SET status='disabled', disabled_reason=%s RETURNING * + INSERT activity row type='enrollment_disabled' carrying summary=reason, detail={"reason": reason}, enrollment_id, contact_id, workflow_id); models (`src/mailpilot/models.py`): `Enrollment` gains `disabled_reason: str \| None`; cli (`src/mailpilot/cli.py:2657-2677`): drop `enrollment_remove` Click cmd, add `enrollment_disable(enrollment_id, --reason)` handler per §V.10 (reason.strip() validation, pre-row `get_enrollment_by_id` read, `cli_mutation("enrollment", "disable", ...)` span, `operator_event("enrollment.disable", changed=[...])`, singular envelope w/ full Enrollment model); agent runtime + tools status-filter sweep — scope = grep pattern `enrollment\.status\b` ∈ {`src/mailpilot/agent/**/*.py`, `src/mailpilot/sync.py`, `src/mailpilot/routing.py`, `src/mailpilot/run.py`, `src/mailpilot/database.py`} (`list_enrollments` tool, routing-pipeline `_ensure_enrollment` guard, drain-loop filters, any `WHERE status = 'active'` ∨ `WHERE status IN (...)` clauses must exclude `'disabled'`); out-of-scope = `schema.sql` CHECK row, `models.py` field declaration; SKILL.md sweep — `src/mailpilot/SKILL.md`, vocab table: `enrollment remove <ENROLLMENT_ID>` → `enrollment disable <ENROLLMENT_ID> --reason "<text>"`, verb-roster `remove` → `remove, disable`; tests sweep — scope = grep pattern `test_enrollment_remove` ∈ `tests/` → rewrite per §V.10 enrollment-coverage surface (validation, not-found, idempotent re-disable, activity-row emit assertion, status-filter exclusion); make clean recycles schema per §T.77 — ⊥ migration script|V4,V10,V14,V15,V17,V31,V54,V64
T92|x|impl §V.10(+) tag coverage — schema (`src/mailpilot/schema.sql`): add `tag.disabled_reason TEXT` nullable, add coupling CHECK `(disabled_reason IS NULL OR TRIM(disabled_reason) <> '')`, extend partial unique indexes `idx_tag_contact_unique` ∧ `idx_tag_company_unique` to also scope `AND disabled_reason IS NULL`, extend `activity.type` CHECK w/ `'tag_disabled'` (retain `'tag_removed'`); database (`src/mailpilot/database.py`): DROP `delete_tag` (`:2840-2865`), `remove_contact_tag` (`:3068-3110`), `remove_company_tag` (`:3113-3149`); ADD `disable_contact_tag(connection, contact_id, name, reason) -> Tag \| None` + `disable_company_tag(connection, company_id, name, reason) -> Tag \| None` (single-txn UPDATE-RETURNING gated on `disabled_reason IS NULL` + activity INSERT per §V.10 (c)); models (`src/mailpilot/models.py`): `Tag` gains `disabled_reason: str \| None`; cli (`src/mailpilot/cli.py`): DROP `tag_remove` Click cmd (`:1487-1560`), ADD `tag_disable(--contact-id, --company-id, name, --reason)` handler per §V.10 shape (XOR validation, `reason.strip()` validation, pre-row `get_contact`/`get_company` FK lookup per §V.64 patch contract, `cli_mutation("tag", "disable", entity_id=name, owner_type=..., owner_id=...)` span, `operator_event("tag.disable", changed=[...])`, singular envelope w/ full Tag model); query-filter sweep — scope = grep pattern `^def (list_tags\|list_contacts_by_tag\|list_companies_by_tag\|search_tags)` ∈ `src/mailpilot/database.py` (4 fns) → add `include_disabled: bool = False` kwarg + `WHERE disabled_reason IS NULL` default filter; CLI-flag sweep — scope = grep `@tag\.command\("(list\|search)"\)` ∈ `src/mailpilot/cli.py` → add `--include-disabled` flag forwarding into kwarg; SKILL.md sweep — `src/mailpilot/SKILL.md` `tag add` example block (`:185-194`) gains `mailpilot tag disable --contact-id <CID> vip --reason "<text>"` peer line; tests sweep — scope = grep pattern `(test_tag_remove\|test_delete_tag\|test_remove_contact_tag\|test_remove_company_tag)` ∈ `tests/` → rewrite per §V.10 (c) (validation, not-found, idempotent re-disable returns None, activity-row emit assertion, query-filter exclusion w/ ∧ w/o `--include-disabled`); make clean recycles schema per §T.77 — ⊥ migration script|V4,V10,V13,V14,V17,V54,V64
T93|x|impl §V.16(+) sweep — convert ∀ bare-INSERT create_<noun> ∈ `src/mailpilot/database.py` w/ natural-unique-key constraint to `INSERT ... ON CONFLICT (<unique_cols>) DO NOTHING RETURNING *` ∧ return type `Model \| None`. Scope = grep pattern `rg -nE '^def create_(account\|company\|contact\|workflow)\b' src/mailpilot/database.py` → 4 sites: `create_account` (`:420`; conflict cols `(email)`), `create_company` (`:554`; `(domain)`), `create_contact` (`:715`; `(email)`), `create_workflow` (`:1054`; composite `(account_id, name)`). Caller updates: (a) CLI handlers — `cli.py:296+` (account), `:471+` (company), `:721+` (contact), `:1791+` ∧ `:2169+` (workflow) — branch on `None` → `output_error(f"<noun> with <natural_key>={value!r} already exists", "duplicate_key")` per §V.4 envelope; ⊥ raise; `cli_mutation` span outcome ≡ ok; `operator_event("<noun>.create", ..., changed=[])` per §V.54 idempotent re-invoke semantics. (b) Sync caller `create_or_get_contact_by_email` (`database.py:797-837`): `create_contact(...) is None` → re-fetch via `get_contact_by_email` ∧ return; backfill branch unchanged. (c) Import handlers `company_import` (`cli.py:610-690`) ∧ `contact_import` (`cli.py:920-1020`): per-row `create_X(...) is None` → existing `{"error": "duplicate", ...}` result row; pre-fetch `existing` set retained as optimization, ⊥ relied on for correctness; `operator_event("<noun>.import", ..., changed=[])` on duplicate branch. (d) Workflow composite-key envelope message: `f"workflow {name!r} already exists for account {account_id}"`. Tests sweep — scope = grep pattern `rg -n 'test_create_(account\|company\|contact\|workflow)\b' tests/` → add (e1) per-fn unit: `create_X` against existing natural key returns `None`, ⊥ raises, ⊥ `logfire.exception`; (e2) per-CLI: re-run cmd against existing row exits 1 w/ envelope `{"error":"duplicate_key","ok":false}` ∧ `capfire: CaptureLogfire` asserts span outcome=ok, ⊥ `.failed` suffix; (e3) sync TOCTOU: 2 concurrent threads call `create_or_get_contact_by_email` w/ same novel email → exactly 1 INSERT, both threads return same Contact row, ⊥ raise. §V.54 envelope tests for FK / NOT NULL / CHECK paths (§T.82 (d)) unchanged — those still raise + §V.54 catches. Manual regression: re-run `mailpilot company create --domain lab5.ca` post-fix → exit 1, stdout duplicate_key envelope, stderr operator-log error line, logfire span outcome=ok.|V16,V54,V64

## §B BUGS

id|date|cause|fix
B1|2026-05-01|3 logfire.exception sites unpaired w/ operator_event ∴ stderr gaps under journald|V51
B2|2026-05-02|out-scope QA verifier flagged echoed-question digits as fabrications|V56
B3|2026-05-02|running tool span ⊥ structured tool_name (misdiagnosis — see §B.6)|V53
B4|2026-05-02|2 skipped_no_workflows spans — 2 distinct email_ids each routed once; §V.22 already held|V22
B5|2026-05-02|demo B1 reply rendered specs w/ asterisks ⊥ pipe-table — prompt-fidelity drift|V42
B6|2026-05-03|B3 misdiagnosis — gen_ai.tool.name already populated by instrument_pydantic_ai|V53
B7|2026-05-03|B4 grounded to broader doc, ⊥ model-specific — top-hit singular collapse, N=1 read|V41
B8|2026-05-03|B4 grounding gate substring-matched expected_tokens ∴ false-negative on phrasing variance|V57
B9|2026-05-03|B4 rendered specs as space-separated `<label>  <number>` ⊥ pipe-table — prompt-only fix didn't hold|V42
B10|2026-05-03|outbound A3 called search_drive_markdown ×5 — Drive tools unconditionally registered|V43
B11|2026-05-04|inbound trigger email inlined twice (Email history + New inbound email)|V29
B12|2026-05-04|A3 prompt rendered Deferred task block for operator-initiated enrollment_run|V30
B13|2026-05-08|company/contact export/import emitted {exported|imported: N, file} ⊥ §V.4 list envelope|V4
B14|2026-05-09|B4 KDF specs rendered w/ ASCII rule-line faux-separators — §V.42 regex required numerics|V42
B15|2026-05-10|GCE crash-loop 46+ iters — ⊥ google_application_credentials → ADC never tried|V37
B16|2026-05-10|2 prod TimeoutError at default 60s httpx read window on long-context agent.invoke|V48,V49
B22|2026-05-11|smoke-test skill carried dotless §V13/§V12/§T18/§B2 cites — auditor regex skipped|V66
B23|2026-05-11|/demo-test G1 polled local-DB; §V.59 setup forbids run-loop ∴ false-FAIL on healthy prod|V60
B24|2026-05-12|code-side §-cites carried dotless V13 / V11 — §V.66(+) binds project-wide|V66
B25|2026-05-12|outbound called record_enrollment_outcome on enrollment_run ∴ 2 terminal activity rows / enrollment|V31
B27|2026-05-12|mailpilot activity list omits workflow_id / email_id / task_id ∴ smoke-test falls back to psql|V7
B28|2026-05-12|outbound first-invoke cache_creation_input_tokens=0 — runtime path issue, ⊥ config|V50
B29|2026-05-14|mailpilot contact import w/o --file from TTY hangs on sys.stdin.read() — ⊥ isatty guard|V9
